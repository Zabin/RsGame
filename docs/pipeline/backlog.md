# Pipeline Backlog

> **Findings & intake register for the documentation-driven-development pipeline.** Two writers:
> **`00-pipeline-manager`** (harvests every invoked skill's findings/recommendations/Outstanding
> Issues after each run, triages every open entry at the start of the next run, and flips
> statuses) and **`00-intake`** (appends `NEW` entries for user-submitted features, bugs, and
> observations). Stage skills never write here — their findings reach this file via the manager's
> harvest step. Rows are never deleted: resolved entries flip to `DONE`, rejected ones to
> `REJECTED` with the reason kept.

[↑ Docs index](../INDEX.md) · [Pipeline journal](pipeline-journal.md) ·
[Pipeline README](../../.claude/skills/README.md)

## Lifecycle

```
NEW ──triage──▶ SCHEDULED (rides a named step)  ──that step runs──▶ DONE
        ├─────▶ DEFERRED  (named revisit trigger) ──trigger fires──▶ NEW (re-triage)
        ├─────▶ NEEDS-USER (named decision)       ──user decides───▶ SCHEDULED / REJECTED
        └─────▶ REJECTED  (reason recorded)
IN PIPELINE = the entry's work is the current/next step's explicit target
```

Every open entry carries a live disposition — `SCHEDULED` names the step it rides with,
`DEFERRED` names its revisit trigger, `NEEDS-USER` names the exact decision required. A
Critical/High entry may not be `DEFERRED` without the user's explicit agreement.

## Entries

| ID | Filed | Type | Source | Summary | Sev/Pri | Entry stage | Disposition | Status |
|---|---|---|---|---|---|---|---|---|
| BL-0001 | 2026-07-06 | bug | scaffold init: `memory.md` §"Active Bug" | Map-screen hearts written to wrong BG map addresses (3-heart layout, rows 6/8/10 col 12). **Superseded — see BL-0008 (run #2, vision revision).** `memory.md` describes the *pre-rewrite* 3-zone game; the current `tilemaps.py`/`asm_game.py` implement a completely different 9-heart 3×3 map-screen mechanism. This entry's specific fix (patch three hex addresses) does not apply to the current code as filed. | Medium | 07 | **Superseded (run #2):** do not implement as filed — re-verify against current `map_screen()`/`update_map_hearts` (asm_game.py ~line 643) once GDS-04/05 baseline the real game; folded into BL-0008's broader re-verification scope. **Resolved (run #21, `07-implementation-planning`): re-verified against current code — does NOT reproduce.** `update_map_hearts` (asm_game.py:644) writes rows 6/9/12 × cols 6/11/16, exactly matching `map_screen()`'s heart placement (tilemaps.py:327), and runs only via `do_screen_redraw` with the LCD off. The rewrite replaced and fixed the filed mechanism; a permanent map-hearts regression check rides IP-9010's rewritten suite. | DONE |
| BL-0002 | 2026-07-06 | finding | scaffold init: `Claude.md` §"Remaining Known Issues" | Bunny renders as two separate 8x8 OAM entries and reads small. **Status uncertain after run #2's discovery that the game was fully rewritten** — a separate, later commit ("Fix invisible bunny: 8x16 OBJ mode + WRAM clear bug", `9a587ac`) suggests this may already be resolved by a switch to 8x16 OBJ mode. Needs direct code verification, not the stale `Claude.md` description. | Low | 03 | **Re-scope (run #2):** before riding GDS-08 authoring as originally scheduled, first check `asm_game.py`'s current OAM-write code and commit `9a587ac`'s diff to see if this is already fixed; only carry forward if still reproducible. **Resolved (run #27 triage): already fixed in the shipped code** — commit `9a587ac` switched to 8×16 OBJ mode; [GDS-08 §2](../architecture/08-presentation-architecture.md)/[ADR-0007](../architecture/adr/ADR-0007-8x16-obj-mode.md) document the single 8×16 OAM pair as-built (run #13), and the rewritten suite's T6 asserts single-entry 8×16 bunny OAM (+X-flip) on every run (run #22, verified run #23). Not reproducible; no work remains. | DONE |
| BL-0003 | 2026-07-06 | bug | scaffold init: `Claude.md` §"Remaining Known Issues" | Score display writes VRAM while the LCD is on. **Needs re-verification against current code** (run #2 found `Claude.md` describes the pre-rewrite game throughout) — the score-bar write path may have changed in the Bunny Quest rewrite. Do not assume the original description of *where* in the code this happens still holds. | Medium | 07 | **Re-scope (run #2):** re-locate the score-bar VRAM-write call site in the current `asm_game.py` before packaging a fix; folded into BL-0008's broader re-verification scope. **Confirmed reproducing (run #21):** `update_status_disp` (asm_game.py:505) writes `0x9802`/`0x9808–0x980A` unguarded, called from `st_playing` at unbounded distance from VBlank wake — packaged as [IP-9020](../implementation/packages/IP-9020-score-bar-vblank-fix.md) (authorized via carve-out; blocked on IP-9010). **Implemented run #24** (call relocated to frame-top VBlank, T8.10a/b added). **Resolved (run #28, [VR-9020](../implementation/verification/VR-9020-score-bar-vblank-fix.md)): independently confirmed** — sole call site at frame top inside VBlank, every other VRAM writer LCD-off, 125/125 pass; IP-9020 VERIFIED. | DONE |
| BL-0004 | 2026-07-06 | design-question | scaffold init: repo-root file survey | Duplicate/legacy build artifacts need reconciling: `BunnyGarden_build_rom.py` (monolithic, 45KB) and `BunnyGarden_logic.json` coexist with the modular canonical chain. **Widened (run #2):** the repo-root also carries a stale `BunnyGarden.gbc` binary (pre-rewrite ROM, still checked in) alongside the *current, correct* `BunnyQuest.gbc` (verified byte-identical to a fresh build of current source) — same archival treatment applies. | Medium | 03 | **User decided (run #1): modular chain is canonical; archive `BunnyGarden_build_rom.py` + `BunnyGarden_logic.json` to `legacy/`.** **Widened (run #2, not yet re-asked of the user): also archive the stale `BunnyGarden.gbc` alongside them; keep `BunnyQuest.gbc` at the repo root as the current shipped artifact.** Rides the first `07-implementation-planning` hygiene pass; confirm the widened scope with the user if it isn't obviously assumed consent. **Packaged (run #21) as [IP-9040](../implementation/packages/IP-9040-legacy-artifact-archival.md)** (authorized: carve-out + run #1 decision; blocked on IP-9010). **Implemented run #25** (three files `git mv`'d to `legacy/` + README). **Resolved (run #29, [VR-9040](../implementation/verification/VR-9040-legacy-artifact-archival.md)): independently confirmed** — root clean, `legacy/` complete with history preserved, zero live references, ROM byte-identical; IP-9040 VERIFIED. | DONE |
| BL-0005 | 2026-07-06 | bug | scaffold init: `build_rom.py`/`test_rom.py` read-through | Hardcoded absolute paths break portable runs. **Compounded (run #2):** `test_rom.py`'s hardcoded `ROM_PATH` also points at the *wrong, stale* ROM name (`BunnyGarden.gbc`) — the current shipped ROM is `BunnyQuest.gbc`. A path-only fix without also fixing the filename would leave the suite pointed at the wrong binary even after portability is fixed. | Medium | 07 | SCHEDULED (run #1 triage): rides the bootstrap's first `07-implementation-planning` remediation pass, sequenced first. **Widened (run #2):** the remediation package must also update `ROM_PATH`'s filename, not just make the path configurable — folded into BL-0008. **Packaged (run #21) into [IP-9010](../implementation/packages/IP-9010-test-suite-rewrite.md)**; **implemented run #22 (2026-07-07)** — all paths repo-relative, `ROM_PATH` retargeted to `BunnyQuest.gbc`, `RAM_PATH` derived not hardcoded. **Verified run #23 ([VR-9010](../implementation/verification/VR-9010-test-suite-rewrite.md)): confirmed independently — no absolute paths remain, ROM byte-identical.** | DONE |
| BL-0006 | 2026-07-06 | bug | `01-vision` run #2 (deliberate vision-change run), direct code inspection | **`test_rom.py` asserts pre-rewrite WRAM semantics against the current shipped code — the G5 permanent gate cannot currently be satisfied.** Specifically: T4.8/T8.8 assert victory at `GS=5` when a 3-bit `GIFTS` bitfield (`0xC009`) reaches `0x07`; T7.9/T9.x assert `CUR_ZONE` (`0xC008`) is bounded to 0–2. The current `asm_game.py` uses `CUR_ZONE` 0–8 and a 9-byte `CARROT_FLAGS` array with victory at `CARROTS_COUNT==9` — none of these old assertions can pass against the real code. `test_results.txt`'s "88/88 passed" is a stale artifact predating the rewrite, not evidence about the current tree. | **Critical** | 07 | SCHEDULED: rides BL-0008's umbrella pass, prioritized above BL-0001/0003/0005's other items — the earliest `07`/`08` package once GDS-04/05 exist to derive correct assertions from. **Not deferred** (Critical severity, per the pipeline's severity-honesty rule) — flagged prominently to the user this run as a recommendation on whether to pull it forward out of numeric order; not blocking upstream GDS/requirements authoring, which can proceed from direct code reads independent of this fix. **Packaged (run #21) into [IP-9010](../implementation/packages/IP-9010-test-suite-rewrite.md)**; **implemented run #22 (2026-07-07)** — T1 kept per R304, T2–T10 rewritten against the current WRAM model (R305/GDS-07), **109/109 checks pass** against a byte-identical rebuilt ROM; `NFR-7100` flipped to MET. The G5 gate is functional again. **Verified run #23 ([VR-9010](../implementation/verification/VR-9010-test-suite-rewrite.md)): independently re-run in a fresh container (PyBoy reinstalled) — 109/109 pass confirmed, no pre-rewrite assertions remain.** | DONE |
| BL-0007 | 2026-07-06 | doc-defect | `01-vision` run #2 (deliberate vision-change run), direct code inspection | **`Claude.md`, `memory.md`, and `README.md` all describe the pre-rewrite "Bunny Garden Adventure" game throughout** (WRAM map, tile index map, zone names/count, "v2.1" status line, known-good-behavior list) — none reflect the shipped "Bunny Quest" 9-zone game. These are the primary onboarding/quick-reference docs; they are actively misleading right now, not just out of date. | High | 07 | SCHEDULED: fold into the same documentation-refresh remediation pass as BL-0006 (a stage-08 content/code peer pass once GDS-01/04/05 have re-baselined against the real code, so the refresh has a correct source to write from rather than guessing again). **Packaged (run #21) as [IP-9030](../implementation/packages/IP-9030-root-doc-refresh.md)** (depends on IP-9010+IP-9020 so it documents the fixed state; awaits G3, see BL-0024). **Implemented run #30** (2026-07-09). **Resolved (fresh-session `09-package-verification`, 2026-07-10, [VR-9030](../implementation/verification/VR-9030-root-doc-refresh.md)): independently confirmed** — all three root docs describe the shipped Bunny Quest game accurately, stale-term sweep clean, quick-start commands executed; IP-9030 VERIFIED. | DONE |
| BL-0008 | 2026-07-06 | finding | `01-vision` run #2 — consolidation entry | **Umbrella tracking entry for the "docs/tests describe the wrong game" remediation**, so BL-0001/0003/0005/0006/0007's overlapping re-verification work rides one coherent `07-implementation-planning` pass instead of five uncoordinated ones. Scope: (1) re-verify BL-0001/BL-0003's specific claims against current code, fixing only what still reproduces; (2) fix BL-0005's path portability *and* filename; (3) rewrite `test_rom.py`'s zone/victory assertions for the real 9-zone/9-carrot model (BL-0006); (4) refresh `Claude.md`/`memory.md`/`README.md` (BL-0007). | High | 07 | SCHEDULED: this is the recommended shape for the bootstrap's first `07-implementation-planning` remediation pass — likely split into 2-3 packages (test-suite rewrite is code-implementation scope; doc refresh may ride alongside or split to its own package) rather than one, per the skill's right-sizing rule. Sequenced ahead of GDS-01 authoring's dependence on accurate current-state facts wherever practical. **Umbrella fully packaged (run #21):** IP-9010 (BL-0005+0006) → IP-9020 (BL-0003) / IP-9040 (BL-0004) / IP-9030 (BL-0007); BL-0001 closed as not-reproducing. **IP-9010 VERIFIED (run #23, VR-9010)** — BL-0005/BL-0006 now DONE; IP-9020/IP-9040/IP-1010 flipped READY. **IP-9020 VERIFIED (run #28, VR-9020)** — BL-0003 DONE; **IP-9040 VERIFIED (run #29, VR-9040)** — BL-0004 DONE. **IP-9030 VERIFIED (2026-07-10, VR-9030)** — BL-0007 DONE. **Umbrella closes: all five packages VERIFIED, the bootstrap tranche's entire remediation scope is complete.** | DONE |
| BL-0009 | 2026-07-06 | finding | `02-research-gbc-hardware` run #3 ([R104](../research/encyclopedia/R104-cgb-palette-system.md)) | **The CGB's fixed palette budget (8 BG + 8 OBJ palettes) is a real, near-term design constraint for MSTR-001 C7's world-scale target — but not as tight as first characterized.** **Corrected (run #12, [GDS-07](../architecture/07-data-model.md)):** direct read of `build_rom.py`'s `BG_PALETTES` confirms palettes are organized by **terrain family**, not one-per-zone, and are already substantially reused — only 5 of 8 BG palettes serve zone terrain today (palette 4 alone covers Mountain+Village+Cave, three zones); the original framing ("already at or near its ceiling… at least one pair shares") understated the existing headroom and the deliberateness of the reuse already in place. Still a real, eventually-binding ceiling as C7 grows further — just later than originally stated. | Medium | 03 | SCHEDULED: GDS-08 (Presentation Architecture) should state the corrected picture (5/8 BG palettes in zone-terrain use, terrain-family reuse pattern) when authored, citing GDS-07/R104 — GDS-01 already named the tension in general terms and needs no further edit. Not blocking any current work. **Resolved (run #27 triage): GDS-08 was authored run #13 carrying exactly this corrected picture** (terrain-family palette reuse, citing GDS-07/BL-0009) — the ride this entry was scheduled on already happened; the underlying palette ceiling remains recorded in GDS-08/R104 as C7's future constraint. | DONE |
| BL-0010 | 2026-07-06 | doc-defect | `02-research-gbc-hardware` run #3, `docs/research/INDEX.md` §Tier R200 | `docs/research/INDEX.md`'s R201 row still describes its scope as "goal hierarchies, gating, **the gifts→victory shape**" — stale "gifts" wording from before the vision correction (the game's win condition is 9 carrots, not gifts). Trivial, and out of this skill's write scope (Tier R200 belongs to `02-research-game-design`), so left as-is rather than edited here. | Low | 02 | **Resolved (run #5):** R201 authored with corrected wording ("9-carrot victory shape"); index row fixed in the same pass. | DONE |
| BL-0011 | 2026-07-06 | finding | `02-research-tooling-and-testing` run #4 ([R301](../research/encyclopedia/R301-pyboy-headless-api.md)) | PyBoy is not installed in this environment (`import pyboy` fails), so R301's `button()`/`button_press()`/`button_release()` behavior and the `stop(ram_file=...)` custom-location detail rest on secondary sources (a GitHub-source search summary, a community Q&A post) rather than a directly-fetched primary doc page or a local experiment — `docs.pyboy.dk` itself returned HTTP 403 to `WebFetch` this session. The core claims are corroborated by this project's own working `test_rom.py` code (which already calls these methods successfully per its historical "88/88 passed" record, predating the semantic drift `BL-0006` found), so confidence is reasonable but not the formal verification bar the research methodology sets. | Low | 02 | **Resolved (run #22, 2026-07-07):** PyBoy **2.7.0** installed in-session for IP-9010; the rewritten suite exercises `button()`, `button_press()`/`button_release()`, `screen.ndarray`/`screen.image`, and the `<rom>.ram` battery-save flush-on-`stop()` behavior across 109 passing checks — R301's uncited claims are now empirically confirmed against a named version. (R301's own citation text can note this the next time an `02` pass touches it — not worth a dedicated research run.) | DONE |
| BL-0012 | 2026-07-06 | recommendation | `02-research-tooling-and-testing` run #4 — cross-reference update | **BL-0005 and BL-0006 now have concrete, code-cited remediation targets** — [R306](../research/encyclopedia/R306-toolchain-portability.md) specifies the exact four hardcoded-path lines to fix and the repo-relative replacement scheme (BL-0005); [R305](../research/encyclopedia/R305-emulator-based-test-design.md) specifies the exact current WRAM addresses/values a rewritten `test_rom.py` must assert (BL-0006), and [R304](../research/encyclopedia/R304-rom-validation.md) confirms the T1 header-check suite needs no changes at all. This doesn't change either entry's disposition (still `SCHEDULED`, riding `BL-0008`'s umbrella pass) but means `07-implementation-planning`'s eventual package-authoring pass for `BL-0008` has everything it needs already grounded — no further research gap exists for that remediation. | — | 07 | Informational — no action needed beyond citing R304/R305/R306 when `BL-0008`'s package is authored. | DONE |
| BL-0013 | 2026-07-06 | research-gap | intake: project owner | **Project owner's own words:** "Add a research series that explores pixel art techniques, using Claude and AI to generate art, best rated Gameboy and Gameboy Color games for visual aesthetics, etc among other similar topics - which generally explore how Claude could develop and implement visually appealing graphics in a structured way that can be iterated upon." No duplicate found — checked BL-0001…BL-0012 and the existing R200 tier (R201–R208, none yet authored) for overlap; closest existing rows are R203 (screen composition), R204 (HUD conventions), R208 (palette/color under CGB constraints) — adjacent, not overlapping. This request is genuinely broader than R208's color-only scope: it spans (a) pixel-art craft technique at 8×8/2bpp scale (silhouette readability, dithering-substitutes under a 4-color budget, anti-aliasing-by-palette-choice), (b) a structured, iterable **workflow** for an AI agent (this project's own coding agent) to generate/critique/revise tile art against a spec rather than freehand, and (c) comparative case-study grounding from acclaimed GBC/GBC-era titles' visual design (e.g. Link's Awakening DX, Wario Land II/3, Pokémon Gold/Silver, Kirby's Dream Land 2, Shantae) — what specifically reads as "good" GBC art and why, at the tile/palette/composition level this project's tooling actually works in. Recommend 3 new R2xx rows for whoever authors R200: a pixel-art-technique topic, an AI-assisted-generation-workflow topic, and a comparative-case-study topic — final numbering/splitting is that skill's call, not intake's. | — (user framed as "add a research series," not stated as urgent) | 02 | **Resolved (run #5):** authored as R209 (pixel-art technique), R210 (AI/agent-assisted generation workflow, grounded directly in this project's own tooling), R211 (comparative case studies: Oracle of Seasons/Ages, Shantae, Pokémon Gold/Silver, GBC DKC) — see [R209](../research/encyclopedia/R209-pixel-art-technique.md)/[R210](../research/encyclopedia/R210-ai-assisted-tile-art-workflow.md)/[R211](../research/encyclopedia/R211-acclaimed-gbc-visual-design-case-studies.md). | DONE |
| BL-0014 | 2026-07-06 | finding | `02-research-game-design` run #5 ([R210](../research/encyclopedia/R210-ai-assisted-tile-art-workflow.md)) | **This project's toolchain has no path to import a raster image (a PNG, an AI-generated sprite mockup) into `tiles.py`'s pixel-array format.** Every tile is authored as a literal hand-written Python `pix` array; no code anywhere quantizes an external image to a 4-color palette and emits the resulting grid. This means the common "AI generates a PNG, we import and refine it" pixel-art workflow is not currently executable in this pipeline — R210's recommended workflow instead has the agent author the pixel grid directly as reviewable text. Not a defect (nothing currently needs this capability), but worth recording as a real, current tooling limitation rather than an assumed-available path. | Low | 07 | DEFERRED: revisit only if a future task specifically wants to start from an external reference/mockup image — at that point this becomes a small, scoped tooling feature (an image-to-palette-grid converter script) for `07-implementation-planning` to package. Not blocking any current work. | DEFERRED |
| BL-0015 | 2026-07-06 | design-question | `03-architecture-design-synthesis` run #6 ([GDS-01 §5](../architecture/01-concept-of-play.md)) | **As MSTR-001 C7's Zelda/Pokémon-scale world target is pursued, does Bunny Quest stay a short single-sitting game over a much larger map ("wider"), or become a deliberately longer multi-session game with fewer/larger zones ("deeper")?** GDS-01 names this tension explicitly rather than picking a default; two concrete constraints bear on it — the CGB's 8-BG-palette ceiling (already substantial at 9 zones, `BL-0009`/R104/R208) makes "wider" harder to sustain with distinct per-zone color identity, and `_zone_arrows()`'s hardcoded-rectangle adjacency logic (R203) needs generalizing sooner under "wider" than "deeper." | Medium | 03 | **Closed (`00-pipeline-manager` run #30, triage):** the C7 world-scale expansion is now planned — the adopted increment plan ([PLAN-requirements-aesthetics-story-map.md](PLAN-requirements-aesthetics-story-map.md) §6) and owner decisions D3/D6 (BL-0031) resolve "wider vs deeper" by making world scale a **user-runtime parameter** rather than an architecture fork: the player picks size per world: within bounds the eventual seed & scale ADR sets. No design fork remains to adjudicate. The palette-ceiling consequence this entry named transfers to BL-0031/GDS-08 in corrected form (binds biome-family count, not blending — see plan §7). | DONE |
| BL-0016 | 2026-07-06 | recommendation | `03-architecture-design-synthesis` run #8 ([GDS-03](../architecture/03-architecture.md)) | **As-built ADRs have not been authored yet** — `docs/architecture/adr/` remains empty. GDS-03 identified several real, already-made decisions worth recording as formal ADRs: single-bank ROM (no MBC bank switching, though C7 anticipates this changing — R106), Python assembler over RGBDS (MSTR-001 C3/A3), shadow-OAM DMA every frame (R105), 8×16 OBJ mode (R103, the `9a587ac` fix), MBC1+RAM+BATTERY (R106), PyBoy-as-verification-gate not real hardware (assumption A2). Deferred from GDS-03's own pass per the one-level-per-advance discipline, not forgotten. **Resolved run #16: eight as-built ADRs authored** ([ADR-0001](../architecture/adr/ADR-0001-single-bank-rom-no-mbc-switching.md)–[ADR-0008](../architecture/adr/ADR-0008-pyboy-headless-verification-target.md)), covering all six original candidates plus one-job-per-file decomposition and the patch-point dict contract; `docs/architecture/adr/INDEX.md` populated. | Low | 03 | DONE: eight ADRs authored and indexed, run #16. | DONE |
| BL-0017 | 2026-07-06 | finding | `03-architecture-design-synthesis` run #9 ([GDS-04](../architecture/04-domain-model.md)) | **The "exactly one Carrot per Zone" rule is not enforced by any code-level invariant** — it holds today only because `ZONE_COLLECTS` happens to be authored with one `type 2` entry per zone list; nothing checks this at build time or runtime. A future content package editing `ZONE_COLLECTS` (adding a zone, rebalancing an existing one) could silently violate the rule with no test catching it. | Low | 07 | SCHEDULED: any future `07-implementation-planning` package touching `ZONE_COLLECTS` should include an explicit Verification Checklist item confirming exactly one carrot per zone list — not a dedicated remediation on its own, since nothing is broken today. **Upgraded (run #21), implemented run #22:** the rewritten suite's T1.11 asserts exactly one carrot per zone on every run (T1.10/T1.12 add zone-count and bitfield-capacity guards) — once IP-9010 verifies, the rule is a tested property, not a convention; the standing checklist rule remains for future `ZONE_COLLECTS`-touching packages. | SCHEDULED |
| BL-0018 | 2026-07-06 | finding | `03-architecture-design-synthesis` run #9 ([GDS-04](../architecture/04-domain-model.md)) | **`SaveGame` does not persist `Player.Direction`/`Player.AnimationFrame`, or per-zone `ScoreItem` collected-state** — confirmed directly against the save routine's field list (only `CUR_ZONE`, `PLAYER_X`/`PLAYER_Y`, `CARROTS_COUNT`, `SCORE`, and `CARROT_FLAGS` are written). A reload always restores a default facing/frame, and every zone's stars/flowers reset to "all present" regardless of what was collected before the save. Cosmetic/minor-progression-loss only — not a functional defect, and plausibly an intentional simplification — but worth confirming as intended rather than assumed. **Revisited run #17** ([RQ-01](../requirements/01-functional-requirements.md) FR-5210/CR-01, [RQ-03](../requirements/03-requirements-review.md) finding #1): the baseline documented current behavior honestly (FR-5210) but explicitly could not resolve whether it's intended scope — needed a user decision. **Resolved run #18 (2026-07-07), explicit user decision:** facing direction/animation frame — "not important," no persistence added (FR-5210 kept as-is, narrowed to just these two fields). Per-zone ScoreItem collected-state — "should save and persist," approved and promoted to new requirement **FR-5220**; NFR-5200 widened accordingly. RQ-01/02/03/04 all updated as a delta (changelog entries, not a regeneration). | Medium | 04 | **DONE:** user decision recorded; FR-5210 reworded, FR-5220 added, NFR-5200/RQ-03 finding #1/RQ-04 rows updated, run #18. No further action — FR-5220's actual implementation is future `07-implementation-planning`/`08-code-implementation` work, tracked via the traceability matrix, not this backlog entry. | DONE |
| BL-0019 | 2026-07-06 | recommendation | `04-requirements-engineering` run #17 ([RQ-03](../requirements/03-requirements-review.md) finding #4) | **No requirement currently watches ROM-budget headroom as it's consumed.** NFR-4000 (32768-byte single-bank budget) is correctly "Met" today (23148/32768 bytes used, ~9.6KB headroom) and correctly names C7/ADR-0001 as its own future-supersession trigger — but nothing re-checks that status as future content packages consume the remaining headroom; a package could push usage near the ceiling with no requirement flagging it. | Low | 04 | SCHEDULED: any future package that materially increases ROM usage should include a checklist item re-affirming NFR-4000's "Met" status against remaining headroom (a concrete re-affirmation threshold, e.g. below 2KB remaining, is future `03-architecture-design-synthesis`/`05-feature-decomposition` work, not decided here). Not blocking any current work. **Honored (run #21):** NFR-4000 re-affirmation checklist items included in both ROM-growing packages (IP-9020, IP-1010). **Re-affirmed (runs #24/#26):** IP-9020 left ROM unchanged (23148 bytes); IP-1010 grew it to 23404/32768 bytes (+256) — ~9.1KB headroom remains, nowhere near a 2KB-remaining concern line. Standing convention continues for future ROM-growing packages. | SCHEDULED |
| BL-0020 | 2026-07-07 | doc-defect | `05-feature-decomposition` run #19 ([FP-05](../feature-planning/05-feature-review.md) finding #1) | **No numbered functional requirement covers player/collectible sprite (OAM) rendering as an observable behavior**, even though it is shipped, architecturally documented (8×16 OBJ pairs, shadow-OAM DMA — GDS-08 §2, ADR-0005, ADR-0007), and exercised by `test_rom.py`'s T6 suite. FR-2100/FR-2200 cover position/facing; FR-3100 covers collision detection; none states sprites are actually rendered. A genuine RQ-01 baseline gap, surfaced (not invented) by the Feature Decomposition review, which may not add FRs itself. | Low-Medium | 04 | SCHEDULED: a future `04-requirements-engineering` delta should add a plausible `FR-6400` ("Player and Collectible sprite rendering") under the Presentation grouping, citing GDS-08 §2/ADR-0005/ADR-0007. Not blocking any current Feature — the behavior is shipped and working; only the formal requirement statement is missing. **Resolved (`04-requirements-engineering` run, 2026-07-10, 04-delta batch):** FR-6400 added exactly as scheduled, citing T6.1–T6.10 as pre-existing evidence. | DONE |
| BL-0021 | 2026-07-07 | design-question | `05-feature-decomposition` run #19 ([FP-03](../feature-planning/03-feature-catalog.md) FEAT-5100 Open Questions) | **FEAT-5100 (per-zone ScoreItem persistence, formalizing FR-5220) has two unresolved design questions that must be answered before implementation:** (1) the exact SRAM byte layout for the new persisted field (a possible GDS-07 Data Model delta); (2) whether a save written *before* this feature ships should be treated on first load as "all ScoreItems already collected" or "all ScoreItems uncollected." Neither is assumed either way in the catalog. **Resolved run #20 ([FS-101](../features/FS-101-per-zone-scoreitem-persistence.md)):** (1) a 9-byte `SCOREITEM_FLAGS` array mirroring `CARROT_FLAGS`, exact WRAM address deferred to `07-implementation-planning`; (2) pre-upgrade saves default to "all uncollected," guarded by a new save-format version byte — both reasoned to confident recommendations, neither needed the user. | Medium | 06 | **DONE:** both questions answered in FS-101 with recorded reasoning. Remaining implementation-level detail (exact WRAM address) rides `07-implementation-planning` as normal package work, not a separate backlog item. | DONE |
| BL-0022 | 2026-07-07 | doc-defect | `06-feature-specification` run #20 ([FS-101](../features/FS-101-per-zone-scoreitem-persistence.md) Open Question 1) | **`FR-3200`'s postcondition is factually incorrect as currently shipped.** It claims a collected `ScoreItem` is "permanently inactive for the remainder of that play session," but direct code reading of `setup_zone_collects` (`asm_game.py`, confirmed by FS-101) shows `ScoreItem`s are reloaded active=1 from ROM on **every** zone entry — only `Carrot`s are protected against this via the `CARROT_FLAGS` check already in that routine. The requirements baseline asserts something not true of the shipped code. | Medium | 04 | SCHEDULED: a future `04-requirements-engineering` delta should correct `FR-3200`'s postcondition to match actual shipped behavior (respawns on zone re-entry within a session) — independent of whether/when FS-101/FEAT-5100 ships, since that Feature will fix the underlying behavior but the requirements text should stop misdescribing the *current* code regardless. Not blocking any current work. **Resolved (`04-requirements-engineering` run, 2026-07-10, 04-delta batch):** postcondition corrected to match `setup_zone_collects` exactly; confirmed consistent with `FR-5220`/`IP-1010`, which build persistence on top of the respawn behavior rather than assuming it away. | DONE |
| BL-0023 | 2026-07-07 | bug | `06-feature-specification` run #20 ([FS-101](../features/FS-101-per-zone-scoreitem-persistence.md) Open Question 2, Risks) | **`ScoreItem`s (stars/flowers) currently respawn every time a zone is re-entered within a play session** (see `BL-0022`) — meaning `Score` is infinitely farmable today by walking back and forth between two zones. `SCORE` (`0xC006`) is a single WRAM/SRAM byte with no documented overflow handling if repeatedly incremented past 255. Discovered incidentally while specifying `FEAT-5100`, not previously caught by `GDS-05`/`RQ-01`'s authoring passes. | Medium | 08 | SCHEDULED: `FEAT-5100`'s implementation (per `FS-101`'s design, extending the same zone-entry check `Carrot`s already use) fixes this as a side effect — no separate remediation package needed unless `FEAT-5100` is deprioritized far enough that the farming behavior needs an interim fix on its own. Not blocking any current work; not escalated to `NEEDS-USER` since `FEAT-5100` already fixes it as designed. **Packaged (run #21):** rides [IP-1010](../implementation/packages/IP-1010-per-zone-scoreitem-persistence.md) with a dedicated regression test (T11.a — collected item stays inactive and `SCORE` does not re-increment on zone re-entry). **Implemented run #26:** T11.a4/T11.a5 confirm the fix (score does not re-increment on zone re-entry); closes on IP-1010's Verification Report. **Resolved (run #27, [VR-1010](../implementation/verification/VR-1010-per-zone-scoreitem-persistence.md)): fix independently confirmed** — T11.a4/T11.a5 re-run in a fresh container prove a collected ScoreItem stays inactive and `SCORE` does not re-increment on zone re-entry; IP-1010 VERIFIED. | DONE |
| BL-0024 | 2026-07-07 | gate | `07-implementation-planning` run #21 ([Master Build Plan](../implementation/00-master-build-plan.md)) | **G3 authorization needed for three planned packages before any can start a stage-08 run:** (1) **IP-9010** (test-suite rewrite, BL-0006 Critical + BL-0005) — READY and the critical-path unblocker for *everything*, but BL-0006 is outside the BL-0001…0005 bootstrap carve-out; (2) **IP-9030** (root-doc refresh, BL-0007) — same reason; (3) **IP-1010** (FS-101 per-zone ScoreItem persistence) — genuinely new work, flagged since run #20. IP-9020/IP-9040 are already authorized (carve-out) but blocked on IP-9010, so **the whole build plan is gated on this decision.** | High (blocks the Critical BL-0006 remediation) | 08 | **Resolved (run #21, same day): user explicitly authorized all three** ("Authorize all three", 2026-07-07) — IP-9010, IP-9030, and IP-1010 all have G3 go-ahead on record; combined with the carve-out (IP-9020/IP-9040), every planned package is now authorized. Execution order remains dependency-driven: IP-9010 first. | DONE |
| BL-0025 | 2026-07-07 | recommendation | `08-code-implementation` run #22 (IP-9010) | **Runtime test artifacts are untracked repo-root noise:** the rewritten suite writes screenshots to `test_shots/` and PyBoy writes `BunnyQuest.gbc.ram` beside the ROM during runs (the suite wipes the `.ram` at exit, but an aborted run leaves it). No `.gitignore` exists to exclude them. Cosmetic only — nothing breaks. | Low | 07 | **Resolved (run #22, same day):** a tracked `.gitignore` already existed covering `*.gbc.ram`/`*.gbc.sav`/`__pycache__`; `test_shots/` appended to it (one-line hygiene fix, prompted by the repo's clean-tree hook — no package needed). | DONE |
| BL-0026 | 2026-07-07 | doc-defect | `09-package-verification` run #23 ([VR-9010](../implementation/verification/VR-9010-test-suite-rewrite.md)) | **`IP-9010`'s §3 "Requirements Covered" cites `NFR-7000`, which does not exist anywhere in the requirements baseline** (`docs/requirements/02-non-functional-requirements.md` has no such section; the RTM has no such row). The nearest match by subject is **`NFR-6100`** ("PyBoy headless as the verification target") — its RTM Test cell is still `UNASSIGNED (inspection-based)` even though every one of the rewritten suite's 109 checks now runs headless via PyBoy, direct repeatable evidence for it. Not a functional defect — the underlying capability is real and independently confirmed by VR-9010 — purely a citation/traceability gap. | Low | 04 | SCHEDULED: a future `04-requirements-engineering` delta should fill `NFR-6100`'s RTM Test cell citing the full T1–T10 suite as headless-execution evidence; `07-implementation-planning` should correct `IP-9010`'s citation from `NFR-7000` to `NFR-6100` next time that package is touched (informational only, does not block anything downstream). | Partially resolved (`04-requirements-engineering` run, 2026-07-10, 04-delta batch): RTM cell filled. **`IP-9010`'s own citation still reads `NFR-7000`** — rides a future `07-implementation-planning` touch of that package (out of `04`'s write scope), not blocking anything. **Fully resolved (07-touch, 2026-07-10):** `IP-9010`'s citation corrected to `NFR-6100`. | DONE |
| BL-0027 | 2026-07-07 | doc-defect | `08-code-implementation` run #25 (IP-9040) | **`.claude/skills/run-bunnygarden/SKILL.md` still documents the pre-`IP-9010` hardcoded paths** (`/mnt/user-data/outputs/BunnyGarden.gbc[.ram]`, `/home/claude/bunnygarden/`) and the stale `BunnyGarden.gbc` filename, even though `test_rom.py` has run repo-relative against `BunnyQuest.gbc` since `IP-9010` (run #22). Not named by `BL-0007`/`IP-9030` (which scope only `Claude.md`/`memory.md`/`README.md`) — a genuine gap between what those track and what's actually still stale. Found incidentally while archiving the legacy artifacts (`IP-9040`); out of that package's scope to fix. | Low | 07 | SCHEDULED: fold into a future `07-implementation-planning` hygiene pass (natural rider for `IP-9030`'s root-doc refresh, or its own small package) — update the skill doc's commands/paths to match `test_rom.py`'s current repo-relative form. Not blocking any current work. **Resolved (07-touch, 2026-07-10):** the skill doc was far more stale than just paths (stale filename, "v2.1"/"88/88" status line, "gifts" variable naming) — rewritten in full to match current reality, with forward notes flagging where FEAT-1100/FEAT-9000 will change two gotchas once those still-unimplemented packages ship. | DONE |
| BL-0028 | 2026-07-07 | doc-defect | `09-package-verification` runs #27/#28 ([VR-1010](../implementation/verification/VR-1010-per-zone-scoreitem-persistence.md) finding #1, [VR-9020](../implementation/verification/VR-9020-score-bar-vblank-fix.md) finding #1) | **Stale "pending independent verification by `09-package-verification`" clauses in the requirements/architecture record** — the verifications now exist, so the hedges are outdated: RQ-02's `NFR-5200` status line (VR-1010), RQ-02's `NFR-1200` status line + GDS-06 N2's remediation note (VR-9020), and the RTM `NFR-1200` cell's snapshot "111/111" count (125/125 today). Cosmetic only; every underlying cell/claim is accurate. **Widened run #28** to cover both VRs' instances in one entry. | Low | 04 | SCHEDULED: fold into the next `04-requirements-engineering` delta batch alongside BL-0020 (missing sprite-rendering FR), BL-0022 (FR-3200 postcondition correction), and BL-0026 (NFR-6100 RTM Test cell) — one pass; the GDS-06 N2 pointer note rides the same pass (03-owned, one-word edit). Not blocking any current work. **Resolved (`04-requirements-engineering` run, 2026-07-10, 04-delta batch):** RQ-02's NFR-5200/NFR-1200 status lines, the RTM's NFR-1200 snapshot count, and NFR-7100's own identical staleness (caught incidentally) all refreshed; GDS-06 N2's pointer note also fixed (a trivial one-line edit, done alongside despite being 03-owned). | DONE |
| BL-0029 | 2026-07-09 | feature | intake: project owner — adopted increment plan ([PLAN-requirements-aesthetics-story-map.md](PLAN-requirements-aesthetics-story-map.md) §4 stream A; owner direction D4 in §0) | **Stream A — aesthetic-quality standard.** Adopt a vision-level presentation-quality commitment and a *normative* aesthetic standard (GDS-08 delta), then derive measurable presentation NFRs, per the adopted plan's Phase 1→3→4 path. Owner's quality bar on record (D4): the result must be **smooth** and **every screen/room/view clean**. Research grounding already exists (R202/R203/R208/R209/R210/R211, filed via BL-0013 — adjacent, not duplicate: that entry produced the research; this one produces the standard and requirements). No duplicate found (checked BL-0001…BL-0028). | Medium (owner: part of adopted increment; no urgency stated) | 01 → 03 → 04 (per plan §1) | **DONE (`00-pipeline-manager` run #42):** stage 01 (C8) → stage 03 (GDS-08 §7 normative standard) → stage 04 (NFR-1300/NFR-6500/NFR-6510, RQ-01..04 delta) all complete. This entry's full plan-stated path is closed; remaining work (05-feature-decomposition onward) rides the normal Master Build Plan, not this backlog entry. | DONE |
| BL-0030 | 2026-07-09 | feature | intake: project owner — adopted increment plan ([PLAN-requirements-aesthetics-story-map.md](PLAN-requirements-aesthetics-story-map.md) §4 stream B; owner direction D1/D2 in §0) | **Stream B — visual story narrative via biome-flow grammar.** New vision-level scope: the game tells its story visually through a **logical biome adjacency grammar between whole screens** (e.g. water→beach→grassland→hills→mountains→sky; one biome per screen, never two in a frame, no disjointed pairings), with an **item-agnostic, child-friendly collect-goal** following the narrative (carrots are an instance, not a requirement). **No text dialogue requirement** (D1 — not ruled out, not required; a future dialogue request re-enters at 01). Agent constructs the grammar from research (proposed R212, plan Phase 2). No duplicate found. | Medium (owner: part of adopted increment) | 01 → 02 → 03 → 04 (per plan §1) | **DONE (`00-pipeline-manager` run #42):** stage 01 (C9) → 02 (R212) → 03 (GDS-01 §3a, GDS-04 KeyItem/grammar rules, GDS-08 §8) → 04 (FR-3220/4300/4310, RQ-01..04 delta) all complete. This entry's full plan-stated path is closed. | DONE |
| BL-0031 | 2026-07-09 | feature | intake: project owner — adopted increment plan ([PLAN-requirements-aesthetics-story-map.md](PLAN-requirements-aesthetics-story-map.md) §4 stream C; owner direction D3/D5/D6/D7/D8/D9/D10 in §0) | **Stream C — deterministic procgen world from (seed, scale) + menu-flow delta + 3×3 archival.** The world becomes **deterministically procedurally generated** from a user-modifiable **seed** and **world-scale** parameter, both set **only at new-game start** and immutable per save (D3/D6/D7); named research topics: procgen map algorithms and GBC homebrew with procgen maps (proposed R213/R214) — the agent picks the algorithm from research. **Menu-flow delta (D8/D9/D10):** boot → title → **main menu** (continue / new game → seed & scale entry), **no auto-load at boot** (amends MSTR-001 C2's auto-load clause — deliberate protected-baseline change), and the start-button save menu gains **exit-to-main-menu with auto-save**. The handcrafted 3×3 world is **archived** per the `legacy/` precedent (D5 — second protected-baseline change, C5). Cross-ref: **closes BL-0015** (wider vs deeper) per plan §6 — scale is now a user parameter, the fork dissolves; manager to flip BL-0015 at triage with that rationale. No other duplicate found. | Medium (owner: part of adopted increment) | 01 → 02 → 03 → 04 (per plan §1) | **DONE (`00-pipeline-manager` run #42):** stage 01 → 02 (R213/R214/R111) → 03 (ADR-0009/0010/0011 + six GDS deltas) → 04 (FR-9100–9200, NFR-1300/2200/4200/5300, RQ-01..04 delta) all complete. This entry's full plan-stated path is closed — **the adopted increment plan's terminal deliverable (Phase 4) is reached.** | DONE |
| BL-0032 | 2026-07-09 | doc-defect | `02-research-gbc-hardware` run #33 (R102 extension) | **R101 and R102 cite two different figures for VBlank's duration** — R101 says "~1140 T-state VBlank period," R102 says "lines 144–153, ~4560 T-states total." Neither topic resolves the discrepancy; R102's new full-screen-redraw-cost analysis (grounding MSTR-001 C10/C8) flagged it inline rather than silently picking one, since the qualitative conclusion (a 1152-byte tilemap copy doesn't fit in VBlank regardless of which figure is right, hence the project's existing LCD-off approach) holds either way. | Low | 02 | SCHEDULED: a future `02-research-gbc-hardware` pass should directly fetch Pan Docs' VBlank-duration figure (both prior citations rested on earlier sessions' fetches or WebSearch summaries) and correct whichever of R101/R102 is wrong. Not blocking any current work — the redraw-cost conclusion doesn't depend on the exact figure. **Resolved (`02-research-gbc-hardware` run, 2026-07-10):** directly fetched Pan Docs (GitHub mirror) — VBlank is exactly 4560 T-states. R101's "~1140 T-state" was a units error (1140 is the M-cycle count, mislabeled); corrected to cite both units. R102's figure upgraded from approximate to exact. | DONE |
| BL-0033 | 2026-07-09 | doc-defect | `03-architecture-design-synthesis` run #37 (GDS-04 delta, incidental finding) | **GDS-04's "Relationships worth naming explicitly" §3rd bullet is stale relative to the shipped code** — it states "all `ScoreItem` positions/consumed-state per zone[] are not persisted... only the scarce tier's collection state persists," but `IP-1010`/`FS-101` (implemented and verified 2026-07-07, before this GDS-04 delta) added exactly that: `SCOREITEM_FLAGS` (`0xC060`–`0xC068`) persists per-zone `ScoreItem` collected-state to SRAM (`0xA013`–`0xA01B`, per `FR-5220`, confirmed in `GDS-07` §2/§3). GDS-04's own bullet was not updated when `FS-101` shipped — noticed incidentally while authoring the GDS-04 procgen-world delta; out of that delta's scope to fix (it's a pre-existing gap, not part of the increment). | Low-Medium | 03 | SCHEDULED: a future `03-architecture-design-synthesis` touch of GDS-04 (or a dedicated small pass) should correct the bullet to state `ScoreItem` collected-state *does* persist per-zone (citing `FR-5220`/`GDS-07`), matching `IP-1010`'s shipped behavior. Not blocking any current work. **Resolved (`03-architecture-design-synthesis` run, 2026-07-10):** bullet corrected exactly as scheduled, citing `IP-1010`/`FS-101`/`VR-1010` and matching GDS-07 §2/§3's WRAM/SRAM tables. | DONE |
| BL-0034 | 2026-07-09 | doc-defect | `03-architecture-design-synthesis` run #41 (GDS-10 delta, incidental finding) | **GDS-10's own merge decision (2026-07-06) was never executed.** It states: "this level stays authoritative for the traceability scheme... until `RQ-04` is authored, at which point `RQ-04` becomes authoritative for row-level data and this level's §3/§4 become the pointer." `RQ-04` was authored at run #17 (2026-07-06, same day) and is already a populated, correctly-cross-referencing matrix — but GDS-10's §3/§4 were never converted to pointers, and its ID-scheme table (§2) sat stale (showing `RQ`/`FP`/`FS`/`IP`/`VR` as "not yet authored" through the entire bootstrap increment) until this same run refreshed the example column only. Noticed while confirming the ID scheme extends to the procgen-world increment's new artifacts; the deeper restructuring (§3/§4 → pointers to `RQ-04`) is out of that light-delta's scope. | Low | 03 | SCHEDULED: a future `03-architecture-design-synthesis` pass should convert GDS-10 §3/§4 into short pointers to `RQ-04`, per this document's own already-recorded merge decision — a bootstrap-increment housekeeping item, not blocking any current work. **Resolved (`03-architecture-design-synthesis` run, 2026-07-10):** §3 converted to a pointer at the populated `RQ-04`; §4 confirmed as an actively-exercised traceability lane (citing `BL-0029`/`0030`/`0031`'s own three-stage thread). Closes the last open item from GDS-10's original merge decision. | DONE |
| BL-0035 | 2026-07-10 | doc-defect | `10-integration-review` run (bootstrap tranche, [integration-review-bootstrap-tranche.md](../reviews/integration-review-bootstrap-tranche.md) finding #1) | **`ROADMAP.md`'s Implementation theme table (`IM-00`/`IP-xxxx`/`VR-xxxx` rows) is stale** — `IP-xxxx` still reads "authored 2026-07-07; none implemented" and `VR-xxxx` still reads "⛔", even though all five bootstrap packages are now `VERIFIED` with five Verification Reports on record. No stage-08/09 run touches this particular ROADMAP table. | Medium | 07 | SCHEDULED: a future `07-implementation-planning` touch (or a small standalone hygiene pass) should refresh these three rows to state all five packages `VERIFIED`. Not blocking any current work — every other ledger (Master Build Plan, packages `INDEX.md`, verification `INDEX.md`) already tells the accurate story. **Resolved (`07-implementation-planning` run, 2026-07-10):** all three rows refreshed while authoring the Release-2 tranche's own package rows in the same table — a natural fold-in since this stage owns the table. | DONE |
| BL-0036 | 2026-07-10 | doc-defect | `10-integration-review` run (bootstrap tranche, [integration-review-bootstrap-tranche.md](../reviews/integration-review-bootstrap-tranche.md) finding #2) | **`docs/feature-planning/01-release-plan.md` §Release 1 and `03-feature-catalog.md`'s `FEAT-5100` entry both still describe it as unimplemented** ("no shipped implementation" / "new — not yet implemented"), even though `IP-1010`/`FS-101` shipped and were independently verified 2026-07-07 — these feature-planning docs were authored the same day but never revisited afterward. | Medium | 05 | SCHEDULED: rides the next `05-feature-decomposition` touch — natural to fold in alongside the procgen-world increment's own upcoming Feature Catalog additions (new `FEAT-xxxx` rows for the RQ-01…04 delta), since that pass will already be editing both documents. Not blocking any current work. **Resolved (`05-feature-decomposition` run, 2026-07-10):** both documents corrected — FEAT-5100's catalog entry now states shipped/VERIFIED status with citations to `IP-1010`/`VR-1010`; Release 1's bucket description and header both corrected. Folded in exactly as scheduled, alongside the five new Feature rows. | DONE |
| BL-0037 | 2026-07-10 | doc-defect | `05-feature-decomposition` run ([05-feature-review.md](../feature-planning/05-feature-review.md) finding #5) | **`docs/feature-planning/03-feature-catalog.md`'s `FEAT-6000`/`FEAT-7000` entries still describe `NFR-1200`/`NFR-7100` as current non-compliances in their own Risk fields** — both are now Met, VERIFIED 2026-07-07 (`IP-9020`/`VR-9020`, `IP-9010`/`VR-9010`) and reconfirmed by `10-integration-review` (2026-07-10). The equivalent statements in the Epic Catalog and Release Plan were corrected in the same pass that found this (per `BL-0036`'s instruction), but this pass's scope did not extend to editing FEAT-6000/FEAT-7000's own catalog entries. | Low | 05 | SCHEDULED: a future light `05-feature-decomposition` touch should update FEAT-6000/FEAT-7000's Risk fields to reflect both resolutions. Not blocking any current work — every ledger that actually gates on these NFRs (RTM, Master Build Plan) already shows the correct Met status; only these two Features' own prose lags. **Resolved (`05-feature-decomposition` run, 2026-07-10):** both Risk fields corrected to Low citing VERIFIED status; FP-05 finding #5 marked resolved. | DONE |
| BL-0038 | 2026-07-10 | design-question | `06-feature-specification` runs ([FS-102](../features/FS-102-procedural-world-generation.md) Open Questions 1–3, [FS-103](../features/FS-103-generated-region-screen-composition.md) Open Questions 1–2) | **FEAT-9000 (procedural world generation) and FEAT-4100 (generated-region screen composition) share five unresolved design questions before implementation can begin, all converging on one underlying deferral:** (1) the concrete biome-adjacency grammar table's exact contents — R212 gives only a conceptual example, GDS-08's delta explicitly defers exact biome-family count/assignment "to the implementation package that sizes it" (FS-102 OQ1 = FS-103 OQ1, the same deferral seen from two Features); (2) the generation algorithm's line-by-line implementation detail (ADR-0009/R213 fix the approach, not the routine's exact step order); (3) whether a generator-data ROM pointer is needed in the `patches` dict, downstream of (1); (4) the exact tile-index ranges/palette assignments for whatever biome-family set is chosen (FS-103 OQ2, also downstream of (1)). All are named with their resolving stage already identified. | Medium | 07 | SCHEDULED: all five ride the same `07-implementation-planning` pass that packages FEAT-9000/FEAT-4100 together — per GDS-08's own explicit deferral (question 1, which questions 3–4 depend on) and this stage's normal implementation-detail authority (question 2). Not escalated to the user — each has a clear, already-identified resolution path, following the FS-101 precedent for confidently-resolvable design questions. **Resolved (`07-implementation-planning` run, 2026-07-10, [IP-1020](../implementation/packages/IP-1020-procedural-world-generation.md)/[IP-1030](../implementation/packages/IP-1030-generated-region-screen-composition-code.md)/[IP-1031](../implementation/packages/IP-1031-generated-region-screen-composition-content.md)):** biome-family vocabulary = the 5 existing shipped terrain families (zero new art); linear grammar axis Water-Sand-Grass-Stone-Brick, adjacency legal iff axis indices differ by ≤1 (no ROM table needed, resolving Q3); flood-fill generation algorithm over the fixed `scale×scale` grid, satisfying determinism/reachability/grammar-validity by construction. | DONE |
| BL-0039 | 2026-07-10 | design-question | `06-feature-specification` run ([FS-104](../features/FS-104-main-menu-new-game-flow.md) Open Questions 1–2) | **FS-104 (FEAT-1100, main menu & new-game flow) has two unresolved design questions:** (1) whether SEED/SCALE ENTRY offers a cancel/back-to-MAIN-MENU path — neither ADR-0010 nor GDS-01 §4a's state diagram names one, leaving a player who selects "new game" by mistake with no way back short of completing a full seed/scale entry and generation; (2) whether "continue"/"new game" selection uses A-confirm-only or a D-pad up/down selector — an input-mapping detail GDS-01 §4a doesn't specify. | Low-Medium | 07 | SCHEDULED: both ride the `07-implementation-planning` pass that packages FEAT-1100 — question 1 is a genuine player-experience gap worth resolving deliberately (most likely a B-cancels convention matching the existing SAVE state's own A/B pattern, per FS-104's own reasoning, but not assumed there); question 2 is ordinary UI implementation detail. Not escalated to the user — both have clear, confidently-resolvable paths, following the FS-101 precedent. **Resolved (`07-implementation-planning` run, 2026-07-10, [IP-1040](../implementation/packages/IP-1040-main-menu-new-game-flow.md)):** B cancels SEED/SCALE ENTRY back to MAIN MENU (matching SAVE's own A/B convention); D-pad up/down selects the highlighted MAIN MENU option, A confirms. | DONE |
| BL-0040 | 2026-07-10 | gate | `00-pipeline-manager` run #53 (both main-thread gates asked in one batch, per Step 4) | **Two G3/G4-class human decisions, both resolved in the same user response:** (1) Bootstrap tranche (Baseline + Release 1, 5 VERIFIED packages, clean integration review) — user directed **"Run 11 release with a GO"**, i.e. produce the evidence-based Release Assessment and record the GO on the strength of that evidence. (2) Release 2 tranche (procgen-world: `IP-1020`/`1030`/`1031`/`1040`/`1050`, genuinely new work, no bootstrap carve-out) — user selected **"Authorize all five."** | High (unblocks both stalled threads) | 11 / 08 | **Resolved same run.** Part (1): `11-release-readiness` invoked this run — [release-assessment-bootstrap-tranche.md](../reviews/release-assessment-bootstrap-tranche.md) written, GO recorded, baseline flipped (ROADMAP.md RV-RELEASE row, `01-release-plan.md` bucket headers, `Claude.md` status line, reviews INDEX). Part (2): G3 authorization recorded here for `IP-1020`/`1030`/`1031`/`1040`/`1050` — rides the next `00-pipeline-manager` advance (`08-code-implementation` on `IP-1020`, the tranche's unblocked foundational package; the other four stay `BLOCKED` on `IP-1020` per the Master Build Plan's own dependency graph, not on authorization). | DONE |
| BL-0041 | 2026-07-10 | doc-defect | `00-pipeline-manager` run #54 drift correction + `09-package-verification` run ([VR-1020](../implementation/verification/VR-1020-procedural-world-generation.md) finding #2) | **`ROADMAP.md`'s `IM-00`/`IP-xxxx` summary rows (lines 109/111) went stale within the same increment that produced `BL-0035`'s identical fix.** They still read "Release 2 tranche's 5 packages planned 2026-07-10, none authorized"/"not authorized — G3 pending" even after (a) the user's explicit G3 authorization of all five packages (`BL-0040`) and (b) `IP-1020`'s own `COMPLETE`→`VERIFIED` transition (this run) — neither the `08-code-implementation` run that implemented `IP-1020` nor the `00-pipeline-manager` run that recorded the G3 answer touched this particular table. Same recurring pattern as `BL-0035` (bootstrap tranche, same table, same root cause: no stage-08/09 run owns this ROADMAP table by default). Also discovered: the journal's run #53 Position block recorded "next step: `08-code-implementation` on IP-1020" as still-pending, when `IP-1020` had already gone `COMPLETE` outside a journaled run by the time run #54 started — corrected in run #54's journal entry, not a backlog item of its own (journal-only drift, no ledger consequence beyond the journal itself). | Medium | 07 | SCHEDULED: rides the next `07-implementation-planning` touch (that stage owns this ROADMAP table per `BL-0035`'s precedent), or a dedicated backlog-hygiene pass if none comes soon. Not blocking any current work — every other ledger (Master Build Plan, `packages/INDEX.md`, verification `INDEX.md`) already tells the accurate story after this run's updates. Recommend: **fold in a standing note** to whichever stage next edits `ROADMAP.md`'s Implementation theme table to double-check `IM-00`/`IP-xxxx`/`VR-xxxx` currency, since this is the second time in one increment the same table drifted unnoticed. **Resolved (`07-implementation-planning` run, 2026-07-11):** all three rows refreshed — Release 2 authorized, 4/5 packages `VERIFIED`, `IP-1031` `COMPLETE`; `VR-xxxx` now lists all nine existing reports. | DONE |
| BL-0042 | 2026-07-10 | doc-defect | `00-pipeline-manager` run #55 Step-1 drift correction | **`packages/INDEX.md` had gone stale for `IP-1030`/`1040`/`1050` — the exact `BL-0041`/`BL-0035` recurrence, one ledger over.** By the time this run started, `IP-1030`/`1040`/`1050` had all been implemented out-of-band (commits `3479dba`/`73cea72`/`5f58ab5`, following run #54's "Then move onto IP" instruction) and stood `COMPLETE` (136/136, 163/163, 180/180) in the Master Build Plan — but `packages/INDEX.md` still read all three `READY`, and the journal's own run #54 Position block recorded "next step: `08-code-implementation` on IP-1030" as still-pending. The out-of-band `08-code-implementation` sessions updated the Master Build Plan (per their own commit messages) but not `packages/INDEX.md`, the same split-ledger-ownership gap `BL-0035`/`BL-0041` already named for `ROADMAP.md`. | Medium | 09 | **Resolved same run.** This run's `09-package-verification` invocation on `IP-1030` corrected its own `packages/INDEX.md` row to `VERIFIED` (unblocking `IP-1031` to `READY`) as part of writing `VR-1030`; the pipeline manager additionally corrected `IP-1040`/`1050`'s rows from stale `READY` to accurate `COMPLETE` (awaiting their own future verification) while reconciling this run, since leaving them `READY` would have been actively misleading (both are already implemented, not merely eligible). Recommend: **fold in a standing note** to `08-code-implementation`/`08-content-authoring`'s own workflow to update `packages/INDEX.md` in the same commit that flips a package `COMPLETE` in the Master Build Plan, since this is now the second ledger (after `ROADMAP.md`) shown to drift from the same root cause (out-of-band sessions touching one ledger but not its sibling). | DONE |
| BL-0043 | 2026-07-11 | recommendation | `08-content-authoring` run (IP-1031) | **The 07→08 code/content package split assumed content work remained separable from code work, but in practice `IP-1030` (code half) delivered `IP-1031`'s (content half) entire payload as an inherent side effect.** `IP-1030` generalized `ALL_SCREENS` from a fixed 14-entry list to a 5-biome-family + 5-UI shape — to keep the code buildable/testable, it had to choose *some* default `(family_name, fn)` mapping, and the mapping it chose (water→lake_screen, sand→beach_screen, grass→forest_screen, stone→mountain_screen, brick→castle_screen) turned out to be exactly what `IP-1031` was planned to author. `IP-1031`'s actual run found zero code left to write — it became a confirmation-and-documentation pass instead. Not a defect (both packages' Definitions of Done are independently satisfied, and 09-package-verification still has a real, non-trivial job confirming IP-1031's DoD claims), but a planning-time blind spot: a code package that generalizes a data structure will often need to populate that structure's *default values* to stay buildable, silently pre-empting a paired content package's reason to exist. | Low | 07 | SCHEDULED: `07-implementation-planning` should watch for this coupling risk when splitting a "generalize the shape" code package from a "populate the shape" content package sharing one data structure — either sequence them so the code package's default values are deliberately left minimal/placeholder (forcing the content package to do real work), or fold both into one package when the split is this tight. Not blocking any current work — no packages are currently mid-split in this shape. | SCHEDULED |
| BL-0044 | 2026-07-11 | finding | `09-content-review` run ([content-review-IP-1031.md](../reviews/content-review-IP-1031.md) finding #1) | **The Stone(3)↔Brick(4) biome-family adjacency is the one grammar-legal pair GDS-08 delta §8's worked example doesn't cover, and it has the largest hue jump of the four pairs the review exercised** (achromatic gray → saturated warm red, versus the other three pairs' close match to the cited water→sand→grass→stone worked example). Not arbitrary — a gray-stone-to-red-brick fortress reading is thematically plausible — but a genuinely different kind of "step" than the rest of the axis. | Low | 03 | SCHEDULED: a future `03-architecture-design-synthesis` touch of GDS-08 §8 could extend the worked example to explicitly address a gray→masonry-red step, confirming or revising this axis's terminal pairing. Informational per NFR-6510's own "Should," not "Must" priority — not blocking any current work. | SCHEDULED |
| BL-0045 | 2026-07-11 | doc-defect | `09-content-review` run ([content-review-IP-1031.md](../reviews/content-review-IP-1031.md) finding #3) | **`NFR-6500`/`NFR-6510`'s Status fields still read "NOT YET IMPLEMENTED... no biome-family palette assignments exist yet to check"** — stale now that `09-content-review` has exercised both NFRs' own Acceptance Criteria against `IP-1031`'s content with a documented clean result (one Low/informational finding, no Critical/High/Medium). Out of `09-content-review`'s own write scope (requirements documents are `04-requirements-engineering`'s). | Low | 04 | SCHEDULED: a future `04-requirements-engineering` delta should flip both NFR-6500/NFR-6510 to Met, citing `content-review-IP-1031.md`. Not blocking any current work. **Resolved (`04-requirements-engineering` run, 2026-07-11):** both flipped to Met, RTM rows/changelogs/INDEX updated, Requirements Review finding #12 confirms the flip against the actual review evidence. | DONE |
| BL-0046 | 2026-07-11 | doc-defect | `00-pipeline-manager` run #60 (BL-0041 fix, incidental finding) | **`ROADMAP.md`'s `FS-101+` row (Features theme table) is stale in the same way the Implementation theme table was** (`BL-0041`/`BL-0035`'s recurring pattern, a third table now) — it still reads "FS-102–106 ✅ 2026-07-10, all five Release-2 Features specified, **none yet implemented** — 7 Open Questions total routed to `07`," even though all five are now implemented (4 packages `VERIFIED`, one `COMPLETE`) and every routed Open Question was resolved at `07-implementation-planning`'s own pass (2026-07-10). Noticed while fixing `BL-0041`'s adjacent Implementation-theme table; out of that fix's own named scope (Features theme is `06-feature-specification`'s table, not `07`'s). | Low | 06 | SCHEDULED: a future `06-feature-specification` touch (or a dedicated small hygiene pass) should refresh the `FS-101+` row to state all five Release-2 Features implemented, zero Open Questions outstanding. Not blocking any current work — every other ledger (Master Build Plan, Features `INDEX.md`) already tells the accurate story. **Resolved (`06-feature-specification` run, 2026-07-11):** `ROADMAP.md`'s `FS-101+` row and `docs/features/INDEX.md`'s FS-102–106 rows all refreshed; `FS-102`'s own forward-reference metadata (still said "verification pending" though `VR-1020` has existed since 2026-07-10) also caught and fixed in the same pass. | DONE |
| BL-0047 | 2026-07-11 | bug | intake: project owner ("The map remains a 3x3 grid regardless of map scale and seed. It should not be a grid. It should be a procedurally generated shaped map.") | **`check_zone_transition` (`asm_game.py:566`) was never updated by `IP-1020`/`1030` — it is still 100% the pre-procgen, hardcoded fixed-9-zone 3×3 logic** (`CUR_ZONE` compared against literal `2`/`5`/`8` for the right edge, `0`/`3`/`6` for the left edge, `<3`/`>=6` for top/bottom, `±1`/`±3` arithmetic to move between "zones") — it never reads `REGION_GRAPH` at all, unlike `dsr_p`/`draw_region_arrows` (rendering), which correctly do. **Reproduced directly:** new game with `seed=7, scale=5` (25 generated regions) — `generate_world` correctly populates a 25-region `REGION_GRAPH` (confirmed by `T12`'s own suite), but walking right from region 0 only ever reaches `CUR_ZONE=2` and then hard-stops, exactly the old 3×3 model's right-edge zone — the other 22 generated regions are completely unreachable regardless of `WORLD_SCALE`. At `scale=3` this coincidentally looks correct only because the old hardcoded topology happens to match a 3×3 layout by accident, not because navigation is actually driven by the generated graph. Confirms the user's report exactly: **the traversable world is always a 3×3 grid, independent of `WORLD_SCALE`/seed**, contradicting `ADR-0009`'s decision that navigation should read the generated adjacency graph and `FR-9100`'s determinism/scale guarantee (generation is real; navigation ignores it). | **Critical** — the procgen-world increment's headline capability (`WORLD_SCALE`-driven map size) is non-functional for any scale ≠ 3, and even at scale 3 the "generated" navigation is coincidental, not real | 07 | Not deferred (Critical). **Remediation shape:** `check_zone_transition` needs to be regeneralized to read `REGION_GRAPH`'s per-region neighbor bytes (the same 5-byte-per-region structure `dsr_p`/`draw_region_arrows` already correctly consume) instead of the hardcoded `CUR_ZONE` arithmetic — mirroring the generalization `IP-1030` already did for rendering, just applied to the navigation half that was missed. Separately: the user's "it should not be a grid, procedurally generated shaped map" framing asks for a non-rectangular world *topology*, which is a different, larger ask than "navigation should respect the grid `WORLD_SCALE` already generates" — `ADR-0009`'s own Decision explicitly commits to a `scale`×`scale` region grid (a deliberate, cited architecture decision, R213/R214-grounded), so a genuinely non-grid/organic world shape would reverse that ADR and needs its own `03-architecture-design-synthesis` pass, separate from this remediation. Recommend the `07` remediation package fix the grid-navigation defect first (restores the already-decided/already-generated behavior); route the non-grid-shape request as its own design-question if the user wants it pursued after seeing the grid actually work end-to-end. **Implemented (`IP-9050`, 2026-07-11): `check_zone_transition` fully regeneralized to `REGION_GRAPH`, 213/213 pass — `IP-9050` reached `COMPLETE`.** Status stays `SCHEDULED`, not `DONE`, pending independent `09-package-verification` (blocked this session on the same-session independence rule). The non-grid-shape follow-up materialized as this session's `BL-0064`–`BL-0067` cluster, exactly as anticipated above. | SCHEDULED |
| BL-0048 | 2026-07-11 | bug | intake: project owner ("Once a game has been saved and the Gameboy is power cycled, the main menu offers continue and new game; however, it is not possible to select new game... up/down arrows should move the cursor.") | **`check_save_valid` (`asm_game.py:1290`) unconditionally resets `MM_CURSOR` to `0` ("continue") every time it runs — and `mm_on_entry` calls `check_save_valid` on *every* MAIN MENU redraw, including the redraw the player's own UP/DOWN toggle just triggered**, immediately clobbering the toggle back to `0` before the cursor is ever drawn at the new position. **Reproduced directly:** boot with a valid save present (`MM_SAVE_VALID=1`), `MM_CURSOR` starts at `0`; pressing DOWN twice then UP once — `MM_CURSOR` reads `0` after every single press. The screen full-redraws each press (LCD-off `copy_screen`, matching the reported "flash white"), but the cursor tile is always drawn at the "continue" position regardless of input, because `check_save_valid`'s own reset runs again immediately after the toggle. With any save present, **"new game" is completely unreachable from MAIN MENU** — the SEED/SCALE ENTRY flow (and therefore starting any new world) cannot be entered at all once a save exists. | **High** — total UI dead-end for a core, just-shipped flow (FR-1170/1180); the only workaround is deleting/corrupting the save file outside the game | 07 | Not deferred (High, a real functional block, not cosmetic). **Remediation shape:** `check_save_valid`'s `MM_CURSOR`-reset side effect needs to stop firing on every redraw — either split "recompute `MM_SAVE_VALID`" from "reset `MM_CURSOR` to its default," calling the reset only on genuine state-entry (boot into MAIN MENU, arrival from VICTORY/SAVE) and not on the redraw a same-state cursor toggle causes, or have `st_main_menu`'s toggle path skip re-invoking `check_save_valid` entirely (it already knows `MM_SAVE_VALID` didn't change, only `MM_CURSOR` did). **Implemented (`IP-9060`, 2026-07-11): the reset moved to `mm_on_entry`, gated on a new `MM_JUST_ENTERED` flag; 205/205 pass — `IP-9060` reached `COMPLETE`.** Status stays `SCHEDULED`, not `DONE`, pending independent `09-package-verification` (blocked this session on the same-session independence rule). | SCHEDULED |
| BL-0049 | 2026-07-11 | bug | intake: project owner ("...there is the option to push a or b to either save or exit back to game but there is no option to return to the main menu... the a or b selection method may need to be reconsidered.") | **The exit-to-main-menu mechanism (`st_save`'s `SELECT` branch, `IP-1040`) is actually wired and functional — reproduced directly, `GameState` goes `SAVE(3)`→`MAIN MENU(6)` on a `SELECT` press — but `save_screen()` (`tilemaps.py:309`) only renders `"A: YES"` / `"B: NO"`, with no on-screen text for the third option at all.** From the player's seat this matches the report exactly: there is no *visible, discoverable* option to return to the main menu, even though a hidden `SELECT` binding exists. Confirmed by direct screenshot of the SAVE menu — only two lines of text render. | Medium — a real feature (`FR-1190`) is present in code but functionally undiscoverable without reading source, which for a player is indistinguishable from "the option doesn't exist" | 07 | SCHEDULED (not urgent — a genuine path exists today, just hidden): a `07-implementation-planning` remediation should (1) add on-screen text for the third option, and (2) resolve the user's own explicit design question — whether to keep `A`/`B`/`SELECT` (three buttons, one option each) or move to a cursor-based two-button scheme matching MAIN MENU's own UP/DOWN-select/A-confirm convention (the user only asks that `B` remain "exit back to game"). Recommend following this project's existing precedent of `07` resolving confidently-answerable UI-input-mapping questions directly (per `FS-104` OQ2's own resolution) rather than escalating, unless the chosen scheme has to touch `save_screen`'s tile budget in a way that needs a second opinion. | SCHEDULED |
| BL-0050 | 2026-07-11 | design-question | intake: project owner ("The select button map menu brings up the status of collectibles in a 3x3 grid which are named by zone. This is leftover from the original layout and no longer meets the intent. A high level replanning... is needed.") | **`map_screen()` (`tilemaps.py:319`) is confirmed, by direct code read, to be the unmodified pre-procgen fixed-9-zone status screen** — a hardcoded 3×3 grid of literal zone-name labels (`"BCH"`, `"FOR"`, `"MTN"`, `"LAK"`, `"VIL"`, `"CAV"`, `"DES"`, `"PLN"`, `"CTL"`) with one heart-icon per cell, entirely independent of `REGION_GRAPH`, `WORLD_SCALE`, or the item-agnostic `KEYITEM_FLAGS`/`SCOREITEM_FLAGS` model `IP-1020` introduced. No `IP-102x` package ever touched this screen. Not merely stale content (unlike `BL-0046`'s doc-drift pattern) — the *shape itself* (a fixed 3×3 grid, 9 named cells) has no coherent mapping onto a generated `scale`×`scale` world (`scale` ranges 2–9, i.e. 4–81 regions, with biome-family screens reused across many regions, not one unique named zone per cell) — this is exactly the kind of gap the user is flagging: there's no obvious "quick status" shape carried over from the old design that still fits. | Medium — cosmetically confusing/misleading today (a player checking progress sees zone names that don't correspond to anything in the generated world), but doesn't block play | 04 | **NEEDS-USER at triage, not a mechanical fix.** The user's own framing ("a high level replanning... is needed") is itself a request for design work, not a specific fix — before any `FS`/`IP` can be authored, `04-requirements-engineering` (or `03` if it turns out to need new architecture, e.g. a scrollable/paginated per-region list) needs to establish *what a generated-world-appropriate quick-status display should show* (candidates: aggregate world-wide collectible/KeyItem counts; a scrollable list of visited regions; a compact per-biome-family summary — none of these are decided anywhere upstream). Recommend the pipeline manager surface this as a real design decision for the user rather than let a downstream stage invent an answer unilaterally, given the user explicitly asked for replanning rather than a patch. | NEEDS-USER |
| BL-0051 | 2026-07-11 | bug | intake: project owner ("The bunny appears to collide with the top of the map one tile early... The top of the bunny should touch the topmost edge of the map.") | **The UP movement clamp (`asm_game.py:469-472`) stops the sprite's top edge (`PLAYER_Y`) at a minimum of `17`, but the actual playable field starts at tile row 1 (pixel `y=8`)** — confirmed by direct code read of `beach_screen()`/`forest_screen()` (`tilemaps.py`), whose terrain fill begins at `y=1` (row 0 is the single-tile-tall HUD via `_score_bar`, writing only row 0). Reproduced directly: holding UP from spawn for 200 frames settles at `PLAYER_Y=17`, ~9px short of the field's true top edge — close to a full 8px tile, matching the reported "one tile gap." The analogous DOWN clamp (max `PLAYER_Y=128`, sprite bottom at `128+15=143`) is correctly flush with the screen's exact bottom edge, confirming top/bottom are asymmetric — only the top clamp is wrong. | Low-Medium — movement-precision defect, doesn't block play but is visibly incorrect and asymmetric with the (correct) bottom clamp | 07 | SCHEDULED: a `07-implementation-planning` remediation should correct the UP clamp's magic bound from `17`/skip-below-or-equal-17 to `8` (mirroring the DOWN clamp's already-correct pattern: allow decrement while `PLAYER_Y > 8`, clamp floor at `8`). Small, self-contained, easily paired with `BL-0052` (same function, same class of defect) in one remediation package. | SCHEDULED |
| BL-0052 | 2026-07-11 | bug | intake: project owner ("The bunny is able to go one bunny width past the far right edge of the map... The bunny should collide with the edge and stop at the rightmost edge.") | **The RIGHT movement clamp (`asm_game.py:450-455`) allows `PLAYER_X` (sprite left edge) up to `159`, but the sprite is 8px wide and the screen is 160px wide (`x` 0–159)** — the correct flush maximum is `152` (`160-8`), matching the sprite's own width the way the LEFT clamp (min `0`, correctly flush) already does. Reproduced directly: holding RIGHT to the wall settles at `PLAYER_X=159`, meaning the sprite occupies screen `x∈[159,166]` — 7 of its 8 pixels render past the visible right edge, matching "about one bunny width" past the edge as reported. | Low-Medium — movement-precision defect (asymmetric with the correctly-flush LEFT clamp), doesn't block play but lets the sprite visibly clip off-screen | 07 | SCHEDULED: a `07-implementation-planning` remediation should correct the RIGHT clamp's comparison from `CP_n(160)` to `CP_n(153)` (so the write is skipped once the incremented value would reach `153`, capping the written maximum at `152`) — mirroring the LEFT clamp's already-correct pattern. Small, self-contained, easily paired with `BL-0051` (same function, same class of defect) in one remediation package. | SCHEDULED |
| BL-0053 | 2026-07-11 | bug | intake: project owner ("The bunny collects items above the topmost edge of the sprite but does not collect items which overlap with the bottommost edge of the sprite. The bunny should collect and only collect items with which the sprite overlaps.") | **`check_collisions`' (`asm_game.py:495`) pickup test is `\|PLAYER_X - item_x\| < 10 AND \|PLAYER_Y - item_y\| < 10` — a symmetric ±9px window centered on `PLAYER_X`/`PLAYER_Y`, which are the sprite's *top-left corner*, not its center.** The sprite's true bounding box is `X∈[PLAYER_X, PLAYER_X+7]` (8px wide), `Y∈[PLAYER_Y, PLAYER_Y+15]` (16px tall, 8×16 OBJ mode). The symmetric window therefore reaches ~9px *above* `PLAYER_Y` (well outside the sprite, which has zero extent above its own top edge) while only reaching `PLAYER_Y+9` downward — 6px short of the sprite's actual bottom edge at `PLAYER_Y+15`. **Reproduced directly** (synthetic collectible placed via WRAM, sprite at `Y∈[80,95]`): an item at `y=75` (5px *above* the sprite's top edge, outside the true bounding box) **is incorrectly collected**; an item at `y=94` (genuinely overlapping the sprite's bottom edge, inside the true bounding box) **is not collected** — both exactly matching the reported symptom. (The X-axis window has a smaller, unreported asymmetry too — ~9px generous to the left of the sprite vs. ~2px generous to the right — noted for whoever fixes this, not filed as a separate bug since the user only reported the vertical case.) | Medium — a core-mechanic correctness/fairness defect: items are missed despite visual overlap and collected despite no overlap, on every single pickup in the game | 07 | Not deferred (a real, constantly-visible correctness bug in the core loop, even though severity is Medium not Critical/High). **Remediation shape:** replace the symmetric ±9/±9 window with an actual axis-aligned bounding-box overlap test against the sprite's real extent (`X∈[PLAYER_X, PLAYER_X+7]`, `Y∈[PLAYER_Y, PLAYER_Y+15]`, vs. the item's own footprint) plus whatever collection-forgiveness margin is actually intended (if any) — a `07-implementation-planning` package should decide the exact intended tolerance (exact-overlap-only vs. a small deliberate margin) since the user's ask ("collect and only collect items with which the sprite overlaps") reads as wanting exact-overlap, not a margin at all. | SCHEDULED |
| BL-0054 | 2026-07-11 | recommendation | user-requested retrospective on `BL-0047`–`BL-0053` (root-cause pattern analysis, this session) | **Process gap — `07-implementation-planning` decomposition missed an entire "verb" of the procgen-world capability.** `IP-1020`/`IP-1030` packaged "generate the world" and "render from the generated world" but no package's Files-to-Modify ever named `check_zone_transition` (navigation) or `map_screen`/status-review screens (`BL-0047`/`BL-0050`) — the increment superseded the fixed-3×3 model in some call sites (`_zone_arrows`, rendering dispatch) and silently left it live in others. Nothing in the `07` workflow currently asks "for a capability spanning generate/render/navigate/persist/review, is there a package (or explicit deferral) for every verb?", nor "when this change supersedes an old model, what *other* artifacts still encode the old model?" (a `grep`-driven sweep for the superseded pattern — literal `CUR_ZONE` grid-index comparisons, hardcoded 3×3 labels — would have flagged both `BL-0047` and `BL-0050` before implementation). | High (two of the six root causes — `BL-0047`/`BL-0050` — trace directly to this gap, and `BL-0047` is Critical) | 07 | SCHEDULED: `07-implementation-planning`'s own `SKILL.md` (Step 1, Technical Work Breakdown) should gain an explicit "verb inventory" discipline for any capability that spans generate/render/navigate/persist/review — enumerate every verb the requirements imply and require a package or a named, deliberate deferral for each — plus a "supersession sweep" step whenever a package retires an old model: search the tree for other call sites still encoding the pattern being retired, not just the one the package's own Files-to-Modify names. **Resolved (user-directed skill-file edit, 2026-07-11):** `07-implementation-planning/SKILL.md` Step 1 now carries both disciplines verbatim, cited to `BL-0047`/`BL-0050`/`BL-0054`; two new Quality gate checklist items make both mandatory going forward. | DONE |
| BL-0055 | 2026-07-11 | recommendation | user-requested retrospective on `BL-0047`–`BL-0053` (root-cause pattern analysis, this session) | **Process gap — `09-package-verification`'s permanent gates (G5: ROM builds, full suite passes) never require actually driving the feature at a non-default parameter value.** `VR-1020`/`VR-1030` both independently confirmed `generate_world`/`ALL_SCREENS` dispatch correct — against the suite's own fixtures, which always enter play at `WORLD_SCALE=3` (`advance_to_playing`'s hardcoded default). `BL-0047` (navigation ignores `REGION_GRAPH` entirely) was invisible to both VRs because nothing in either verification pass drove the game at `scale≠3` — the coincidental agreement between the old hardcoded 3×3 zone math and a `scale=3` generated world masked the defect completely. `VR-1020`/`VR-1030` are not wrong given what they checked; the checklist itself had no item that would have caught this. | High (the single highest-leverage gap — this would have caught `BL-0047`, the Critical bug, before it ever reached the user) | 09 | SCHEDULED: `09-package-verification`'s `SKILL.md` should add a standing verification-checklist item for any package whose Definition of Done references a tunable/generated parameter (`WORLD_SCALE`, seed, etc.) — an actual emulator drive at a *non-default* value, not just a memory-state assertion against the oracle. This is stronger than "run the suite" (the suite's own fixtures share the same scale=3 default blind spot) — it needs to be a live, driven check the verifier performs itself. **Resolved (user-directed skill-file edit, 2026-07-11):** `09-package-verification/SKILL.md` Step 4 ("Run the gates") now requires the verifier itself drive the ROM at a non-default parameter value whenever the DoD references one, cited to `BL-0047`/`BL-0055`; a new Quality gate checklist item makes it mandatory before writing `VERIFIED` going forward. | DONE |
| BL-0056 | 2026-07-11 | recommendation | user-requested retrospective on `BL-0047`–`BL-0053` (root-cause pattern analysis, this session) | **Process gap — `09-content-review` never ran against `IP-1040`'s menu screens, because it only triggers after `08-content-authoring` packages, and `IP-1040` (the SAVE/MAIN-MENU screens) was a `08-code-implementation` package.** `BL-0049` (the exit-to-main-menu option has no on-screen text) is exactly the class of defect `09-content-review`'s "readability & composition" dimension exists to catch — but it was never exercised, because the code/content package split routes UI screens authored inside a code package around content review entirely. `09-package-verification`'s own checklist correctly confirmed the mechanism (input→state-transition) works; nothing checked what's actually on screen. | Medium (one confirmed miss, `BL-0049`; the mechanism is a real, recurring blind spot for any future UI authored inside a code package) | 09 | SCHEDULED: `09-content-review`'s own `SKILL.md` scope-selection section should be widened from "after `08-content-authoring`" to "any package (`08-code-implementation` or `08-content-authoring`) that changes what's rendered on a screen" — trigger on screen/UI changes, not on which stage-08 peer implemented them. **Resolved (user-directed skill-file edit, 2026-07-11):** `09-content-review/SKILL.md`'s frontmatter description, Scope selection, and Pipeline-position/routing sections all widened to trigger on any stage-08 peer's screen-content change, cited to `BL-0049`/`BL-0056`. | DONE |
| BL-0057 | 2026-07-11 | research-gap | user-requested retrospective on `BL-0047`–`BL-0053` (root-cause pattern analysis, this session) | **Testing-convention gaps in `R305` (emulator-based test design), surfaced by six bugs that all survived a 180/180 suite:** (1) the suite's shared fixture (`advance_to_playing`) always enters play at `WORLD_SCALE=3`, so no test exercises a different scale — masked `BL-0047` entirely (§BL-0055's root cause); (2) tests for spatial/numeric behavior (movement clamps, collision) assert "the behavior occurs somewhere valid" rather than the exact boundary, missing `BL-0051`/`BL-0052`/`BL-0053` (a wrong clamp constant still passes "up decreases Y"); (3) menu/state-machine tests didn't cover the full (entry-condition × action) cross-product — `BL-0048`'s broken cell (save-present × select-new-game) was the one untested combination; (4) reachability was verified against the build-side oracle graph (`worldgen.py`/`T12.c`) but never against the actual runtime navigation path a player drives — "the data is correct" and "the game is playable" were conflated. | High (a research-documentation gap behind four of the six confirmed bugs) | 02 | SCHEDULED: a `02-research-tooling-and-testing` pass should extend `R305` with these four conventions (parameterize shared fixtures across the tunable's realistic range, not just its default; assert exact boundaries for spatial/numeric behavior, not just directional correctness; enumerate full state×action coverage for menus; treat build-side-oracle checks as necessary but not sufficient for player-facing reachability/behavior claims — pair with a runtime-driven check). This session's user explicitly asked for this as a concrete pipeline artifact; actioned directly rather than left purely as a backlog note. | SCHEDULED |
| BL-0058 | 2026-07-11 | bug | `07-implementation-planning` run (BL-0047's own mandatory supersession sweep) | **`SCOREITEM_FLAGS` (`asm_game.py:44`, 9 bytes at `0xC060`) was never generalized alongside `KEYITEM_FLAGS`'s identical widening (`IP-1020`, 9→81 bytes) — it is still a fixed 9-byte array indexed directly by `CUR_ZONE`, with zero bounds relationship to `WORLD_SCALE`.** `BL-0047`'s own remediation (regeneralizing `check_zone_transition` to read `REGION_GRAPH`) will make `CUR_ZONE` values above 8 reachable for the first time — at which point `SCOREITEM_FLAGS[CUR_ZONE]` (`setup_zone_collects`/`check_collisions`, both indexing `SCOREITEM_FLAGS + CUR_ZONE` directly) writes/reads **past the 9-byte array into adjacent WRAM**, corrupting whatever follows (`SEED`/`WORLD_SCALE` at `0xC069`/`0xC06B`, then `REGION_GRAPH` itself at `0xC070` — the exact structure navigation depends on). **Currently dormant/unreproducible** only because `BL-0047`'s own defect keeps `CUR_ZONE` from ever exceeding 8 today — fixing `BL-0047` alone, without this, would take a latent bug and make it live. The equivalent SRAM mirror (`SRAM_SCOREITEM`, `0xA013`, 9 bytes) has the identical defect, compounded by the fact that `SRAM_SEED`/`SRAM_WORLD_SCALE`/`SRAM_KEYITEM_FLAGS` are all laid out at fixed offsets immediately following it. | **Critical** — a fix for `BL-0047` that ships without this would convert a navigation bug into active WRAM corruption of the generated-world state itself | 07 | Not deferred (Critical, and directly causally coupled to `BL-0047`'s own remediation — see `IP-9070`, packaged in the same `07` pass as `BL-0047`/`BL-0048`). **Remediation shape:** widen `SCOREITEM_FLAGS` to 81 bytes, relocated to unused WRAM (`0xC286`–`0xC2D6`, the free gap between `SSE_CURSOR` and `OAM_BUF` — confirmed unused by direct WRAM-map read) rather than grown in place (growing in place at `0xC060` would itself collide with `REGION_GRAPH` at `0xC070`); widen `SRAM_SCOREITEM` to 81 bytes, relocated to `0xA070` (immediately after the existing `SRAM_KEYITEM_FLAGS`'s end, leaving `SRAM_SEED`/`SRAM_WORLD_SCALE`/`SRAM_KEYITEM_FLAGS` at their current addresses untouched); bump `SAVE_VERSION_VAL` `0x02`→`0x03` (third bump, following `IP-1010`→`IP-1050`'s established strictly-monotonic precedent exactly) since the save layout's shape changes. **Sequencing: `IP-9070` must land before or together with `BL-0047`'s own `IP-9050`** — widening the arrays alone is harmless (nothing indexes past 8 until navigation is fixed), but shipping `IP-9050` without `IP-9070` is actively unsafe. **Implemented (`IP-9070`, 2026-07-11): exactly as shaped above — 193/193 pass, `IP-9070` reached `COMPLETE` before `IP-9050` started.** Status stays `SCHEDULED`, not `DONE`, pending independent `09-package-verification` (blocked this session on the same-session independence rule). | SCHEDULED |
| BL-0059 | 2026-07-11 | bug | `07-implementation-planning` run (BL-0047's own mandatory supersession sweep) | **`ZONE_COLLECTS`/`zc_table` (`tilemaps.py:407`, `setup_zone_collects` in `asm_game.py:991`) is still the original 9 hand-authored, zone-named collectible spawn-position lists — `tilemaps.py`'s own module docstring says so verbatim: "ZONE_COLLECTS is still 9 zones in a 3x3 world grid, index = row*3 + col."** `setup_zone_collects` computes `CUR_ZONE*2` to index directly into `zc_table` (a ROM lookup table with exactly 9 entries). Once `BL-0047`'s fix makes `CUR_ZONE` values above 8 reachable, this indexes **past the ROM table's own 9 entries**, reading whatever ROM bytes happen to follow as if they were a valid spawn-table pointer — misinterpreted ROM data as an address, then dereferenced as spawn (x,y,type) tuples, with no bounds check anywhere in the chain. Likely outcomes range from nonsensical/garbage collectible placement to reading unmapped/invalid addresses. Same dormancy pattern as `BL-0058`: invisible today only because `BL-0047`'s own defect caps `CUR_ZONE` at 8. | **Critical** — identical severity class to `BL-0058`, same causal coupling to `BL-0047` | 07 | Not deferred (Critical, packaged with `BL-0047`/`BL-0058` as `IP-9070`). **Remediation shape — confidently resolvable by direct precedent, not escalated:** replace the 9 zone-authored lists with **5 biome-family-representative lists**, keyed by `REGION_GRAPH[CUR_ZONE].biome_id` (0–4) rather than `CUR_ZONE` itself — mirroring `IP-1031`'s own already-shipped, already-verified screen-reuse pattern exactly, reusing the *identical* representative-zone choice `IP-1031` already made per family: Water→Z3(Lake)'s list, Sand→Z0(Beach)'s list, Grass→Z1(Forest)'s list, Stone→Z2(Mountain)'s list, Brick→Z8(Castle)'s list (the other four original zone lists — Village/Cave/Desert/Plains — become unused, matching `IP-1031`'s own precedent of orphaning 4 of 9 zone *screens* the same way). `setup_zone_collects` reads the biome-id from `REGION_GRAPH[CUR_ZONE]` first, then indexes `zc_table` by biome-id (0–4) instead of `CUR_ZONE` directly. **Implemented (`IP-9070`, 2026-07-11): exactly as shaped above — 193/193 pass, `IP-9070` reached `COMPLETE`.** Status stays `SCHEDULED`, not `DONE`, pending independent `09-package-verification` (blocked this session on the same-session independence rule). | SCHEDULED |
| BL-0060 | 2026-07-11 | doc-defect | `07-implementation-planning` run (`IP-9070` authoring, Risks §13) | **No numbered FR covers collectible-spawn content for a generated region at all.** `ZONE_COLLECTS` predates `FEAT-9000` entirely — `FR-3210`/`FR-9130` cover *carrot/KeyItem placement rules* (exactly one per zone/region), but nothing states that spawn *content* (the star/flower/carrot positions themselves) should track the generated world rather than a retired fixed layout. `IP-9070` repairs `ZONE_COLLECTS` as a defect against this unstated intent (mirroring `BL-0020`'s precedent — a shipped, evidenced capability with no formal requirement statement), not as new requirement-covered behavior. | Low | 04 | SCHEDULED: a future `04-requirements-engineering` delta should add an FR for generated-region collectible-spawn sourcing (biome-family-representative reuse, per `IP-1031`/`IP-9070`'s established pattern), citing `IP-9070`'s T16.b as pre-existing evidence once it ships — following `BL-0020`'s own precedent exactly. Not blocking `IP-9070` or any current work. | SCHEDULED |
| BL-0061 | 2026-07-11 | doc-defect | `07-implementation-planning` run (`IP-9050` authoring) | **`FR-2300`/`FR-2310` (zone-boundary transition on a valid/no neighbor) are still worded entirely in terms of "the 3×3 grid"** — their Description/Rationale/Acceptance Criteria fields all cite the fixed 3×3 layout explicitly, never `REGION_GRAPH` or a generated `scale`×`scale` world. No target-state FR generalizing them exists — unlike `FR-1120`→`FR-1170`/`1180`/`1190` (auto-load bypass superseded by a named target FR triad, `RQ-03` finding #7's own precedent), navigation never got its target-state counterpart when `FEAT-9000`/`FEAT-4100` shipped. `IP-9050` (fixing `BL-0047`) restores the *behavior* `FR-2300`/`FR-2310` describe in spirit (transition on a valid neighbor, halt at a true boundary) generalized to the real generated graph, but the requirements text itself still only formally describes the retired 3×3 case. | Low | 04 | SCHEDULED: a future `04-requirements-engineering` delta should add target-state FRs generalizing `FR-2300`/`FR-2310` to `REGION_GRAPH`-driven navigation (mirroring the `FR-1120`/`1170`-`1190` coexistence pattern `RQ-03` finding #7 already established as this project's working convention), citing `IP-9050`'s shipped evidence once it lands. Not blocking `IP-9050` — the package cites `FR-2300`/`FR-2310` as the closest existing statement of intent plus `ADR-0009` as the binding architecture decision it must honor, with this gap noted explicitly rather than silently assumed covered. | SCHEDULED |
| BL-0062 | 2026-07-11 | gate | `00-pipeline-manager` run #64 (G3 gate for `IP-9050`/`IP-9060`/`IP-9070`) | **G3 authorization needed for the three post-ship remediation packages** (`IP-9070` — `CUR_ZONE`-indexed structure generalization, `IP-9050` — generated-world navigation fix, `IP-9060` — main menu cursor fix) — none fall under the `BL-0001`…`BL-0005` bootstrap carve-out. | High (blocks all remediation work for four Critical/High confirmed bugs) | 08 | **Resolved same run — user explicitly authorized all three** ("Authorize all three (recommended)", 2026-07-11). `IP-9070`/`IP-9060`/`IP-9050`'s Master Build Plan `Authorized?` columns updated accordingly; rides the next `00-pipeline-manager` advance (`08-code-implementation` on `IP-9070`, the tranche's `READY` prerequisite). | DONE |
| BL-0063 | 2026-07-11 | bug | `08-code-implementation` run (IP-9060 authoring, incidental code read of `asm_game.py`'s MAIN MENU/VICTORY block) | **`KEYITEM_FLAGS` (`asm_game.py:48`, up to 81 bytes at `0xC220`) is fully round-tripped through save/load (81-byte `memcpy`, `IP-1050`) but its two reset sites — `st_intro`'s new-game reset (`asm_game.py:251`) and `st_victory`'s progress-clear (`asm_game.py:316`) — still only zero the first 9 bytes each, matching the same "only indices 0-8 are live until `FEAT-1100`" comment `IP-1020` wrote before `CUR_ZONE` could ever exceed 8.** Once `IP-9050` (`BL-0047`'s fix) makes `CUR_ZONE > 8` reachable, a "new game"/victory reset within the same power-on session (no console reset) leaves `KEYITEM_FLAGS[9..80]`'s prior-playthrough bits intact — a freshly generated region could show its KeyItem as already collected. The full boot-time WRAM clear (`0xC000`–`0xC2FF`, `main`) does zero the full 81 bytes on cold boot, so this is not visible on a fresh power-on; it manifests only on a same-session replay/new-game, once regions past index 8 exist. Same causal-coupling pattern `BL-0058`/`BL-0059` established: currently dormant only because `BL-0047`'s own defect keeps `CUR_ZONE` capped at 8 today. | High — a real correctness/data-integrity bug against `FR-5220`'s new-game-reset intent, but not memory-unsafe (no OOB write/read, no corruption of adjacent WRAM — `KEYITEM_FLAGS`'s own 81-byte allocation was already correctly sized by `IP-1020`, only its reset-loop width was left at the old 9-byte figure) | 08 | Not deferred — same causal coupling to `BL-0047`/`IP-9050` that `BL-0058`/`BL-0059` had to `IP-9070`. **Remediation shape:** widen both `st_intro`'s and `st_victory`'s `KEYITEM_FLAGS` clear loops from `rom.LD_B_n(9)` to `rom.LD_B_n(81)`, mirroring exactly what `IP-9070` already did for `SCOREITEM_FLAGS`'s own two clear sites in the same two routines. Trivial in isolation (a one-argument change at two call sites, no address relocation, no save-format version bump needed — `KEYITEM_FLAGS`'s address/size/round-trip are already correct, only the reset width is stale) — should be folded into `IP-9050`'s own implementation as a same-package companion fix (discovered by `08-code-implementation`'s own mandatory supersession sweep, to be re-confirmed when `IP-9050` is implemented) rather than spun up as a separate package, unless that sweep finds it's already been superseded by then. **Not yet authorized as its own G3 item** — flagged here for the next `IP-9050` implementation pass to pick up explicitly, with a fresh authorization check if it turns out to need one beyond what `BL-0062` already covers (it is textually outside the three-package "IP-9050/9060/9070" G3 answer, since it was undiscovered at that time). **Implemented (`IP-9050`, 2026-07-11): folded in exactly as shaped above (both clear loops widened to 81 bytes), no separate authorization needed in practice — the fix rode `IP-9050`'s own already-authorized `BL-0062` scope without objection.** Status stays `SCHEDULED`, not `DONE`, pending `IP-9050`'s independent `09-package-verification` (blocked this session on the same-session independence rule). | SCHEDULED |
| BL-0064 | 2026-07-11 | feature | intake: project owner (procgen-map design discussion, this session — explicitly "not necessarily verbatim locked in but open to research driven implementation") | **Replace `REGION_GRAPH`'s current full-lattice adjacency (every grid-adjacent region pair is always connected) with a generated maze: a randomized spanning tree over the `scale`×`scale` region grid, plus a "braid" pass that reopens a fraction of the pruned edges** (avoiding a pure single-path maze — adds loops/shortcuts). Must derive deterministically from the existing `(SEED, WORLD_SCALE)` pair, same as today's biome generation — no new persisted state; `REGION_GRAPH` still regenerates from `(seed, scale)` alone, preserving `ADR-0009`'s determinism guarantee and the already-tested "full reachability" domain invariant (a spanning tree guarantees this by construction). This directly reverses part of `ADR-0009`'s own Decision (today's model is a deliberate full lattice, not an oversight) — **`BL-0047`'s own Disposition explicitly anticipated and recommended this exact follow-up** ("route the non-grid-shape request as its own design-question if the user wants it pursued after seeing the grid actually work end-to-end" — `IP-9050` shipped 2026-07-11, closing that loop). Needs research grounding before an architecture decision can be made: which maze algorithm (randomized Prim/Kruskal/recursive-backtracker) fits the SM83's shift/XOR-only PRNG (`R111`'s no-`DIV`/no-multiply constraint, the same one today's biome PRNG honors) and the WRAM budget at `scale=9` (81 regions) — a spanning-tree algorithm needs per-region visited/set-id state today's one-pass generation doesn't carry, a real cost against `NFR-4200`'s already-tracked headroom, not just an algorithm-choice question. | Medium (owner: exploratory/open to research, not an urgent fix) | 02 → 03 | **Triaged (`00-pipeline-manager` run #69): SCHEDULED, riding this run's step.** `02-research-gbc-hardware` first (maze-algorithm-vs-SM83-PRNG/WRAM-cost comparison — a new R1xx topic), then `03-architecture-design-synthesis` to record the ADR-0009-amending decision (which algorithm, braid-fraction default, WRAM/ROM cost accepted) before any `04-requirements-engineering`/`06-feature-specification` work names concrete FRs. **Hard-blocking dependency for `BL-0065`/`BL-0066`/`BL-0067`** (all three build on this package's own output — a maze structure to scale, cluster, or render). **Research half done (run #69, [R112](../research/encyclopedia/R112-maze-generation-hardware-feasibility.md)): compared randomized Kruskal/Prim/recursive-backtracker against the existing shift/XOR-only PRNG and current WRAM headroom (928 bytes used, 3168 bytes/3.09 KiB free — re-measured directly, not estimated); recursive backtracker grounded as the strongest SM83 fit, WRAM budget doesn't differentiate the three (all comfortably affordable).** Remains `SCHEDULED`, now riding `03-architecture-design-synthesis` next (the actual ADR-0009-amendment decision — algorithm pick, braid-fraction default — still pending). | SCHEDULED |
| BL-0065 | 2026-07-11 | feature | intake: project owner (same procgen-map design discussion as `BL-0064`) | **Add a maze-difficulty scaling variable** — a parameter (independent of `WORLD_SCALE`, or possibly a new player-entered value alongside `SEED`/`WORLD_SCALE`) controlling the maze's braid-back-edge fraction/density from `BL-0064`'s generator: low value = many braided loops (forgiving, close to today's open lattice), high value = a near-pure spanning tree (mostly single-path, more backtracking). Needs a design decision on where this fits the existing SEED/SCALE ENTRY UI (`IP-1040`'s digit-cursor picker) — a third entry field, a value derived from `WORLD_SCALE`, or a fixed default with no UI exposed initially. | Medium (owner: exploratory) | 04 | **Triaged (`00-pipeline-manager` run #69): DEFERRED.** Revisit trigger: `BL-0064`'s `03-architecture-design-synthesis` step closes (the generator algorithm is picked) — the variable's semantics (what "braid fraction" means, its valid range) can't be specified before then. Routes to `04-requirements-engineering` once that trigger fires. | DEFERRED |
| BL-0066 | 2026-07-11 | feature | intake: project owner (same procgen-map design discussion as `BL-0064`) | **Cluster biome assignment into cohesive multi-region blobs instead of today's independent per-cell grammar-constrained draw** — so a "Forest area" spans several adjacent regions before the biome drifts, giving the world distinct named areas (Zelda/Pokémon-style overworld feel) rather than a checkerboard. The owner specifically proposed **seeding blob centers from `BL-0064`'s maze's own dead-ends** — a deliberate hard dependency between this item and `BL-0064` (blob clustering can't be generated or tested independently of the maze generator existing first); worth evaluating at design time against simpler alternatives (e.g. N random seed points + flood-fill, no maze dependency) as part of the same pass, not assumed as the only option. Must still respect the existing adjacency-grammar invariant (`R212`: adjacent regions' biome-ids differ by at most 1) — blob boundaries are a *clustering* change layered on top of the existing grammar-constrained draw, not a replacement for it. Loosely related to `BL-0050` (MAP/status screen redesign, `NEEDS-USER`) — a blob/named-area model could inform what a generated-world-appropriate quick-status display shows, but the two are independent decisions; not merging them here. | Medium (owner: exploratory) | 04 | **Triaged (`00-pipeline-manager` run #69): DEFERRED.** Revisit trigger: `BL-0064`'s `03-architecture-design-synthesis` step closes (dead-end-seeding is explicitly dependent on the maze existing; even the flood-fill alternative should be decided alongside `BL-0064`'s own architecture pass, not before it, since both change the same generation routine). Routes to `04-requirements-engineering` once that trigger fires. | DEFERRED |
| BL-0067 | 2026-07-11 | feature | intake: project owner (same procgen-map design discussion as `BL-0064`) | **Cosmetic indication of which of a region's up/down/left/right edges the maze allows crossing vs. blocks** — extending the existing arrow-indicator system (`draw_region_arrows`, `asm_game.py:860`, `IP-1030`) from today's 2-state signal (arrow = grid-adjacent region exists, no arrow = true grid boundary) to a 3-state signal once `BL-0064` ships (open path / blocked-by-maze-but-grid-adjacent / true grid boundary) — today those last two cases render identically since adjacency is always fully open. **Explicitly presentation-layer only** — the owner and assistant both scoped this as *not* mid-screen walls/collision; a maze-blocked edge is signaled at the screen boundary the same way an open edge already is, not enforced by new in-screen collision geometry (that would be a separate, larger feature if ever pursued). | Low–Medium (cosmetic, but real player-facing legibility need — without it, a maze-blocked edge looks identical to a dead end, indistinguishable from "just walk further") | 04 | **Triaged (`00-pipeline-manager` run #69): DEFERRED.** Revisit trigger: `BL-0064`'s `03-architecture-design-synthesis` step closes — there is no "blocked but grid-adjacent" case to indicate until the maze generator exists; the specific 3rd visual state also needs a tile-art decision (`GDS-08`) once `BL-0064`'s data shape is known (does `REGION_GRAPH` need a new bit, or does absence-of-edge-but-presence-of-grid-neighbor already imply it structurally). Routes to `04-requirements-engineering` once that trigger fires. | DEFERRED |
