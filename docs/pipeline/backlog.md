# Pipeline Backlog

> **Findings & intake register for the documentation-driven-development pipeline.** Two writers:
> **`00-pipeline-manager`** (harvests every invoked skill's findings/recommendations/Outstanding
> Issues after each run, triages every open entry at the start of the next run, and flips
> statuses) and **`00-intake`** (appends `NEW` entries for user-submitted features, bugs, and
> observations). Stage skills never write here — their findings reach this file via the manager's
> harvest step. Rows are never deleted: resolved entries flip to `DONE`, rejected ones to
> `REJECTED` with the reason kept.

[↑ Docs index](../INDEX.md) · [Pipeline journal](pipeline-journal.md) ·
[Pipeline README](../../.claude/skills/README.md)

## Lifecycle

```
NEW ──triage──▶ SCHEDULED (rides a named step)  ──that step runs──▶ DONE
        ├─────▶ DEFERRED  (named revisit trigger) ──trigger fires──▶ NEW (re-triage)
        ├─────▶ NEEDS-USER (named decision)       ──user decides───▶ SCHEDULED / REJECTED
        └─────▶ REJECTED  (reason recorded)
IN PIPELINE = the entry's work is the current/next step's explicit target
```

Every open entry carries a live disposition — `SCHEDULED` names the step it rides with,
`DEFERRED` names its revisit trigger, `NEEDS-USER` names the exact decision required. A
Critical/High entry may not be `DEFERRED` without the user's explicit agreement.

## Entries

| ID | Filed | Type | Source | Summary | Sev/Pri | Entry stage | Disposition | Status |
|---|---|---|---|---|---|---|---|---|
| BL-0001 | 2026-07-06 | bug | scaffold init: `memory.md` §"Active Bug" | Map-screen hearts written to wrong BG map addresses (3-heart layout, rows 6/8/10 col 12). **Superseded — see BL-0008 (run #2, vision revision).** `memory.md` describes the *pre-rewrite* 3-zone game; the current `tilemaps.py`/`asm_game.py` implement a completely different 9-heart 3×3 map-screen mechanism. This entry's specific fix (patch three hex addresses) does not apply to the current code as filed. | Medium | 07 | **Superseded (run #2):** do not implement as filed — re-verify against current `map_screen()`/`update_map_hearts` (asm_game.py ~line 643) once GDS-04/05 baseline the real game; folded into BL-0008's broader re-verification scope. **Resolved (run #21, `07-implementation-planning`): re-verified against current code — does NOT reproduce.** `update_map_hearts` (asm_game.py:644) writes rows 6/9/12 × cols 6/11/16, exactly matching `map_screen()`'s heart placement (tilemaps.py:327), and runs only via `do_screen_redraw` with the LCD off. The rewrite replaced and fixed the filed mechanism; a permanent map-hearts regression check rides IP-9010's rewritten suite. | DONE |
| BL-0002 | 2026-07-06 | finding | scaffold init: `Claude.md` §"Remaining Known Issues" | Bunny renders as two separate 8x8 OAM entries and reads small. **Status uncertain after run #2's discovery that the game was fully rewritten** — a separate, later commit ("Fix invisible bunny: 8x16 OBJ mode + WRAM clear bug", `9a587ac`) suggests this may already be resolved by a switch to 8x16 OBJ mode. Needs direct code verification, not the stale `Claude.md` description. | Low | 03 | **Re-scope (run #2):** before riding GDS-08 authoring as originally scheduled, first check `asm_game.py`'s current OAM-write code and commit `9a587ac`'s diff to see if this is already fixed; only carry forward if still reproducible. **Resolved (run #27 triage): already fixed in the shipped code** — commit `9a587ac` switched to 8×16 OBJ mode; [GDS-08 §2](../architecture/08-presentation-architecture.md)/[ADR-0007](../architecture/adr/ADR-0007-8x16-obj-mode.md) document the single 8×16 OAM pair as-built (run #13), and the rewritten suite's T6 asserts single-entry 8×16 bunny OAM (+X-flip) on every run (run #22, verified run #23). Not reproducible; no work remains. | DONE |
| BL-0003 | 2026-07-06 | bug | scaffold init: `Claude.md` §"Remaining Known Issues" | Score display writes VRAM while the LCD is on. **Needs re-verification against current code** (run #2 found `Claude.md` describes the pre-rewrite game throughout) — the score-bar write path may have changed in the Bunny Quest rewrite. Do not assume the original description of *where* in the code this happens still holds. | Medium | 07 | **Re-scope (run #2):** re-locate the score-bar VRAM-write call site in the current `asm_game.py` before packaging a fix; folded into BL-0008's broader re-verification scope. **Confirmed reproducing (run #21):** `update_status_disp` (asm_game.py:505) writes `0x9802`/`0x9808–0x980A` unguarded, called from `st_playing` at unbounded distance from VBlank wake — packaged as [IP-9020](../implementation/packages/IP-9020-score-bar-vblank-fix.md) (authorized via carve-out; blocked on IP-9010). **Implemented run #24** (call relocated to frame-top VBlank, T8.10a/b added). **Resolved (run #28, [VR-9020](../implementation/verification/VR-9020-score-bar-vblank-fix.md)): independently confirmed** — sole call site at frame top inside VBlank, every other VRAM writer LCD-off, 125/125 pass; IP-9020 VERIFIED. | DONE |
| BL-0004 | 2026-07-06 | design-question | scaffold init: repo-root file survey | Duplicate/legacy build artifacts need reconciling: `BunnyGarden_build_rom.py` (monolithic, 45KB) and `BunnyGarden_logic.json` coexist with the modular canonical chain. **Widened (run #2):** the repo-root also carries a stale `BunnyGarden.gbc` binary (pre-rewrite ROM, still checked in) alongside the *current, correct* `BunnyQuest.gbc` (verified byte-identical to a fresh build of current source) — same archival treatment applies. | Medium | 03 | **User decided (run #1): modular chain is canonical; archive `BunnyGarden_build_rom.py` + `BunnyGarden_logic.json` to `legacy/`.** **Widened (run #2, not yet re-asked of the user): also archive the stale `BunnyGarden.gbc` alongside them; keep `BunnyQuest.gbc` at the repo root as the current shipped artifact.** Rides the first `07-implementation-planning` hygiene pass; confirm the widened scope with the user if it isn't obviously assumed consent. **Packaged (run #21) as [IP-9040](../implementation/packages/IP-9040-legacy-artifact-archival.md)** (authorized: carve-out + run #1 decision; blocked on IP-9010). **Implemented run #25** (three files `git mv`'d to `legacy/` + README). **Resolved (run #29, [VR-9040](../implementation/verification/VR-9040-legacy-artifact-archival.md)): independently confirmed** — root clean, `legacy/` complete with history preserved, zero live references, ROM byte-identical; IP-9040 VERIFIED. | DONE |
| BL-0005 | 2026-07-06 | bug | scaffold init: `build_rom.py`/`test_rom.py` read-through | Hardcoded absolute paths break portable runs. **Compounded (run #2):** `test_rom.py`'s hardcoded `ROM_PATH` also points at the *wrong, stale* ROM name (`BunnyGarden.gbc`) — the current shipped ROM is `BunnyQuest.gbc`. A path-only fix without also fixing the filename would leave the suite pointed at the wrong binary even after portability is fixed. | Medium | 07 | SCHEDULED (run #1 triage): rides the bootstrap's first `07-implementation-planning` remediation pass, sequenced first. **Widened (run #2):** the remediation package must also update `ROM_PATH`'s filename, not just make the path configurable — folded into BL-0008. **Packaged (run #21) into [IP-9010](../implementation/packages/IP-9010-test-suite-rewrite.md)**; **implemented run #22 (2026-07-07)** — all paths repo-relative, `ROM_PATH` retargeted to `BunnyQuest.gbc`, `RAM_PATH` derived not hardcoded. **Verified run #23 ([VR-9010](../implementation/verification/VR-9010-test-suite-rewrite.md)): confirmed independently — no absolute paths remain, ROM byte-identical.** | DONE |
| BL-0006 | 2026-07-06 | bug | `01-vision` run #2 (deliberate vision-change run), direct code inspection | **`test_rom.py` asserts pre-rewrite WRAM semantics against the current shipped code — the G5 permanent gate cannot currently be satisfied.** Specifically: T4.8/T8.8 assert victory at `GS=5` when a 3-bit `GIFTS` bitfield (`0xC009`) reaches `0x07`; T7.9/T9.x assert `CUR_ZONE` (`0xC008`) is bounded to 0–2. The current `asm_game.py` uses `CUR_ZONE` 0–8 and a 9-byte `CARROT_FLAGS` array with victory at `CARROTS_COUNT==9` — none of these old assertions can pass against the real code. `test_results.txt`'s "88/88 passed" is a stale artifact predating the rewrite, not evidence about the current tree. | **Critical** | 07 | SCHEDULED: rides BL-0008's umbrella pass, prioritized above BL-0001/0003/0005's other items — the earliest `07`/`08` package once GDS-04/05 exist to derive correct assertions from. **Not deferred** (Critical severity, per the pipeline's severity-honesty rule) — flagged prominently to the user this run as a recommendation on whether to pull it forward out of numeric order; not blocking upstream GDS/requirements authoring, which can proceed from direct code reads independent of this fix. **Packaged (run #21) into [IP-9010](../implementation/packages/IP-9010-test-suite-rewrite.md)**; **implemented run #22 (2026-07-07)** — T1 kept per R304, T2–T10 rewritten against the current WRAM model (R305/GDS-07), **109/109 checks pass** against a byte-identical rebuilt ROM; `NFR-7100` flipped to MET. The G5 gate is functional again. **Verified run #23 ([VR-9010](../implementation/verification/VR-9010-test-suite-rewrite.md)): independently re-run in a fresh container (PyBoy reinstalled) — 109/109 pass confirmed, no pre-rewrite assertions remain.** | DONE |
| BL-0007 | 2026-07-06 | doc-defect | `01-vision` run #2 (deliberate vision-change run), direct code inspection | **`Claude.md`, `memory.md`, and `README.md` all describe the pre-rewrite "Bunny Garden Adventure" game throughout** (WRAM map, tile index map, zone names/count, "v2.1" status line, known-good-behavior list) — none reflect the shipped "Bunny Quest" 9-zone game. These are the primary onboarding/quick-reference docs; they are actively misleading right now, not just out of date. | High | 07 | SCHEDULED: fold into the same documentation-refresh remediation pass as BL-0006 (a stage-08 content/code peer pass once GDS-01/04/05 have re-baselined against the real code, so the refresh has a correct source to write from rather than guessing again). **Packaged (run #21) as [IP-9030](../implementation/packages/IP-9030-root-doc-refresh.md)** (depends on IP-9010+IP-9020 so it documents the fixed state; awaits G3, see BL-0024). **Implemented run #30** (2026-07-09). **Resolved (fresh-session `09-package-verification`, 2026-07-10, [VR-9030](../implementation/verification/VR-9030-root-doc-refresh.md)): independently confirmed** — all three root docs describe the shipped Bunny Quest game accurately, stale-term sweep clean, quick-start commands executed; IP-9030 VERIFIED. | DONE |
| BL-0008 | 2026-07-06 | finding | `01-vision` run #2 — consolidation entry | **Umbrella tracking entry for the "docs/tests describe the wrong game" remediation**, so BL-0001/0003/0005/0006/0007's overlapping re-verification work rides one coherent `07-implementation-planning` pass instead of five uncoordinated ones. Scope: (1) re-verify BL-0001/BL-0003's specific claims against current code, fixing only what still reproduces; (2) fix BL-0005's path portability *and* filename; (3) rewrite `test_rom.py`'s zone/victory assertions for the real 9-zone/9-carrot model (BL-0006); (4) refresh `Claude.md`/`memory.md`/`README.md` (BL-0007). | High | 07 | SCHEDULED: this is the recommended shape for the bootstrap's first `07-implementation-planning` remediation pass — likely split into 2-3 packages (test-suite rewrite is code-implementation scope; doc refresh may ride alongside or split to its own package) rather than one, per the skill's right-sizing rule. Sequenced ahead of GDS-01 authoring's dependence on accurate current-state facts wherever practical. **Umbrella fully packaged (run #21):** IP-9010 (BL-0005+0006) → IP-9020 (BL-0003) / IP-9040 (BL-0004) / IP-9030 (BL-0007); BL-0001 closed as not-reproducing. **IP-9010 VERIFIED (run #23, VR-9010)** — BL-0005/BL-0006 now DONE; IP-9020/IP-9040/IP-1010 flipped READY. **IP-9020 VERIFIED (run #28, VR-9020)** — BL-0003 DONE; **IP-9040 VERIFIED (run #29, VR-9040)** — BL-0004 DONE. **IP-9030 VERIFIED (2026-07-10, VR-9030)** — BL-0007 DONE. **Umbrella closes: all five packages VERIFIED, the bootstrap tranche's entire remediation scope is complete.** | DONE |
| BL-0009 | 2026-07-06 | finding | `02-research-gbc-hardware` run #3 ([R104](../research/encyclopedia/R104-cgb-palette-system.md)) | **The CGB's fixed palette budget (8 BG + 8 OBJ palettes) is a real, near-term design constraint for MSTR-001 C7's world-scale target — but not as tight as first characterized.** **Corrected (run #12, [GDS-07](../architecture/07-data-model.md)):** direct read of `build_rom.py`'s `BG_PALETTES` confirms palettes are organized by **terrain family**, not one-per-zone, and are already substantially reused — only 5 of 8 BG palettes serve zone terrain today (palette 4 alone covers Mountain+Village+Cave, three zones); the original framing ("already at or near its ceiling… at least one pair shares") understated the existing headroom and the deliberateness of the reuse already in place. Still a real, eventually-binding ceiling as C7 grows further — just later than originally stated. | Medium | 03 | SCHEDULED: GDS-08 (Presentation Architecture) should state the corrected picture (5/8 BG palettes in zone-terrain use, terrain-family reuse pattern) when authored, citing GDS-07/R104 — GDS-01 already named the tension in general terms and needs no further edit. Not blocking any current work. **Resolved (run #27 triage): GDS-08 was authored run #13 carrying exactly this corrected picture** (terrain-family palette reuse, citing GDS-07/BL-0009) — the ride this entry was scheduled on already happened; the underlying palette ceiling remains recorded in GDS-08/R104 as C7's future constraint. | DONE |
| BL-0010 | 2026-07-06 | doc-defect | `02-research-gbc-hardware` run #3, `docs/research/INDEX.md` §Tier R200 | `docs/research/INDEX.md`'s R201 row still describes its scope as "goal hierarchies, gating, **the gifts→victory shape**" — stale "gifts" wording from before the vision correction (the game's win condition is 9 carrots, not gifts). Trivial, and out of this skill's write scope (Tier R200 belongs to `02-research-game-design`), so left as-is rather than edited here. | Low | 02 | **Resolved (run #5):** R201 authored with corrected wording ("9-carrot victory shape"); index row fixed in the same pass. | DONE |
| BL-0011 | 2026-07-06 | finding | `02-research-tooling-and-testing` run #4 ([R301](../research/encyclopedia/R301-pyboy-headless-api.md)) | PyBoy is not installed in this environment (`import pyboy` fails), so R301's `button()`/`button_press()`/`button_release()` behavior and the `stop(ram_file=...)` custom-location detail rest on secondary sources (a GitHub-source search summary, a community Q&A post) rather than a directly-fetched primary doc page or a local experiment — `docs.pyboy.dk` itself returned HTTP 403 to `WebFetch` this session. The core claims are corroborated by this project's own working `test_rom.py` code (which already calls these methods successfully per its historical "88/88 passed" record, predating the semantic drift `BL-0006` found), so confidence is reasonable but not the formal verification bar the research methodology sets. | Low | 02 | **Resolved (run #22, 2026-07-07):** PyBoy **2.7.0** installed in-session for IP-9010; the rewritten suite exercises `button()`, `button_press()`/`button_release()`, `screen.ndarray`/`screen.image`, and the `<rom>.ram` battery-save flush-on-`stop()` behavior across 109 passing checks — R301's uncited claims are now empirically confirmed against a named version. (R301's own citation text can note this the next time an `02` pass touches it — not worth a dedicated research run.) | DONE |
| BL-0012 | 2026-07-06 | recommendation | `02-research-tooling-and-testing` run #4 — cross-reference update | **BL-0005 and BL-0006 now have concrete, code-cited remediation targets** — [R306](../research/encyclopedia/R306-toolchain-portability.md) specifies the exact four hardcoded-path lines to fix and the repo-relative replacement scheme (BL-0005); [R305](../research/encyclopedia/R305-emulator-based-test-design.md) specifies the exact current WRAM addresses/values a rewritten `test_rom.py` must assert (BL-0006), and [R304](../research/encyclopedia/R304-rom-validation.md) confirms the T1 header-check suite needs no changes at all. This doesn't change either entry's disposition (still `SCHEDULED`, riding `BL-0008`'s umbrella pass) but means `07-implementation-planning`'s eventual package-authoring pass for `BL-0008` has everything it needs already grounded — no further research gap exists for that remediation. | — | 07 | Informational — no action needed beyond citing R304/R305/R306 when `BL-0008`'s package is authored. | DONE |
| BL-0013 | 2026-07-06 | research-gap | intake: project owner | **Project owner's own words:** "Add a research series that explores pixel art techniques, using Claude and AI to generate art, best rated Gameboy and Gameboy Color games for visual aesthetics, etc among other similar topics - which generally explore how Claude could develop and implement visually appealing graphics in a structured way that can be iterated upon." No duplicate found — checked BL-0001…BL-0012 and the existing R200 tier (R201–R208, none yet authored) for overlap; closest existing rows are R203 (screen composition), R204 (HUD conventions), R208 (palette/color under CGB constraints) — adjacent, not overlapping. This request is genuinely broader than R208's color-only scope: it spans (a) pixel-art craft technique at 8×8/2bpp scale (silhouette readability, dithering-substitutes under a 4-color budget, anti-aliasing-by-palette-choice), (b) a structured, iterable **workflow** for an AI agent (this project's own coding agent) to generate/critique/revise tile art against a spec rather than freehand, and (c) comparative case-study grounding from acclaimed GBC/GBC-era titles' visual design (e.g. Link's Awakening DX, Wario Land II/3, Pokémon Gold/Silver, Kirby's Dream Land 2, Shantae) — what specifically reads as "good" GBC art and why, at the tile/palette/composition level this project's tooling actually works in. Recommend 3 new R2xx rows for whoever authors R200: a pixel-art-technique topic, an AI-assisted-generation-workflow topic, and a comparative-case-study topic — final numbering/splitting is that skill's call, not intake's. | — (user framed as "add a research series," not stated as urgent) | 02 | **Resolved (run #5):** authored as R209 (pixel-art technique), R210 (AI/agent-assisted generation workflow, grounded directly in this project's own tooling), R211 (comparative case studies: Oracle of Seasons/Ages, Shantae, Pokémon Gold/Silver, GBC DKC) — see [R209](../research/encyclopedia/R209-pixel-art-technique.md)/[R210](../research/encyclopedia/R210-ai-assisted-tile-art-workflow.md)/[R211](../research/encyclopedia/R211-acclaimed-gbc-visual-design-case-studies.md). | DONE |
| BL-0014 | 2026-07-06 | finding | `02-research-game-design` run #5 ([R210](../research/encyclopedia/R210-ai-assisted-tile-art-workflow.md)) | **This project's toolchain has no path to import a raster image (a PNG, an AI-generated sprite mockup) into `tiles.py`'s pixel-array format.** Every tile is authored as a literal hand-written Python `pix` array; no code anywhere quantizes an external image to a 4-color palette and emits the resulting grid. This means the common "AI generates a PNG, we import and refine it" pixel-art workflow is not currently executable in this pipeline — R210's recommended workflow instead has the agent author the pixel grid directly as reviewable text. Not a defect (nothing currently needs this capability), but worth recording as a real, current tooling limitation rather than an assumed-available path. | Low | 07 | DEFERRED: revisit only if a future task specifically wants to start from an external reference/mockup image — at that point this becomes a small, scoped tooling feature (an image-to-palette-grid converter script) for `07-implementation-planning` to package. Not blocking any current work. | DEFERRED |
| BL-0015 | 2026-07-06 | design-question | `03-architecture-design-synthesis` run #6 ([GDS-01 §5](../architecture/01-concept-of-play.md)) | **As MSTR-001 C7's Zelda/Pokémon-scale world target is pursued, does Bunny Quest stay a short single-sitting game over a much larger map ("wider"), or become a deliberately longer multi-session game with fewer/larger zones ("deeper")?** GDS-01 names this tension explicitly rather than picking a default; two concrete constraints bear on it — the CGB's 8-BG-palette ceiling (already substantial at 9 zones, `BL-0009`/R104/R208) makes "wider" harder to sustain with distinct per-zone color identity, and `_zone_arrows()`'s hardcoded-rectangle adjacency logic (R203) needs generalizing sooner under "wider" than "deeper." | Medium | 03 | **Closed (`00-pipeline-manager` run #30, triage):** the C7 world-scale expansion is now planned — the adopted increment plan ([PLAN-requirements-aesthetics-story-map.md](PLAN-requirements-aesthetics-story-map.md) §6) and owner decisions D3/D6 (BL-0031) resolve "wider vs deeper" by making world scale a **user-runtime parameter** rather than an architecture fork: the player picks size per world: within bounds the eventual seed & scale ADR sets. No design fork remains to adjudicate. The palette-ceiling consequence this entry named transfers to BL-0031/GDS-08 in corrected form (binds biome-family count, not blending — see plan §7). | DONE |
| BL-0016 | 2026-07-06 | recommendation | `03-architecture-design-synthesis` run #8 ([GDS-03](../architecture/03-architecture.md)) | **As-built ADRs have not been authored yet** — `docs/architecture/adr/` remains empty. GDS-03 identified several real, already-made decisions worth recording as formal ADRs: single-bank ROM (no MBC bank switching, though C7 anticipates this changing — R106), Python assembler over RGBDS (MSTR-001 C3/A3), shadow-OAM DMA every frame (R105), 8×16 OBJ mode (R103, the `9a587ac` fix), MBC1+RAM+BATTERY (R106), PyBoy-as-verification-gate not real hardware (assumption A2). Deferred from GDS-03's own pass per the one-level-per-advance discipline, not forgotten. **Resolved run #16: eight as-built ADRs authored** ([ADR-0001](../architecture/adr/ADR-0001-single-bank-rom-no-mbc-switching.md)–[ADR-0008](../architecture/adr/ADR-0008-pyboy-headless-verification-target.md)), covering all six original candidates plus one-job-per-file decomposition and the patch-point dict contract; `docs/architecture/adr/INDEX.md` populated. | Low | 03 | DONE: eight ADRs authored and indexed, run #16. | DONE |
| BL-0017 | 2026-07-06 | finding | `03-architecture-design-synthesis` run #9 ([GDS-04](../architecture/04-domain-model.md)) | **The "exactly one Carrot per Zone" rule is not enforced by any code-level invariant** — it holds today only because `ZONE_COLLECTS` happens to be authored with one `type 2` entry per zone list; nothing checks this at build time or runtime. A future content package editing `ZONE_COLLECTS` (adding a zone, rebalancing an existing one) could silently violate the rule with no test catching it. | Low | 07 | SCHEDULED: any future `07-implementation-planning` package touching `ZONE_COLLECTS` should include an explicit Verification Checklist item confirming exactly one carrot per zone list — not a dedicated remediation on its own, since nothing is broken today. **Upgraded (run #21), implemented run #22:** the rewritten suite's T1.11 asserts exactly one carrot per zone on every run (T1.10/T1.12 add zone-count and bitfield-capacity guards) — once IP-9010 verifies, the rule is a tested property, not a convention; the standing checklist rule remains for future `ZONE_COLLECTS`-touching packages. | SCHEDULED |
| BL-0018 | 2026-07-06 | finding | `03-architecture-design-synthesis` run #9 ([GDS-04](../architecture/04-domain-model.md)) | **`SaveGame` does not persist `Player.Direction`/`Player.AnimationFrame`, or per-zone `ScoreItem` collected-state** — confirmed directly against the save routine's field list (only `CUR_ZONE`, `PLAYER_X`/`PLAYER_Y`, `CARROTS_COUNT`, `SCORE`, and `CARROT_FLAGS` are written). A reload always restores a default facing/frame, and every zone's stars/flowers reset to "all present" regardless of what was collected before the save. Cosmetic/minor-progression-loss only — not a functional defect, and plausibly an intentional simplification — but worth confirming as intended rather than assumed. **Revisited run #17** ([RQ-01](../requirements/01-functional-requirements.md) FR-5210/CR-01, [RQ-03](../requirements/03-requirements-review.md) finding #1): the baseline documented current behavior honestly (FR-5210) but explicitly could not resolve whether it's intended scope — needed a user decision. **Resolved run #18 (2026-07-07), explicit user decision:** facing direction/animation frame — "not important," no persistence added (FR-5210 kept as-is, narrowed to just these two fields). Per-zone ScoreItem collected-state — "should save and persist," approved and promoted to new requirement **FR-5220**; NFR-5200 widened accordingly. RQ-01/02/03/04 all updated as a delta (changelog entries, not a regeneration). | Medium | 04 | **DONE:** user decision recorded; FR-5210 reworded, FR-5220 added, NFR-5200/RQ-03 finding #1/RQ-04 rows updated, run #18. No further action — FR-5220's actual implementation is future `07-implementation-planning`/`08-code-implementation` work, tracked via the traceability matrix, not this backlog entry. | DONE |
| BL-0019 | 2026-07-06 | recommendation | `04-requirements-engineering` run #17 ([RQ-03](../requirements/03-requirements-review.md) finding #4) | **No requirement currently watches ROM-budget headroom as it's consumed.** NFR-4000 (32768-byte single-bank budget) is correctly "Met" today (23148/32768 bytes used, ~9.6KB headroom) and correctly names C7/ADR-0001 as its own future-supersession trigger — but nothing re-checks that status as future content packages consume the remaining headroom; a package could push usage near the ceiling with no requirement flagging it. | Low | 04 | SCHEDULED: any future package that materially increases ROM usage should include a checklist item re-affirming NFR-4000's "Met" status against remaining headroom (a concrete re-affirmation threshold, e.g. below 2KB remaining, is future `03-architecture-design-synthesis`/`05-feature-decomposition` work, not decided here). Not blocking any current work. **Honored (run #21):** NFR-4000 re-affirmation checklist items included in both ROM-growing packages (IP-9020, IP-1010). **Re-affirmed (runs #24/#26):** IP-9020 left ROM unchanged (23148 bytes); IP-1010 grew it to 23404/32768 bytes (+256) — ~9.1KB headroom remains, nowhere near a 2KB-remaining concern line. Standing convention continues for future ROM-growing packages. | SCHEDULED |
| BL-0020 | 2026-07-07 | doc-defect | `05-feature-decomposition` run #19 ([FP-05](../feature-planning/05-feature-review.md) finding #1) | **No numbered functional requirement covers player/collectible sprite (OAM) rendering as an observable behavior**, even though it is shipped, architecturally documented (8×16 OBJ pairs, shadow-OAM DMA — GDS-08 §2, ADR-0005, ADR-0007), and exercised by `test_rom.py`'s T6 suite. FR-2100/FR-2200 cover position/facing; FR-3100 covers collision detection; none states sprites are actually rendered. A genuine RQ-01 baseline gap, surfaced (not invented) by the Feature Decomposition review, which may not add FRs itself. | Low-Medium | 04 | SCHEDULED: a future `04-requirements-engineering` delta should add a plausible `FR-6400` ("Player and Collectible sprite rendering") under the Presentation grouping, citing GDS-08 §2/ADR-0005/ADR-0007. Not blocking any current Feature — the behavior is shipped and working; only the formal requirement statement is missing. **Resolved (`04-requirements-engineering` run, 2026-07-10, 04-delta batch):** FR-6400 added exactly as scheduled, citing T6.1–T6.10 as pre-existing evidence. | DONE |
| BL-0021 | 2026-07-07 | design-question | `05-feature-decomposition` run #19 ([FP-03](../feature-planning/03-feature-catalog.md) FEAT-5100 Open Questions) | **FEAT-5100 (per-zone ScoreItem persistence, formalizing FR-5220) has two unresolved design questions that must be answered before implementation:** (1) the exact SRAM byte layout for the new persisted field (a possible GDS-07 Data Model delta); (2) whether a save written *before* this feature ships should be treated on first load as "all ScoreItems already collected" or "all ScoreItems uncollected." Neither is assumed either way in the catalog. **Resolved run #20 ([FS-101](../features/FS-101-per-zone-scoreitem-persistence.md)):** (1) a 9-byte `SCOREITEM_FLAGS` array mirroring `CARROT_FLAGS`, exact WRAM address deferred to `07-implementation-planning`; (2) pre-upgrade saves default to "all uncollected," guarded by a new save-format version byte — both reasoned to confident recommendations, neither needed the user. | Medium | 06 | **DONE:** both questions answered in FS-101 with recorded reasoning. Remaining implementation-level detail (exact WRAM address) rides `07-implementation-planning` as normal package work, not a separate backlog item. | DONE |
| BL-0022 | 2026-07-07 | doc-defect | `06-feature-specification` run #20 ([FS-101](../features/FS-101-per-zone-scoreitem-persistence.md) Open Question 1) | **`FR-3200`'s postcondition is factually incorrect as currently shipped.** It claims a collected `ScoreItem` is "permanently inactive for the remainder of that play session," but direct code reading of `setup_zone_collects` (`asm_game.py`, confirmed by FS-101) shows `ScoreItem`s are reloaded active=1 from ROM on **every** zone entry — only `Carrot`s are protected against this via the `CARROT_FLAGS` check already in that routine. The requirements baseline asserts something not true of the shipped code. | Medium | 04 | SCHEDULED: a future `04-requirements-engineering` delta should correct `FR-3200`'s postcondition to match actual shipped behavior (respawns on zone re-entry within a session) — independent of whether/when FS-101/FEAT-5100 ships, since that Feature will fix the underlying behavior but the requirements text should stop misdescribing the *current* code regardless. Not blocking any current work. **Resolved (`04-requirements-engineering` run, 2026-07-10, 04-delta batch):** postcondition corrected to match `setup_zone_collects` exactly; confirmed consistent with `FR-5220`/`IP-1010`, which build persistence on top of the respawn behavior rather than assuming it away. | DONE |
| BL-0023 | 2026-07-07 | bug | `06-feature-specification` run #20 ([FS-101](../features/FS-101-per-zone-scoreitem-persistence.md) Open Question 2, Risks) | **`ScoreItem`s (stars/flowers) currently respawn every time a zone is re-entered within a play session** (see `BL-0022`) — meaning `Score` is infinitely farmable today by walking back and forth between two zones. `SCORE` (`0xC006`) is a single WRAM/SRAM byte with no documented overflow handling if repeatedly incremented past 255. Discovered incidentally while specifying `FEAT-5100`, not previously caught by `GDS-05`/`RQ-01`'s authoring passes. | Medium | 08 | SCHEDULED: `FEAT-5100`'s implementation (per `FS-101`'s design, extending the same zone-entry check `Carrot`s already use) fixes this as a side effect — no separate remediation package needed unless `FEAT-5100` is deprioritized far enough that the farming behavior needs an interim fix on its own. Not blocking any current work; not escalated to `NEEDS-USER` since `FEAT-5100` already fixes it as designed. **Packaged (run #21):** rides [IP-1010](../implementation/packages/IP-1010-per-zone-scoreitem-persistence.md) with a dedicated regression test (T11.a — collected item stays inactive and `SCORE` does not re-increment on zone re-entry). **Implemented run #26:** T11.a4/T11.a5 confirm the fix (score does not re-increment on zone re-entry); closes on IP-1010's Verification Report. **Resolved (run #27, [VR-1010](../implementation/verification/VR-1010-per-zone-scoreitem-persistence.md)): fix independently confirmed** — T11.a4/T11.a5 re-run in a fresh container prove a collected ScoreItem stays inactive and `SCORE` does not re-increment on zone re-entry; IP-1010 VERIFIED. | DONE |
| BL-0024 | 2026-07-07 | gate | `07-implementation-planning` run #21 ([Master Build Plan](../implementation/00-master-build-plan.md)) | **G3 authorization needed for three planned packages before any can start a stage-08 run:** (1) **IP-9010** (test-suite rewrite, BL-0006 Critical + BL-0005) — READY and the critical-path unblocker for *everything*, but BL-0006 is outside the BL-0001…0005 bootstrap carve-out; (2) **IP-9030** (root-doc refresh, BL-0007) — same reason; (3) **IP-1010** (FS-101 per-zone ScoreItem persistence) — genuinely new work, flagged since run #20. IP-9020/IP-9040 are already authorized (carve-out) but blocked on IP-9010, so **the whole build plan is gated on this decision.** | High (blocks the Critical BL-0006 remediation) | 08 | **Resolved (run #21, same day): user explicitly authorized all three** ("Authorize all three", 2026-07-07) — IP-9010, IP-9030, and IP-1010 all have G3 go-ahead on record; combined with the carve-out (IP-9020/IP-9040), every planned package is now authorized. Execution order remains dependency-driven: IP-9010 first. | DONE |
| BL-0025 | 2026-07-07 | recommendation | `08-code-implementation` run #22 (IP-9010) | **Runtime test artifacts are untracked repo-root noise:** the rewritten suite writes screenshots to `test_shots/` and PyBoy writes `BunnyQuest.gbc.ram` beside the ROM during runs (the suite wipes the `.ram` at exit, but an aborted run leaves it). No `.gitignore` exists to exclude them. Cosmetic only — nothing breaks. | Low | 07 | **Resolved (run #22, same day):** a tracked `.gitignore` already existed covering `*.gbc.ram`/`*.gbc.sav`/`__pycache__`; `test_shots/` appended to it (one-line hygiene fix, prompted by the repo's clean-tree hook — no package needed). | DONE |
| BL-0026 | 2026-07-07 | doc-defect | `09-package-verification` run #23 ([VR-9010](../implementation/verification/VR-9010-test-suite-rewrite.md)) | **`IP-9010`'s §3 "Requirements Covered" cites `NFR-7000`, which does not exist anywhere in the requirements baseline** (`docs/requirements/02-non-functional-requirements.md` has no such section; the RTM has no such row). The nearest match by subject is **`NFR-6100`** ("PyBoy headless as the verification target") — its RTM Test cell is still `UNASSIGNED (inspection-based)` even though every one of the rewritten suite's 109 checks now runs headless via PyBoy, direct repeatable evidence for it. Not a functional defect — the underlying capability is real and independently confirmed by VR-9010 — purely a citation/traceability gap. | Low | 04 | SCHEDULED: a future `04-requirements-engineering` delta should fill `NFR-6100`'s RTM Test cell citing the full T1–T10 suite as headless-execution evidence; `07-implementation-planning` should correct `IP-9010`'s citation from `NFR-7000` to `NFR-6100` next time that package is touched (informational only, does not block anything downstream). | Partially resolved (`04-requirements-engineering` run, 2026-07-10, 04-delta batch): RTM cell filled. **`IP-9010`'s own citation still reads `NFR-7000`** — rides a future `07-implementation-planning` touch of that package (out of `04`'s write scope), not blocking anything. **Fully resolved (07-touch, 2026-07-10):** `IP-9010`'s citation corrected to `NFR-6100`. | DONE |
| BL-0027 | 2026-07-07 | doc-defect | `08-code-implementation` run #25 (IP-9040) | **`.claude/skills/run-bunnygarden/SKILL.md` still documents the pre-`IP-9010` hardcoded paths** (`/mnt/user-data/outputs/BunnyGarden.gbc[.ram]`, `/home/claude/bunnygarden/`) and the stale `BunnyGarden.gbc` filename, even though `test_rom.py` has run repo-relative against `BunnyQuest.gbc` since `IP-9010` (run #22). Not named by `BL-0007`/`IP-9030` (which scope only `Claude.md`/`memory.md`/`README.md`) — a genuine gap between what those track and what's actually still stale. Found incidentally while archiving the legacy artifacts (`IP-9040`); out of that package's scope to fix. | Low | 07 | SCHEDULED: fold into a future `07-implementation-planning` hygiene pass (natural rider for `IP-9030`'s root-doc refresh, or its own small package) — update the skill doc's commands/paths to match `test_rom.py`'s current repo-relative form. Not blocking any current work. **Resolved (07-touch, 2026-07-10):** the skill doc was far more stale than just paths (stale filename, "v2.1"/"88/88" status line, "gifts" variable naming) — rewritten in full to match current reality, with forward notes flagging where FEAT-1100/FEAT-9000 will change two gotchas once those still-unimplemented packages ship. | DONE |
| BL-0028 | 2026-07-07 | doc-defect | `09-package-verification` runs #27/#28 ([VR-1010](../implementation/verification/VR-1010-per-zone-scoreitem-persistence.md) finding #1, [VR-9020](../implementation/verification/VR-9020-score-bar-vblank-fix.md) finding #1) | **Stale "pending independent verification by `09-package-verification`" clauses in the requirements/architecture record** — the verifications now exist, so the hedges are outdated: RQ-02's `NFR-5200` status line (VR-1010), RQ-02's `NFR-1200` status line + GDS-06 N2's remediation note (VR-9020), and the RTM `NFR-1200` cell's snapshot "111/111" count (125/125 today). Cosmetic only; every underlying cell/claim is accurate. **Widened run #28** to cover both VRs' instances in one entry. | Low | 04 | SCHEDULED: fold into the next `04-requirements-engineering` delta batch alongside BL-0020 (missing sprite-rendering FR), BL-0022 (FR-3200 postcondition correction), and BL-0026 (NFR-6100 RTM Test cell) — one pass; the GDS-06 N2 pointer note rides the same pass (03-owned, one-word edit). Not blocking any current work. **Resolved (`04-requirements-engineering` run, 2026-07-10, 04-delta batch):** RQ-02's NFR-5200/NFR-1200 status lines, the RTM's NFR-1200 snapshot count, and NFR-7100's own identical staleness (caught incidentally) all refreshed; GDS-06 N2's pointer note also fixed (a trivial one-line edit, done alongside despite being 03-owned). | DONE |
| BL-0029 | 2026-07-09 | feature | intake: project owner — adopted increment plan ([PLAN-requirements-aesthetics-story-map.md](PLAN-requirements-aesthetics-story-map.md) §4 stream A; owner direction D4 in §0) | **Stream A — aesthetic-quality standard.** Adopt a vision-level presentation-quality commitment and a *normative* aesthetic standard (GDS-08 delta), then derive measurable presentation NFRs, per the adopted plan's Phase 1→3→4 path. Owner's quality bar on record (D4): the result must be **smooth** and **every screen/room/view clean**. Research grounding already exists (R202/R203/R208/R209/R210/R211, filed via BL-0013 — adjacent, not duplicate: that entry produced the research; this one produces the standard and requirements). No duplicate found (checked BL-0001…BL-0028). | Medium (owner: part of adopted increment; no urgency stated) | 01 → 03 → 04 (per plan §1) | **DONE (`00-pipeline-manager` run #42):** stage 01 (C8) → stage 03 (GDS-08 §7 normative standard) → stage 04 (NFR-1300/NFR-6500/NFR-6510, RQ-01..04 delta) all complete. This entry's full plan-stated path is closed; remaining work (05-feature-decomposition onward) rides the normal Master Build Plan, not this backlog entry. | DONE |
| BL-0030 | 2026-07-09 | feature | intake: project owner — adopted increment plan ([PLAN-requirements-aesthetics-story-map.md](PLAN-requirements-aesthetics-story-map.md) §4 stream B; owner direction D1/D2 in §0) | **Stream B — visual story narrative via biome-flow grammar.** New vision-level scope: the game tells its story visually through a **logical biome adjacency grammar between whole screens** (e.g. water→beach→grassland→hills→mountains→sky; one biome per screen, never two in a frame, no disjointed pairings), with an **item-agnostic, child-friendly collect-goal** following the narrative (carrots are an instance, not a requirement). **No text dialogue requirement** (D1 — not ruled out, not required; a future dialogue request re-enters at 01). Agent constructs the grammar from research (proposed R212, plan Phase 2). No duplicate found. | Medium (owner: part of adopted increment) | 01 → 02 → 03 → 04 (per plan §1) | **DONE (`00-pipeline-manager` run #42):** stage 01 (C9) → 02 (R212) → 03 (GDS-01 §3a, GDS-04 KeyItem/grammar rules, GDS-08 §8) → 04 (FR-3220/4300/4310, RQ-01..04 delta) all complete. This entry's full plan-stated path is closed. | DONE |
| BL-0031 | 2026-07-09 | feature | intake: project owner — adopted increment plan ([PLAN-requirements-aesthetics-story-map.md](PLAN-requirements-aesthetics-story-map.md) §4 stream C; owner direction D3/D5/D6/D7/D8/D9/D10 in §0) | **Stream C — deterministic procgen world from (seed, scale) + menu-flow delta + 3×3 archival.** The world becomes **deterministically procedurally generated** from a user-modifiable **seed** and **world-scale** parameter, both set **only at new-game start** and immutable per save (D3/D6/D7); named research topics: procgen map algorithms and GBC homebrew with procgen maps (proposed R213/R214) — the agent picks the algorithm from research. **Menu-flow delta (D8/D9/D10):** boot → title → **main menu** (continue / new game → seed & scale entry), **no auto-load at boot** (amends MSTR-001 C2's auto-load clause — deliberate protected-baseline change), and the start-button save menu gains **exit-to-main-menu with auto-save**. The handcrafted 3×3 world is **archived** per the `legacy/` precedent (D5 — second protected-baseline change, C5). Cross-ref: **closes BL-0015** (wider vs deeper) per plan §6 — scale is now a user parameter, the fork dissolves; manager to flip BL-0015 at triage with that rationale. No other duplicate found. | Medium (owner: part of adopted increment) | 01 → 02 → 03 → 04 (per plan §1) | **DONE (`00-pipeline-manager` run #42):** stage 01 → 02 (R213/R214/R111) → 03 (ADR-0009/0010/0011 + six GDS deltas) → 04 (FR-9100–9200, NFR-1300/2200/4200/5300, RQ-01..04 delta) all complete. This entry's full plan-stated path is closed — **the adopted increment plan's terminal deliverable (Phase 4) is reached.** | DONE |
| BL-0032 | 2026-07-09 | doc-defect | `02-research-gbc-hardware` run #33 (R102 extension) | **R101 and R102 cite two different figures for VBlank's duration** — R101 says "~1140 T-state VBlank period," R102 says "lines 144–153, ~4560 T-states total." Neither topic resolves the discrepancy; R102's new full-screen-redraw-cost analysis (grounding MSTR-001 C10/C8) flagged it inline rather than silently picking one, since the qualitative conclusion (a 1152-byte tilemap copy doesn't fit in VBlank regardless of which figure is right, hence the project's existing LCD-off approach) holds either way. | Low | 02 | SCHEDULED: a future `02-research-gbc-hardware` pass should directly fetch Pan Docs' VBlank-duration figure (both prior citations rested on earlier sessions' fetches or WebSearch summaries) and correct whichever of R101/R102 is wrong. Not blocking any current work — the redraw-cost conclusion doesn't depend on the exact figure. **Resolved (`02-research-gbc-hardware` run, 2026-07-10):** directly fetched Pan Docs (GitHub mirror) — VBlank is exactly 4560 T-states. R101's "~1140 T-state" was a units error (1140 is the M-cycle count, mislabeled); corrected to cite both units. R102's figure upgraded from approximate to exact. | DONE |
| BL-0033 | 2026-07-09 | doc-defect | `03-architecture-design-synthesis` run #37 (GDS-04 delta, incidental finding) | **GDS-04's "Relationships worth naming explicitly" §3rd bullet is stale relative to the shipped code** — it states "all `ScoreItem` positions/consumed-state per zone[] are not persisted... only the scarce tier's collection state persists," but `IP-1010`/`FS-101` (implemented and verified 2026-07-07, before this GDS-04 delta) added exactly that: `SCOREITEM_FLAGS` (`0xC060`–`0xC068`) persists per-zone `ScoreItem` collected-state to SRAM (`0xA013`–`0xA01B`, per `FR-5220`, confirmed in `GDS-07` §2/§3). GDS-04's own bullet was not updated when `FS-101` shipped — noticed incidentally while authoring the GDS-04 procgen-world delta; out of that delta's scope to fix (it's a pre-existing gap, not part of the increment). | Low-Medium | 03 | SCHEDULED: a future `03-architecture-design-synthesis` touch of GDS-04 (or a dedicated small pass) should correct the bullet to state `ScoreItem` collected-state *does* persist per-zone (citing `FR-5220`/`GDS-07`), matching `IP-1010`'s shipped behavior. Not blocking any current work. **Resolved (`03-architecture-design-synthesis` run, 2026-07-10):** bullet corrected exactly as scheduled, citing `IP-1010`/`FS-101`/`VR-1010` and matching GDS-07 §2/§3's WRAM/SRAM tables. | DONE |
| BL-0034 | 2026-07-09 | doc-defect | `03-architecture-design-synthesis` run #41 (GDS-10 delta, incidental finding) | **GDS-10's own merge decision (2026-07-06) was never executed.** It states: "this level stays authoritative for the traceability scheme... until `RQ-04` is authored, at which point `RQ-04` becomes authoritative for row-level data and this level's §3/§4 become the pointer." `RQ-04` was authored at run #17 (2026-07-06, same day) and is already a populated, correctly-cross-referencing matrix — but GDS-10's §3/§4 were never converted to pointers, and its ID-scheme table (§2) sat stale (showing `RQ`/`FP`/`FS`/`IP`/`VR` as "not yet authored" through the entire bootstrap increment) until this same run refreshed the example column only. Noticed while confirming the ID scheme extends to the procgen-world increment's new artifacts; the deeper restructuring (§3/§4 → pointers to `RQ-04`) is out of that light-delta's scope. | Low | 03 | SCHEDULED: a future `03-architecture-design-synthesis` pass should convert GDS-10 §3/§4 into short pointers to `RQ-04`, per this document's own already-recorded merge decision — a bootstrap-increment housekeeping item, not blocking any current work. **Resolved (`03-architecture-design-synthesis` run, 2026-07-10):** §3 converted to a pointer at the populated `RQ-04`; §4 confirmed as an actively-exercised traceability lane (citing `BL-0029`/`0030`/`0031`'s own three-stage thread). Closes the last open item from GDS-10's original merge decision. | DONE |
| BL-0035 | 2026-07-10 | doc-defect | `10-integration-review` run (bootstrap tranche, [integration-review-bootstrap-tranche.md](../reviews/integration-review-bootstrap-tranche.md) finding #1) | **`ROADMAP.md`'s Implementation theme table (`IM-00`/`IP-xxxx`/`VR-xxxx` rows) is stale** — `IP-xxxx` still reads "authored 2026-07-07; none implemented" and `VR-xxxx` still reads "⛔", even though all five bootstrap packages are now `VERIFIED` with five Verification Reports on record. No stage-08/09 run touches this particular ROADMAP table. | Medium | 07 | SCHEDULED: a future `07-implementation-planning` touch (or a small standalone hygiene pass) should refresh these three rows to state all five packages `VERIFIED`. Not blocking any current work — every other ledger (Master Build Plan, packages `INDEX.md`, verification `INDEX.md`) already tells the accurate story. **Resolved (`07-implementation-planning` run, 2026-07-10):** all three rows refreshed while authoring the Release-2 tranche's own package rows in the same table — a natural fold-in since this stage owns the table. | DONE |
| BL-0036 | 2026-07-10 | doc-defect | `10-integration-review` run (bootstrap tranche, [integration-review-bootstrap-tranche.md](../reviews/integration-review-bootstrap-tranche.md) finding #2) | **`docs/feature-planning/01-release-plan.md` §Release 1 and `03-feature-catalog.md`'s `FEAT-5100` entry both still describe it as unimplemented** ("no shipped implementation" / "new — not yet implemented"), even though `IP-1010`/`FS-101` shipped and were independently verified 2026-07-07 — these feature-planning docs were authored the same day but never revisited afterward. | Medium | 05 | SCHEDULED: rides the next `05-feature-decomposition` touch — natural to fold in alongside the procgen-world increment's own upcoming Feature Catalog additions (new `FEAT-xxxx` rows for the RQ-01…04 delta), since that pass will already be editing both documents. Not blocking any current work. **Resolved (`05-feature-decomposition` run, 2026-07-10):** both documents corrected — FEAT-5100's catalog entry now states shipped/VERIFIED status with citations to `IP-1010`/`VR-1010`; Release 1's bucket description and header both corrected. Folded in exactly as scheduled, alongside the five new Feature rows. | DONE |
| BL-0037 | 2026-07-10 | doc-defect | `05-feature-decomposition` run ([05-feature-review.md](../feature-planning/05-feature-review.md) finding #5) | **`docs/feature-planning/03-feature-catalog.md`'s `FEAT-6000`/`FEAT-7000` entries still describe `NFR-1200`/`NFR-7100` as current non-compliances in their own Risk fields** — both are now Met, VERIFIED 2026-07-07 (`IP-9020`/`VR-9020`, `IP-9010`/`VR-9010`) and reconfirmed by `10-integration-review` (2026-07-10). The equivalent statements in the Epic Catalog and Release Plan were corrected in the same pass that found this (per `BL-0036`'s instruction), but this pass's scope did not extend to editing FEAT-6000/FEAT-7000's own catalog entries. | Low | 05 | SCHEDULED: a future light `05-feature-decomposition` touch should update FEAT-6000/FEAT-7000's Risk fields to reflect both resolutions. Not blocking any current work — every ledger that actually gates on these NFRs (RTM, Master Build Plan) already shows the correct Met status; only these two Features' own prose lags. **Resolved (`05-feature-decomposition` run, 2026-07-10):** both Risk fields corrected to Low citing VERIFIED status; FP-05 finding #5 marked resolved. | DONE |
| BL-0038 | 2026-07-10 | design-question | `06-feature-specification` runs ([FS-102](../features/FS-102-procedural-world-generation.md) Open Questions 1–3, [FS-103](../features/FS-103-generated-region-screen-composition.md) Open Questions 1–2) | **FEAT-9000 (procedural world generation) and FEAT-4100 (generated-region screen composition) share five unresolved design questions before implementation can begin, all converging on one underlying deferral:** (1) the concrete biome-adjacency grammar table's exact contents — R212 gives only a conceptual example, GDS-08's delta explicitly defers exact biome-family count/assignment "to the implementation package that sizes it" (FS-102 OQ1 = FS-103 OQ1, the same deferral seen from two Features); (2) the generation algorithm's line-by-line implementation detail (ADR-0009/R213 fix the approach, not the routine's exact step order); (3) whether a generator-data ROM pointer is needed in the `patches` dict, downstream of (1); (4) the exact tile-index ranges/palette assignments for whatever biome-family set is chosen (FS-103 OQ2, also downstream of (1)). All are named with their resolving stage already identified. | Medium | 07 | SCHEDULED: all five ride the same `07-implementation-planning` pass that packages FEAT-9000/FEAT-4100 together — per GDS-08's own explicit deferral (question 1, which questions 3–4 depend on) and this stage's normal implementation-detail authority (question 2). Not escalated to the user — each has a clear, already-identified resolution path, following the FS-101 precedent for confidently-resolvable design questions. **Resolved (`07-implementation-planning` run, 2026-07-10, [IP-1020](../implementation/packages/IP-1020-procedural-world-generation.md)/[IP-1030](../implementation/packages/IP-1030-generated-region-screen-composition-code.md)/[IP-1031](../implementation/packages/IP-1031-generated-region-screen-composition-content.md)):** biome-family vocabulary = the 5 existing shipped terrain families (zero new art); linear grammar axis Water-Sand-Grass-Stone-Brick, adjacency legal iff axis indices differ by ≤1 (no ROM table needed, resolving Q3); flood-fill generation algorithm over the fixed `scale×scale` grid, satisfying determinism/reachability/grammar-validity by construction. | DONE |
| BL-0039 | 2026-07-10 | design-question | `06-feature-specification` run ([FS-104](../features/FS-104-main-menu-new-game-flow.md) Open Questions 1–2) | **FS-104 (FEAT-1100, main menu & new-game flow) has two unresolved design questions:** (1) whether SEED/SCALE ENTRY offers a cancel/back-to-MAIN-MENU path — neither ADR-0010 nor GDS-01 §4a's state diagram names one, leaving a player who selects "new game" by mistake with no way back short of completing a full seed/scale entry and generation; (2) whether "continue"/"new game" selection uses A-confirm-only or a D-pad up/down selector — an input-mapping detail GDS-01 §4a doesn't specify. | Low-Medium | 07 | SCHEDULED: both ride the `07-implementation-planning` pass that packages FEAT-1100 — question 1 is a genuine player-experience gap worth resolving deliberately (most likely a B-cancels convention matching the existing SAVE state's own A/B pattern, per FS-104's own reasoning, but not assumed there); question 2 is ordinary UI implementation detail. Not escalated to the user — both have clear, confidently-resolvable paths, following the FS-101 precedent. **Resolved (`07-implementation-planning` run, 2026-07-10, [IP-1040](../implementation/packages/IP-1040-main-menu-new-game-flow.md)):** B cancels SEED/SCALE ENTRY back to MAIN MENU (matching SAVE's own A/B convention); D-pad up/down selects the highlighted MAIN MENU option, A confirms. | DONE |
| BL-0040 | 2026-07-10 | gate | `00-pipeline-manager` run #53 (both main-thread gates asked in one batch, per Step 4) | **Two G3/G4-class human decisions, both resolved in the same user response:** (1) Bootstrap tranche (Baseline + Release 1, 5 VERIFIED packages, clean integration review) — user directed **"Run 11 release with a GO"**, i.e. produce the evidence-based Release Assessment and record the GO on the strength of that evidence. (2) Release 2 tranche (procgen-world: `IP-1020`/`1030`/`1031`/`1040`/`1050`, genuinely new work, no bootstrap carve-out) — user selected **"Authorize all five."** | High (unblocks both stalled threads) | 11 / 08 | **Resolved same run.** Part (1): `11-release-readiness` invoked this run — [release-assessment-bootstrap-tranche.md](../reviews/release-assessment-bootstrap-tranche.md) written, GO recorded, baseline flipped (ROADMAP.md RV-RELEASE row, `01-release-plan.md` bucket headers, `Claude.md` status line, reviews INDEX). Part (2): G3 authorization recorded here for `IP-1020`/`1030`/`1031`/`1040`/`1050` — rides the next `00-pipeline-manager` advance (`08-code-implementation` on `IP-1020`, the tranche's unblocked foundational package; the other four stay `BLOCKED` on `IP-1020` per the Master Build Plan's own dependency graph, not on authorization). | DONE |
| BL-0041 | 2026-07-10 | doc-defect | `00-pipeline-manager` run #54 drift correction + `09-package-verification` run ([VR-1020](../implementation/verification/VR-1020-procedural-world-generation.md) finding #2) | **`ROADMAP.md`'s `IM-00`/`IP-xxxx` summary rows (lines 109/111) went stale within the same increment that produced `BL-0035`'s identical fix.** They still read "Release 2 tranche's 5 packages planned 2026-07-10, none authorized"/"not authorized — G3 pending" even after (a) the user's explicit G3 authorization of all five packages (`BL-0040`) and (b) `IP-1020`'s own `COMPLETE`→`VERIFIED` transition (this run) — neither the `08-code-implementation` run that implemented `IP-1020` nor the `00-pipeline-manager` run that recorded the G3 answer touched this particular table. Same recurring pattern as `BL-0035` (bootstrap tranche, same table, same root cause: no stage-08/09 run owns this ROADMAP table by default). Also discovered: the journal's run #53 Position block recorded "next step: `08-code-implementation` on IP-1020" as still-pending, when `IP-1020` had already gone `COMPLETE` outside a journaled run by the time run #54 started — corrected in run #54's journal entry, not a backlog item of its own (journal-only drift, no ledger consequence beyond the journal itself). | Medium | 07 | SCHEDULED: rides the next `07-implementation-planning` touch (that stage owns this ROADMAP table per `BL-0035`'s precedent), or a dedicated backlog-hygiene pass if none comes soon. Not blocking any current work — every other ledger (Master Build Plan, `packages/INDEX.md`, verification `INDEX.md`) already tells the accurate story after this run's updates. Recommend: **fold in a standing note** to whichever stage next edits `ROADMAP.md`'s Implementation theme table to double-check `IM-00`/`IP-xxxx`/`VR-xxxx` currency, since this is the second time in one increment the same table drifted unnoticed. **Resolved (`07-implementation-planning` run, 2026-07-11):** all three rows refreshed — Release 2 authorized, 4/5 packages `VERIFIED`, `IP-1031` `COMPLETE`; `VR-xxxx` now lists all nine existing reports. | DONE |
| BL-0042 | 2026-07-10 | doc-defect | `00-pipeline-manager` run #55 Step-1 drift correction | **`packages/INDEX.md` had gone stale for `IP-1030`/`1040`/`1050` — the exact `BL-0041`/`BL-0035` recurrence, one ledger over.** By the time this run started, `IP-1030`/`1040`/`1050` had all been implemented out-of-band (commits `3479dba`/`73cea72`/`5f58ab5`, following run #54's "Then move onto IP" instruction) and stood `COMPLETE` (136/136, 163/163, 180/180) in the Master Build Plan — but `packages/INDEX.md` still read all three `READY`, and the journal's own run #54 Position block recorded "next step: `08-code-implementation` on IP-1030" as still-pending. The out-of-band `08-code-implementation` sessions updated the Master Build Plan (per their own commit messages) but not `packages/INDEX.md`, the same split-ledger-ownership gap `BL-0035`/`BL-0041` already named for `ROADMAP.md`. | Medium | 09 | **Resolved same run.** This run's `09-package-verification` invocation on `IP-1030` corrected its own `packages/INDEX.md` row to `VERIFIED` (unblocking `IP-1031` to `READY`) as part of writing `VR-1030`; the pipeline manager additionally corrected `IP-1040`/`1050`'s rows from stale `READY` to accurate `COMPLETE` (awaiting their own future verification) while reconciling this run, since leaving them `READY` would have been actively misleading (both are already implemented, not merely eligible). Recommend: **fold in a standing note** to `08-code-implementation`/`08-content-authoring`'s own workflow to update `packages/INDEX.md` in the same commit that flips a package `COMPLETE` in the Master Build Plan, since this is now the second ledger (after `ROADMAP.md`) shown to drift from the same root cause (out-of-band sessions touching one ledger but not its sibling). | DONE |
| BL-0043 | 2026-07-11 | recommendation | `08-content-authoring` run (IP-1031) | **The 07→08 code/content package split assumed content work remained separable from code work, but in practice `IP-1030` (code half) delivered `IP-1031`'s (content half) entire payload as an inherent side effect.** `IP-1030` generalized `ALL_SCREENS` from a fixed 14-entry list to a 5-biome-family + 5-UI shape — to keep the code buildable/testable, it had to choose *some* default `(family_name, fn)` mapping, and the mapping it chose (water→lake_screen, sand→beach_screen, grass→forest_screen, stone→mountain_screen, brick→castle_screen) turned out to be exactly what `IP-1031` was planned to author. `IP-1031`'s actual run found zero code left to write — it became a confirmation-and-documentation pass instead. Not a defect (both packages' Definitions of Done are independently satisfied, and 09-package-verification still has a real, non-trivial job confirming IP-1031's DoD claims), but a planning-time blind spot: a code package that generalizes a data structure will often need to populate that structure's *default values* to stay buildable, silently pre-empting a paired content package's reason to exist. | Low | 07 | SCHEDULED: `07-implementation-planning` should watch for this coupling risk when splitting a "generalize the shape" code package from a "populate the shape" content package sharing one data structure — either sequence them so the code package's default values are deliberately left minimal/placeholder (forcing the content package to do real work), or fold both into one package when the split is this tight. Not blocking any current work — no packages are currently mid-split in this shape. | SCHEDULED |
| BL-0044 | 2026-07-11 | finding | `09-content-review` run ([content-review-IP-1031.md](../reviews/content-review-IP-1031.md) finding #1) | **The Stone(3)↔Brick(4) biome-family adjacency is the one grammar-legal pair GDS-08 delta §8's worked example doesn't cover, and it has the largest hue jump of the four pairs the review exercised** (achromatic gray → saturated warm red, versus the other three pairs' close match to the cited water→sand→grass→stone worked example). Not arbitrary — a gray-stone-to-red-brick fortress reading is thematically plausible — but a genuinely different kind of "step" than the rest of the axis. | Low | 03 | SCHEDULED: a future `03-architecture-design-synthesis` touch of GDS-08 §8 could extend the worked example to explicitly address a gray→masonry-red step, confirming or revising this axis's terminal pairing. Informational per NFR-6510's own "Should," not "Must" priority — not blocking any current work. | SCHEDULED |
| BL-0045 | 2026-07-11 | doc-defect | `09-content-review` run ([content-review-IP-1031.md](../reviews/content-review-IP-1031.md) finding #3) | **`NFR-6500`/`NFR-6510`'s Status fields still read "NOT YET IMPLEMENTED... no biome-family palette assignments exist yet to check"** — stale now that `09-content-review` has exercised both NFRs' own Acceptance Criteria against `IP-1031`'s content with a documented clean result (one Low/informational finding, no Critical/High/Medium). Out of `09-content-review`'s own write scope (requirements documents are `04-requirements-engineering`'s). | Low | 04 | SCHEDULED: a future `04-requirements-engineering` delta should flip both NFR-6500/NFR-6510 to Met, citing `content-review-IP-1031.md`. Not blocking any current work. **Resolved (`04-requirements-engineering` run, 2026-07-11):** both flipped to Met, RTM rows/changelogs/INDEX updated, Requirements Review finding #12 confirms the flip against the actual review evidence. | DONE |
| BL-0046 | 2026-07-11 | doc-defect | `00-pipeline-manager` run #60 (BL-0041 fix, incidental finding) | **`ROADMAP.md`'s `FS-101+` row (Features theme table) is stale in the same way the Implementation theme table was** (`BL-0041`/`BL-0035`'s recurring pattern, a third table now) — it still reads "FS-102–106 ✅ 2026-07-10, all five Release-2 Features specified, **none yet implemented** — 7 Open Questions total routed to `07`," even though all five are now implemented (4 packages `VERIFIED`, one `COMPLETE`) and every routed Open Question was resolved at `07-implementation-planning`'s own pass (2026-07-10). Noticed while fixing `BL-0041`'s adjacent Implementation-theme table; out of that fix's own named scope (Features theme is `06-feature-specification`'s table, not `07`'s). | Low | 06 | SCHEDULED: a future `06-feature-specification` touch (or a dedicated small hygiene pass) should refresh the `FS-101+` row to state all five Release-2 Features implemented, zero Open Questions outstanding. Not blocking any current work — every other ledger (Master Build Plan, Features `INDEX.md`) already tells the accurate story. **Resolved (`06-feature-specification` run, 2026-07-11):** `ROADMAP.md`'s `FS-101+` row and `docs/features/INDEX.md`'s FS-102–106 rows all refreshed; `FS-102`'s own forward-reference metadata (still said "verification pending" though `VR-1020` has existed since 2026-07-10) also caught and fixed in the same pass. | DONE |
| BL-0047 | 2026-07-11 | bug | intake: project owner ("The map remains a 3x3 grid regardless of map scale and seed. It should not be a grid. It should be a procedurally generated shaped map.") | **`check_zone_transition` (`asm_game.py:566`) was never updated by `IP-1020`/`1030` — it is still 100% the pre-procgen, hardcoded fixed-9-zone 3×3 logic** (`CUR_ZONE` compared against literal `2`/`5`/`8` for the right edge, `0`/`3`/`6` for the left edge, `<3`/`>=6` for top/bottom, `±1`/`±3` arithmetic to move between "zones") — it never reads `REGION_GRAPH` at all, unlike `dsr_p`/`draw_region_arrows` (rendering), which correctly do. **Reproduced directly:** new game with `seed=7, scale=5` (25 generated regions) — `generate_world` correctly populates a 25-region `REGION_GRAPH` (confirmed by `T12`'s own suite), but walking right from region 0 only ever reaches `CUR_ZONE=2` and then hard-stops, exactly the old 3×3 model's right-edge zone — the other 22 generated regions are completely unreachable regardless of `WORLD_SCALE`. At `scale=3` this coincidentally looks correct only because the old hardcoded topology happens to match a 3×3 layout by accident, not because navigation is actually driven by the generated graph. Confirms the user's report exactly: **the traversable world is always a 3×3 grid, independent of `WORLD_SCALE`/seed**, contradicting `ADR-0009`'s decision that navigation should read the generated adjacency graph and `FR-9100`'s determinism/scale guarantee (generation is real; navigation ignores it). | **Critical** — the procgen-world increment's headline capability (`WORLD_SCALE`-driven map size) is non-functional for any scale ≠ 3, and even at scale 3 the "generated" navigation is coincidental, not real | 07 | Not deferred (Critical). **Remediation shape:** `check_zone_transition` needs to be regeneralized to read `REGION_GRAPH`'s per-region neighbor bytes (the same 5-byte-per-region structure `dsr_p`/`draw_region_arrows` already correctly consume) instead of the hardcoded `CUR_ZONE` arithmetic — mirroring the generalization `IP-1030` already did for rendering, just applied to the navigation half that was missed. Separately: the user's "it should not be a grid, procedurally generated shaped map" framing asks for a non-rectangular world *topology*, which is a different, larger ask than "navigation should respect the grid `WORLD_SCALE` already generates" — `ADR-0009`'s own Decision explicitly commits to a `scale`×`scale` region grid (a deliberate, cited architecture decision, R213/R214-grounded), so a genuinely non-grid/organic world shape would reverse that ADR and needs its own `03-architecture-design-synthesis` pass, separate from this remediation. Recommend the `07` remediation package fix the grid-navigation defect first (restores the already-decided/already-generated behavior); route the non-grid-shape request as its own design-question if the user wants it pursued after seeing the grid actually work end-to-end. **Implemented (`IP-9050`, 2026-07-11): `check_zone_transition` fully regeneralized to `REGION_GRAPH`, 213/213 pass — `IP-9050` reached `COMPLETE`.** **Verified (`09-package-verification`, 2026-07-12, [VR-9050](../implementation/verification/VR-9050-generated-world-navigation-fix.md)): independently confirmed, `IP-9050` → `VERIFIED`.** The non-grid-shape follow-up materialized as this session's `BL-0064`–`BL-0067` cluster, exactly as anticipated above. | DONE |
| BL-0048 | 2026-07-11 | bug | intake: project owner ("Once a game has been saved and the Gameboy is power cycled, the main menu offers continue and new game; however, it is not possible to select new game... up/down arrows should move the cursor.") | **`check_save_valid` (`asm_game.py:1290`) unconditionally resets `MM_CURSOR` to `0` ("continue") every time it runs — and `mm_on_entry` calls `check_save_valid` on *every* MAIN MENU redraw, including the redraw the player's own UP/DOWN toggle just triggered**, immediately clobbering the toggle back to `0` before the cursor is ever drawn at the new position. **Reproduced directly:** boot with a valid save present (`MM_SAVE_VALID=1`), `MM_CURSOR` starts at `0`; pressing DOWN twice then UP once — `MM_CURSOR` reads `0` after every single press. The screen full-redraws each press (LCD-off `copy_screen`, matching the reported "flash white"), but the cursor tile is always drawn at the "continue" position regardless of input, because `check_save_valid`'s own reset runs again immediately after the toggle. With any save present, **"new game" is completely unreachable from MAIN MENU** — the SEED/SCALE ENTRY flow (and therefore starting any new world) cannot be entered at all once a save exists. | **High** — total UI dead-end for a core, just-shipped flow (FR-1170/1180); the only workaround is deleting/corrupting the save file outside the game | 07 | Not deferred (High, a real functional block, not cosmetic). **Remediation shape:** `check_save_valid`'s `MM_CURSOR`-reset side effect needs to stop firing on every redraw — either split "recompute `MM_SAVE_VALID`" from "reset `MM_CURSOR` to its default," calling the reset only on genuine state-entry (boot into MAIN MENU, arrival from VICTORY/SAVE) and not on the redraw a same-state cursor toggle causes, or have `st_main_menu`'s toggle path skip re-invoking `check_save_valid` entirely (it already knows `MM_SAVE_VALID` didn't change, only `MM_CURSOR` did). **Implemented (`IP-9060`, 2026-07-11): the reset moved to `mm_on_entry`, gated on a new `MM_JUST_ENTERED` flag; 205/205 pass — `IP-9060` reached `COMPLETE`.** **Verified (`09-package-verification`, 2026-07-12, [VR-9060](../implementation/verification/VR-9060-main-menu-cursor-fix.md)): independently confirmed, `IP-9060` → `VERIFIED`.** | DONE |
| BL-0049 | 2026-07-11 | bug | intake: project owner ("...there is the option to push a or b to either save or exit back to game but there is no option to return to the main menu... the a or b selection method may need to be reconsidered.") | **The exit-to-main-menu mechanism (`st_save`'s `SELECT` branch, `IP-1040`) is actually wired and functional — reproduced directly, `GameState` goes `SAVE(3)`→`MAIN MENU(6)` on a `SELECT` press — but `save_screen()` (`tilemaps.py:309`) only renders `"A: YES"` / `"B: NO"`, with no on-screen text for the third option at all.** From the player's seat this matches the report exactly: there is no *visible, discoverable* option to return to the main menu, even though a hidden `SELECT` binding exists. Confirmed by direct screenshot of the SAVE menu — only two lines of text render. | Medium — a real feature (`FR-1190`) is present in code but functionally undiscoverable without reading source, which for a player is indistinguishable from "the option doesn't exist" | 07 | **Packaged (run #83, `07-implementation-planning`, 2026-07-11): [IP-9080](../implementation/packages/IP-9080-save-screen-third-option-labeling.md) — implemented, `VERIFIED` 2026-07-12 ([VR-9080](../implementation/verification/VR-9080-save-screen-third-option-labeling.md)): independently confirmed via a fresh PyBoy screenshot, "SELECT: SAVE"/"AND EXIT" renders cleanly.** | DONE |
| BL-0050 | 2026-07-11 | design-question | intake: project owner ("The select button map menu brings up the status of collectibles in a 3x3 grid which are named by zone. This is leftover from the original layout and no longer meets the intent. A high level replanning... is needed.") | **`map_screen()` (`tilemaps.py:319`) is confirmed, by direct code read, to be the unmodified pre-procgen fixed-9-zone status screen** — a hardcoded 3×3 grid of literal zone-name labels (`"BCH"`, `"FOR"`, `"MTN"`, `"LAK"`, `"VIL"`, `"CAV"`, `"DES"`, `"PLN"`, `"CTL"`) with one heart-icon per cell, entirely independent of `REGION_GRAPH`, `WORLD_SCALE`, or the item-agnostic `KEYITEM_FLAGS`/`SCOREITEM_FLAGS` model `IP-1020` introduced. No `IP-102x` package ever touched this screen. Not merely stale content (unlike `BL-0046`'s doc-drift pattern) — the *shape itself* (a fixed 3×3 grid, 9 named cells) has no coherent mapping onto a generated `scale`×`scale` world (`scale` ranges 2–9, i.e. 4–81 regions, with biome-family screens reused across many regions, not one unique named zone per cell) — this is exactly the kind of gap the user is flagging: there's no obvious "quick status" shape carried over from the old design that still fits. | Medium — cosmetically confusing/misleading today (a player checking progress sees zone names that don't correspond to anything in the generated world), but doesn't block play | 04 | **NEEDS-USER at triage, not a mechanical fix.** The user's own framing ("a high level replanning... is needed") is itself a request for design work, not a specific fix — before any `FS`/`IP` can be authored, `04-requirements-engineering` (or `03` if it turns out to need new architecture, e.g. a scrollable/paginated per-region list) needs to establish *what a generated-world-appropriate quick-status display should show* (candidates: aggregate world-wide collectible/KeyItem counts; a scrollable list of visited regions; a compact per-biome-family summary — none of these are decided anywhere upstream). Recommend the pipeline manager surface this as a real design decision for the user rather than let a downstream stage invent an answer unilaterally, given the user explicitly asked for replanning rather than a patch. **Resolved at triage (2026-07-12), explicit user decision:** "the solution must be researched and it is blocked by a need to update win conditions… win conditions must be a new BL item requiring research." The user identified the real blocker themselves — none of this entry's own candidate answers (aggregate counts, visited-region list, biome summary) can be meaningfully chosen until the win condition itself is redesigned, since the screen's whole purpose is showing progress toward winning. Filed **[BL-0081](../pipeline/backlog.md)** (win-condition research, `02-research-game-design`) to carry that upstream question. Re-dispositioned **`NEEDS-USER`→`DEFERRED`**, revisit trigger: `BL-0081` reaching a research conclusion `04-requirements-engineering` can build a new win-condition requirement from — only then can this entry's own screen-redesign question be meaningfully answered. **Trigger fired (`00-pipeline-manager` run #122): `BL-0081` delivered its research conclusion** ([R215](../research/encyclopedia/R215-procgen-win-condition-design.md)) — four evidence-grounded candidates (scale-relative count, percentage threshold, fixed goal-region, hybrid), no single answer decided (that's `03`/`04`'s own call, not research's). Re-dispositioned **`DEFERRED`→`SCHEDULED`**, riding a future `04-requirements-engineering` pass that picks a candidate from R215 and derives the actual win-condition FR, which this screen-redesign question can then build against. **Fully ripe now (`00-pipeline-manager` triage, 2026-07-13): the win-condition FR this entry was waiting on has landed and shipped** — `FR-9160`/`FR-9161` derived (`04-requirements-engineering`), specified (`FS-102` revision), packaged and implemented as `IP-1021`, independently `VERIFIED` ([VR-1021](../implementation/verification/VR-1021-win-condition-redesign.md)), and integration-reviewed clean ([integration-review-ip-1021.md](../reviews/integration-review-ip-1021.md)). This entry's own blocker is fully resolved — ready to ride the next `04-requirements-engineering` pass to derive what a generated-world-appropriate quick-status display should show (candidates from the original triage: aggregate world-wide KeyItem/collectible counts against the new `WORLD_SCALE` threshold; a scrollable visited-region list; a compact per-biome-family summary), now that the concrete target ("collect `WORLD_SCALE` dead-end-prioritized KeyItems") is known. Not actioned this run — the user's own stated priority for this session is `BL-0082` (streaming/infinite-world research) first; this entry stays `SCHEDULED` and is the natural next `04` candidate once that priority is addressed. **Re-dispositioned `SCHEDULED`→`DEFERRED` (2026-07-13, user direct instruction, same-session correction):** premature to call this "fully ripe" — `IP-1021` is only the *finite*-world win condition; the *final* win-condition shape is still open, since `BL-0082`'s own infinite-world thread (`R114`/`R216`) has a live, unresolved candidate (score-chasing, structurally different from "collect `WORLD_SCALE` items") that would change what a status/progress display needs to show if adopted. Designing the MAP/status-screen replacement against `IP-1021` alone risks a second rework once `BL-0082` resolves. **Trigger fired (2026-07-13): `BL-0082` reached its final resolution.** [ADR-0016](../architecture/adr/ADR-0016-streaming-infinite-mode-generation-architecture.md) decides finite and infinite modes **coexist** (Infinite Mode is additive, selected at new-game creation alongside the existing `(seed, scale)` model — neither supersedes the other). This entry's own finite-mode MAP/status-screen redesign can therefore proceed against `IP-1021`'s already-shipped finite win condition without waiting on Infinite Mode's own separate high-score display (which needs its own, different screen content once that mode is built — out of this entry's scope). Re-dispositioned **`SCHEDULED`**, riding the next `04-requirements-engineering` pass. | SCHEDULED |
| BL-0051 | 2026-07-11 | bug | intake: project owner ("The bunny appears to collide with the top of the map one tile early... The top of the bunny should touch the topmost edge of the map.") | **The UP movement clamp (`asm_game.py:469-472`) stops the sprite's top edge (`PLAYER_Y`) at a minimum of `17`, but the actual playable field starts at tile row 1 (pixel `y=8`)** — confirmed by direct code read of `beach_screen()`/`forest_screen()` (`tilemaps.py`), whose terrain fill begins at `y=1` (row 0 is the single-tile-tall HUD via `_score_bar`, writing only row 0). Reproduced directly: holding UP from spawn for 200 frames settles at `PLAYER_Y=17`, ~9px short of the field's true top edge — close to a full 8px tile, matching the reported "one tile gap." The analogous DOWN clamp (max `PLAYER_Y=128`, sprite bottom at `128+15=143`) is correctly flush with the screen's exact bottom edge, confirming top/bottom are asymmetric — only the top clamp is wrong. | Low-Medium — movement-precision defect, doesn't block play but is visibly incorrect and asymmetric with the (correct) bottom clamp | 07 | **Packaged (run #83, `07-implementation-planning`, 2026-07-11): [IP-9090](../implementation/packages/IP-9090-movement-clamp-boundary-fix.md) — implemented, `VERIFIED` 2026-07-12 ([VR-9090](../implementation/verification/VR-9090-movement-clamp-boundary-fix.md)): UP floor `Y=8` confirmed by direct code read, `T7.8`/`T7.8b` confirmed passing.** | DONE |
| BL-0052 | 2026-07-11 | bug | intake: project owner ("The bunny is able to go one bunny width past the far right edge of the map... The bunny should collide with the edge and stop at the rightmost edge.") | **The RIGHT movement clamp (`asm_game.py:450-455`) allows `PLAYER_X` (sprite left edge) up to `159`, but the sprite is 8px wide and the screen is 160px wide (`x` 0–159)** — the correct flush maximum is `152` (`160-8`), matching the sprite's own width the way the LEFT clamp (min `0`, correctly flush) already does. Reproduced directly: holding RIGHT to the wall settles at `PLAYER_X=159`, meaning the sprite occupies screen `x∈[159,166]` — 7 of its 8 pixels render past the visible right edge, matching "about one bunny width" past the edge as reported. | Low-Medium — movement-precision defect (asymmetric with the correctly-flush LEFT clamp), doesn't block play but lets the sprite visibly clip off-screen | 07 | **Packaged (run #83, `07-implementation-planning`, 2026-07-11): [IP-9090](../implementation/packages/IP-9090-movement-clamp-boundary-fix.md) — implemented, `VERIFIED` 2026-07-12 ([VR-9090](../implementation/verification/VR-9090-movement-clamp-boundary-fix.md)): RIGHT ceiling `X=152` confirmed by direct code read, `T7.10`/`T7.10b` confirmed passing.** | DONE |
| BL-0053 | 2026-07-11 | bug | intake: project owner ("The bunny collects items above the topmost edge of the sprite but does not collect items which overlap with the bottommost edge of the sprite. The bunny should collect and only collect items with which the sprite overlaps.") | **`check_collisions`' (`asm_game.py:495`) pickup test is `\|PLAYER_X - item_x\| < 10 AND \|PLAYER_Y - item_y\| < 10` — a symmetric ±9px window centered on `PLAYER_X`/`PLAYER_Y`, which are the sprite's *top-left corner*, not its center.** The sprite's true bounding box is `X∈[PLAYER_X, PLAYER_X+7]` (8px wide), `Y∈[PLAYER_Y, PLAYER_Y+15]` (16px tall, 8×16 OBJ mode). The symmetric window therefore reaches ~9px *above* `PLAYER_Y` (well outside the sprite, which has zero extent above its own top edge) while only reaching `PLAYER_Y+9` downward — 6px short of the sprite's actual bottom edge at `PLAYER_Y+15`. **Reproduced directly** (synthetic collectible placed via WRAM, sprite at `Y∈[80,95]`): an item at `y=75` (5px *above* the sprite's top edge, outside the true bounding box) **is incorrectly collected**; an item at `y=94` (genuinely overlapping the sprite's bottom edge, inside the true bounding box) **is not collected** — both exactly matching the reported symptom. (The X-axis window has a smaller, unreported asymmetry too — ~9px generous to the left of the sprite vs. ~2px generous to the right — noted for whoever fixes this, not filed as a separate bug since the user only reported the vertical case.) | Medium — a core-mechanic correctness/fairness defect: items are missed despite visual overlap and collected despite no overlap, on every single pickup in the game | 07 | **Packaged (run #83, `07-implementation-planning`, 2026-07-11): [IP-9100](../implementation/packages/IP-9100-collectible-pickup-hitbox-fix.md) — implemented (asymmetric point-in-box test, not the originally-planned symmetric formula, found wrong during implementation), `VERIFIED` 2026-07-12 ([VR-9100](../implementation/verification/VR-9100-collectible-pickup-hitbox-fix.md)): all four boundary checks confirmed passing at the exact reported reproduction points.** This fix directly contradicts `FR-3100`'s own currently-baselined `10px`/`10px` Acceptance Criteria — implemented anyway per this package's own scope, correction routed to a future `04-requirements-engineering` pass (Notes-only forward pointer), not silently absorbed. | DONE |
| BL-0054 | 2026-07-11 | recommendation | user-requested retrospective on `BL-0047`–`BL-0053` (root-cause pattern analysis, this session) | **Process gap — `07-implementation-planning` decomposition missed an entire "verb" of the procgen-world capability.** `IP-1020`/`IP-1030` packaged "generate the world" and "render from the generated world" but no package's Files-to-Modify ever named `check_zone_transition` (navigation) or `map_screen`/status-review screens (`BL-0047`/`BL-0050`) — the increment superseded the fixed-3×3 model in some call sites (`_zone_arrows`, rendering dispatch) and silently left it live in others. Nothing in the `07` workflow currently asks "for a capability spanning generate/render/navigate/persist/review, is there a package (or explicit deferral) for every verb?", nor "when this change supersedes an old model, what *other* artifacts still encode the old model?" (a `grep`-driven sweep for the superseded pattern — literal `CUR_ZONE` grid-index comparisons, hardcoded 3×3 labels — would have flagged both `BL-0047` and `BL-0050` before implementation). | High (two of the six root causes — `BL-0047`/`BL-0050` — trace directly to this gap, and `BL-0047` is Critical) | 07 | SCHEDULED: `07-implementation-planning`'s own `SKILL.md` (Step 1, Technical Work Breakdown) should gain an explicit "verb inventory" discipline for any capability that spans generate/render/navigate/persist/review — enumerate every verb the requirements imply and require a package or a named, deliberate deferral for each — plus a "supersession sweep" step whenever a package retires an old model: search the tree for other call sites still encoding the pattern being retired, not just the one the package's own Files-to-Modify names. **Resolved (user-directed skill-file edit, 2026-07-11):** `07-implementation-planning/SKILL.md` Step 1 now carries both disciplines verbatim, cited to `BL-0047`/`BL-0050`/`BL-0054`; two new Quality gate checklist items make both mandatory going forward. | DONE |
| BL-0055 | 2026-07-11 | recommendation | user-requested retrospective on `BL-0047`–`BL-0053` (root-cause pattern analysis, this session) | **Process gap — `09-package-verification`'s permanent gates (G5: ROM builds, full suite passes) never require actually driving the feature at a non-default parameter value.** `VR-1020`/`VR-1030` both independently confirmed `generate_world`/`ALL_SCREENS` dispatch correct — against the suite's own fixtures, which always enter play at `WORLD_SCALE=3` (`advance_to_playing`'s hardcoded default). `BL-0047` (navigation ignores `REGION_GRAPH` entirely) was invisible to both VRs because nothing in either verification pass drove the game at `scale≠3` — the coincidental agreement between the old hardcoded 3×3 zone math and a `scale=3` generated world masked the defect completely. `VR-1020`/`VR-1030` are not wrong given what they checked; the checklist itself had no item that would have caught this. | High (the single highest-leverage gap — this would have caught `BL-0047`, the Critical bug, before it ever reached the user) | 09 | SCHEDULED: `09-package-verification`'s `SKILL.md` should add a standing verification-checklist item for any package whose Definition of Done references a tunable/generated parameter (`WORLD_SCALE`, seed, etc.) — an actual emulator drive at a *non-default* value, not just a memory-state assertion against the oracle. This is stronger than "run the suite" (the suite's own fixtures share the same scale=3 default blind spot) — it needs to be a live, driven check the verifier performs itself. **Resolved (user-directed skill-file edit, 2026-07-11):** `09-package-verification/SKILL.md` Step 4 ("Run the gates") now requires the verifier itself drive the ROM at a non-default parameter value whenever the DoD references one, cited to `BL-0047`/`BL-0055`; a new Quality gate checklist item makes it mandatory before writing `VERIFIED` going forward. | DONE |
| BL-0056 | 2026-07-11 | recommendation | user-requested retrospective on `BL-0047`–`BL-0053` (root-cause pattern analysis, this session) | **Process gap — `09-content-review` never ran against `IP-1040`'s menu screens, because it only triggers after `08-content-authoring` packages, and `IP-1040` (the SAVE/MAIN-MENU screens) was a `08-code-implementation` package.** `BL-0049` (the exit-to-main-menu option has no on-screen text) is exactly the class of defect `09-content-review`'s "readability & composition" dimension exists to catch — but it was never exercised, because the code/content package split routes UI screens authored inside a code package around content review entirely. `09-package-verification`'s own checklist correctly confirmed the mechanism (input→state-transition) works; nothing checked what's actually on screen. | Medium (one confirmed miss, `BL-0049`; the mechanism is a real, recurring blind spot for any future UI authored inside a code package) | 09 | SCHEDULED: `09-content-review`'s own `SKILL.md` scope-selection section should be widened from "after `08-content-authoring`" to "any package (`08-code-implementation` or `08-content-authoring`) that changes what's rendered on a screen" — trigger on screen/UI changes, not on which stage-08 peer implemented them. **Resolved (user-directed skill-file edit, 2026-07-11):** `09-content-review/SKILL.md`'s frontmatter description, Scope selection, and Pipeline-position/routing sections all widened to trigger on any stage-08 peer's screen-content change, cited to `BL-0049`/`BL-0056`. | DONE |
| BL-0057 | 2026-07-11 | research-gap | user-requested retrospective on `BL-0047`–`BL-0053` (root-cause pattern analysis, this session) | **Testing-convention gaps in `R305` (emulator-based test design), surfaced by six bugs that all survived a 180/180 suite:** (1) the suite's shared fixture (`advance_to_playing`) always enters play at `WORLD_SCALE=3`, so no test exercises a different scale — masked `BL-0047` entirely (§BL-0055's root cause); (2) tests for spatial/numeric behavior (movement clamps, collision) assert "the behavior occurs somewhere valid" rather than the exact boundary, missing `BL-0051`/`BL-0052`/`BL-0053` (a wrong clamp constant still passes "up decreases Y"); (3) menu/state-machine tests didn't cover the full (entry-condition × action) cross-product — `BL-0048`'s broken cell (save-present × select-new-game) was the one untested combination; (4) reachability was verified against the build-side oracle graph (`worldgen.py`/`T12.c`) but never against the actual runtime navigation path a player drives — "the data is correct" and "the game is playable" were conflated. | High (a research-documentation gap behind four of the six confirmed bugs) | 02 | SCHEDULED: a `02-research-tooling-and-testing` pass should extend `R305` with these four conventions (parameterize shared fixtures across the tunable's realistic range, not just its default; assert exact boundaries for spatial/numeric behavior, not just directional correctness; enumerate full state×action coverage for menus; treat build-side-oracle checks as necessary but not sufficient for player-facing reachability/behavior claims — pair with a runtime-driven check). This session's user explicitly asked for this as a concrete pipeline artifact; actioned directly rather than left purely as a backlog note. | SCHEDULED |
| BL-0058 | 2026-07-11 | bug | `07-implementation-planning` run (BL-0047's own mandatory supersession sweep) | **`SCOREITEM_FLAGS` (`asm_game.py:44`, 9 bytes at `0xC060`) was never generalized alongside `KEYITEM_FLAGS`'s identical widening (`IP-1020`, 9→81 bytes) — it is still a fixed 9-byte array indexed directly by `CUR_ZONE`, with zero bounds relationship to `WORLD_SCALE`.** `BL-0047`'s own remediation (regeneralizing `check_zone_transition` to read `REGION_GRAPH`) will make `CUR_ZONE` values above 8 reachable for the first time — at which point `SCOREITEM_FLAGS[CUR_ZONE]` (`setup_zone_collects`/`check_collisions`, both indexing `SCOREITEM_FLAGS + CUR_ZONE` directly) writes/reads **past the 9-byte array into adjacent WRAM**, corrupting whatever follows (`SEED`/`WORLD_SCALE` at `0xC069`/`0xC06B`, then `REGION_GRAPH` itself at `0xC070` — the exact structure navigation depends on). **Currently dormant/unreproducible** only because `BL-0047`'s own defect keeps `CUR_ZONE` from ever exceeding 8 today — fixing `BL-0047` alone, without this, would take a latent bug and make it live. The equivalent SRAM mirror (`SRAM_SCOREITEM`, `0xA013`, 9 bytes) has the identical defect, compounded by the fact that `SRAM_SEED`/`SRAM_WORLD_SCALE`/`SRAM_KEYITEM_FLAGS` are all laid out at fixed offsets immediately following it. | **Critical** — a fix for `BL-0047` that ships without this would convert a navigation bug into active WRAM corruption of the generated-world state itself | 07 | Not deferred (Critical, and directly causally coupled to `BL-0047`'s own remediation — see `IP-9070`, packaged in the same `07` pass as `BL-0047`/`BL-0048`). **Remediation shape:** widen `SCOREITEM_FLAGS` to 81 bytes, relocated to unused WRAM (`0xC286`–`0xC2D6`, the free gap between `SSE_CURSOR` and `OAM_BUF` — confirmed unused by direct WRAM-map read) rather than grown in place (growing in place at `0xC060` would itself collide with `REGION_GRAPH` at `0xC070`); widen `SRAM_SCOREITEM` to 81 bytes, relocated to `0xA070` (immediately after the existing `SRAM_KEYITEM_FLAGS`'s end, leaving `SRAM_SEED`/`SRAM_WORLD_SCALE`/`SRAM_KEYITEM_FLAGS` at their current addresses untouched); bump `SAVE_VERSION_VAL` `0x02`→`0x03` (third bump, following `IP-1010`→`IP-1050`'s established strictly-monotonic precedent exactly) since the save layout's shape changes. **Sequencing: `IP-9070` must land before or together with `BL-0047`'s own `IP-9050`** — widening the arrays alone is harmless (nothing indexes past 8 until navigation is fixed), but shipping `IP-9050` without `IP-9070` is actively unsafe. **Implemented (`IP-9070`, 2026-07-11): exactly as shaped above — 193/193 pass, `IP-9070` reached `COMPLETE` before `IP-9050` started.** **Verified (`09-package-verification`, 2026-07-12, [VR-9070](../implementation/verification/VR-9070-cur-zone-indexed-structures-generalization.md)): independently confirmed, `IP-9070` → `VERIFIED`.** | DONE |
| BL-0059 | 2026-07-11 | bug | `07-implementation-planning` run (BL-0047's own mandatory supersession sweep) | **`ZONE_COLLECTS`/`zc_table` (`tilemaps.py:407`, `setup_zone_collects` in `asm_game.py:991`) is still the original 9 hand-authored, zone-named collectible spawn-position lists — `tilemaps.py`'s own module docstring says so verbatim: "ZONE_COLLECTS is still 9 zones in a 3x3 world grid, index = row*3 + col."** `setup_zone_collects` computes `CUR_ZONE*2` to index directly into `zc_table` (a ROM lookup table with exactly 9 entries). Once `BL-0047`'s fix makes `CUR_ZONE` values above 8 reachable, this indexes **past the ROM table's own 9 entries**, reading whatever ROM bytes happen to follow as if they were a valid spawn-table pointer — misinterpreted ROM data as an address, then dereferenced as spawn (x,y,type) tuples, with no bounds check anywhere in the chain. Likely outcomes range from nonsensical/garbage collectible placement to reading unmapped/invalid addresses. Same dormancy pattern as `BL-0058`: invisible today only because `BL-0047`'s own defect caps `CUR_ZONE` at 8. | **Critical** — identical severity class to `BL-0058`, same causal coupling to `BL-0047` | 07 | Not deferred (Critical, packaged with `BL-0047`/`BL-0058` as `IP-9070`). **Remediation shape — confidently resolvable by direct precedent, not escalated:** replace the 9 zone-authored lists with **5 biome-family-representative lists**, keyed by `REGION_GRAPH[CUR_ZONE].biome_id` (0–4) rather than `CUR_ZONE` itself — mirroring `IP-1031`'s own already-shipped, already-verified screen-reuse pattern exactly, reusing the *identical* representative-zone choice `IP-1031` already made per family: Water→Z3(Lake)'s list, Sand→Z0(Beach)'s list, Grass→Z1(Forest)'s list, Stone→Z2(Mountain)'s list, Brick→Z8(Castle)'s list (the other four original zone lists — Village/Cave/Desert/Plains — become unused, matching `IP-1031`'s own precedent of orphaning 4 of 9 zone *screens* the same way). `setup_zone_collects` reads the biome-id from `REGION_GRAPH[CUR_ZONE]` first, then indexes `zc_table` by biome-id (0–4) instead of `CUR_ZONE` directly. **Implemented (`IP-9070`, 2026-07-11): exactly as shaped above — 193/193 pass, `IP-9070` reached `COMPLETE`.** **Verified (`09-package-verification`, 2026-07-12, [VR-9070](../implementation/verification/VR-9070-cur-zone-indexed-structures-generalization.md)): independently confirmed, `IP-9070` → `VERIFIED`.** | DONE |
| BL-0060 | 2026-07-11 | doc-defect | `07-implementation-planning` run (`IP-9070` authoring, Risks §13) | **No numbered FR covers collectible-spawn content for a generated region at all.** `ZONE_COLLECTS` predates `FEAT-9000` entirely — `FR-3210`/`FR-9130` cover *carrot/KeyItem placement rules* (exactly one per zone/region), but nothing states that spawn *content* (the star/flower/carrot positions themselves) should track the generated world rather than a retired fixed layout. `IP-9070` repairs `ZONE_COLLECTS` as a defect against this unstated intent (mirroring `BL-0020`'s precedent — a shipped, evidenced capability with no formal requirement statement), not as new requirement-covered behavior. | Low | 04 | SCHEDULED: a future `04-requirements-engineering` delta should add an FR for generated-region collectible-spawn sourcing (biome-family-representative reuse, per `IP-1031`/`IP-9070`'s established pattern), citing `IP-9070`'s T16.b as pre-existing evidence once it ships — following `BL-0020`'s own precedent exactly. Not blocking `IP-9070` or any current work. | SCHEDULED |
| BL-0061 | 2026-07-11 | doc-defect | `07-implementation-planning` run (`IP-9050` authoring) | **`FR-2300`/`FR-2310` (zone-boundary transition on a valid/no neighbor) are still worded entirely in terms of "the 3×3 grid"** — their Description/Rationale/Acceptance Criteria fields all cite the fixed 3×3 layout explicitly, never `REGION_GRAPH` or a generated `scale`×`scale` world. No target-state FR generalizing them exists — unlike `FR-1120`→`FR-1170`/`1180`/`1190` (auto-load bypass superseded by a named target FR triad, `RQ-03` finding #7's own precedent), navigation never got its target-state counterpart when `FEAT-9000`/`FEAT-4100` shipped. `IP-9050` (fixing `BL-0047`) restores the *behavior* `FR-2300`/`FR-2310` describe in spirit (transition on a valid neighbor, halt at a true boundary) generalized to the real generated graph, but the requirements text itself still only formally describes the retired 3×3 case. | Low | 04 | SCHEDULED: a future `04-requirements-engineering` delta should add target-state FRs generalizing `FR-2300`/`FR-2310` to `REGION_GRAPH`-driven navigation (mirroring the `FR-1120`/`1170`-`1190` coexistence pattern `RQ-03` finding #7 already established as this project's working convention), citing `IP-9050`'s shipped evidence once it lands. Not blocking `IP-9050` — the package cites `FR-2300`/`FR-2310` as the closest existing statement of intent plus `ADR-0009` as the binding architecture decision it must honor, with this gap noted explicitly rather than silently assumed covered. | SCHEDULED |
| BL-0062 | 2026-07-11 | gate | `00-pipeline-manager` run #64 (G3 gate for `IP-9050`/`IP-9060`/`IP-9070`) | **G3 authorization needed for the three post-ship remediation packages** (`IP-9070` — `CUR_ZONE`-indexed structure generalization, `IP-9050` — generated-world navigation fix, `IP-9060` — main menu cursor fix) — none fall under the `BL-0001`…`BL-0005` bootstrap carve-out. | High (blocks all remediation work for four Critical/High confirmed bugs) | 08 | **Resolved same run — user explicitly authorized all three** ("Authorize all three (recommended)", 2026-07-11). `IP-9070`/`IP-9060`/`IP-9050`'s Master Build Plan `Authorized?` columns updated accordingly; rides the next `00-pipeline-manager` advance (`08-code-implementation` on `IP-9070`, the tranche's `READY` prerequisite). | DONE |
| BL-0063 | 2026-07-11 | bug | `08-code-implementation` run (IP-9060 authoring, incidental code read of `asm_game.py`'s MAIN MENU/VICTORY block) | **`KEYITEM_FLAGS` (`asm_game.py:48`, up to 81 bytes at `0xC220`) is fully round-tripped through save/load (81-byte `memcpy`, `IP-1050`) but its two reset sites — `st_intro`'s new-game reset (`asm_game.py:251`) and `st_victory`'s progress-clear (`asm_game.py:316`) — still only zero the first 9 bytes each, matching the same "only indices 0-8 are live until `FEAT-1100`" comment `IP-1020` wrote before `CUR_ZONE` could ever exceed 8.** Once `IP-9050` (`BL-0047`'s fix) makes `CUR_ZONE > 8` reachable, a "new game"/victory reset within the same power-on session (no console reset) leaves `KEYITEM_FLAGS[9..80]`'s prior-playthrough bits intact — a freshly generated region could show its KeyItem as already collected. The full boot-time WRAM clear (`0xC000`–`0xC2FF`, `main`) does zero the full 81 bytes on cold boot, so this is not visible on a fresh power-on; it manifests only on a same-session replay/new-game, once regions past index 8 exist. Same causal-coupling pattern `BL-0058`/`BL-0059` established: currently dormant only because `BL-0047`'s own defect keeps `CUR_ZONE` capped at 8 today. | High — a real correctness/data-integrity bug against `FR-5220`'s new-game-reset intent, but not memory-unsafe (no OOB write/read, no corruption of adjacent WRAM — `KEYITEM_FLAGS`'s own 81-byte allocation was already correctly sized by `IP-1020`, only its reset-loop width was left at the old 9-byte figure) | 08 | Not deferred — same causal coupling to `BL-0047`/`IP-9050` that `BL-0058`/`BL-0059` had to `IP-9070`. **Remediation shape:** widen both `st_intro`'s and `st_victory`'s `KEYITEM_FLAGS` clear loops from `rom.LD_B_n(9)` to `rom.LD_B_n(81)`, mirroring exactly what `IP-9070` already did for `SCOREITEM_FLAGS`'s own two clear sites in the same two routines. Trivial in isolation (a one-argument change at two call sites, no address relocation, no save-format version bump needed — `KEYITEM_FLAGS`'s address/size/round-trip are already correct, only the reset width is stale) — should be folded into `IP-9050`'s own implementation as a same-package companion fix (discovered by `08-code-implementation`'s own mandatory supersession sweep, to be re-confirmed when `IP-9050` is implemented) rather than spun up as a separate package, unless that sweep finds it's already been superseded by then. **Not yet authorized as its own G3 item** — flagged here for the next `IP-9050` implementation pass to pick up explicitly, with a fresh authorization check if it turns out to need one beyond what `BL-0062` already covers (it is textually outside the three-package "IP-9050/9060/9070" G3 answer, since it was undiscovered at that time). **Implemented (`IP-9050`, 2026-07-11): folded in exactly as shaped above (both clear loops widened to 81 bytes), no separate authorization needed in practice — the fix rode `IP-9050`'s own already-authorized `BL-0062` scope without objection.** **Verified (`09-package-verification`, 2026-07-12, [VR-9050](../implementation/verification/VR-9050-generated-world-navigation-fix.md)): independently confirmed, `IP-9050` → `VERIFIED`.** | DONE |
| BL-0064 | 2026-07-11 | feature | intake: project owner (procgen-map design discussion, this session — explicitly "not necessarily verbatim locked in but open to research driven implementation") | **Replace `REGION_GRAPH`'s current full-lattice adjacency (every grid-adjacent region pair is always connected) with a generated maze: a randomized spanning tree over the `scale`×`scale` region grid, plus a "braid" pass that reopens a fraction of the pruned edges** (avoiding a pure single-path maze — adds loops/shortcuts). Must derive deterministically from the existing `(SEED, WORLD_SCALE)` pair, same as today's biome generation — no new persisted state; `REGION_GRAPH` still regenerates from `(seed, scale)` alone, preserving `ADR-0009`'s determinism guarantee and the already-tested "full reachability" domain invariant (a spanning tree guarantees this by construction). This directly reverses part of `ADR-0009`'s own Decision (today's model is a deliberate full lattice, not an oversight) — **`BL-0047`'s own Disposition explicitly anticipated and recommended this exact follow-up** ("route the non-grid-shape request as its own design-question if the user wants it pursued after seeing the grid actually work end-to-end" — `IP-9050` shipped 2026-07-11, closing that loop). Needs research grounding before an architecture decision can be made: which maze algorithm (randomized Prim/Kruskal/recursive-backtracker) fits the SM83's shift/XOR-only PRNG (`R111`'s no-`DIV`/no-multiply constraint, the same one today's biome PRNG honors) and the WRAM budget at `scale=9` (81 regions) — a spanning-tree algorithm needs per-region visited/set-id state today's one-pass generation doesn't carry, a real cost against `NFR-4200`'s already-tracked headroom, not just an algorithm-choice question. | Medium (owner: exploratory/open to research, not an urgent fix) | 02 → 03 | **Triaged (`00-pipeline-manager` run #69): SCHEDULED, riding this run's step.** `02-research-gbc-hardware` first (maze-algorithm-vs-SM83-PRNG/WRAM-cost comparison — a new R1xx topic), then `03-architecture-design-synthesis` to record the ADR-0009-amending decision (which algorithm, braid-fraction default, WRAM/ROM cost accepted) before any `04-requirements-engineering`/`06-feature-specification` work names concrete FRs. **Hard-blocking dependency for `BL-0065`/`BL-0066`/`BL-0067`** (all three build on this package's own output — a maze structure to scale, cluster, or render). **Research half done (run #69, [R112](../research/encyclopedia/R112-maze-generation-hardware-feasibility.md)): compared randomized Kruskal/Prim/recursive-backtracker against the existing shift/XOR-only PRNG and current WRAM headroom (928 bytes used, 3168 bytes/3.09 KiB free — re-measured directly, not estimated); recursive backtracker grounded as the strongest SM83 fit, WRAM budget doesn't differentiate the three (all comfortably affordable).** **Architecture decision made (run #70, [ADR-0012](../architecture/adr/ADR-0012-maze-shaped-region-adjacency.md)): randomized DFS/recursive backtracker adopted, iterative form reusing `REGION_GRAPH`'s own neighbor bytes as backtracking state; braid-fraction mechanism established (default deferred to `BL-0065`); `REGION_GRAPH`'s data format unchanged, so `dsr_p`/`draw_region_arrows`/`check_zone_transition` need zero code changes.** **Requirements baselined (run #71, `FR-9140`/`FR-9150`): candidate FRs for the maze-generation pass and the braid-fraction parameter authored, traceable to `ADR-0012`/`R112`.** **Decomposed (run #72, [FEAT-9100](../feature-planning/03-feature-catalog.md)): joins EP-5000/Release 2 as a post-ship addendum, depends only on the already-shipped `FEAT-9000`, fully unblocked.** **Specified (run #73, [FS-107](../features/FS-107-maze-shaped-region-adjacency.md)): fully specified, 3 Open Questions (visited-flag packing, transient-scratch addresses, starting-region backtrack init), all routed to `07-implementation-planning`, none blocking.** **Resolved — stale, caught at triage (`00-pipeline-manager`, 2026-07-13): this row was never flipped `DONE` even though the work it tracks has long since shipped.** Packaged and implemented as [IP-1070](../implementation/packages/IP-1070-maze-shaped-region-adjacency.md), independently `VERIFIED` ([VR-1070](../implementation/verification/VR-1070-maze-shaped-region-adjacency.md), all seven FS-107 ACs confirmed), and integration-reviewed clean as part of the maze-shaped region adjacency tranche ([integration-review-maze-shaped-adjacency-tranche.md](../reviews/integration-review-maze-shaped-adjacency-tranche.md)) — also folded into the Release 2 bundled GO. No further work remains. | DONE |
| BL-0065 | 2026-07-11 | feature | intake: project owner (same procgen-map design discussion as `BL-0064`) | **Add a maze-difficulty scaling variable** — a parameter (independent of `WORLD_SCALE`, or possibly a new player-entered value alongside `SEED`/`WORLD_SCALE`) controlling the maze's braid-back-edge fraction/density from `BL-0064`'s generator: low value = many braided loops (forgiving, close to today's open lattice), high value = a near-pure spanning tree (mostly single-path, more backtracking). Needs a design decision on where this fits the existing SEED/SCALE ENTRY UI (`IP-1040`'s digit-cursor picker) — a third entry field, a value derived from `WORLD_SCALE`, or a fixed default with no UI exposed initially. | Medium (owner: exploratory) | 04 | **Trigger fired (run #70): `BL-0064`'s `03` step closed ([ADR-0012](../architecture/adr/ADR-0012-maze-shaped-region-adjacency.md)).** Re-dispositioned: `SCHEDULED`, riding a future `04-requirements-engineering` pass alongside `BL-0064`'s own FRs — `ADR-0012` establishes the braid-fraction mechanism (a single byte, "reopen if drawn PRNG byte ≤ threshold") but explicitly leaves this item's own scope (default value, UI placement) to that pass. **Baselined (run #71, `FR-9150`): mechanism + a fixed initial default (~25% of pruned edges reopened) baselined and unblocked for implementation; the UI-exposure sub-question (new SEED/SCALE ENTRY field vs. derived vs. fixed) is deliberately left open — not a blocker for `FR-9140`'s own implementation, surfaces to the user only if/when a future stage reaches the SEED/SCALE ENTRY UI specifically.** **Decomposed (run #72): folded into `FEAT-9100`, not a separate Feature (the same generation pass, `FR-9150` rides `FR-9140`'s own Feature).** **Specified (run #73): covered by `FS-107` alongside `FR-9140` (§6/§7's braid-pass workflow step, §15 AC-5's statistical check) — no separate spec needed.** **Resolved — stale, caught at triage (`00-pipeline-manager`, 2026-07-13): this row was never flipped `DONE` even though `FR-9150`'s mechanism shipped alongside `FR-9140` in `IP-1070`** (`VERIFIED`, `T19.e`'s braid-fraction statistical check confirms the ~25% default holds, 24.35% measured). The UI-exposure sub-question was deliberately left open per this entry's own run #71 note and never became a blocker — no future stage has needed it (the SEED/SCALE ENTRY UI shipped with no braid-fraction field, `IP-1040`, and no request to add one has surfaced since). No further work remains. | DONE |
| BL-0066 | 2026-07-11 | feature | intake: project owner (same procgen-map design discussion as `BL-0064`) | **Cluster biome assignment into cohesive multi-region blobs instead of today's independent per-cell grammar-constrained draw** — so a "Forest area" spans several adjacent regions before the biome drifts, giving the world distinct named areas (Zelda/Pokémon-style overworld feel) rather than a checkerboard. The owner specifically proposed **seeding blob centers from `BL-0064`'s maze's own dead-ends** — a deliberate hard dependency between this item and `BL-0064` (blob clustering can't be generated or tested independently of the maze generator existing first); worth evaluating at design time against simpler alternatives (e.g. N random seed points + flood-fill, no maze dependency) as part of the same pass, not assumed as the only option. Must still respect the existing adjacency-grammar invariant (`R212`: adjacent regions' biome-ids differ by at most 1) — blob boundaries are a *clustering* change layered on top of the existing grammar-constrained draw, not a replacement for it. Loosely related to `BL-0050` (MAP/status screen redesign, `NEEDS-USER`) — a blob/named-area model could inform what a generated-world-appropriate quick-status display shows, but the two are independent decisions; not merging them here. | Medium (owner: exploratory) | 04 | **Trigger fired (run #70): `BL-0064`'s `03` step closed ([ADR-0012](../architecture/adr/ADR-0012-maze-shaped-region-adjacency.md)).** Re-dispositioned: `SCHEDULED`, riding a future `04-requirements-engineering` pass alongside `BL-0064`'s own FRs — `ADR-0012` decided the spanning-tree algorithm (recursive backtracker) but did not itself evaluate dead-end-seeding vs. flood-fill for blob centers; that evaluation remains this item's own scope, per `ADR-0012`'s Consequences section. **Genuine architecture conflict found (run #71, `04-requirements-engineering`, RQ-03 finding #13): the owner's own first-suggested approach (dead-end seeding) requires maze-generation to run *before* biome assignment, directly contradicting `ADR-0012` point 1's fixed biome-first pass ordering.** Not resolved unilaterally — routed as `CR-05` (Candidate Requirements, RQ-01), not baselined. Two real paths forward: (a) revisit `ADR-0012`'s pass ordering (a `03-architecture-design-synthesis` re-open) to allow dead-end seeding, or (b) use the compatible flood-fill alternative (N independent seed points, no maze dependency) instead. Re-dispositioned to **`NEEDS-USER`** — a genuine decision point, not urgent (doesn't block `BL-0064`/`0065`/`0067`), but ripe. **User decision recorded (2026-07-12): "hold on this as-is."** Neither path (a) nor (b) chosen — deliberately left open. The user separately filed **[BL-0082](../pipeline/backlog.md)** (streaming/on-the-fly, theoretically unbounded procgen research) and explicitly connected it to this entry: an unbounded/streaming world would change what a "clustering pass over the whole grid" even means (there is no fixed whole grid to cluster over), so this decision may be worth revisiting only after `BL-0082` lands, not before. **Formally blocked on `BL-0082` (2026-07-13, user direct instruction): "Neither option of preseeding or dead-end seeding will necessarily work" once generation goes streaming/lazy** — dead-end seeding needs a completed maze pass over a known extent to find dead-ends from, and N-random-point flood-fill needs a bounded grid to place and flood-fill points across; a local/on-demand generation model (`BL-0082`'s own subject) may support neither without rework, or may need a third strategy entirely (e.g. a per-region deterministic blob-id derived from `(seed, region coordinate)` alone, no whole-grid/whole-maze pass required). This is not resolvable as a mere "pick (a) or (b)" choice anymore — it depends on `BL-0082`'s own architecture, so it must be evaluated *as part of* that research, not decided ahead of it (see `BL-0082`'s own updated scope). Re-dispositioned **`DEFERRED`** — revisit trigger: `BL-0082`'s `02-research-*` step closes with a concrete streaming-generation model, at which point this item's (a)/(b)/(third-option) choice can be re-evaluated against real constraints instead of guessed at now. **Half-fired (`02-research-gbc-hardware` run, 2026-07-13, [R114](../research/encyclopedia/R114-streaming-world-generation-feasibility.md)): confirmed neither original candidate survives a streaming model, exactly as the user's own instinct predicted** — and names a concrete third option, a per-super-cell "blob identity" derived via `hash(SEED, supercell_row, supercell_col)` (the same shift/XOR-only xorshift-reseed technique `R114` proposes for biome positional determinism generally), needing no whole-grid pass. **`03-architecture-design-synthesis` run (2026-07-13): the Infinite Mode half of this entry is resolved** — [ADS-001](../architecture/ADS-001-streaming-infinite-world-generation.md)/[ADR-0016](../architecture/adr/ADR-0016-streaming-infinite-mode-generation-architecture.md) adopt the per-super-cell `hash(SEED, supercell_row, supercell_col)` blob-identity mechanism as the streaming-compatible substitute, for whenever Infinite Mode is actually implemented. **This entry's own original finite-mode question is explicitly NOT resolved by that decision**: the `ADR-0012` pass-ordering conflict (dead-end seeding needs maze-before-biome, contradicting the shipped biome-first ordering) still stands for the *finite* mode, independent of whatever Infinite Mode does. Re-dispositioned **`NEEDS-USER`** — three live paths, genuinely undecided: (a) revisit `ADR-0012`'s pass ordering for the finite mode specifically; (b) adopt the finite-mode's own flood-fill alternative (no maze dependency) instead; (c) treat blob-clustering as an Infinite-Mode-only feature and close this entry's finite-mode ask as no-longer-pursued, given the user's own repeated framing connected this entry to `BL-0082` rather than treating it as a standalone finite-mode request. Not urgent (blocks no in-flight package); ripe for the user's decision whenever convenient. **Decided (2026-07-14): a fourth path, not among the three originally offered.** Project owner: "If there is a blob mechanism that works for infinite mode, use that concept for the finite mode as well." [ADR-0018](../architecture/adr/ADR-0018-finite-mode-biome-blob-clustering.md) adopts the per-super-cell `hash(SEED, supercell_row, supercell_col)` technique for the finite mode too — a deterministic snap-to-blob layered on `ADR-0009` point 2's existing draw, needing no `ADR-0012` pass-ordering change at all (the hash requires no maze to exist first, so the original conflict is sidestepped rather than resolved by picking (a) or (b)). `RQ-01`'s `CR-05` updated to point at this resolution ([RQ-03](../requirements/03-requirements-review.md) finding #18); not yet baselined into a real `FR-xxxx` — that's a future `04-requirements-engineering` pass. Re-dispositioned **`NEEDS-USER`→`SCHEDULED`**, riding a future `04-requirements-engineering` pass to derive the actual FR from `ADR-0018`, mirroring `CR-06`'s own `03→04` precedent. **Resolved (run #156, `04-requirements-engineering`): baselined as [FR-9170](../requirements/01-functional-requirements.md#fr-9170--finite-mode-biome-blob-clustering-via-per-super-cell-positional-hash)** — `CR-05` closed, `RQ-03` finding #19. This entry's own original ask (cohesive multi-region biome blobs) is now fully realized end to end: research (`R114`) → architecture (`ADR-0018`) → requirements (`FR-9170`). Implementation (`07`/`08`) is a separate, not-yet-scheduled future step — `BL-0110` (super-cell size tuning) already tracks that thread's own open sizing question. | DONE |
| BL-0067 | 2026-07-11 | feature | intake: project owner (same procgen-map design discussion as `BL-0064`) | **Cosmetic indication of which of a region's up/down/left/right edges the maze allows crossing vs. blocks** — extending the existing arrow-indicator system (`draw_region_arrows`, `asm_game.py:860`, `IP-1030`) from today's 2-state signal (arrow = grid-adjacent region exists, no arrow = true grid boundary) to a 3-state signal once `BL-0064` ships (open path / blocked-by-maze-but-grid-adjacent / true grid boundary) — today those last two cases render identically since adjacency is always fully open. **Explicitly presentation-layer only** — the owner and assistant both scoped this as *not* mid-screen walls/collision; a maze-blocked edge is signaled at the screen boundary the same way an open edge already is, not enforced by new in-screen collision geometry (that would be a separate, larger feature if ever pursued). | Low–Medium (cosmetic, but real player-facing legibility need — without it, a maze-blocked edge looks identical to a dead end, indistinguishable from "just walk further") | 04 | **Trigger fired (run #70): `BL-0064`'s `03` step closed ([ADR-0012](../architecture/adr/ADR-0012-maze-shaped-region-adjacency.md)).** Re-dispositioned: `SCHEDULED`, riding a future `04-requirements-engineering` pass alongside `BL-0064`'s own FRs — `ADR-0012` point 2 confirmed `REGION_GRAPH`'s data format needs no new bit (a maze-blocked edge and a true grid boundary are both `0xFF`); this item's own 3-state render must re-derive "is this a grid boundary" from `(row, col, scale)` arithmetic at render time, a `GDS-08`/render-layer question this item still owns. **Baselined (run #71, `FR-2330`, generalizes `FR-2320` which is left unmodified per the established `FR-1120`→`FR-1170`-`1190` coexistence precedent).** Also needs a `GDS-08` tile-art delta for the new blocked-edge indicator (flagged, routed to `03-architecture-design-synthesis`, not decided by the FR itself). **Decomposed (run #72, [FEAT-2100](../feature-planning/03-feature-catalog.md)): joins EP-1000, depends on `FEAT-9100`. Feature Review finding #7 (FP-05): the tile-art blocker is real and still open — this Feature's rendering half cannot be fully specified until a `GDS-08` delta lands; its logic half can proceed independently.** **Specified (run #73, [FS-108](../features/FS-108-maze-aware-transition-edge-signaling.md)): logic half only — the open/blocked/absent render-time classification, fully specified. Rendering half remains genuinely blocked; the still-unrouted `GDS-08` delta need is now tracked as its own entry, `BL-0068`, rather than left only as prose inside this row.** **Logic half implemented and `VERIFIED`** ([IP-1080](../implementation/packages/IP-1080-maze-aware-edge-classification.md)). `BL-0068`'s `GDS-08` §10 delta landed, unblocking the rendering half's own spec. **`FS-108` revised (rendering half fully specified) → packaged as `IP-1081`(content)/`IP-1082`(render) → `IP-1081` `VERIFIED` 2026-07-13 → `IP-1082` implemented, `COMPLETE`, 234/234, live-driven rendering confirmed 2026-07-13** — the full 3-state signal (`FR-2330`) is now shipped end-to-end. **Remains `SCHEDULED`, not `DONE`** — tracks the identical remaining step as its twin bug report `BL-0075`: `IP-1082` still needs its own fresh-session `09-package-verification` pass. Will flip `DONE` alongside `BL-0075` once that lands. | SCHEDULED |
| BL-0068 | 2026-07-11 | finding | `06-feature-specification` run #73 ([FS-108](../features/FS-108-maze-aware-transition-edge-signaling.md) Open Question 1, restating `05-feature-decomposition`'s own Feature Review finding #7, [05-feature-review.md](../feature-planning/05-feature-review.md)) | **A `GDS-08` (presentation architecture) delta is needed for `FEAT-2100`'s blocked-edge indicator — its tile art and palette assignment are undecided, and no artifact has yet authored one.** `ADR-0012` point 2 confirms the distinction is render-time-only (no `REGION_GRAPH` change), but the actual visual (which tile, which palette slot, whether it fits the existing 8-BG-palette budget per `GDS-07` §5) has never been specified. This finding surfaced twice now (Feature Review finding #7, then `FS-108`'s own Open Question 1) without ever being given its own tracked backlog entry — filed now so it can actually be scheduled, rather than re-surfacing as prose in `BL-0067`/`FS-108` indefinitely. | Low-Medium (blocks `FEAT-2100`'s rendering half only; the logic half — `FS-108` — is unaffected and can implement/ship independently) | 03 | **Resolved (run #81, `03-architecture-design-synthesis`, 2026-07-11): [GDS-08 §10](../architecture/08-presentation-architecture.md) authored.** Decision: a distinct broken/dashed-bar tile shape (not a recolored arrow — silhouette-first per R209, avoids relying on color alone), 4 new directional tiles at `0x1A`–`0x1D` (continuing the existing UI-icon block, before `Digits` at `0x20`), reusing the open arrow's own palette-2 (UI/gold) attribute verbatim — **0 new palette entries spent**, palettes 6/7 remain free. Exact pixel silhouette and screen-position offsets left to the future `06`/`07`/`08-content-authoring` passes that actually implement `FS-108`'s rendering half, per this skill's own no-production-code scope. `FS-108`'s metadata updated with a forward-reference pointer to the new delta; its own §19 OQ1 text left for `06-feature-specification` to formally close. Rides `06-feature-specification` next (close `FS-108` OQ1, then the rendering half becomes plannable at `07`). | DONE |
| BL-0069 | 2026-07-11 | gate | `00-pipeline-manager` run #74 (G3 gate for `IP-1070`) | **G3 authorization needed for `IP-1070`** (Maze-Shaped Region Adjacency, `FEAT-9100`) — not covered by the `BL-0001`…`BL-0005` bootstrap carve-out. | High (blocks the maze-shaped-adjacency thread's own critical path, `IP-1070` → `IP-1080`) | 08 | **Resolved same run — user explicitly authorized** ("Authorize IP-1070 (Recommended)", 2026-07-11). `IP-1070`'s Master Build Plan `Authorized?` column flipped to **YES — explicit user G3, 2026-07-11 (BL-0069)**; `packages/INDEX.md`/`ROADMAP.md` updated in sync. `IP-1080` remains unauthorized and `BLOCKED` (on `IP-1070` reaching `VERIFIED`, not merely on this authorization) — a separate G3 question when it becomes eligible. Rides the next `00-pipeline-manager` advance (`08-code-implementation` on `IP-1070`, now `READY` and authorized). | DONE |
| BL-0070 | 2026-07-11 | bug | `08-code-implementation` run #76 (`IP-1070` implementation attempt, Blocking Report) | **`gw_prng_step`'s (`asm_game.py`, shipped/`VERIFIED` `IP-1020`) final mixing step (`x ^= byteswap(x)`) always makes the new state's high and low bytes equal by construction — collapsing the effective state space from 65536 to 256 values, and frequently collapsing further, within 1-2 steps, to the `0` absorbing state (a fixed point of any pure-XOR construction) or a short repeating cycle.** Directly confirmed via PyBoy: seed 12345 reaches state 0 after 2 `gw_prng_step` calls and stays there forever; other seeds (999, 42424) cycle `{128,129,1}`. **`IP-1070`'s braid pass is the first caller in this codebase to draw many `gw_prng_step` values back-to-back with no intervening non-PRNG state change** — the existing biome-assignment loop's single-clamped-delta-per-region usage never exercised this failure mode, and none of `T12`'s existing checks (determinism, reachability, grammar-validity) are sensitive to PRNG *quality*, only to determinism, which a degenerate-but-deterministic stream still satisfies. Measured impact on `IP-1070`'s own braid-fraction target (`FR-9150`, ~25% reopen): seeds 12345/1 produced 0/16 pruned (stuck at 0, always reopens); seeds 999/42424 produced 11/16 pruned (stuck cycling) — neither approximates the target. | High (blocks `IP-1070`'s own critical path and `FR-9150`'s correctness guarantee; does not corrupt already-shipped biome-generation output, which only ever needed one clamped draw per region and coincidentally still produces grammar-valid, deterministic — if less "random" than intended — results even from a collapsed stream) | 02 | SCHEDULED, riding a future `02-research-gbc-hardware` pass to properly characterize `gw_prng_step`'s actual period/degeneracy (the byteswap-XOR step's every-state-has-hi==lo property, the observed collapse-to-0/short-cycle risk), then `03-architecture-design-synthesis` to decide the fix: repair the mixing step (an `R111`/`ADR-0009` amendment — but changes biome-generation output for every existing `(seed,scale)` combination, a compatibility-relevant decision needing explicit user authorization before shipping), add a decorrelating perturbation scoped to the maze pass's own draws specifically (narrower blast radius, still a design decision outside `07`/`08`'s own authority), or redesign the braid mechanism to need less of the PRNG's quality. Blocks `IP-1070` (`BLOCKED`, G3 authorization `BL-0069` stands, unaffected) and transitively `IP-1080`. **Research done ([R113](../research/encyclopedia/R113-sm83-prng-degeneracy-mitigation.md), 2026-07-11): root cause formally grounded — a literal byte-swap substituted for a byte-shift in the final mixing step, forcing the new state's high/low bytes equal on every call by algebraic construction (a rank-reducing, non-full-rank transformation, not a valid xorshift variant). Two candidate fixes presented (repair the mixing step, compatibility-relevant; or a narrowly-scoped per-draw perturbation, zero compatibility impact), neither adopted — `03-architecture-design-synthesis`'s own call.** **Architecture decision made ([ADR-0013](../architecture/adr/ADR-0013-maze-pass-prng-decorrelation.md), 2026-07-11): adopt a loop-local counter-XOR perturbation scoped to `IP-1070`'s own carve+braid draws (both phases, not just braid) — `gw_prng_step` itself and every existing call site left untouched. The alternative (repairing `gw_prng_step`'s own mixing step) explicitly declined: `REGION_GRAPH` regenerates from `(seed, scale)` on every save load, so that fix would silently redefine any existing save's world — a real save-compatibility break, deferred as a future option gated on explicit user authorization, not decided here.** **Package touch done (2026-07-11): `IP-1070` re-planned with a light touch (§4/§6/§7/§13), back to `READY`, authorization `BL-0069` unaffected.** **Implemented (run #80, `08-code-implementation`, 2026-07-11): the counter-XOR perturbation (`GW_MAZE_DRAW_CTR`, +97/draw) shipped in `asm_game.py`/`worldgen.py`; `T19.e`'s braid-fraction statistical check now measures 25.80% (89/345) against the ~25% target, matching neither the old 0/16-stuck-at-0 nor 11/16-stuck-cycling failure modes — the defect's practical symptom is resolved within `IP-1070`'s own scope. `gw_prng_step` itself remains unrepaired, exactly as `ADR-0013` decided; any future caller with the same "many back-to-back draws" shape would need its own perturbation or a real fix, per that ADR's own accepted-limitation framing.** | DONE |
| BL-0071 | 2026-07-11 | finding | `08-code-implementation` run #80 (`IP-1070` implementation, Step 7) | **A second supersession-sweep gap, same class as `BL-0054`/`BL-0047`/`BL-0050`: `IP-1070`'s own `07-implementation-planning` pass (run #74) searched `dsr_p`/`draw_region_arrows`/`check_zone_transition`/`tilemaps.py`/`T12` for full-lattice-adjacency assumptions but never checked `test_rom.py`'s own `T11`/`T17` suites, both of which hardcoded specific full-lattice traversal paths (a scale=5 snake walk, a scale=3 "move right from region 0" step) that the maze pass's own sparsification directly invalidates — including at the *default* scale=3 fixture (`advance_to_playing`) used throughout much of the rest of the suite, not just the scale=5 corpus the package's own testing focused on.** Confirmed via direct test run: `T17` crashed with a `TypeError` (a `None` neighbor indexed as a list), and `T11.a2` failed a hardcoded zone-transition assertion, on the first full-suite run after the maze pass was wired in. Both were fixed within the same `08-code-implementation` run (in scope per that skill's own Step 7 — "a failure caused by this package's changes is in scope to fix"): `T11.a`/`T17.a`/`T17.b`/`T17.c`/`T17.d` rewritten to walk a real, graph-driven DFS tour (or pick the oracle's actual open edge) instead of a hardcoded path, so they work under any maze topology, not just the old full lattice. Full suite confirmed green after the fix (211/211). | Medium (found and fixed same-run, no shipped defect; but the *process* gap — `07`'s own supersession sweep not covering `test_rom.py`'s hardcoded-path test methodology — is real and could recur for a future package that changes another structural invariant) | 07 | SCHEDULED, riding a future `07-implementation-planning` pass: extend that skill's own supersession-sweep discipline (`BL-0054`'s fix, `07-implementation-planning/SKILL.md` Step 1) to explicitly include `test_rom.py` itself as a search target — a `grep` for hardcoded paths/coordinates/zone-sequences tied to the model being retired, not just production-code call sites — the next time a package proposes retiring a structural invariant `test_rom.py`'s existing suites might assume. Not urgent; no user decision needed. | SCHEDULED |
| BL-0072 | 2026-07-11 | gate | `00-pipeline-manager` run #83 (G3 gate for `IP-9080`/`IP-9090`/`IP-9100`) | **G3 authorization needed for `IP-9080`/`IP-9090`/`IP-9100`** (movement/pickup/UI bug-remediation tranche, `BL-0049`/`0051`/`0052`/`0053`) — not covered by the `BL-0001`…`BL-0005` bootstrap carve-out, nor by `BL-0062`'s prior answer (which named only the earlier five-bug tranche explicitly). | Medium (blocks three independent, small, already-planned remediation packages — no critical path, no urgency beyond "these are real, reported bugs") | 08 | **Resolved same run — user explicitly authorized all three** ("Authorize IP-9080 (SAVE screen text), Authorize IP-9090 (movement clamps), Authorize IP-9100 (pickup hitbox)", 2026-07-11). All three packages' Master Build Plan `Authorized?` column flipped to **YES — explicit user G3, 2026-07-11 (BL-0072)**; `packages/INDEX.md`/`ROADMAP.md` updated in sync. Rides the next `00-pipeline-manager` advance (`08-code-implementation`/`08-content-authoring`, all three now `READY` and authorized, mutually independent — no critical path). | DONE |
| BL-0073 | 2026-07-11 | finding | `08-code-implementation` run #85 (`IP-9100` implementation) | **A `07-implementation-planning` package (`IP-9100`) specified an implementation formula that direct verification during `08-code-implementation` found does not actually fix the reported bug.** `IP-9100`'s own §6 originally proposed a symmetric `\|item_x-PLAYER_X\|<8`/`\|item_y-PLAYER_Y\|<16` threshold change (keeping the existing abs-value code shape, just widening the constants) — a plausible-looking generalization of "two same-sized boxes overlap." Direct PyBoy verification against `BL-0053`'s own two concrete reproduction data points (embedded in that same backlog entry: item at `item_y=75` must NOT collect, item at `item_y=94` must collect) showed the symmetric formula still incorrectly collected the `item_y=75` case (`\|80-75\|=5<16`) — the actually-correct model treats the Collectible as a collision *point*, not a same-sized second box, requiring an asymmetric one-directional range test instead. The error was caught and corrected within the same `08-code-implementation` run (re-derived, re-verified against both reproduction points plus all four boundary values, package document itself corrected to match), so no defect shipped — but the planning-stage formula itself was wrong and would have shipped unverified if `08` hadn't independently checked it against the bug's own concrete data. | Medium (caught same-run, no shipped defect; but `07-implementation-planning` packages specifying an exact algorithmic formula are not currently required to verify that formula against any concrete reproduction data when such data exists, which is a real process gap) | 07 | SCHEDULED, riding a future `07-implementation-planning` pass: extend that skill's own Step 2 discipline (verifying cited files/functions/constants against the tree) to also require, whenever a bug-remediation package's own source backlog entry embeds concrete reproduction data (specific input→output pairs, not just a general description), that the package's own proposed formula be checked by hand against that data before being written down — not merely plausible-sounding, but actually verified to reproduce the correct output for the cited cases. Not urgent; no user decision needed. | SCHEDULED |
| BL-0074 | 2026-07-11 | bug | intake: project owner ("I started two new games. Both with default 0 seed. One with scale 3 and one with scale 9. Both produced maps that were 3 screens tall and 1 wide (grass beach, water). There is either an issue with the procgen or the transition conditions.") | **`SEED=0` (the literal new-game default — confirmed live: MAIN MENU → new game → A with no digits touched writes `SEED=0`, `WORLD_SCALE=3`) triggers the same PRNG-degeneracy defect `R113`/`ADR-0013` already characterized, but in the biome-assignment loop, not the maze pass.** Per `ADR-0010`, `seed=0` normalizes to `1`. Direct trace of `gw_prng_step`'s state from `x=1`: draw 0 → `0x0202` (delta=+1), draw 1 → `0x0000` (delta=-1) — and **every subsequent draw stays at `0x0000` forever** (the absorbing fixed point `R113` already formally proved). This means every biome-assignment draw after the second one returns a constant `delta=-1`. **Confirmed directly against the real built ROM** (`REGION_GRAPH`'s biome-id grid, read via PyBoy after an actual new-game creation at `seed=0`): `scale=3` gives `[[2,3,2],[1,2,1],[0,1,0]]` — passably varied *by coincidence* (the adjacency-grammar clamp produces a monotonic gradient-decay pattern even under a constant delta, which still looks "textured" at only 3×3); `scale=9` gives row 0 = `[2,3,2,1,0,0,0,0,0]`, **rows 1–8 all zero** — over 90% of the 81-region world floods to Water. **Maze/adjacency connectivity itself is not broken** — oracle-level BFS reachability is 9/9 (scale=3) and 81/81 (scale=9), canonical edge counts are reasonable; `ADR-0013`'s counter-XOR perturbation is working as intended for the maze pass specifically (it decorrelates from the stuck-at-zero raw PRNG via a cycling counter never fed back into `gw_prng_step`'s own state). The defect is isolated to biome-assignment's own *unperturbed* draws. The reporter's own "3 tall, 1 wide" description most likely reflects their own play experience (limited apparent variety while navigating) rather than a literal fixed shape — the biome-flooding above is the technically-confirmed, precisely-reproduced defect and should be the primary framing. | **High** (breaks procgen's core visual-variety promise for the single most common seed value — the unmodified default — at every scale; not an edge case, every new player who accepts the default seed hits this) | 07 → **03** (re-dispositioned at triage) | **Triaged (`00-pipeline-manager` run #87): SCHEDULED, riding `03-architecture-design-synthesis`, not `07` directly** — intake's own suggested entry stage (`07`, "root cause already diagnosed, mirrors `BL-0047`/etc.") undersold a real compatibility tension this bug's own fix must resolve first, the same class of decision `ADR-0013` already made once for the maze pass: **any fix to biome-assignment's own PRNG consumption — whether repairing `gw_prng_step` itself or adding a scoped perturbation like the maze pass got — changes generated biome output for every existing seed, not just degenerate ones, because biome-assignment (unlike the maze pass) is the foundational, already-shipped-and-`VERIFIED` (`IP-1020`) first generation step every `(seed, scale)` combination depends on, and `REGION_GRAPH` regenerates from `(seed, scale)` alone on every save load.** This is a materially bigger compatibility question than `ADR-0013` faced (the maze pass is brand-new work this session with no existing saves to break) and needs an explicit architecture decision — R113 already provides sufficient research grounding (no new `02` pass needed unless `03` finds a gap), so this rides `03-architecture-design-synthesis` directly, mirroring `BL-0070`'s own actual trajectory (`02`→`03`→`04`→`06`→`07`→`08`), not a straight-to-`07` remediation package. **Architecture decision made ([ADR-0014](../architecture/adr/ADR-0014-gw-prng-step-repair-needs-user-authorization.md), 2026-07-11): the technical fix is confirmed correct — repair `gw_prng_step`'s own mixing step to the `7,9,8` shift triplet (verified directly: 0/2000 seeds degenerate under it, vs. 2000/2000 under the shipped version — this is not a "seed=0 is unlucky" bug, effectively every seed is degenerate, confirmed at scale=9: 55% of 200 tested seeds already produce a majority-Water world today). Authorization to SHIP it is explicitly declined by that ADR and routed to the user — `REGION_GRAPH` regenerates from `(seed, scale)` on every save load, so this fix changes every existing save's world on its next load, the same class of consequence `ADR-0013` already established needs explicit sign-off.** Re-dispositioned to **`NEEDS-USER`** — the exact decision needed: authorize shipping the `gw_prng_step` repair (accepting that every existing save regenerates a different world on next load), or decline (leaving biome-assignment broken for roughly half of all `scale=9` worlds, including the literal default seed, indefinitely). **Resolved same session — user explicitly authorized** ("Yes, ship the fix (Recommended)"). Re-dispositioned: `SCHEDULED`, riding `07-implementation-planning` next (package the `gw_prng_step` mixing-step repair per `ADR-0014`'s own recommended shape: `7,9,8` shift triplet, `SAVE_VERSION_VAL` bump, `worldgen.py` oracle updated in lockstep). Authorization for the resulting package's own `08` execution is separate (G3, per this project's standing convention) — this only authorizes the *decision to ship*, not yet a specific `IP-xxxx`'s own G3 answer. **Packaged (run #90, [IP-9110](../implementation/packages/IP-9110-gw-prng-step-mixing-step-repair.md)): `READY`. G3 authorization for `IP-9110`'s own `08-code-implementation` run recorded as already satisfied by this entry's own "Yes, ship the fix (Recommended)" answer — that question and answer were specifically and unambiguously about shipping the fix with its exact save-compatibility consequence spelled out (every existing save regenerates a different world on next load), functionally identical in form to the explicit `AskUserQuestion` gate-checks `BL-0069`/`BL-0072` recorded separately, not a vaguer expression of interest; no second authorization round required.** Remains `SCHEDULED`, riding `08-code-implementation` next. **Implemented (run #91, [IP-9110](../implementation/packages/IP-9110-gw-prng-step-mixing-step-repair.md)): `COMPLETE`, 222/222 checks pass.** `T12.j`'s statistical corpus check confirms the fix (mean Water fraction 15.6% at `scale=9`, down from ~46%); `T12.k` directly re-checks this entry's own original reproduction case (`seed=0`, `scale=9`) — now 25.9% Water, no longer near-total-flooded. Awaits independent (fresh-session) verification (`09-package-verification`). | DONE |
| BL-0075 | 2026-07-11 | bug | intake: project owner ("The scale at 9 is still just like 6 screens vertically and scale at 2 is like 1 or 2 screens.") | **Reported after `IP-9110` (`BL-0074`) shipped this session — a separate observation about the world's apparent navigable *size*, not biome variety.** Investigated directly before filing (using `worldgen.py`, which `T19.c`'s own oracle-parity check confirms matches the real SM83-built ROM byte-for-byte): **reachability is not broken** — full BFS from region 0 reaches all 81 regions at `scale=9` for every seed tested (0, 1, 42, 777, 12345), matching `IP-1070`'s own `T19.b` guarantee; real path depth (BFS eccentricity from region 0) ranges 16–25 moves, mean ~8–13 — the maze genuinely is scale-sized and fully connected. **But a straight-line walk (repeatedly pressing one direction — exactly what a player does to gauge "how big is this world") dead-ends far short of that**: at `scale=9` seed=0, walking DOWN from region 0 reaches only 7 regions (`[0,9,18,27,36,45,54]`) before a maze-blocked edge; RIGHT dead-ends immediately (length 1). At `scale=2` seed=0, DOWN dead-ends immediately (length 1); RIGHT reaches the other region (length 2) — matching the reporter's own description almost exactly ("scale=9 like 6 screens vertically" — measured 7; "scale=2 like 1 or 2 screens" — measured 1–2). **Root cause: the direct, foreseeable consequence of `ADR-0012`'s own spanning-tree-plus-braid maze design (~25% braid-reopen, `FR-9150`) combined with a still-open gap** — `FS-108`/`IP-1080` (maze-aware transition-edge classification, `BL-0067`; its own `GDS-08` tile-art delta `BL-0068` already landed) has not been implemented yet, so `draw_region_arrows` still renders a maze-blocked-but-grid-adjacent edge identically to a true grid-boundary edge (no arrow either way) — a player hitting a wall has zero signal distinguishing "maze dead-end, turn" from "true world edge," so the world reads as much smaller than it structurally is. `IP-1080` is currently `BLOCKED` (on `IP-1070` reaching `VERIFIED` — `IP-1070` is `COMPLETE`, not yet `VERIFIED`, needs a fresh-session `09-package-verification` run) and **not authorized** (no G3 on record for `IP-1080` itself). Not a regeneration/procgen defect (distinct from the already-fixed `BL-0074`) — a legibility/UX gap in an already-identified, already-specified, not-yet-implemented feature. **Secondary, lower-priority open question:** whether `ADR-0012`/`FR-9150`'s ~25% braid-reopen default produces long enough straight corridors for the "distinct Zelda/Pokémon-style regions and paths" feel the owner originally asked for — best evaluated once `IP-1080` ships and the maze is actually legible/playable, not tuned blind beforehand. | High (the maze's core legibility is broken for every player until `IP-1080` ships — not a cosmetic nicety, it directly determines whether the generated world reads as the intended size) | 09 → 07 (this bug's own resolution path runs through `09-package-verification` on `IP-1070` first, unblocking `IP-1080` to `READY`, then a G3 authorization gate, then `08-code-implementation`; no new `07` planning work is needed, `FS-108`'s logic half and `IP-1080`'s own package already exist — this entry's own disposition is the manager's to set) | **Triaged (`00-pipeline-manager` run #92): `SCHEDULED`** — no new work needed at any stage right now; this entry's own resolution already rides the existing `IP-1070`(→`VERIFIED`)→`IP-1080`(authorize→implement) chain, tracked independently. Not `DEFERRED` (High severity, per the pipeline's own severity-honesty rule) — `SCHEDULED` against that chain's own next step, which is `09-package-verification` on `IP-1070` (blocked on a fresh session). Re-check when `IP-1070` reaches `VERIFIED`. **Trigger fired (`00-pipeline-manager` run #94): `IP-1070` reached `VERIFIED` 2026-07-12 ([VR-1070](../implementation/verification/VR-1070-maze-shaped-region-adjacency.md)), unblocking `IP-1080` to `READY`.** Remains `SCHEDULED` — the chain's next link is a G3 authorization decision for `IP-1080` (not yet asked of the user), then `08-code-implementation`. **`IP-1080` authorized 2026-07-12** (explicit user G3, `BL-0083` — "IP-1080 approved"), implemented, and independently `VERIFIED` 2026-07-12 ([VR-1080](../implementation/verification/VR-1080-maze-aware-edge-classification.md)). **Remains `SCHEDULED`, not `DONE`** — `IP-1080` only shipped the *classification logic* half (open/blocked/absent, confirmed via `T20.a–c`); its own Definition of Done and Verification Checklist both explicitly leave AC-4 (the actual visual rendering of the blocked-edge indicator) open, confirmed still uncovered by any test in `VR-1080`'s own audit. The player-facing symptom this entry reports — a maze dead-end reading identically to a true world edge — is **not yet resolved**, since no on-screen signal differentiates them until the rendering half ships. That rendering half is `BL-0068`'s own `GDS-08` delta (already landed, §10) plus a still-unplanned `07`/`08-content-authoring` package to actually draw the indicator. Re-check when that package ships. **Routing corrected (`00-pipeline-manager` run #119, Release 2 baseline-flip follow-up):** `FS-108` itself (§14/§19 OQ2) states explicitly that "the rendering half's actual behavior/acceptance criteria still need their own future spec pass before FEAT-2100's rendering half can be planned or implemented" — this entry's own prior disposition ("09 → 07," no new `06` work needed) is stale/wrong; the correct chain is `06-feature-specification` (extend `FS-108` or author a sibling spec for the rendering half's FR/AC) → `07-implementation-planning` → `08-content-authoring`/`08-code-implementation`. Remains `SCHEDULED`, next step `06`. **`06-feature-specification` run #119: `FS-108` revised** — the rendering half is now fully specified (§6 Workflow C, §15 AC-4/5), closing the document's own last Open Question. Not yet implemented — no `IP-xxxx` package exists for it. Remains `SCHEDULED`, next step `07-implementation-planning` (package the rendering half against `FS-108`'s now-complete spec — a small `08-content-authoring`-shaped package: 4 new tile bitmaps plus a small `asm_game.py` branch reusing already-specified arithmetic/constants). **`07-implementation-planning` run #120: packaged as [IP-1081](../implementation/packages/IP-1081-maze-blocked-edge-indicator-content.md) (content, `NOT STARTED`) → [IP-1082](../implementation/packages/IP-1082-maze-blocked-edge-indicator-render.md) (render, `BLOCKED` on `IP-1081`)**, mirroring `IP-1030`/`IP-1031`'s own code/content peer-split precedent for the same FS. Neither package authorized (no G3 on record). Remains `SCHEDULED` — next step is a G3 authorization decision from the user, then `08-content-authoring` on `IP-1081` first (critical path). **Both authorized 2026-07-12** (`BL-0092`, "Yes, build both"). **`08-content-authoring` run #121: `IP-1081` implemented, reached `COMPLETE`** (231/231, tile art added, zero new palette entries). Remains `SCHEDULED`, not `DONE` — `IP-1082` (the actual rendering) hasn't shipped yet, and `IP-1081` itself needs a fresh-session `09-package-verification` pass before `IP-1082` can build on it (same-session independence rule — `IP-1081` was implemented this session). **`09-package-verification` (fresh session, 2026-07-13): `IP-1081` independently confirmed** ([VR-1081](../implementation/verification/VR-1081-maze-blocked-edge-indicator-content.md)) — 233/233 pass, all DoD/checklist items confirmed by direct diff. `IP-1081` → `VERIFIED`, unblocking `IP-1082` → `READY`. One Medium finding (`BL-0097`, direction-pair tiles pixel-identical, routed to a future `09-content-review`, non-blocking). **`08-code-implementation` (same session, 2026-07-13): `IP-1082` implemented, reached `COMPLETE`** — `draw_region_arrows`'s four `0xFF`-branches extended with the blocked-vs-absent render decision, drawing `IP-1081`'s tiles; a same-package register/fallthrough defect (blocked-test running unconditionally after the open-arrow write, overwriting it) found and fixed during implementation. `T20.b` corrected, new `T20.e`. Full suite 234/234. Live-driven via PyBoy screenshot at `(seed=0, scale=3)` region 0's blocked right edge — confirmed rendering both by WRAM tile-index read and visual screenshot. Closes `FR-2330` in full. **Remains `SCHEDULED`, not `DONE`** — `IP-1082` needs its own fresh-session `09-package-verification` pass (same-session independence rule, implemented this session) before this entry can close; a `09-content-review` pass on the shipped art (weighing `BL-0097`) is also still owed. | SCHEDULED |
| BL-0076 | 2026-07-11 | bug | intake: project owner ("No, I have tested the right edge of all tiles including the ones you've indicated and I am unable to move right. There is a code bug.") | **Critical regression: no player can ever transition rightward into a new zone, in any generated world, as of `IP-9090`'s commit this session.** Root-caused precisely: `check_zone_transition`'s (`asm_game.py:662`) RIGHT-edge trigger requires `PLAYER_X >= 156` to fire, but `IP-9090` (`BL-0052`) correctly changed `handle_play_input`'s own RIGHT movement clamp (`asm_game.py:518`) from `CP_n(160)` (old, buggy, allowed max `X=159`) to `CP_n(153)` (new, correct — the true flush boundary for an 8px-wide sprite on a 160px-wide screen; `BL-0052`'s own diagnosis and fix were entirely correct in isolation, confirmed independently reproducible). **The two thresholds used to overlap by coincidence under the OLD buggy clamp** (`159 >= 156`, so the transition still fired despite the clamp itself being visually wrong) — **now that the clamp is correctly `152`, `152 < 156` means the RIGHT transition can never be satisfied through normal gameplay input, unconditionally, for every seed/scale/region.** Confirmed empirically via PyBoy with real, sustained button-press input (not memory-forced teleportation, which was this session's own first, insufficiently-rigorous check and missed this): at region 9 (`seed=0`, `scale=9`), which has a confirmed-open RIGHT neighbor (region 10) and a confirmed-correctly-rendered RIGHT arrow tile in VRAM (`0x16` at the arrow slot), walking right for 300 real button-press ticks settles `PLAYER_X` at exactly `152` and `CUR_ZONE` never changes. Cross-checked the other three directions against their own transition thresholds: LEFT (`PLAYER_X==0` exactly, movement floor `0` — consistent, unaffected), UP (`PLAYER_Y<18`, movement floor `8` post-`IP-9090` — `8<18`, transition range still reached while walking up, unaffected), DOWN (`PLAYER_Y>=128`, movement ceiling exactly `128`, unchanged by `IP-9090` — exact match, the pattern RIGHT should also follow). Only RIGHT is broken. **The fix is a single-constant change, precisely analogous to `IP-9090`'s own class of fix**: `check_zone_transition`'s RIGHT-edge comparison (`asm_game.py:662`, currently `CP_n(156)`) should become `CP_n(152)`, mirroring DOWN's already-consistent "transition threshold == movement clamp's own max reachable value" pattern. This was a pre-existing latent inconsistency between `check_zone_transition`'s own hardcoded edge constants and `handle_play_input`'s movement clamps that `IP-9090`'s own supersession sweep did not catch — that sweep searched for the OLD clamp's own magic numbers, not for `check_zone_transition`'s independently-hardcoded edge thresholds, a real gap in that package's own sweep scope (same class of process gap as `BL-0071`/`BL-0073`, worth folding into `07`'s own future discipline: when a package changes a movement-clamp boundary, also sweep for every other hardcoded comparison against the *old* boundary value elsewhere in the file). **Distinct from `BL-0075`** (the maze-legibility/`IP-1080` gap) — `BL-0075`'s own diagnosis (the world is fully connected, arrows render correctly) remains accurate and unaffected by this bug, but this bug independently and completely prevented verifying that diagnosis through normal play, which is exactly how it surfaced. | **Critical** (breaks core navigation universally — literally no player can ever move rightward into a new zone right now, in the currently pushed build; not an edge case, not scale/seed-specific) | 07 (root cause and fix are both fully diagnosed and precise — a one-line constant change plus a regression test; no research or architecture decision needed) | **Triaged (`00-pipeline-manager` run #92): `SCHEDULED`, riding `07-implementation-planning` this run** — Critical severity, outranks every other open item (a live, universal navigation-breaking regression). Root cause and fix already fully diagnosed at intake; no research or architecture step needed. **Packaged (run #92, [IP-9120](../implementation/packages/IP-9120-right-zone-transition-threshold-fix.md)): `READY`. G3 authorization resolved same run (`BL-0077`, "Yes, ship the fix").** **Implemented (run #92, `08-code-implementation`): `COMPLETE`, 224/224 checks pass.** Directly re-verified at this entry's own original reproduction point (region 9, `seed=0`/`scale=9`) — sustained rightward input now carries the player through multiple real transitions. Awaits independent (fresh-session) verification. | DONE |
| BL-0077 | 2026-07-12 | gate | `00-pipeline-manager` run #92 (G3 gate for `IP-9120`) | **G3 authorization needed for `IP-9120`** (RIGHT zone-transition threshold fix, `BL-0076`) — not covered by the `BL-0001`…`BL-0005` bootstrap carve-out; asked immediately given Critical severity rather than letting it queue. | Critical (blocks the fix for a universal, live-breaking regression) | 08 | **Resolved same run — user explicitly authorized** ("Yes, ship the fix (Recommended)," 2026-07-12). `IP-9120`'s Master Build Plan `Authorized?` column flipped to **YES — explicit user G3, 2026-07-12 (BL-0077)**; `packages/INDEX.md`/`ROADMAP.md` updated in sync. Rides the next `00-pipeline-manager` advance (`08-code-implementation` on `IP-9120`, now `READY` and authorized). | DONE |
| BL-0078 | 2026-07-12 | bug | intake: project owner ("If the bunny walks to the right edge where it cannot pass, it correctly stops; however, if the bunny then walks down without moving left/right, when it enters a region where it can move right, it triggers the move right conditions. It should only move right if the bunny attempts to move in that direction and does not hit a wall. It should not move right if it is simply at the edge.") | **Spurious zone transitions: `check_zone_transition` is purely position-based and never checks whether the player is actually holding the corresponding direction — reachable in normal play as of the maze pass (`IP-1070`), invisible before it.** Confirmed directly via PyBoy with real, sustained button-press input at the literal default new-game fixture (`seed=0`→`1`, `scale=3`): walked RIGHT from region 0 until blocked by the (correctly fixed, `IP-9120`) clamp — `PLAYER_X` settles at `152`, region 0 has no right neighbor, correctly no transition. Then walked DOWN only, never touching left/right again. Result: `CUR_ZONE` went `0`→`3` (a correct, real down transition) and **immediately, in the same down-walk, `3`→`4` (a spurious rightward transition)** — without the player ever pressing right in region 3. Root cause: `check_zone_transition`'s (`asm_game.py`) four branches (RIGHT ~line 662, LEFT ~671, UP ~680, DOWN ~689) each test only `PLAYER_X`/`PLAYER_Y` against a threshold plus `REGION_GRAPH`'s own neighbor byte — never whether the corresponding `JOY_CUR` direction bit is actually held this frame. `PLAYER_X`/`PLAYER_Y` persist unchanged across a transition in the *other* axis (a down transition resets `PLAYER_Y` but leaves `PLAYER_X` exactly as it was), so sitting at the RIGHT clamp ceiling when entering a new region via DOWN, where that new region happens to have an open right neighbor, fires the RIGHT branch on the very next frame regardless of what button is actually held. **This is a pre-existing design gap in `check_zone_transition`'s own trigger model** (present since `IP-9050`'s rewrite, plausibly the original pre-procgen implementation too), almost certainly invisible under the old full-lattice adjacency model — "blocked right" was a per-COLUMN property there (a true grid boundary), uniform down an entire column, so this exact scenario could never arise. The maze pass (`IP-1070`, `ADR-0012`) made open/blocked a per-REGION property instead, varying row-to-row within the same column — exactly the condition that makes this defect reachable, and it reproduces on the very first down-move from the literal default game start (region 0 → 3). **Likely correct fix**: gate each of `check_zone_transition`'s four branches on the corresponding `JOY_CUR` direction bit being held (mirroring exactly how `handle_play_input` already gates its own movement — `BIT_b_B(J_RIGHT)`/`J_LEFT`/`J_UP`/`J_DOWN` against `JOY_CUR`), applied uniformly to all four directions (the same latent gap plausibly affects LEFT/UP/DOWN too, not just the RIGHT case the user happened to notice — worth confirming directly, not assumed). **Real, non-trivial ripple, not a one-line fix**: a substantial slice of the existing `test_rom.py` suite (`T11.a2`, several `T16` `CUR_ZONE`-forcing patterns, `T17`'s/`T19`'s own DFS-tour traversal helpers) tests transitions by directly writing `PLAYER_X`/`PLAYER_Y` via memory with **no simulated button hold** — a deliberate, previously legitimate way to isolate `check_zone_transition` from movement-clamp behavior (`T17`'s own code comments say this explicitly). Under an intent-gated fix, every one of those tests would need to also inject the correct `JOY_CUR` bit alongside the position write or they will start failing — the exact scope is unknown until swept. | High (a real, easily-reproduced, confusing navigation bug reachable from the literal default game start — causes unintended sideways warps during ordinary play — but the player is not fully blocked the way `BL-0076` was, so not Critical) | 07 (root cause fully diagnosed, but the fix's own scope/ripple needs genuine implementation-planning judgment — a mandatory `test_rom.py` supersession sweep per `BL-0071`'s own established discipline, not a copy-paste one-liner like `IP-9120` was) | **Triaged (`00-pipeline-manager` run #93): `SCHEDULED`, riding `07-implementation-planning` next** — outranks the standing `09-package-verification`/`NEEDS-USER` items (a real, reproducible navigation bug with clear evidence and a named fix direction). **Packaged (run #93, [IP-9130](../implementation/packages/IP-9130-zone-transition-intent-gate.md)): `READY`. G3 authorization resolved same run (`BL-0079`, "Yes, ship the fix").** **Implemented (`08-code-implementation`, 2026-07-12, harvested at `00-pipeline-manager` run #94 — this implementation ran outside the manager, in a separate merged session/PR, and was reconciled as drift this run): `COMPLETE`, 226/226 checks pass.** Directly re-verified via PyBoy reproducing this entry's own exact reported sequence (walk RIGHT until blocked in zone 0, then DOWN only) — zone now correctly settles at 3 and stays there; confirmed a legitimate rightward press in zone 3 still transitions to zone 4. Awaits independent (fresh-session) verification (`09-package-verification`). | DONE |
| BL-0079 | 2026-07-12 | gate | `00-pipeline-manager` (G3 gate for `IP-9130`) | **G3 authorization needed for `IP-9130`** (zone-transition intent gate, `BL-0078`) — not covered by the `BL-0001`…`BL-0005` bootstrap carve-out. | High (blocks the fix for a real, reproducible spurious-transition regression) | 08 | **Resolved same run — user explicitly authorized** ("Yes, ship the fix (Recommended)," 2026-07-12). `IP-9130`'s Master Build Plan `Authorized?` column flipped to **YES — explicit user G3, 2026-07-12 (BL-0079)**; `packages/INDEX.md`/`ROADMAP.md` updated in sync. Rides the next `00-pipeline-manager` advance (`08-code-implementation` on `IP-9130`, now `READY` and authorized). | DONE |
| BL-0080 | 2026-07-12 | finding | `09-package-verification` run on `IP-1070` ([VR-1070](../implementation/verification/VR-1070-maze-shaped-region-adjacency.md)), `00-pipeline-manager` run #94 | **`IP-1070`'s own package document (§6 task 4) understates the prune-pass tree-edge test's real logic.** The package states the test is a single check ("`GW_MAZE_STATE[V]` bit 7 is set and its parent-direction equals `opposite(Di)` … this single check correctly identifies every tree edge"). The shipped code (`asm_game.py:1532–1544`) and the `worldgen.py` oracle (which explicitly documents "mirroring the SM83 side's own unconditional Check 1/Check 2") both actually implement **two** checks — the stated one plus a symmetric check for the case where `R` (not `V`) is the child, i.e. `R`'s own parent-direction equals `Di`. This second check is genuinely necessary: a randomized DFS spanning tree can carve an edge in either direction, so a single-direction check would misclassify roughly half of all real tree edges as prunable, corrupting connectivity. The shipped algorithm is correct — VR-1070 confirmed this via T19.b (reachability) and T19.c (oracle/SM83 parity, 0 mismatches) — only the package's own §6 prose is inaccurate about *why* it's correct. | Low (documentation accuracy only; the as-built `GDS-07`/`FS-107` docs are correct, and the shipped code/oracle are both correct and mutually verified — no re-implementation needed) | 07 | SCHEDULED: correct `IP-1070`'s own §6 task 4 text the next time that package document is touched (describe both checks, cite the `worldgen.py` oracle's own comment as the tell), or fold into a future doc-hygiene pass. Not urgent, no user decision needed. | SCHEDULED |
| BL-0081 | 2026-07-12 | research-gap | intake: project owner (deciding `BL-0050`) — "The solution must be researched and it is blocked by a need to update win conditions. The current win condition is still 9 carrots (hold over from the 9 tile map). That is not necessarily the best condition with the scalable world. The player can currently collect carrots from any 9 tiles without exploring or feeling a story. Win conditions must be a new BL item requiring research." | **The shipped win condition (`CARROTS_COUNT==9`) is an unexamined holdover from the pre-procgen fixed 3×3/9-zone map (one carrot per zone, 9 zones = win) and never scaled with `WORLD_SCALE`.** Under the current procedurally generated world (2–81 regions depending on scale, one `Carrot` per region per `FR-9140`/`IP-1070`'s maze-shaped adjacency), a player can still win by collecting any 9 carrots — from adjacent starting regions, without meaningfully navigating the generated world — undermining both the "explore a generated world" premise (`C7`, the world-scale commitment) and the point of the maze/adjacency work (`ADR-0012`/`IP-1070`/`IP-1080`) the pipeline is actively building. Open research question, not a specified fix: what win condition fits a scalable, procedurally generated world — candidates include a scale-relative carrot count, a percentage-of-world-collected threshold, reaching a generated "goal" region, or some hybrid. Directly blocks `BL-0050` (the MAP/status-screen redesign) per the owner's own stated reasoning — that screen is meant to show progress toward winning, which can't be meaningfully designed until the win condition itself is settled. | Medium (exploratory/foundational research request, not urgent, blocks no in-flight package — but does block `BL-0050`) | 02 (`02-research-game-design` — a design/pacing question, not a hardware or tooling one) | **Triaged (`00-pipeline-manager`, 2026-07-12): `SCHEDULED`**, riding a future `02-research-game-design` pass. Not urgent, not blocking any in-flight package; blocks `BL-0050` specifically (which is now `DEFERRED` against this entry's own resolution). **Resolved (`02-research-game-design` run #122): [R215](../research/encyclopedia/R215-procgen-win-condition-design.md) authored** — surveys fixed-goal-node, percentage-completion, and scale-relative-threshold conventions (roguelike/Metroidvania precedent), confirms `FR-9140`'s reachability guarantee makes every candidate viable, and presents four evidence-grounded candidates (A: scale-relative count, B: percentage threshold, C: fixed goal region, D: hybrid of B+C) as a tradeoff table for `03`/`04` to choose from — deliberately not picking one, per this skill's own scope (research grounds, never decides). Unblocks `BL-0050`. **Owner reviewed the tradeoff table directly in chat and resolved it (2026-07-12)** — a fifth shape not in the original four (dead-end-priority, scale-count target), filed as [BL-0093](../pipeline/backlog.md) (finite, implement now) and [BL-0094](../pipeline/backlog.md) (infinite variant, deferred). | DONE |
| BL-0082 | 2026-07-12 | research-gap | intake: project owner — "Add another BL research topic on how the procgen architecture might be able to build the map on the fly as the player moves, not generating all tiles at once but as the player moves, use the seed to establish adjacency, maze progression, biome, etc. with the goal that a theoretical infinite map could be played. That may place limitations on win conditions ideas and biome seeding." | **Whether the procgen architecture could generate the world lazily/on-demand as the player approaches each region (deriving biome/adjacency/maze state deterministically from `(seed, region coordinates)`) instead of the current single upfront pass over the whole `WORLD_SCALE`×`WORLD_SCALE` grid, with the goal of a theoretically unbounded/infinite playable world.** Today's `generate_world` (`asm_game.py`, `IP-1020`/`IP-1070`) generates the entire region graph in one pass at new-game time, bounded to `WORLD_SCALE<=9` (81 regions) by `NFR-4200`'s confirmed WRAM/SRAM headroom. A streaming/local-generation model raises real open questions spanning both hardware feasibility and design: whether `GW_MAZE_STATE`'s backtracking spanning-tree carve (which currently assumes global visited-state across the whole graph) is representable in a streaming/local fashion at all, what a maze spanning-tree even means without a fixed global extent, the WRAM/ROM/compute cost of doing this on real SM83 hardware, and interaction with save/load (`REGION_GRAPH` currently regenerates from `(seed, scale)` on every load per `ADR-0012` point 6 — an unbounded world has no fixed "scale" to save). Foundational, exploratory — the owner explicitly connects it to `BL-0081` (win-condition research) and `BL-0066` (biome-blob clustering, on hold): both may need revisiting once/if this research lands, since an unbounded world changes what a "win condition" or a "clustering pass over the whole grid" even means. **In scope, per the owner's direct instruction (2026-07-13): neither of `BL-0066`'s two candidate blob-seeding strategies is guaranteed to survive a streaming model** — dead-end seeding needs a completed maze pass over a *known extent* to find dead-ends from, and the N-random-point-flood-fill alternative needs a *bounded grid* to place and flood-fill points across; a lazy/on-demand region-by-region generation model may support neither without rework. This research must evaluate what (if anything) a per-region-clustering concept even means under whichever streaming model it lands on — e.g. a per-region deterministic blob-id derived purely from `(seed, region coordinate)`, with no whole-grid/whole-maze pass required — not just the hardware-feasibility/WRAM questions already named above. `BL-0066` is formally blocked on this research's own conclusion, not merely "connected" to it. | Medium (exploratory/foundational research request, not urgent, blocks no in-flight package — but does now formally block `BL-0066`) | 02 (spans `02-research-gbc-hardware` — WRAM/ROM/compute feasibility of streaming generation on real SM83 hardware — and `02-research-game-design` — what "infinite" means for the collect-a-thon/win-condition design *and* what biome-blob clustering means without a fixed whole-grid extent; the manager should decide whether one pass or both is warranted) | **Triaged (`00-pipeline-manager`, 2026-07-12): `SCHEDULED`**, riding a future `02-research-gbc-hardware` pass first (the hardware-feasibility half gates whether the design half is even worth pursuing), then `02-research-game-design` if hardware confirms it's viable. Not urgent, not blocking any in-flight package. **Scope widened (2026-07-13, user direct instruction):** must explicitly evaluate blob-clustering/seeding-strategy viability under whatever streaming model this research lands on, since `BL-0066` now formally depends on that conclusion (see `BL-0066`'s own updated entry). **`02-research-gbc-hardware` run #133 (2026-07-13): [R114](../research/encyclopedia/R114-streaming-world-generation-feasibility.md) authored.** Hardware-feasibility half answered: **yes, representable, but not as an incremental patch** — the shipped algorithm's two load-bearing global-sequential dependencies (biome's raster-scan anchor-clamp chain; the maze carve's global backtracking DFS) both require a genuine algorithm-family swap to a positionally-deterministic local form (per-region xorshift reseeding from `SEED` XOR/shift-mixed with `(row, col)`, no multiplication — SM83 has none, matching this codebase's own existing PRNG construction). Named a proven zero-memory streaming-compatible maze option (the Binary Tree algorithm) but flagged its own directional dead-end bias as a **direct conflict with `IP-1021`'s just-shipped dead-end-priority win condition** and `BL-0094`'s carried-forward infinite-mode design — a real cross-cutting risk for `03`/`02-research-game-design` to weigh, not resolved here. WRAM headroom re-measured (3082 bytes bank-0 free, `SVBK` banking untouched/available); ROM headroom re-measured (9784 bytes free). Save/load needs a genuinely different persisted shape (position + a bounded visited-region ledger, not a regenerate-from-scale model) if adopted. **`02-research-game-design` run #134 (2026-07-13): [R216](../research/encyclopedia/R216-infinite-mode-win-condition-design.md) authored.** Design-half answered: score-chasing (`BL-0094`'s own top-3-high-score design) confirmed as the historically correct genre answer for non-terminating play (arcade high-score convention, cited), not an improvisation; name-entry UI's deprioritization also well-grounded (its original social/cabinet purpose doesn't transfer to a single-player handheld save). **Proposes resolving `R114`'s dead-end-bias conflict by decoupling treasure placement from maze structure entirely** — a region holds treasure per `hash(SEED,row,col) mod K`, the same positional-determinism technique `R114` already established, independent of whichever maze algorithm `03` eventually picks; flagged explicitly as a real departure from `BL-0094`'s literal "at dead ends" wording, not a silent substitution. States plainly that "infinite" means unbounded exploration extent with bounded persisted memory (an SRAM visited-region ledger), not unbounded save data. Surfaces (does not resolve) a new question: whether an infinite run is indefinitely resumable or its own bounded "run" needing a new end-condition mechanic. Both `02-research-*` halves of `BL-0082` are now complete. **`03-architecture-design-synthesis` run (2026-07-13, user-authorized via `00-pipeline-manager` gate stop): adopted.** [ADS-001](../architecture/ADS-001-streaming-infinite-world-generation.md) synthesizes both research halves; [ADR-0016](../architecture/adr/ADR-0016-streaming-infinite-mode-generation-architecture.md) adopts streaming, positionally-deterministic generation (per-region `hash(SEED,row,col)` reseeding, Binary Tree maze) as the architecture for a new, additive **Infinite Mode**, selectable alongside the existing finite `(seed, scale)` model — the finite mode (`ADR-0009`/`ADR-0012`/`ADR-0013`) is cross-referenced, not amended. [ADR-0017](../architecture/adr/ADR-0017-infinite-mode-treasure-placement-and-win-condition.md) resolves the dead-end-bias conflict by decoupling treasure placement from maze structure (`hash(SEED,row,col) mod K`) and adopts `BL-0094`'s score-chasing win condition unamended. This authorizes the architecture target only — no code shipped; unblocks `BL-0094` (now `SCHEDULED`, rides `04-requirements-engineering`) and `BL-0050` (now `SCHEDULED`, finite mode proceeds independently). `BL-0066`'s finite-mode question (separate from the Infinite Mode answer this decision gives it) remains open, re-dispositioned `NEEDS-USER` in its own entry. Four Open Questions filed as new entries (renumbered `BL-0106`–`BL-0109` to avoid colliding with a concurrent session's own `BL-0104`/`BL-0105`, reconciled at merge — see run #146): `BL-0106` (run/session shape), `BL-0107` (Binary Tree aesthetic), `BL-0108` (visited-region ledger SRAM sizing), `BL-0109` (materialization cycle cost); the treasure-density constant `K` is a minor implementation-time tuning detail folded into `BL-0094`'s own scope, not filed separately. **This entry's own research-gap ask is fully answered — `DONE`.** | DONE |
| BL-0083 | 2026-07-12 | gate | `00-pipeline-manager` (G3 gate for `IP-1080`) | **G3 authorization needed for `IP-1080`** (maze-aware transition-edge classification, logic half) — not covered by the `BL-0001`…`BL-0005` bootstrap carve-out; became eligible this same session once `IP-1070` reached `VERIFIED` ([VR-1070](../implementation/verification/VR-1070-maze-shaped-region-adjacency.md)). | Medium (unblocks the direct fix for `BL-0075`'s maze-legibility complaint, but not urgent on its own) | 08 | **Resolved — user explicitly authorized** ("IP-1080 approved," 2026-07-12). `IP-1080`'s Master Build Plan `Authorized?` column flipped to **YES — explicit user G3, 2026-07-12 (BL-0083)**; `packages/INDEX.md`/`ROADMAP.md` updated in sync. Rides the next `00-pipeline-manager` advance (`08-code-implementation` on `IP-1080`, now `READY` and authorized). | DONE |
| BL-0084 | 2026-07-12 | bug | intake: project owner ("With default settings of seed 0 and scale 3, if the bunny starts at the first screen then goes down one, there should be an arrow pointing right but there is not.") | **The right-edge navigation arrow has never been visible, on any screen, since before this pipeline's own work began — a pre-existing bug inherited unchanged through `IP-1030`'s runtime port, not something `IP-1080` introduced.** Investigated directly: `draw_region_arrows`'s `ARROW_ADDR_R` constant (`asm_game.py`) places the right arrow at tilemap column 30 (`9*32 + (32-2)`). The GBC background window is a fixed 32×32-tile hardware surface, but only a 160×144px (20×18-tile) window is ever displayed, positioned by `SCX`/`SCY` — confirmed directly via PyBoy (`SCX=0`, `SCY=0` at all times; `asm_game.py` never writes either register) and empirically (poking a distinct marker tile at column 30, row 9 produced zero visible change on a captured screenshot). Column 30 is outside the visible 0–19 range; the up/down/left arrows (columns 15/15/1, well within range) are unaffected. Traced via `git log`/`git show 3479dba~1:tilemaps.py` to the retired build-time `_zone_arrows` (pre-`IP-1030`, pre-procgen): `_put(tiles, attrs, W-2, 9, TL_ARROW_R, 2)` with `W=32` (the full tilemap width) — the *original* off-by-wrong-constant was already there; `IP-1030`'s own stated goal ("arrow placement matches the shipped 3x3 grid exactly," confirmed via `T13.c`) faithfully preserved it, since neither `T13.c` nor `T19`/`T20` ever checked screen-window visibility, only the tilemap byte value at the address — a real test-coverage gap (checking the write, not the observable on-screen result). Confirmed the classification logic `IP-1080` added is **not implicated**: a full DFS-driven traversal via real button-driven navigation (not synthetic) across five `(seed,scale)` combinations found zero discrepancies between `IP-1080`'s open/blocked/absent decisions and the oracle. **Likely correct fix:** `ARROW_ADDR_R = 0x9800 + 9*32 + (32-2)` → `0x9800 + 9*32 + (20-2)` (column 18) — mirrors the left arrow's column-1 margin symmetrically within the true 20-column visible window (`W-2` should have used the *visible* width, not the full tilemap width). | High (a core navigation affordance — the on-screen indicator for "you can go this way" — has been silently missing for the right direction on literally every screen the game has ever shipped; not a regression from any current session's work, but a real, systemic, long-standing defect) | 07 (root cause and fix are both fully diagnosed and precise — a one-constant change plus a genuine screen-visibility regression test, the exact gap that let this ship undetected) | **Gate resolved same session — user explicitly authorized** ("Yes, ship the fix (Recommended)," 2026-07-12), recorded as `BL-0085`. **Packaged ([IP-9140](../implementation/packages/IP-9140-right-arrow-offscreen-position-fix.md)) and implemented same session: `COMPLETE`, 231/231 checks pass.** `ARROW_ADDR_R` column `30`→`18`; new check `T13.d` (screen-visibility audit). Directly re-verified via PyBoy screenshot at this entry's own exact reported sequence — the right arrow is now visibly present (confirmed by side-by-side screenshot comparison sent to the user). Awaits independent (fresh-session) verification (`09-package-verification`). | DONE |
| BL-0085 | 2026-07-12 | gate | `00-pipeline-manager` (G3 gate for the `ARROW_ADDR_R` off-screen fix, `BL-0084`) | **G3 authorization needed to ship the right-arrow position fix** (`BL-0084`) — not covered by the `BL-0001`…`BL-0005` bootstrap carve-out. | High (a long-standing, systemic navigation-affordance defect) | 08 | **Resolved same session — user explicitly authorized** ("Yes, ship the fix (Recommended)," 2026-07-12). Rides the next `08-code-implementation` on the package `07-implementation-planning` cuts for `BL-0084`. | DONE |
| BL-0086 | 2026-07-12 | finding | investigation prompted by the project owner's own question, while confirming `BL-0084`'s fix ("Why does the tile map extend to 30 when only up to 19 are visible? ... Are there any other offscreen areas?") | **Every one of the nine zone screens' hand-authored decorative landmarks includes elements placed at tilemap column ≥20 — permanently off-screen, since the true visible background window is only 20 tiles wide (`SCX=0` always; confirmed directly, same finding underlying `BL-0084`).** This is distinct from `BL-0084` (a single navigation-affordance tile) and distinct from the deliberate full-32-column terrain-texture fill (`_fill()`'s repetitive sprinkle pattern, which intentionally covers the invisible columns too as cheap, content-agnostic filler — not a defect). What's affected here is *named, hand-placed* landmark content — trees, crystals, houses, banners, torches, bats, cacti, bone piles, etc. — authored via literal coordinates or small fixed lists, each screen independently. Confirmed by direct source audit of every `*_screen()` function in `tilemaps.py`: **beach** (a palm tree + a shell-position element), **forest** (a mushroom), **mountain** (a rock + 2 icicles), **lake** (3 of 7 lilypad/fish/pier-adjacent elements), **village** (2 houses + several fence/lantern pieces), **cave** (3 of 7 crystals, 1 of 3 drips, 1 of 2 bats), **desert** (a cactus + several bone piles), **plains** (2 elements), **castle** (2 of 4 banners, 1 of 4 torches) — every single zone loses roughly a third to half of its designed landmark density to invisibility. Root cause: `_put()`'s own bounds check (`0 <= x < W` where `W=32`, the full *allocated* buffer width) only guards against writing outside the buffer entirely — it provides zero protection against "allocated but off the true 20-column *visible* window," the exact gap `BL-0084` also exploited. Every zone screen reads as if it was authored assuming the full 32-column canvas would render. | Medium-High (a real, widespread content defect — every zone screen is measurably less visually rich than designed — but purely cosmetic, not a functional/navigation blocker like `BL-0084`) | 08-content-authoring (a content-only fix — repositioning or dropping landmarks within each screen's own 20×18 visible canvas — once scheduled; artistic judgment is needed per screen, not a mechanical constant swap) | **User's own explicit direction (2026-07-12): do not fix now — file it to be captured by the planned graphics-overhaul body of work, and make sure that work reviews the existing game-aesthetics research first** (`R209` pixel-art technique, `R210` AI-assisted tile-art workflow, `R211` acclaimed-GBC-visual-design case studies — all filed via `BL-0013`; the normative aesthetic standard `BL-0029`/[GDS-08 §7](../architecture/08-presentation-architecture.md) already established but not yet applied to existing content). **`DEFERRED`** — revisit trigger: the graphics/aesthetics overhaul work being scheduled (a future `01`/`03`/`06`/`07`/`08-content-authoring` pass in the shape `BL-0029`'s own precedent set) — that work's own planning stage should read this entry and `_put()`'s own off-screen-write gap before touching any zone screen's tile layout, so the fix lands as part of the overhaul rather than needing a second pass. | DEFERRED |
| BL-0087 | 2026-07-12 | finding | `09-package-verification` run on `IP-9070` ([VR-9070](../implementation/verification/VR-9070-cur-zone-indexed-structures-generalization.md)) | **The RTM's `FR-5220` row cited only `IP-1010`/`T11.a–e` (125/125) and had never been updated to also name `IP-9070`/`T16`, even though `IP-9070` relocated and widened `FR-5220`'s own storage (`SCOREITEM_FLAGS`/`SRAM_SCOREITEM`, 9→81 bytes).** Not a defect in `IP-9070` itself — its own §9 Documentation Updates list never named the RTM as a file it would touch, so the gap predates this verification run. | Low | 04 (RTM cell correction) | **Resolved same run** — corrected in place by the verifying `09-package-verification` run itself (within its own "correct cells the audit proved wrong" allowance): `FR-5220`'s RTM row now cites `IP-1010, IP-9070` and `T11.a–e, T16.a–e` (231/231). No further action needed. | DONE |
| BL-0088 | 2026-07-12 | finding | `10-integration-review` run on the Release 2 tranche ([integration-review-release-2-tranche.md](../reviews/integration-review-release-2-tranche.md)) | **`docs/features/INDEX.md`'s `FS-103` row still describes `IP-1031` (content half) as "own verification pending," stale since `VR-1031` closed it 2026-07-12 the same session.** `09-package-verification`'s own workflow updates the Master Build Plan, verification `INDEX.md`, and (when proven wrong) the RTM, but by convention never touches `docs/features/INDEX.md` — so this row was never revisited after `IP-1031` reached `VERIFIED`. All other `FS-10x` rows checked current. | Low | Whichever skill next touches `docs/features/INDEX.md` — no owning stage currently carries a standing instruction to revisit this file post-verification; a light metadata-only correction | **Triaged (`00-pipeline-manager`, 2026-07-12): `SCHEDULED`**, riding the next pass that naturally touches `docs/features/INDEX.md` (e.g. a future `06-feature-specification` run, or folded into a `03` GDS-07 delta pass alongside `BL-0089` if convenient) — not urgent, purely cosmetic staleness, no functional impact. **Resolved (11-release-readiness's Release 2 baseline flip, 2026-07-12):** `docs/features/INDEX.md`'s `FS-103` row corrected in place to cite `VR-1031` and read "both halves VERIFIED" — this file was already being touched to update `FS-107`/`FS-108`'s own stale rows for the same baseline flip, a natural rider. | DONE |
| BL-0089 | 2026-07-12 | finding | `10-integration-review` run on the Release 2 tranche ([integration-review-release-2-tranche.md](../reviews/integration-review-release-2-tranche.md)) | **`docs/architecture/07-data-model.md` (GDS-07)'s `SEED`/`KEYITEM_FLAGS` WRAM table rows (lines 163/166) carry two stale narrative clauses.** Line 163: `SEED` is described as "Written by `FEAT-1100` (not yet shipped)" — `FEAT-1100` is `IP-1040`, `VERIFIED` 2026-07-11; `SEED` is actually written by `sse_compose_seed` (`asm_game.py:1158`). Line 166: `KEYITEM_FLAGS`'s clear-loop widening (9→81 bytes) is described as still `FEAT-1100`'s own pending scope — doubly wrong, since `FEAT-1100` shipped **and** the widening was actually done by a different package, `IP-9050`/`BL-0063` (confirmed by direct code read, `asm_game.py:309-314`/`:377-379`). The table's own address/size *data* is accurate throughout — only these two forward-looking narrative clauses, written before the packages they anticipated shipped, were never revisited. | Medium | `03-architecture-design-synthesis` (a GDS-07 delta correcting both cells, citing `IP-1040` and `IP-9050` as the packages that actually resolved each) | **Triaged (`00-pipeline-manager`, 2026-07-12): `SCHEDULED`**, riding the next `03-architecture-design-synthesis` pass touching GDS-07 — not blocking any in-flight package, the underlying WRAM data is correct, only the prose is stale. | SCHEDULED |
| BL-0090 | 2026-07-12 | finding | `10-integration-review` run on the IP-9110/9120/9130/9140 package set ([integration-review-ip-9110-9120-9130-9140.md](../reviews/integration-review-ip-9110-9120-9130-9140.md)) | **`docs/architecture/07-data-model.md` (GDS-07)'s SRAM save-format table (§3, line 89) states `SAVE_VERSION_VAL = 0x03` and its §7a bump-history note (line 227) stops at "third bump" — neither mentions `IP-9110`'s later `0x03`→`0x04` bump.** The shipped value is `0x04`, independently confirmed in `VR-9110`. Distinct from `BL-0089` (which covers `SEED`/`KEYITEM_FLAGS`, not the version guard) — same class of gap (a GDS-07 cell never revisited after the package that changed it shipped), different cell. | Low | `03-architecture-design-synthesis` (a GDS-07 delta adding `IP-9110`'s own version-bump row, mirroring `IP-9070`'s §7a precedent — could ride the same pass as `BL-0088`/`BL-0089`) | **Triaged (`00-pipeline-manager`, 2026-07-12): `SCHEDULED`**, riding the next `03-architecture-design-synthesis` pass touching GDS-07 — not blocking any in-flight package. | SCHEDULED |
| BL-0091 | 2026-07-12 | doc-defect | `11-release-readiness` run on the Release 2 bundled scope (baseline flip, [release-assessment-release-2-bundled.md](../reviews/release-assessment-release-2-bundled.md)) | **`Claude.md`'s "Known Good Behavior" bulleted list (lines 84-101) still describes the pre-Release-2 fixed-9-zone/9-carrot/3×3-map-screen model in full** — title/movement/collectibles/victory/save/map-screen behavior all narrated as the original bootstrap game, with no mention of `FEAT-9000`'s seed/scale-driven procedural world generation, generated-region screen composition, the maze-shaped adjacency pass, or any of the 15 post-ship remediation fixes. Flagged in place (a note added above the list) rather than fixed inline, since this baseline flip's own scope is trackers/status only. Related to, but broader than, `BL-0050` (map-screen shape specifically) and `BL-0081` (win-condition specifically) — this entry covers the whole section's prose, not just those two mechanics. | Medium — actively misleading onboarding doc for anyone reading it as current, though the pipeline's own authoritative docs (GDS-01, FS-102-108, `01-release-plan.md`) are accurate | `07-implementation-planning` (a doc-refresh package once `BL-0050`'s map-screen redesign and `BL-0081`'s win-condition research are resolved, so the refresh has a stable target to describe rather than needing a second pass) | **Triaged (`00-pipeline-manager` run #119): `DEFERRED`.** Revisit trigger: `BL-0050` (map-screen redesign) and `BL-0081` (win-condition research) both reach a resolved/shipped state — describing the current mechanics accurately needs those two settled first, or the refresh would need a second pass. Medium severity, not blocking any in-flight package; deferring (not scheduling blind) is honest since no target step exists yet. **Half-fired (`00-pipeline-manager` triage, 2026-07-13):** `BL-0081` is fully `DONE`; the win-condition mechanic itself has since shipped and `VERIFIED` (`IP-1021`), giving this refresh a stable target for that half of the section's prose. **Still waiting on `BL-0050` itself** — the map-screen redesign (now unblocked and ripe for `04-requirements-engineering`, see `BL-0050`'s own updated entry) has not yet been specified/shipped, only its own blocker has cleared. Stays `DEFERRED` until `BL-0050` reaches a shipped state too. | DEFERRED |
| BL-0092 | 2026-07-12 | gate | `00-pipeline-manager` (G3 gate for `IP-1081`/`IP-1082`) | **G3 authorization needed for `IP-1081`/`IP-1082`** (`FS-108` rendering half, `BL-0075`'s own resolution) — neither covered by the `BL-0001`…`BL-0005` bootstrap carve-out, both newly planned this session. | Medium (closes Release 2's one remaining known gap, `FEAT-2100`'s partial delivery — not urgent on its own, the release itself was already GO'd with this gap explicitly carried forward) | 08 | **Resolved — user explicitly authorized both** ("Yes, build both (Recommended)," 2026-07-12). `IP-1081`/`IP-1082` flip `NOT STARTED`/`BLOCKED` → authorized, `08-content-authoring` proceeds on `IP-1081` first (critical path). | DONE |
| BL-0093 | 2026-07-12 | design-decision | project owner, direct (resolving `BL-0081`/`R215`) — "Distribute a minimum of 'scale number' of treasures, starting at dead ends before adding the braid, if there are too few dead ends, randomly distribute the remainder, and win conditions are 'collect scale number of treasures'." | **Resolved win-condition redesign for the finite (current, non-infinite) world — implement now.** Generation: place a minimum of `WORLD_SCALE` treasures per generated world (not `WORLD_SCALE²`/region count — a small, scale-proportional target, 2 at `scale=2` up to 9 at `scale=9`), prioritizing the pre-braid spanning tree's own dead-end (leaf) regions first (computable from `GW_MAZE_STATE`'s parent-direction bits at `maze_carve_done`, before `maze_prune`/the braid pass runs — braiding can eliminate leaves by reopening edges, so the decision must snapshot leaf status at that exact pipeline point, not after braiding). If pre-braid dead-end count < `WORLD_SCALE` (confirmed a real, common case at scale ≤7 by [R215](../research/encyclopedia/R215-procgen-win-condition-design.md)'s own measured data — some seeds have zero dead ends at every scale 2–7), randomly distribute the remainder among the other regions. Win condition: collect all `WORLD_SCALE` treasures — replaces `CARROTS_COUNT==9`. This is a real architecture-level change, not a threshold tweak: today every region universally gets exactly one KeyItem (baked into each biome-family screen template, `ZONE_COLLECTS`/`FS-102`) — the new design needs a per-region "does this region have a treasure at all" decision made at generation time, which the current data model has no place for (`KEYITEM_FLAGS` today only distinguishes collected/not-collected, not present/absent). Requires interaction between the previously-independent item-placement pass and the maze-carve pass (`IP-1070`/`ADR-0012`), which the current architecture docs (GDS-04/GDS-07/`ADR-0012`) describe as decoupled. | High (a real, user-directed gameplay redesign — the current win condition is confirmed broken relative to the shipped procgen world, and this is the resolved fix, not exploratory) | 03 (`03-architecture-design-synthesis` — an ADR/GDS delta is needed for the new generation-pass interaction and data-model concept before `04` can derive a clean FR, or `07` can plan a package) | **Filed this run, `00-pipeline-manager` routing to `03-architecture-design-synthesis` next** — not yet actioned. **`03-architecture-design-synthesis` run #123: [ADR-0015](../architecture/adr/ADR-0015-dead-end-anchored-treasure-and-win-condition.md) authored** — records the placement algorithm and win-condition check as a binding decision, corrects `GDS-04`'s "exactly one `KeyItem` per `Region`" rule (superseded, not left stale), adds `GDS-07` §7c naming the new per-region treasure-presence concept without dictating its byte encoding. Remains `SCHEDULED`, next step `04-requirements-engineering` (derive/update the FR this ADR grounds), then `06`→`07`→`08`. **`04-requirements-engineering` run #124: FR-9160/FR-9161 authored** — supersede `FR-9130`/`FR-3300` respectively (both left textually unmodified, gained Notes pointers only, per this project's own coexistence precedent). RTM/Review updated, finding #14 clean. Remains `SCHEDULED`, next step `06-feature-specification` (update `FS-102`'s own KeyItem-placement workflow/AC to match). **`06-feature-specification` run #125: `FS-102` revised** — Workflow A/B, Data Model, AC, Verification Plan all updated for `FR-9160`/`FR-9161`. Two new Open Questions filed (catalog-entry citation lag, routed `05`; per-region encoding, routed `07`), both explicitly non-blocking for implementation-readiness. Remains `SCHEDULED`, next step `07-implementation-planning`. **`07-implementation-planning` run #126: packaged as [IP-1021](../implementation/packages/IP-1021-win-condition-redesign.md)** (`08-code-implementation`, `NOT STARTED`), resolving OQ5. Not authorized. **User authorized (`BL-0096`, "Yes, build it," 2026-07-12).** **`08-code-implementation` run #127: `IP-1021` implemented, `COMPLETE`** — `generate_world`'s new placement pass, `check_complete`'s corrected `WORLD_SCALE` comparison, `worldgen.py` oracle mirror (0 mismatches, full corpus), `KEYITEM_FLAGS` widened to a tri-state (confirmed both real consumers already handle it correctly, no downstream changes needed). Found and fixed a same-package defect during implementation: `st_intro`'s own `KEYITEM_FLAGS` clear ran after `generate_world` and silently destroyed the placement output — relocated to the SEED/SCALE ENTRY confirm handler, before `CALL('generate_world')`. Full suite 233/233 (`T12.e` revised, `T12.n` new). `FR-9130`/`FR-3300` formally superseded in their own Notes. **`09-package-verification` (fresh session, 2026-07-13): `IP-1021` independently confirmed** ([VR-1021](../implementation/verification/VR-1021-win-condition-redesign.md)) — 233/233 re-run, live-driven at non-corpus scales 6/8 via the real UI path, all DoD/checklist items re-derived from the tree. `IP-1021` → `VERIFIED`. **`10-integration-review` (2026-07-13): clean** ([integration-review-ip-1021.md](../reviews/integration-review-ip-1021.md)) — no Critical/High; 2 non-blocking findings filed separately (`BL-0098`/`BL-0099`). **This closes the win-condition redesign end-to-end** — `03`→`04`→`06`→`07`→`08`→`09`→`10` all complete, no further work remains against this entry. | DONE |
| BL-0094 | 2026-07-12 | feature | project owner, direct (same message as `BL-0093`) — "In an infinite game, populate a treasure only at dead ends and save the top 3 high scores of number of treasures collected (BL but deprioritize saving a three character name with each high score but don't implement now)." | **Deferred future work, explicitly not implemented now.** For a hypothetical infinite/streaming-generated world (`BL-0082`, itself unresearched): treasure exists only at maze dead ends (no random-remainder fallback — an unbounded world always has more dead ends ahead as it streams), and the win condition becomes a running high score of treasures collected, top 3 persisted — replacing a fixed completion threshold with score-chasing (a real, precedented convention for unbounded play, per [R215](../research/encyclopedia/R215-procgen-win-condition-design.md)'s Concepts section). **Within this future work, when it's eventually built:** the classic arcade 3-character-initials-entry UI per high score is explicitly deprioritized — track the numeric scores only, skip the name-entry UI, unless a later pass decides it's worth the extra work. | Low (exploratory future scope, hard-blocked on `BL-0082`'s own unresolved streaming-generation feasibility research — not actionable until that lands) | 02 (`02-research-gbc-hardware`/`02-research-tooling-and-testing` — `BL-0082` itself must resolve first; this entry's own scope starts at `03`/`04` only once that's done) | **Filed this run.** `DEFERRED` — revisit trigger: `BL-0082` reaches a research conclusion (the streaming/infinite-generation feasibility question this entire entry depends on). **Research conclusion reached (2026-07-13): [R114](../research/encyclopedia/R114-streaming-world-generation-feasibility.md)/[R216](../research/encyclopedia/R216-infinite-mode-win-condition-design.md) both landed.** This entry's own top-3-high-score/no-name-entry design confirmed genre-correct and well-grounded (R216). One real amendment flagged, not yet decided: `R216` proposes replacing this entry's literal "treasure only at dead ends" rule with a structure-independent `hash(SEED,row,col)`-density rule, to sidestep `R114`'s finding that the cheapest streaming maze algorithm has zero dead ends in two of four directions — a genuine design substitution for `03`/`04` to accept or reject, not applied here. **Re-dispositioned `SCHEDULED` (2026-07-13): the adoption decision has been made.** [ADR-0016](../architecture/adr/ADR-0016-streaming-infinite-mode-generation-architecture.md)/[ADR-0017](../architecture/adr/ADR-0017-infinite-mode-treasure-placement-and-win-condition.md) adopt streaming generation for a new, additive Infinite Mode and adopt this entry's own score-chasing design with the one named amendment (decoupled hash-density treasure placement in place of literal dead-end placement). **`04-requirements-engineering` run (2026-07-13): baselined.** Seven new FRs (`FR-10100`–`FR-10500`) and four new NFRs (`NFR-1400`/`2300`/`4300`/`5400`) formalize this entry's own design (score-chasing win condition as `FR-10400`, decoupled hash-density treasure placement as `FR-10300`, streaming generation/save-format as `FR-10100`/`10200`/`10210`/`10500`) — see [01-functional-requirements.md](../requirements/01-functional-requirements.md) FR-10000 group. The run/session-shape question could not be baselined (genuine unresolved design question, not a requirements-derivation gap) — filed as **CR-07**, not this entry's own scope to carry further (see `BL-0106`'s own updated entry). Treasure-density constant `K` remains an implementation-time tuning value (`FR-10300`'s own Notes), not fixed here. **Re-dispositioned `SCHEDULED`→`SCHEDULED`, riding `05-feature-decomposition`** — the FR/NFR baseline is complete enough to decompose into a Feature; `CR-07`'s own open question does not block that (per `ADR-0017` point 4, the win-condition state is compatible with either run/session-shape answer). **`05-feature-decomposition` run (2026-07-14): decomposed as `FEAT-10000`** (Infinite Mode), joining new **`EP-6000`**, placed in the `Future` release bucket (zero graph-blocking dependencies — every one of its five upstream Features already shipped/`VERIFIED` — but no release commitment made). `FP-05` reviewed clean (finding #9). **This entry's own original ask (a design for the infinite-mode win condition) is now fully realized end-to-end: research (`R216`) → architecture (`ADR-0017`) → requirements (`FR-10300`/`FR-10400`) → feature decomposition (`FEAT-10000`). `DONE`.** | DONE |
| BL-0095 | 2026-07-12 | recommendation | project owner, direct (same message as `BL-0093`) — "with a plan to follow up later as other components come together (BL with a check at major releases if we want to adjust the win conditions)" | **Standing instruction, not a one-time backlog item: revisit the win condition (`BL-0093`'s resolved design, once shipped) at every future `11-release-readiness` pass**, not treat it as permanently fixed. As other components come together (the MAP/status-screen redesign `BL-0050`, any future infinite-mode work `BL-0082`/`BL-0094`, biome-blob clustering `BL-0066`), the win condition may need adjusting again — this entry exists so that check happens deliberately at release checkpoints rather than being forgotten. | Low (process/durability note, not a defect) | 11 (a standing checklist item for every future `11-release-readiness` Release Assessment's own residual-risk sweep, not a one-time fix) | **Filed this run.** `SCHEDULED` as a recurring reminder — never flips `DONE` in the normal sense; re-affirmed (not closed) at each release assessment that considers and re-confirms the win condition still fits. | SCHEDULED |
| BL-0096 | 2026-07-12 | gate | `00-pipeline-manager` (G3 gate for `IP-1021`) | **G3 authorization needed for `IP-1021`** (win-condition redesign, `BL-0093`) — not covered by the `BL-0001`…`BL-0005` bootstrap carve-out, newly planned this session. | High (closes the project owner's own directly-decided gameplay redesign) | 08 | **Resolved — user explicitly authorized** ("Yes, build it (Recommended)," 2026-07-12). `IP-1021` flips `NOT STARTED` → authorized, `08-code-implementation` proceeds. | DONE |
| BL-0097 | 2026-07-13 | finding | `09-package-verification` run ([VR-1081](../implementation/verification/VR-1081-maze-blocked-edge-indicator-content.md)) | **`IP-1081`'s shipped `blocked_up()`/`blocked_down()` tile functions are pixel-for-pixel identical, as are `blocked_left()`/`blocked_right()`** (`tiles.py:829-845`) — the package's own §6 text explicitly called for "4 new pixel-art functions... oriented per direction the same way the arrow tiles are (a directional glyph, not a direction-agnostic icon)," but the shipped art collapses to 2 distinct bitmaps (a vertical bar, a horizontal bar) reused across direction pairs — by contrast `arrow_up()`/`arrow_down()`/`arrow_left()`/`arrow_right()` are genuinely distinct mirror-image shapes. Does not fail `IP-1081`'s own enumerated Definition of Done/Verification Checklist (both are satisfied or explicitly deferred to `09-content-review` per the package's own §13 Risks) — did not block `VERIFIED`. | Medium | 09 (`09-content-review`, once `IP-1082` ships and that review runs against `FS-108`'s full visible art — per the package's own §1 review-scheduling note) | **Trigger fired (`00-pipeline-manager`, 2026-07-13): `IP-1082` reached `VERIFIED`** ([VR-1082](../implementation/verification/VR-1082-maze-blocked-edge-indicator-render.md)) — re-dispositioned `DEFERRED`→`SCHEDULED`, riding the now-ripe `09-content-review` pass on the `IP-1081`/`IP-1082` shipped tile art (rides alongside `BL-0103`'s `10-integration-review` pass on the same package set — either may run first). **`09-content-review` ran 2026-07-16** ([content-review-ip-1081-ip-1082.md](../reviews/content-review-ip-1081-ip-1082.md)) — confirmed by direct live-rendered evidence (screenshots of all four blocked glyphs simultaneously on screen), not just source-level pixel comparison: the up/down pair and left/right pair are genuinely indistinguishable by shape, disambiguated only by fixed screen position. Confirmed **not** a play-fairness or comprehension defect (position fully disambiguates every real region layout) — remains a craft-intent gap. Re-dispositioned `SCHEDULED`, now riding a future `08-content-authoring` remediation package (re-author `blocked_up`/`blocked_left`, or all four, as genuinely distinct directional bitmaps) rather than the now-closed `09-content-review` step. Not `DONE` — the underlying art itself is unchanged; only the review/characterization step is complete. | SCHEDULED |
| BL-0098 | 2026-07-13 | doc-defect | `10-integration-review` run ([integration-review-ip-1021.md](../reviews/integration-review-ip-1021.md) finding #2) | **`docs/feature-planning/03-feature-catalog.md`'s `FEAT-9000` entry is stale relative to `IP-1021`'s shipped win-condition redesign** — "Included Requirements" still cites `FR-9130` (formally superseded by `FR-9160`), and the "Purpose" text still claims "exactly one collectible KeyItem per region," contradicted by the shipped scale-relative, dead-end-prioritized placement. Not newly discovered by this review — `FS-102`'s own forward-reference note and the `FEAT-9000` entry's own metadata block already name this as "OQ4... routed to `05-feature-decomposition`" — but no backlog entry existed to carry it forward across sessions until now. | Low | 05 (`05-feature-decomposition`, per `FS-102`'s own routing — update the catalog entry's Included Requirements and Purpose text to cite `FR-9160`/the scale-relative rule) | SCHEDULED: rides the next `05-feature-decomposition` touch of the Feature Catalog. Not blocking any current work. | SCHEDULED |
| BL-0099 | 2026-07-13 | doc-defect | `10-integration-review` run ([integration-review-ip-1021.md](../reviews/integration-review-ip-1021.md) finding #1) | **`GW_KI_PLACED` (`asm_game.py:136`, `0xC3F5`, `IP-1021`'s own new transient scratch counter) has no row in GDS-07's WRAM table**, breaking this codebase's own established convention that every `generate_world` transient scratch byte (`GW_TOP_ROW`/`GW_REGION_IDX`/`GW_B_SCRATCH`/`GW_SCALE_SQ`/`GW_MAZE_DIR`/`GW_BRAID_IDX`/`GW_MAZE_DRAW_CTR`, all documented) gets a table row. GDS-07's own "Net new WRAM: 85 bytes... entirely inside the `0xC3A0`–`0xC3F4` range" summary line (line 260) is also now inaccurate — should read 86 bytes, `0xC3A0`–`0xC3F5`. Confirmed harmless functionally (well inside bank-0, unconditionally reset every placement-pass run, no collision) — a documentation-completeness gap only. | Medium | 03 (`03-architecture-design-synthesis`, a GDS-07 delta adding the missing `GW_KI_PLACED` row and correcting the range summary) | SCHEDULED: rides the next `03-architecture-design-synthesis` touch of GDS-07. Not blocking any current work — no functional defect, confirmed by this review's own live re-drive and code trace. | SCHEDULED |
| BL-0100 | 2026-07-13 | feature | intake: project owner — "Add a BL to create a menu screen under the select menu that explains the 'maze-blocked edge indicator' symbols." | **Add a legend/help screen, reachable via the SELECT button (alongside/near the existing MAP status screen), that explains what the on-screen edge-indicator tiles mean to the player** — the open-arrow (`TL_ARROW_U/D/L/R`, a valid maze-connected neighbor exists, `FR-2320`) versus the blocked-edge indicator (`TL_BLOCKED_U/D/L/R`, `0x1A`-`0x1D`, a grid-adjacent region exists but the maze doesn't connect to it, `FR-2330`/`IP-1081`/`IP-1082`), versus no indicator at all (a true grid boundary). Today a player has no in-game explanation that these are two distinct signals, not one — `FR-2330`'s own three-state distinction (open / blocked / absent) is entirely undocumented in-game. Framing implies SELECT may need to become a small menu (MAP vs. this new legend screen) rather than jumping straight to MAP as it does today — a UI-shape detail for `04`/`06` to resolve, not decided here. No duplicate found (checked the backlog for "legend"/"help screen"/"symbol"/"tutorial" — none). | Medium (owner-requested; not flagged urgent) | 04 (`04-requirements-engineering` — a new screen/capability with no home in the approved requirements baseline, per this skill's own routing table; not `03`, since it follows the same GAMESTATE-driven screen/menu-cursor pattern `IP-1040`/`IP-9060`'s MAIN MENU already established, no new architecture concept implied) | **Triaged (`00-pipeline-manager`, 2026-07-13): `SCHEDULED`**, riding this run's own `04-requirements-engineering` pass — genuinely unblocked (no architecture prerequisite, no gate), no other open backlog item outranks it as the next actionable step. **`04-requirements-engineering` run (2026-07-13): could not baseline an FR.** Intake's own routing guess ("no new architecture concept implied") was wrong on inspection — `GDS-01` §4's six-state game-state machine names `MAP` as SELECT's *sole* destination, with no node for a legend/help screen; `MAIN MENU`/`SEED SCALE ENTRY` (the precedent intake cited) were themselves added via a `03-architecture-design-synthesis` GDS-01 §4a delta first (`BL-0031`'s own `01→02→03→04` routing), not derived directly at `04`. Filed as **CR-06** (Candidate Requirements, RQ-01, un-baselined) and RQ-03 finding #15 rather than inventing a new `GAMESTATE`/SELECT-menu structure. **Re-dispositioned `SCHEDULED`→`DEFERRED`** — revisit trigger: `03-architecture-design-synthesis` authors the GDS-01 §4/§4a delta (new state, or SELECT-menu redefinition) and a GDS-08 delta (the screen's own layout/content); `04-requirements-engineering` returns to derive the real FR from `CR-06` once that lands. Not urgent, not blocking any current work. **Trigger fired (`03-architecture-design-synthesis`, 2026-07-13, user-confirmed): GDS-01 §4c** (`SELECT` opens a small `MAP`/`LEGEND` cursor menu, reusing `MAIN MENU`'s own established pattern; `MAP` itself entirely unchanged, so `BL-0050`'s own future redesign stays independent) **and GDS-08 §11** (the `LEGEND` screen's static three-row content — open-arrow tile / blocked-bar tile / blank, each beside a plain-language label — reusing existing `TL_ARROW_U`/`TL_BLOCKED_U` tiles and palette 2, zero new art/palette cost) **both authored.** Named tradeoff recorded explicitly (one extra button press to reach `MAP`, accepted per `MAIN MENU`'s own no-complaint precedent). Re-dispositioned **`DEFERRED`→`SCHEDULED`**, riding a future `04-requirements-engineering` pass to derive `CR-06`'s real FR from this delta. **`04-requirements-engineering` (2026-07-13, second pass): baselined as FR-1200** (SELECT MENU state, supersedes FR-1150's own SELECT→MAP clause) **and FR-1210** (LEGEND state itself) — both target, not yet implemented. RTM rows added, RQ-03 finding #16 (clean), `CR-06` closed pointing forward to the two new FRs. Remains **`SCHEDULED`**, riding a future `05-feature-decomposition` pass next (this Feature has no `FEAT-xxxx` catalog entry yet). **`05-feature-decomposition` (2026-07-13): decomposed as `FEAT-1200`** (SELECT Menu & Edge-Indicator Legend Screen) — joins `EP-1000`, Release 2 as a second/independent addendum (immediately buildable, not serialized behind `FEAT-2100`'s own in-flight render branch). Dependency graph updated (no cycles); Feature Review finding #8 (clean). Remains **`SCHEDULED`**, riding a future `06-feature-specification` pass next (author `FS-xxx` for `FEAT-1200`). **`06-feature-specification` (2026-07-13): specified as `FS-109`** — full 20-field spec, 3 Open Questions (WRAM cursor byte, new `GAMESTATE` values, SELECT-while-already-in-menu behavior), all deliberately routed to `07-implementation-planning` to resolve as implementation choices. Remains **`SCHEDULED`**, riding a future `07-implementation-planning` pass next. **`07-implementation-planning` (2026-07-13): packaged as `IP-1090`** — single package (`08-code-implementation` peer), all 3 Open Questions resolved (`GS_SELECT_MENU`/`GS_LEGEND` = 8/9; `MM_CURSOR`/`MM_JUST_ENTERED` reused rather than new WRAM bytes; SELECT resolved as a no-op inside the two new states). Supersession sweep found and recorded three existing `test_rom.py` sites (`T4.6`/`T8.11`/`T14.e2`) needing a corrective `A`-press insertion for the new two-hop SELECT path. All dependencies (`IP-1040`/`IP-1030`/`IP-1081`) `VERIFIED` — package is `READY`. Remained `SCHEDULED`, riding `08-code-implementation` once authorized. **`08-code-implementation` (2026-07-13, user G3 "Yes"): implemented.** `GS_SELECT_MENU`/`GS_LEGEND` = 8/9 added; `handle_play_input`'s SELECT branch retargeted (`st_map` itself confirmed byte-for-byte unchanged); `st_select_menu`/`st_legend` state handlers, `sm_on_entry`/`draw_select_menu_cursor` (reusing `MM_CURSOR`/`MM_JUST_ENTERED`), two new static screens — zero new tile art/palette entries. New suite **T21** (12 checks); corrected `T4.6`/`T8.11`/`T14.e2` for the new two-hop path. Full suite 246/246, ROM 25544/32768 bytes. Independently re-driven via PyBoy screenshot — SELECT MENU cursor and LEGEND content both confirmed rendering live. GDS-07 backfilled (`MM_SAVE_VALID`/`MM_CURSOR` rows); `FR-1200`/`FR-1210` marked Implemented. `IP-1090` → `COMPLETE`. **`09-package-verification` (2026-07-13, fresh session): independently verified as `VR-1090`.** 246/246 re-confirmed (fresh PyBoy+Pillow install), every DoD/checklist item confirmed by direct code read, `st_map` reconfirmed byte-for-byte unchanged, live PyBoy screenshot walkthrough confirms SELECT MENU/LEGEND render exactly as GDS-08 §11 specifies. `IP-1090` → `VERIFIED`. No findings. **Closes `BL-0100` in full.** | DONE |
| BL-0101 | 2026-07-13 | doc-defect | `00-pipeline-manager` run (reconciliation, this run) | **`docs/implementation/00-master-build-plan.md`'s own top-of-file `> Status:` summary line (lines 3-6) is stale relative to this session's own verification/implementation work** — still reads "22 of 25 packages VERIFIED... `IP-1081`... `COMPLETE` same day, awaiting verification; `IP-1082` (render) `BLOCKED` on it; `IP-1021`... `COMPLETE`... awaiting verification." The real current state (confirmed against the file's own per-package status rows, which are accurate): 24 of 25 `VERIFIED`; `IP-1081` `VERIFIED` ([VR-1081](../implementation/verification/VR-1081-maze-blocked-edge-indicator-content.md)); `IP-1082` `COMPLETE` (not `BLOCKED` — `IP-1081` reaching `VERIFIED` already unblocked it, and it was implemented the same session), awaiting its own fresh-session verification; `IP-1021` `VERIFIED` ([VR-1021](../implementation/verification/VR-1021-win-condition-redesign.md)). Only this one summary header drifted — every per-package row is accurate. | Low (cosmetic; no ledger a downstream stage actually reads from is wrong, only the human-facing summary line) | 08/09 (whichever stage next touches this file — a natural one-line fold-in, not worth a standalone pass) | **Resolved (run #141, `08-code-implementation`): corrected as a natural byproduct of `IP-1090`'s own §9 Documentation Updates touch of this file** — header now reads "24 of 26 packages VERIFIED; `IP-1082`... `COMPLETE`... awaiting independent verification; `IP-1090`...", matching every per-package row. | DONE |
| BL-0102 | 2026-07-13 | doc-defect | `05-feature-decomposition` run (FP-05 requirement-assignment tally, this delta) | **`FR-9160`/`FR-9161` (baselined 2026-07-12 for `ADR-0015`'s win-condition redesign) were never added to `docs/feature-planning/05-feature-review.md`'s own requirement-assignment tally.** They supersede `FR-9130`/`FR-3300` in place within the already-catalogued `FEAT-9000` (a requirements-level supersession, not a new Feature), so `05-feature-decomposition` was never re-invoked for them — but the tally itself (last totaled at 56, `ADR-0012`'s delta) was never updated to reflect that these 2 new FR IDs now also exist and are (correctly) owned by `FEAT-9000`. Found while adding this delta's own tally entry (58, for `FR-1200`/`FR-1210`) and confirming no overlap against every prior list — not a double-assignment risk (both IDs are unambiguously `FEAT-9000`'s), purely a bookkeeping gap in the tally's own running total. | Low (cosmetic; no actual mis-assignment, the tally total is just stale by 2) | 05 (`05-feature-decomposition`, a one-line tally addition the next time this document is touched) | SCHEDULED: rides the next `05-feature-decomposition` pass that touches `FP-05` — add "FEAT-9000: FR-9160, FR-9161 (2)" to the `ADR-0012`-delta-adjacent tally section and bump the running total by 2. Not blocking any current work. **Closed (2026-07-14): folded in during the Infinite Mode `05-feature-decomposition` pass that touched `FP-05` anyway** — `FR-9160`/`FR-9161` added to the running tally (58→60), `BL-0102` cited directly in the fold-in note per this entry's own instruction. | DONE |
| BL-0103 | 2026-07-13 | recommendation | `09-package-verification` run ([VR-1082](../implementation/verification/VR-1082-maze-blocked-edge-indicator-render.md)) | **The `IP-1081`/`IP-1082` maze-blocked edge indicator package set (`FS-108`/`FEAT-2100`/`BL-0075`) has never been through `10-integration-review`**, unlike every other closed tranche this session (`IP-1021` standalone-reviewed run #131; bootstrap/Release-2/remediation/maze-adjacency/movement-UI tranches all reviewed earlier). Both packages are now independently `VERIFIED` ([VR-1081](../implementation/verification/VR-1081-maze-blocked-edge-indicator-content.md), [VR-1082](../implementation/verification/VR-1082-maze-blocked-edge-indicator-render.md)), so the set is eligible — the review should check the same class of cross-package seams prior reviews caught elsewhere (e.g. `BL-0098`/`BL-0099` from `IP-1021`'s own review): `IP-1081`'s tile-index allocation (`0x1A`-`0x1D`) against every other tile consumer, `IP-1082`'s `_arrow_write`/`ARROW_ADDR_*` reuse against `IP-1090`'s own unrelated but adjacent `asm_game.py` touch, and GDS-07/RTM coherence across the set. | Low (no known defect — routine tranche-closure step, not a symptom of anything broken) | 10 (`10-integration-review` on the `IP-1081`/`IP-1082` package set) | **`10-integration-review` run (2026-07-13): clean, no findings.** [integration-review-ip-1081-ip-1082.md](../reviews/integration-review-ip-1081-ip-1082.md) confirmed interface consistency (including `IP-1090`'s legend screen, a consumer that post-dated both packages' own verification), invariant sweep, behavioral coherence (`T20`/`T21` full-corpus checks), and traceability/documentation coherence all clean at commit `b6e5f78`. Full gates re-run: 246/246. `BL-0097` (`09-content-review`) remains separately owed, unaffected by this closure. | DONE |
| BL-0104 | 2026-07-13 | research-gap | user request (this session): "add a skill or skills to the pipeline to handle refactoring of code and documentation… use the pipeline skill to add research to encyclopedia where required" | **The pipeline gained a refactoring stage (`08-refactoring`, `IP-8xx0` packages, explicit `00-pipeline-manager` scheduling conditions — infrastructure commit `f7172dc`, outside stage scope) with no encyclopedia grounding for its equivalence discipline** — what proves a code refactor behavior-preserving in this toolchain, and what the analogous meaning-preservation invariants are for the `docs/` tree. Resolved by [R307](../research/encyclopedia/R307-refactoring-practices.md): byte-identical ROM as golden master (build proven deterministic by local experiment — two builds under different `PYTHONHASHSEED` values, same SHA-256), enumerated-predicted-deltas fallback, characterization-suite invariants, doc-tree link/ID/status-token invariants + migration maps. | Medium (user-directed) | 02 (`02-research-tooling-and-testing`) | Rode this run (#144, `override`) — filed and resolved in the same run per the `BL-0082`→`R114`/`R216` precedent. | DONE |
| BL-0105 | 2026-07-13 | finding | `02-research-tooling-and-testing` run #144 ([R307](../research/encyclopedia/R307-refactoring-practices.md)) | **Five of R307's external citations could not be fetch-verified** (refactoring.com, Feathers 2004, reproducible-builds.org "Definition", Write the Docs "Docs as Code", docs.python.org dict-ordering guarantee) — this session's network policy returns HTTP 403 for those hosts (proxy status checked, policy-level denial). All five are canonical, well-established sources cited by edition/ISBN, and R307's load-bearing claim (build determinism) rests on the recorded local experiment, not on them — but the tier's methodology requires the "needs fetch-verification" flags to be cleared. | Low | 02 (`02-research-tooling-and-testing`) | DEFERRED: revisit trigger — the next R300-tier research run that executes in an environment whose network policy permits fetching those hosts (or the user grants broader network access); clear the five flags in R307 then. | DEFERRED |
| BL-0106 | 2026-07-13 | design-question | `03-architecture-design-synthesis` run ([ADS-001](../architecture/ADS-001-streaming-infinite-world-generation.md) Open Question 1) | **Infinite Mode's run/session shape is undecided**: is a playthrough indefinitely resumable (matching the finite game's existing save/continue convention), or is it its own bounded "run" needing a new end-condition mechanic (death/retreat/checkpoint) this game does not currently have? `R216` surfaces this explicitly without resolving it — genuine new mechanic-design scope, not a generation-architecture question. | Medium (blocks finalizing Infinite Mode's win-condition `FR`s, but blocks no in-flight package) | 04 (`04-requirements-engineering`, alongside `BL-0094`) | **`04-requirements-engineering` run (2026-07-13): attempted, found a genuine gap, correctly did not invent an answer.** `R216`/`ADR-0017` (point 4) both explicitly decline to decide this question; per this stage's own SHALL-NOT-invent-requirements rule, filed as **CR-07** in [01-functional-requirements.md](../requirements/01-functional-requirements.md) rather than baselined (RQ-03 finding #17). `FR-10400`'s win-condition state (running count, top-3 table) confirmed compatible with either answer — no rework risk from leaving this open. **Re-dispositioned `SCHEDULED`→`DEFERRED`** — revisit trigger: `05-feature-decomposition`/`06-feature-specification` reaching the Infinite Mode epic (RQ-03 finding #17's own recommendation: surface to the user directly, `NEEDS-USER`, once that point is reached — not urgent before then). **Trigger fired early (2026-07-13, same session): the project owner answered directly — "for now assume indefinitely resumable."** Baselined as **FR-10600** (Indefinitely resumable Infinite Mode run), RQ-03 finding #17 updated to record the resolution, `CR-07` closed. Explicitly a "for now" decision, not permanently closed — carried into `FR-10600`'s own Notes verbatim. **This entry's own question is fully answered — `DONE`.** | DONE |
| BL-0107 | 2026-07-13 | design-question | `03-architecture-design-synthesis` run ([ADS-001](../architecture/ADS-001-streaming-infinite-world-generation.md) Open Question 2) | **Does the Binary Tree maze algorithm's directional (NW-corridor) bias read as acceptable "maze feel" once actually played?** `ADR-0016` adopts Binary Tree for Infinite Mode specifically because decoupling treasure placement (`ADR-0017`) makes the bias irrelevant to the win condition — but the bias may still be aesthetically noticeable. Growing Tree remains a named fallback if not, at the cost of designing a canonical coordinate-driven enumeration order for revisit-consistency (not designed anywhere yet). | Low (cosmetic/aesthetic; not resolvable before Infinite Mode is actually implemented and playable) | 02 (`02-research-game-design`) / 09 (`09-content-review`) | DEFERRED — revisit trigger: the first Infinite Mode implementation package (post `BL-0094`'s `04`→`08` path) reaches a playable/screenshottable state. Cannot be judged from documentation alone. | DEFERRED |
| BL-0108 | 2026-07-13 | recommendation | `03-architecture-design-synthesis` run ([ADS-001](../architecture/ADS-001-streaming-infinite-world-generation.md) Open Question 3) | **The visited-region ledger's real SRAM capacity (how many distinct visited regions a save can remember) is not sized.** `ADR-0016` establishes the ledger *concept* (persist player position + which visited regions have had treasure collected, not the region graph itself) but not its byte-level capacity against real SRAM budget. `R106`'s existing SRAM/battery-save grounding is the starting point. | Medium (a real implementation-time constraint on how "resumable" an Infinite Mode save actually is, but blocks nothing until `BL-0094` reaches `07`) | 02 (`02-research-gbc-hardware`) / 07 (`07-implementation-planning`) | SCHEDULED: rides the `07-implementation-planning` pass for whichever `IP-xxxx` implements Infinite Mode's save format (post `BL-0094`'s `04`→`06` path); a dedicated `02-research-gbc-hardware` pass first is optional, at the pipeline manager's discretion when that point is reached. | SCHEDULED |
| BL-0109 | 2026-07-13 | recommendation | `03-architecture-design-synthesis` run ([ADS-001](../architecture/ADS-001-streaming-infinite-world-generation.md) Open Question 4) | **Whether a single region's materialization cost (one hash-reseed-and-clamp computation) fits inside `check_zone_transition`'s existing safe timing window, without a new LCD-off-style bracket or player-visible stall, is not confirmed** — `R114` flags this as likely affordable but explicitly unmeasured. Needs direct cycle-counting against real `check_zone_transition` call context. | Medium (a real technical risk to Infinite Mode's feel — a stutter on every region transition would be a serious defect — but not resolvable before implementation exists to measure) | 07 (`07-implementation-planning`) / 08 (`08-code-implementation`) | **Measured (`IP-1102`/`T24.e`, 2026-07-14): 78,860–81,792 T-cycles, exceeds the 70,224-cycle single-frame budget.** This entry's own ask (get the real measurement) is answered — `R114`'s "likely affordable" judgment did not hold. The confirmed-real stall risk is carried forward as a new entry, `BL-0118` — this one closes. | DONE |
| BL-0110 | 2026-07-14 | recommendation | `03-architecture-design-synthesis` run ([ADR-0018](../architecture/adr/ADR-0018-finite-mode-biome-blob-clustering.md) Consequences) | **The finite-mode biome-blob super-cell size is not fixed by `ADR-0018`** — `WORLD_SCALE` ranges 2–9 (4–81 regions), and a fixed super-cell size tuned for a large streaming extent would span or exceed the entire grid at small finite scales (e.g. `scale=2`'s 2×2 grid), collapsing to a single blob covering the whole world. Not necessarily wrong, but a real design choice `07-implementation-planning` should make deliberately — e.g. sizing the super-cell relative to `WORLD_SCALE` so at least 2–3 distinct blobs exist at any supported scale, rather than a fixed constant. | Low (a tuning/UX question, not a correctness risk — blocks nothing until the finite-mode blob FR is implemented) | 07 (`07-implementation-planning`) | SCHEDULED: rides the `07-implementation-planning` pass that packages the finite-mode blob-clustering FR (once `04-requirements-engineering` derives it from `ADR-0018`, per `BL-0066`'s own updated entry) — not before. | SCHEDULED |
| BL-0111 | 2026-07-14 | doc-defect | `06-feature-specification` run ([FS-110](../features/FS-110-infinite-mode.md) Open Question 1) | **`FEAT-10000`'s own catalog Dependencies field may be missing `FEAT-4100`.** `FS-110` found that rendering a materialized Infinite Mode region requires `FEAT-4100`'s existing biome-family screen-composition dispatch (the same one the finite mode's `REGION_GRAPH`-sourced biome-id already selects through), but `FEAT-10000`'s own catalog entry lists only `FEAT-1000`/`1100`/`3000`/`5000`/`9000` as Dependencies — `FEAT-4100` is absent. Not fixed by `06-feature-specification` itself (that skill's own SHALL-NOT-modify-the-catalog-entry rule). | Low-Medium (a real, correctly-surfaced gap in the dependency graph — not a defect in what's cataloged so far, since nothing has been implemented yet to actually break) | 05 (`05-feature-decomposition`, a Dependencies-field correction to `FEAT-10000`'s own catalog entry plus the corresponding `FP-04` graph edge) | **Resolved (run #153, `05-feature-decomposition`):** `FEAT-4100` added to `FEAT-10000`'s Dependencies, `FEAT-10000` added to `FEAT-4100`'s Dependent Features, `FP-04`'s edge/summary-row/critical-path note updated, `FP-05` finding #10 recorded. Documentation correction only — `FEAT-4100` already shipped/`VERIFIED`, no build-order risk, requirement tally unaffected. | DONE |
| BL-0112 | 2026-07-14 | design-question | `06-feature-specification` run ([FS-110](../features/FS-110-infinite-mode.md) Open Question 3) | **The run-end trigger for Infinite Mode's top-3 high-score comparison is not decided.** `CR-07`/`FR-10600` resolved *whether* a forced end-condition mechanic exists (no — "for now assume indefinitely resumable") but not *when* the running treasure count is compared against the persisted top-3 table, now that a run can span indefinitely many sessions. `FS-110` surfaces one reasoned candidate without deciding it: starting a new Infinite Mode game necessarily abandons whatever run preceded it (single save-slot precedent, `ADR-0006`) — that moment could be the natural "run end" trigger. | Medium (blocks `FS-110`'s own AC-4/win-condition test from being fully driveable, but blocks no in-flight package) | 04 (`04-requirements-engineering`, a delta to `FR-10400`'s own Preconditions) | SCHEDULED: rides a future `04-requirements-engineering` pass on `FEAT-10000`, or a direct user decision if the pipeline manager judges it ripe sooner (mirroring `CR-07`'s own resolution path — the user answered that one unprompted, in the very next turn after it was filed). Not urgent, not blocking any in-flight package. | SCHEDULED |
| BL-0113 | 2026-07-14 | design-question | `06-feature-specification` run ([FS-110](../features/FS-110-infinite-mode.md) Open Question 6) | **Infinite Mode's mode-selection UI shape has no `GDS-01` delta authored yet** — whether new-game mode choice (Finite vs. Infinite) is a new menu step, a repurposed SEED/SCALE ENTRY screen, or another shape, and whether it implies a new `GameState` value, is undecided. The same class of gap `CR-06` (edge-indicator legend screen) named before its own `GDS-01` §4c delta landed. | Low-Medium (a real, cleanly-scoped gap — blocks full `06`/`07` readiness for `FR-10100`'s own UI half, not the rest of the Feature) | 03 (`03-architecture-design-synthesis`, a `GDS-01` delta) | **Resolved (run #152, `03-architecture-design-synthesis`):** [GDS-01 §4d](../architecture/01-concept-of-play.md) — `MAIN MENU`'s "new game" now opens a `MODE SELECT` cursor menu (finite/infinite); `finite` reuses `SEED/SCALE ENTRY` completely unchanged (protects `IP-1040`'s shipped B-cancel target), `infinite` adds a new seed-only `INFINITE SEED ENTRY` state. Target state only — `04-requirements-engineering` derives the real FR next. | DONE |
| BL-0114 | 2026-07-14 | recommendation | `06-feature-specification` run ([FS-110](../features/FS-110-infinite-mode.md) Open Question 8) | **Infinite Mode's materialized-window radius and its exact per-region WRAM byte cost are not fixed** (`NFR-4300`) — R114 estimates a 5×5 or 7×7 region-neighborhood window "stays comfortably inside bank-0... without banking at all," but the exact radius and per-region byte cost (biome-id, connectivity nibble, treasure-collected state) are not decided by any binding artifact. Distinct from `BL-0110`'s own finite-mode super-cell-sizing question (a different Feature, a different NFR). | Low (a tuning/sizing question, not a correctness risk — blocks nothing until an Infinite Mode implementation package exists) | 07 (`07-implementation-planning`) | **Resolved (run #154, `07-implementation-planning`, [IP-1102](../implementation/packages/IP-1102-infinite-mode-streaming-window-and-render.md)):** 3×3 window (not R114's speculative 5×5/7×7 ceiling), sized to the actual Binary Tree connectivity dependency (self + south/east neighbor), 1 byte/region (3-bit biome + 4-bit connectivity) — 13 bytes total, trivial against the confirmed ~3.1 KiB bank-0 headroom. | DONE |
| BL-0115 | 2026-07-14 | doc-defect | `09-package-verification` run ([VR-1101](../implementation/verification/VR-1101-infinite-mode-region-materialization.md)) | **`IP-1101` §4 (Architecture Components) misattributes `ADR-0016` point numbers** — cites a nonexistent "point 8" for the oracle-lockstep discipline and "point 5" for the Binary Tree maze, when the real content lives at different point numbers in `ADR-0016` (Binary Tree content is actually point 4, not 5). Does not affect the correctness of what was built — `T22.b`'s oracle-parity check confirms the actual lockstep discipline holds regardless of the citation's own accuracy. | Low (citation-only, no functional impact) | 07 (`07-implementation-planning`, a citation correction to `IP-1101`'s own §4) | SCHEDULED: rides the next `07-implementation-planning` touch of `IP-1101`'s own document, or a dedicated small citation-fix pass whenever convenient. Not blocking any in-flight package. | SCHEDULED |
| BL-0116 | 2026-07-14 | doc-defect | `09-package-verification` run ([VR-1101](../implementation/verification/VR-1101-infinite-mode-region-materialization.md)) | **`IP-1101` §9 (Documentation Updates) committed to marking `FS-110` §19 Open Questions 2 and 4 as Resolved, but this was not done** — only `FS-110`'s own top forward-reference pointer was updated during implementation, not the §19 body text itself. A real, small documentation-completeness gap, not a functional defect (`K=16`/no-spawn-special-case are both confirmed correctly implemented and tested — `T22.d`/`T22.g` — only the paper trail marking the Open Questions closed is missing). | Low (documentation completeness only) | 08 (`08-code-implementation`, a follow-up doc-only touch to `FS-110` §19, or `07-implementation-planning` if routed there first) | **Fixed (`IP-1102` run, 2026-07-14):** `FS-110` §19 Open Questions 2 and 4 marked Resolved with a pointer to `IP-1101`, one `08-code-implementation` cycle late (rode `IP-1102`'s own doc-update pass instead of `IP-1101`'s). | DONE |
| BL-0117 | 2026-07-14 | doc-defect | `09-package-verification` run ([VR-1101](../implementation/verification/VR-1101-infinite-mode-region-materialization.md)) | **`docs/architecture/07-data-model.md` §7d states "eight new bytes" for the `INF_MZ_*` WRAM range (`0xC40D`–`0xC418`), but the range is actually 12 bytes** (2+2+1+1+1+1+2+2 per the table immediately below the prose, which is itself correct — only the summary prose sentence undercounts). Cosmetic — the address table and the actual shipped code (`asm_game.py`) both agree on the real 12-byte extent; only the one summary sentence is wrong. | Low (cosmetic, self-correcting via the adjacent table) | 03 (`03-architecture-design-synthesis`, a one-sentence correction to `GDS-07` §7d) | SCHEDULED: rides the next `03-architecture-design-synthesis` touch of `GDS-07`, or a dedicated small fix whenever convenient. Not blocking any in-flight package. | SCHEDULED |
| BL-0118 | 2026-07-14 | finding | `08-code-implementation` run (`IP-1102`, `T24.e` — NFR-1400 direct cycle-count) | **`inf_ensure_window`'s real per-transition cost (all 9 materialized-window cells recomputed fresh, no incremental shift logic per `IP-1102`'s own §6 design) measures 78,860–81,792 T-cycles, exceeding a single CGB-single-speed frame's 70,224-cycle budget by ~12–16%** — a confirmed, real, honestly-measured stall risk (`NFR-1400`, `NOT MET`), not the "small constant amount of work... almost certainly well under `generate_world`'s existing per-region cost" `R114` originally judged. Root cause identified, not merely observed: `gw_prng_step` (the shift-heavy xorshift PRNG, 13 calls per `inf_materialize_region` — 3 reseeds × ~2-3 draws each, roughly 500-700 cycles/call) compounds across the 9 window cells. Confirms `BL-0109`'s own carried-forward risk was real. | Medium-High (an observable ~1-frame hitch on every Infinite Mode region transition — not a crash, data-loss, or unplayable stall, so not an MVP blocker per `IP-1102`'s own explicit Definition of Done and Risks framing, but a real, felt UX defect once a player is actually navigating) | 07 (`07-implementation-planning`, a follow-up optimization package — candidates not decided here: incremental window-shift instead of full 9-cell recompute per transition, since up to 6 of 9 cells are already resident after a single-axis move; a cheaper per-region PRNG; or a deliberate, reasoned decision to accept the measured cost as within tolerance) | SCHEDULED: rides a future `07-implementation-planning` pass once the current Infinite Mode MVP tranche (`IP-1100`/`1103`/`1104`) reaches its own milestone — not blocking any of them, since none of their own scope depends on this routine's timing. Not urgent enough to interrupt the in-flight tranche; not silently dropped either. | SCHEDULED |
| BL-0119 | 2026-07-16 | finding | `08-code-implementation` run (`IP-1103`, dependency-seam read of [IP-1104](../implementation/packages/IP-1104-infinite-mode-ledger-save-persistence.md) §6 vs. [FS-110](../features/FS-110-infinite-mode.md) Workflow C/D + §7 edge case) | **`IP-1104`'s package text covers the ledger's load-path restore of `INF_TREASURE_HERE` but never names the mid-session re-entry path** — `inf_ensure_window`'s ordinary transition-triggered cache re-derivation (the write `IP-1103` added at the center cell) consults only the presence predicate, so after `IP-1104` ships *as currently written*, a collected treasure still respawns when the player leaves the materialized window and walks back **within one session** (out-and-back farming of a single region), violating `FS-110` §7's own "re-approaching a previously-visited, treasure-collected region… with the treasure reading as collected" edge case everywhere except across a save/load boundary. The fix is small and belongs in `IP-1104`'s own scope (its `inf_ledger_mark_collected` already maintains exactly the needed data): the cache re-derivation must also cross-reference the ledger's collected-flag, the same bounded lookup its load path already specifies. | Medium-High (a real farming exploit + spec-edge-case violation if `IP-1104` executes as written; blocks nothing until `IP-1104` runs) | 07 (`07-implementation-planning`, an `IP-1104` §6 amendment — before `08-code-implementation` picks it up) | **Resolved (run #163):** `07-implementation-planning` amended `IP-1104` §2/§5/§6/§7/§8/§9/§10/§11/§13 — a new 642-byte WRAM ledger working copy (`LEDGER_COUNT`/`LEDGER_CURSOR`/`LEDGER`, `0xC419`–`0xC69A`) that `inf_ensure_window` now consults on every materialization (new-game entry, ordinary navigation, post-load restore alike), not only at save/load; new check `T27.g` exercises the in-session case directly. `IP-1104` flips `NOT STARTED`→`READY`. | DONE |
| BL-0120 | 2026-07-16 | doc-defect | `09-package-verification` run ([VR-1100](../implementation/verification/VR-1100-infinite-mode-mode-selection.md)) | **`IP-1100` §5/§6 (Interfaces / Files to Modify) still describe `INFINITE SEED ENTRY`'s A-confirm as calling "`IP-1101`'s per-region materialization routine... for the starting region `(0,0)`"** — the package document was never amended to state the shipped deviation (a call to `IP-1102`'s `inf_ensure_window`, which populates the full 3×3 window via nine internal calls to `IP-1101`'s routine, instead of one direct call). The deviation is real, correct, and better than the original plan (avoids an uninitialized-`INF_WINDOW` read) — and it *is* documented, just not in the package file itself (a code comment and the journal's run #160 narrative both state it). Same class as `VR-1101`/`VR-1102`'s own citation findings this tranche — a recurring, low-stakes pattern across the `IP-110x` series worth one future sweep. | Low (documentation accuracy only; behavior verified correct independently) | 07 (`07-implementation-planning`, amend `IP-1100` §5/§6 in place next time this package is touched — bundle with the `ADR-0016` citation sweep `VR-1102`'s own Finding 1 already recommended) | **SCHEDULED:** rides a future citation/text-accuracy sweep across the `IP-110x` package series (not urgent enough alone). | SCHEDULED |
| BL-0121 | 2026-07-16 | doc-defect | `09-package-verification` run ([VR-1100](../implementation/verification/VR-1100-infinite-mode-mode-selection.md)) | **`IP-1100`'s own DoD/Master-Build-Plan/`packages/INDEX.md`/journal/`FS-110` text all describe the new suite as "`T25`, 10 checks," but the actual `test_rom.py` has 19 individual `check("T25...` assertions** — the package's own §8 names 7 lettered scenario groups (a/b1/b2/c1/d/e1/f), several of which the implementation correctly split into multiple sub-assertions (e.g. `d` → `d1`/`d1b`/`d2a`/`d2b`/`d2c`, 5 checks). Every named scenario is covered — this is a stale summary count, not a coverage gap (cross-checked against `T22`/`T24`, whose own "N checks" claims *do* match their literal `check()` counts exactly, confirming this is an isolated miscount for `T25` specifically). | Low (cosmetic — a stale number in four documents, no missing coverage) | 07 (`07-implementation-planning`/manager, a one-line correction to `IP-1100`'s own text and the derived MBP/INDEX/journal/FS-110 mentions, whenever next touched) | **SCHEDULED:** rides the same future `IP-110x` text-accuracy sweep as `BL-0120` (not urgent enough alone). | SCHEDULED |
| BL-0122 | 2026-07-16 | doc-defect | `09-package-verification` run ([VR-1103](../implementation/verification/VR-1103-infinite-mode-treasure-and-win-condition.md)) | **`docs/features/FS-110-infinite-mode.md`'s own header block still lists `IP-1100` as `COMPLETE 2026-07-14`** (not `VERIFIED`) — stale since `VR-1100` (2026-07-16) flipped it; `VR-1100`'s own commit did not touch `FS-110` (confirmed via `git show --stat`), and `09-package-verification`'s own edit scope doesn't include amending `FS-110`. Same low-stakes class as the other `IP-110x` package-text/ledger-drift findings this tranche has repeatedly surfaced (`BL-0120`/`BL-0121`, `BL-0115`/`BL-0117`). | Low (documentation currency only; the authoritative status lives on the Master Build Plan and `packages/INDEX.md`, both accurate) | 07 (`07-implementation-planning`/manager, a one-line `FS-110` header refresh whenever next touched) | **Resolved (run #164, incidentally):** fixed while `08-code-implementation` touched `FS-110`'s own header for `IP-1104`'s required metadata update (§9) — `IP-1100`'s status line corrected `COMPLETE`→`VERIFIED` in the same edit, not deferred to a dedicated sweep. | DONE |
| BL-0123 | 2026-07-16 | finding | `08-code-implementation` run (`IP-1104`, own Implementation Tasks/Risks) | **`try_load_save`'s existing finite-mode restore path (including its `CALL('generate_world')`) now runs unconditionally even when loading an Infinite Mode (`GAME_MODE == 1`) save** — `generate_world` builds a whole `REGION_GRAPH` array for a bounded `scale x scale` grid, a concept Infinite Mode never reads (it uses `inf_materialize_region`/`inf_ensure_window` instead). This is a deliberate, named scope choice by `IP-1104` itself (§6: "deliberately not gated on `GAME_MODE` — this package's own scope is additive, not a restructuring of already-`VERIFIED` finite-mode code"), not an oversight — the wasted cycles are a few hundred T-states, once per load, not a correctness issue (the resulting `REGION_GRAPH` is simply never read by any Infinite Mode code path). | Low (a small, one-time-per-load inefficiency; no functional impact, no player-visible effect) | 07 (`07-implementation-planning`, an optional future optimization package gating `try_load_save`'s finite-mode restore body on `GAME_MODE == 0` — only worth doing if a future `NFR-1400`-style budget audit ever finds load-time cost material) | **DEFERRED:** no concrete cost measured yet and no player-visible symptom; revisit only if a future timing audit specifically flags load-time cost, or if `BL-0118`'s own optimization pass finds it convenient to bundle. | DEFERRED |
| BL-0124 | 2026-07-16 | doc-defect | `09-package-verification` run ([VR-1104](../implementation/verification/VR-1104-infinite-mode-ledger-save-persistence.md)) | **Every ledger describing `T27` (`00-master-build-plan.md`, `packages/INDEX.md`, `01-functional-requirements.md`, `FS-110-infinite-mode.md`, the pipeline journal) states "7 checks"** — the package's own §8 names 7 lettered scenarios (a-g), but the shipped suite correctly splits several into multiple `check()` assertions (`a`→`a1/a2/a3`, `c`→`c1/c2/c3/c4`, `d`→`d1/d2`). Direct count confirms 13 actual `check()` calls, not 7. Every named scenario is present and covered — a stale summary count, not a coverage gap, the identical pattern `VR-1100` found for `T25` ("10 checks" vs. actual 19). | Low (cosmetic — a stale number in five documents, no missing coverage) | 07 (`07-implementation-planning`/manager, a one-line correction whenever next touched) | **SCHEDULED:** rides the same future `IP-110x` text-accuracy sweep as `BL-0115`/`BL-0117`/`BL-0120`/`BL-0121` (now five instances). | SCHEDULED |
| BL-0125 | 2026-07-16 | doc-defect | `09-package-verification` run ([VR-1104](../implementation/verification/VR-1104-infinite-mode-ledger-save-persistence.md)) | **`IP-1104` §9's own text names `docs/requirements/01-functional-requirements.md` as the location for the "NFR-5400 status → Met" update** — `NFR-5400` is a non-functional requirement and correctly lives in, and was correctly updated in, `docs/requirements/02-non-functional-requirements.md`; the implementation did the right thing despite the package's own §9 prose naming the wrong file. Not a scope excursion — a citation slip in the package text itself. | Low (documentation-text accuracy only; no incorrect ledger update resulted) | 07 (`07-implementation-planning`/manager, a one-line correction to `IP-1104` §9's own wording whenever next touched) | **SCHEDULED:** rides the same future `IP-110x` text-accuracy sweep as `BL-0124`. | SCHEDULED |
| BL-0126 | 2026-07-16 | doc-defect | `10-integration-review` run ([integration-review-infinite-mode-tranche.md](../reviews/integration-review-infinite-mode-tranche.md)) | **`docs/features/INDEX.md`'s `FS-110` row and `docs/feature-planning/03-feature-catalog.md`'s `FEAT-10000` header/forward-reference note both understate the Infinite Mode tranche's actual status** — both still describe 4 of 5 packages as `NOT STARTED`/unimplemented (last touched near the tranche's own start, 2026-07-14), when all five packages are now `VERIFIED`. Metadata-only fix — mirrors `03-feature-catalog.md`'s own existing precedent for recording implementation-status forward-references (e.g. its `BL-0111` note); does not require reopening either artifact's substantive content, and does not imply a release-bucket change (`Future` remains correct pending an `11-release-readiness` decision). | Medium (genuinely misleading to a reader relying on feature-planning artifacts instead of implementation artifacts; no functional/correctness impact, no release-readiness impact) | 05 (`05-feature-decomposition`, a metadata-only forward-reference refresh to both files) | **Resolved (run #167):** `05-feature-decomposition` refreshed `FEAT-10000`'s own forward-reference status in `03-feature-catalog.md`; `docs/features/INDEX.md`'s `FS-110` row (`06-feature-specification`'s own scope, not 05's) fixed in the same pass. Both now accurately state all five packages `VERIFIED`, integration-reviewed clean. | DONE |
