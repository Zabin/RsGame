# Pipeline Backlog

> **Findings & intake register for the documentation-driven-development pipeline.** Two writers:
> **`00-pipeline-manager`** (harvests every invoked skill's findings/recommendations/Outstanding
> Issues after each run, triages every open entry at the start of the next run, and flips
> statuses) and **`00-intake`** (appends `NEW` entries for user-submitted features, bugs, and
> observations). Stage skills never write here — their findings reach this file via the manager's
> harvest step. Rows are never deleted: resolved entries flip to `DONE`, rejected ones to
> `REJECTED` with the reason kept.

[↑ Docs index](../INDEX.md) · [Pipeline journal](pipeline-journal.md) ·
[Pipeline README](../../.claude/skills/README.md)

## Lifecycle

```
NEW ──triage──▶ SCHEDULED (rides a named step)  ──that step runs──▶ DONE
        ├─────▶ DEFERRED  (named revisit trigger) ──trigger fires──▶ NEW (re-triage)
        ├─────▶ NEEDS-USER (named decision)       ──user decides───▶ SCHEDULED / REJECTED
        └─────▶ REJECTED  (reason recorded)
IN PIPELINE = the entry's work is the current/next step's explicit target
```

Every open entry carries a live disposition — `SCHEDULED` names the step it rides with,
`DEFERRED` names its revisit trigger, `NEEDS-USER` names the exact decision required. A
Critical/High entry may not be `DEFERRED` without the user's explicit agreement.

## Entries

| ID | Filed | Type | Source | Summary | Sev/Pri | Entry stage | Disposition | Status |
|---|---|---|---|---|---|---|---|---|
| BL-0001 | 2026-07-06 | bug | scaffold init: `memory.md` §"Active Bug" | Map-screen hearts written to wrong BG map addresses (3-heart layout, rows 6/8/10 col 12). **Superseded — see BL-0008 (run #2, vision revision).** `memory.md` describes the *pre-rewrite* 3-zone game; the current `tilemaps.py`/`asm_game.py` implement a completely different 9-heart 3×3 map-screen mechanism. This entry's specific fix (patch three hex addresses) does not apply to the current code as filed. | Medium | 07 | **Superseded (run #2):** do not implement as filed — re-verify against current `map_screen()`/`update_map_hearts` (asm_game.py ~line 643) once GDS-04/05 baseline the real game; folded into BL-0008's broader re-verification scope. **Resolved (run #21, `07-implementation-planning`): re-verified against current code — does NOT reproduce.** `update_map_hearts` (asm_game.py:644) writes rows 6/9/12 × cols 6/11/16, exactly matching `map_screen()`'s heart placement (tilemaps.py:327), and runs only via `do_screen_redraw` with the LCD off. The rewrite replaced and fixed the filed mechanism; a permanent map-hearts regression check rides IP-9010's rewritten suite. | DONE |
| BL-0002 | 2026-07-06 | finding | scaffold init: `Claude.md` §"Remaining Known Issues" | Bunny renders as two separate 8x8 OAM entries and reads small. **Status uncertain after run #2's discovery that the game was fully rewritten** — a separate, later commit ("Fix invisible bunny: 8x16 OBJ mode + WRAM clear bug", `9a587ac`) suggests this may already be resolved by a switch to 8x16 OBJ mode. Needs direct code verification, not the stale `Claude.md` description. | Low | 03 | **Re-scope (run #2):** before riding GDS-08 authoring as originally scheduled, first check `asm_game.py`'s current OAM-write code and commit `9a587ac`'s diff to see if this is already fixed; only carry forward if still reproducible. **Resolved (run #27 triage): already fixed in the shipped code** — commit `9a587ac` switched to 8×16 OBJ mode; [GDS-08 §2](../architecture/08-presentation-architecture.md)/[ADR-0007](../architecture/adr/ADR-0007-8x16-obj-mode.md) document the single 8×16 OAM pair as-built (run #13), and the rewritten suite's T6 asserts single-entry 8×16 bunny OAM (+X-flip) on every run (run #22, verified run #23). Not reproducible; no work remains. | DONE |
| BL-0003 | 2026-07-06 | bug | scaffold init: `Claude.md` §"Remaining Known Issues" | Score display writes VRAM while the LCD is on. **Needs re-verification against current code** (run #2 found `Claude.md` describes the pre-rewrite game throughout) — the score-bar write path may have changed in the Bunny Quest rewrite. Do not assume the original description of *where* in the code this happens still holds. | Medium | 07 | **Re-scope (run #2):** re-locate the score-bar VRAM-write call site in the current `asm_game.py` before packaging a fix; folded into BL-0008's broader re-verification scope. **Confirmed reproducing (run #21):** `update_status_disp` (asm_game.py:505) writes `0x9802`/`0x9808–0x980A` unguarded, called from `st_playing` at unbounded distance from VBlank wake — packaged as [IP-9020](../implementation/packages/IP-9020-score-bar-vblank-fix.md) (authorized via carve-out; blocked on IP-9010). **Implemented run #24** (call relocated to frame-top VBlank, T8.10a/b added). **Resolved (run #28, [VR-9020](../implementation/verification/VR-9020-score-bar-vblank-fix.md)): independently confirmed** — sole call site at frame top inside VBlank, every other VRAM writer LCD-off, 125/125 pass; IP-9020 VERIFIED. | DONE |
| BL-0004 | 2026-07-06 | design-question | scaffold init: repo-root file survey | Duplicate/legacy build artifacts need reconciling: `BunnyGarden_build_rom.py` (monolithic, 45KB) and `BunnyGarden_logic.json` coexist with the modular canonical chain. **Widened (run #2):** the repo-root also carries a stale `BunnyGarden.gbc` binary (pre-rewrite ROM, still checked in) alongside the *current, correct* `BunnyQuest.gbc` (verified byte-identical to a fresh build of current source) — same archival treatment applies. | Medium | 03 | **User decided (run #1): modular chain is canonical; archive `BunnyGarden_build_rom.py` + `BunnyGarden_logic.json` to `legacy/`.** **Widened (run #2, not yet re-asked of the user): also archive the stale `BunnyGarden.gbc` alongside them; keep `BunnyQuest.gbc` at the repo root as the current shipped artifact.** Rides the first `07-implementation-planning` hygiene pass; confirm the widened scope with the user if it isn't obviously assumed consent. **Packaged (run #21) as [IP-9040](../implementation/packages/IP-9040-legacy-artifact-archival.md)** (authorized: carve-out + run #1 decision; blocked on IP-9010). **Implemented run #25** (three files `git mv`'d to `legacy/` + README). **Resolved (run #29, [VR-9040](../implementation/verification/VR-9040-legacy-artifact-archival.md)): independently confirmed** — root clean, `legacy/` complete with history preserved, zero live references, ROM byte-identical; IP-9040 VERIFIED. | DONE |
| BL-0005 | 2026-07-06 | bug | scaffold init: `build_rom.py`/`test_rom.py` read-through | Hardcoded absolute paths break portable runs. **Compounded (run #2):** `test_rom.py`'s hardcoded `ROM_PATH` also points at the *wrong, stale* ROM name (`BunnyGarden.gbc`) — the current shipped ROM is `BunnyQuest.gbc`. A path-only fix without also fixing the filename would leave the suite pointed at the wrong binary even after portability is fixed. | Medium | 07 | SCHEDULED (run #1 triage): rides the bootstrap's first `07-implementation-planning` remediation pass, sequenced first. **Widened (run #2):** the remediation package must also update `ROM_PATH`'s filename, not just make the path configurable — folded into BL-0008. **Packaged (run #21) into [IP-9010](../implementation/packages/IP-9010-test-suite-rewrite.md)**; **implemented run #22 (2026-07-07)** — all paths repo-relative, `ROM_PATH` retargeted to `BunnyQuest.gbc`, `RAM_PATH` derived not hardcoded. **Verified run #23 ([VR-9010](../implementation/verification/VR-9010-test-suite-rewrite.md)): confirmed independently — no absolute paths remain, ROM byte-identical.** | DONE |
| BL-0006 | 2026-07-06 | bug | `01-vision` run #2 (deliberate vision-change run), direct code inspection | **`test_rom.py` asserts pre-rewrite WRAM semantics against the current shipped code — the G5 permanent gate cannot currently be satisfied.** Specifically: T4.8/T8.8 assert victory at `GS=5` when a 3-bit `GIFTS` bitfield (`0xC009`) reaches `0x07`; T7.9/T9.x assert `CUR_ZONE` (`0xC008`) is bounded to 0–2. The current `asm_game.py` uses `CUR_ZONE` 0–8 and a 9-byte `CARROT_FLAGS` array with victory at `CARROTS_COUNT==9` — none of these old assertions can pass against the real code. `test_results.txt`'s "88/88 passed" is a stale artifact predating the rewrite, not evidence about the current tree. | **Critical** | 07 | SCHEDULED: rides BL-0008's umbrella pass, prioritized above BL-0001/0003/0005's other items — the earliest `07`/`08` package once GDS-04/05 exist to derive correct assertions from. **Not deferred** (Critical severity, per the pipeline's severity-honesty rule) — flagged prominently to the user this run as a recommendation on whether to pull it forward out of numeric order; not blocking upstream GDS/requirements authoring, which can proceed from direct code reads independent of this fix. **Packaged (run #21) into [IP-9010](../implementation/packages/IP-9010-test-suite-rewrite.md)**; **implemented run #22 (2026-07-07)** — T1 kept per R304, T2–T10 rewritten against the current WRAM model (R305/GDS-07), **109/109 checks pass** against a byte-identical rebuilt ROM; `NFR-7100` flipped to MET. The G5 gate is functional again. **Verified run #23 ([VR-9010](../implementation/verification/VR-9010-test-suite-rewrite.md)): independently re-run in a fresh container (PyBoy reinstalled) — 109/109 pass confirmed, no pre-rewrite assertions remain.** | DONE |
| BL-0007 | 2026-07-06 | doc-defect | `01-vision` run #2 (deliberate vision-change run), direct code inspection | **`Claude.md`, `memory.md`, and `README.md` all describe the pre-rewrite "Bunny Garden Adventure" game throughout** (WRAM map, tile index map, zone names/count, "v2.1" status line, known-good-behavior list) — none reflect the shipped "Bunny Quest" 9-zone game. These are the primary onboarding/quick-reference docs; they are actively misleading right now, not just out of date. | High | 07 | SCHEDULED: fold into the same documentation-refresh remediation pass as BL-0006 (a stage-08 content/code peer pass once GDS-01/04/05 have re-baselined against the real code, so the refresh has a correct source to write from rather than guessing again). **Packaged (run #21) as [IP-9030](../implementation/packages/IP-9030-root-doc-refresh.md)** (depends on IP-9010+IP-9020 so it documents the fixed state; awaits G3, see BL-0024). **Implemented run #30** (2026-07-09). **Resolved (fresh-session `09-package-verification`, 2026-07-10, [VR-9030](../implementation/verification/VR-9030-root-doc-refresh.md)): independently confirmed** — all three root docs describe the shipped Bunny Quest game accurately, stale-term sweep clean, quick-start commands executed; IP-9030 VERIFIED. | DONE |
| BL-0008 | 2026-07-06 | finding | `01-vision` run #2 — consolidation entry | **Umbrella tracking entry for the "docs/tests describe the wrong game" remediation**, so BL-0001/0003/0005/0006/0007's overlapping re-verification work rides one coherent `07-implementation-planning` pass instead of five uncoordinated ones. Scope: (1) re-verify BL-0001/BL-0003's specific claims against current code, fixing only what still reproduces; (2) fix BL-0005's path portability *and* filename; (3) rewrite `test_rom.py`'s zone/victory assertions for the real 9-zone/9-carrot model (BL-0006); (4) refresh `Claude.md`/`memory.md`/`README.md` (BL-0007). | High | 07 | SCHEDULED: this is the recommended shape for the bootstrap's first `07-implementation-planning` remediation pass — likely split into 2-3 packages (test-suite rewrite is code-implementation scope; doc refresh may ride alongside or split to its own package) rather than one, per the skill's right-sizing rule. Sequenced ahead of GDS-01 authoring's dependence on accurate current-state facts wherever practical. **Umbrella fully packaged (run #21):** IP-9010 (BL-0005+0006) → IP-9020 (BL-0003) / IP-9040 (BL-0004) / IP-9030 (BL-0007); BL-0001 closed as not-reproducing. **IP-9010 VERIFIED (run #23, VR-9010)** — BL-0005/BL-0006 now DONE; IP-9020/IP-9040/IP-1010 flipped READY. **IP-9020 VERIFIED (run #28, VR-9020)** — BL-0003 DONE; **IP-9040 VERIFIED (run #29, VR-9040)** — BL-0004 DONE. **IP-9030 VERIFIED (2026-07-10, VR-9030)** — BL-0007 DONE. **Umbrella closes: all five packages VERIFIED, the bootstrap tranche's entire remediation scope is complete.** | DONE |
| BL-0009 | 2026-07-06 | finding | `02-research-gbc-hardware` run #3 ([R104](../research/encyclopedia/R104-cgb-palette-system.md)) | **The CGB's fixed palette budget (8 BG + 8 OBJ palettes) is a real, near-term design constraint for MSTR-001 C7's world-scale target — but not as tight as first characterized.** **Corrected (run #12, [GDS-07](../architecture/07-data-model.md)):** direct read of `build_rom.py`'s `BG_PALETTES` confirms palettes are organized by **terrain family**, not one-per-zone, and are already substantially reused — only 5 of 8 BG palettes serve zone terrain today (palette 4 alone covers Mountain+Village+Cave, three zones); the original framing ("already at or near its ceiling… at least one pair shares") understated the existing headroom and the deliberateness of the reuse already in place. Still a real, eventually-binding ceiling as C7 grows further — just later than originally stated. | Medium | 03 | SCHEDULED: GDS-08 (Presentation Architecture) should state the corrected picture (5/8 BG palettes in zone-terrain use, terrain-family reuse pattern) when authored, citing GDS-07/R104 — GDS-01 already named the tension in general terms and needs no further edit. Not blocking any current work. **Resolved (run #27 triage): GDS-08 was authored run #13 carrying exactly this corrected picture** (terrain-family palette reuse, citing GDS-07/BL-0009) — the ride this entry was scheduled on already happened; the underlying palette ceiling remains recorded in GDS-08/R104 as C7's future constraint. | DONE |
| BL-0010 | 2026-07-06 | doc-defect | `02-research-gbc-hardware` run #3, `docs/research/INDEX.md` §Tier R200 | `docs/research/INDEX.md`'s R201 row still describes its scope as "goal hierarchies, gating, **the gifts→victory shape**" — stale "gifts" wording from before the vision correction (the game's win condition is 9 carrots, not gifts). Trivial, and out of this skill's write scope (Tier R200 belongs to `02-research-game-design`), so left as-is rather than edited here. | Low | 02 | **Resolved (run #5):** R201 authored with corrected wording ("9-carrot victory shape"); index row fixed in the same pass. | DONE |
| BL-0011 | 2026-07-06 | finding | `02-research-tooling-and-testing` run #4 ([R301](../research/encyclopedia/R301-pyboy-headless-api.md)) | PyBoy is not installed in this environment (`import pyboy` fails), so R301's `button()`/`button_press()`/`button_release()` behavior and the `stop(ram_file=...)` custom-location detail rest on secondary sources (a GitHub-source search summary, a community Q&A post) rather than a directly-fetched primary doc page or a local experiment — `docs.pyboy.dk` itself returned HTTP 403 to `WebFetch` this session. The core claims are corroborated by this project's own working `test_rom.py` code (which already calls these methods successfully per its historical "88/88 passed" record, predating the semantic drift `BL-0006` found), so confidence is reasonable but not the formal verification bar the research methodology sets. | Low | 02 | **Resolved (run #22, 2026-07-07):** PyBoy **2.7.0** installed in-session for IP-9010; the rewritten suite exercises `button()`, `button_press()`/`button_release()`, `screen.ndarray`/`screen.image`, and the `<rom>.ram` battery-save flush-on-`stop()` behavior across 109 passing checks — R301's uncited claims are now empirically confirmed against a named version. (R301's own citation text can note this the next time an `02` pass touches it — not worth a dedicated research run.) | DONE |
| BL-0012 | 2026-07-06 | recommendation | `02-research-tooling-and-testing` run #4 — cross-reference update | **BL-0005 and BL-0006 now have concrete, code-cited remediation targets** — [R306](../research/encyclopedia/R306-toolchain-portability.md) specifies the exact four hardcoded-path lines to fix and the repo-relative replacement scheme (BL-0005); [R305](../research/encyclopedia/R305-emulator-based-test-design.md) specifies the exact current WRAM addresses/values a rewritten `test_rom.py` must assert (BL-0006), and [R304](../research/encyclopedia/R304-rom-validation.md) confirms the T1 header-check suite needs no changes at all. This doesn't change either entry's disposition (still `SCHEDULED`, riding `BL-0008`'s umbrella pass) but means `07-implementation-planning`'s eventual package-authoring pass for `BL-0008` has everything it needs already grounded — no further research gap exists for that remediation. | — | 07 | Informational — no action needed beyond citing R304/R305/R306 when `BL-0008`'s package is authored. | DONE |
| BL-0013 | 2026-07-06 | research-gap | intake: project owner | **Project owner's own words:** "Add a research series that explores pixel art techniques, using Claude and AI to generate art, best rated Gameboy and Gameboy Color games for visual aesthetics, etc among other similar topics - which generally explore how Claude could develop and implement visually appealing graphics in a structured way that can be iterated upon." No duplicate found — checked BL-0001…BL-0012 and the existing R200 tier (R201–R208, none yet authored) for overlap; closest existing rows are R203 (screen composition), R204 (HUD conventions), R208 (palette/color under CGB constraints) — adjacent, not overlapping. This request is genuinely broader than R208's color-only scope: it spans (a) pixel-art craft technique at 8×8/2bpp scale (silhouette readability, dithering-substitutes under a 4-color budget, anti-aliasing-by-palette-choice), (b) a structured, iterable **workflow** for an AI agent (this project's own coding agent) to generate/critique/revise tile art against a spec rather than freehand, and (c) comparative case-study grounding from acclaimed GBC/GBC-era titles' visual design (e.g. Link's Awakening DX, Wario Land II/3, Pokémon Gold/Silver, Kirby's Dream Land 2, Shantae) — what specifically reads as "good" GBC art and why, at the tile/palette/composition level this project's tooling actually works in. Recommend 3 new R2xx rows for whoever authors R200: a pixel-art-technique topic, an AI-assisted-generation-workflow topic, and a comparative-case-study topic — final numbering/splitting is that skill's call, not intake's. | — (user framed as "add a research series," not stated as urgent) | 02 | **Resolved (run #5):** authored as R209 (pixel-art technique), R210 (AI/agent-assisted generation workflow, grounded directly in this project's own tooling), R211 (comparative case studies: Oracle of Seasons/Ages, Shantae, Pokémon Gold/Silver, GBC DKC) — see [R209](../research/encyclopedia/R209-pixel-art-technique.md)/[R210](../research/encyclopedia/R210-ai-assisted-tile-art-workflow.md)/[R211](../research/encyclopedia/R211-acclaimed-gbc-visual-design-case-studies.md). | DONE |
| BL-0014 | 2026-07-06 | finding | `02-research-game-design` run #5 ([R210](../research/encyclopedia/R210-ai-assisted-tile-art-workflow.md)) | **This project's toolchain has no path to import a raster image (a PNG, an AI-generated sprite mockup) into `tiles.py`'s pixel-array format.** Every tile is authored as a literal hand-written Python `pix` array; no code anywhere quantizes an external image to a 4-color palette and emits the resulting grid. This means the common "AI generates a PNG, we import and refine it" pixel-art workflow is not currently executable in this pipeline — R210's recommended workflow instead has the agent author the pixel grid directly as reviewable text. Not a defect (nothing currently needs this capability), but worth recording as a real, current tooling limitation rather than an assumed-available path. | Low | 07 | DEFERRED: revisit only if a future task specifically wants to start from an external reference/mockup image — at that point this becomes a small, scoped tooling feature (an image-to-palette-grid converter script) for `07-implementation-planning` to package. Not blocking any current work. | DEFERRED |
| BL-0015 | 2026-07-06 | design-question | `03-architecture-design-synthesis` run #6 ([GDS-01 §5](../architecture/01-concept-of-play.md)) | **As MSTR-001 C7's Zelda/Pokémon-scale world target is pursued, does Bunny Quest stay a short single-sitting game over a much larger map ("wider"), or become a deliberately longer multi-session game with fewer/larger zones ("deeper")?** GDS-01 names this tension explicitly rather than picking a default; two concrete constraints bear on it — the CGB's 8-BG-palette ceiling (already substantial at 9 zones, `BL-0009`/R104/R208) makes "wider" harder to sustain with distinct per-zone color identity, and `_zone_arrows()`'s hardcoded-rectangle adjacency logic (R203) needs generalizing sooner under "wider" than "deeper." | Medium | 03 | **Closed (`00-pipeline-manager` run #30, triage):** the C7 world-scale expansion is now planned — the adopted increment plan ([PLAN-requirements-aesthetics-story-map.md](PLAN-requirements-aesthetics-story-map.md) §6) and owner decisions D3/D6 (BL-0031) resolve "wider vs deeper" by making world scale a **user-runtime parameter** rather than an architecture fork: the player picks size per world: within bounds the eventual seed & scale ADR sets. No design fork remains to adjudicate. The palette-ceiling consequence this entry named transfers to BL-0031/GDS-08 in corrected form (binds biome-family count, not blending — see plan §7). | DONE |
| BL-0016 | 2026-07-06 | recommendation | `03-architecture-design-synthesis` run #8 ([GDS-03](../architecture/03-architecture.md)) | **As-built ADRs have not been authored yet** — `docs/architecture/adr/` remains empty. GDS-03 identified several real, already-made decisions worth recording as formal ADRs: single-bank ROM (no MBC bank switching, though C7 anticipates this changing — R106), Python assembler over RGBDS (MSTR-001 C3/A3), shadow-OAM DMA every frame (R105), 8×16 OBJ mode (R103, the `9a587ac` fix), MBC1+RAM+BATTERY (R106), PyBoy-as-verification-gate not real hardware (assumption A2). Deferred from GDS-03's own pass per the one-level-per-advance discipline, not forgotten. **Resolved run #16: eight as-built ADRs authored** ([ADR-0001](../architecture/adr/ADR-0001-single-bank-rom-no-mbc-switching.md)–[ADR-0008](../architecture/adr/ADR-0008-pyboy-headless-verification-target.md)), covering all six original candidates plus one-job-per-file decomposition and the patch-point dict contract; `docs/architecture/adr/INDEX.md` populated. | Low | 03 | DONE: eight ADRs authored and indexed, run #16. | DONE |
| BL-0017 | 2026-07-06 | finding | `03-architecture-design-synthesis` run #9 ([GDS-04](../architecture/04-domain-model.md)) | **The "exactly one Carrot per Zone" rule is not enforced by any code-level invariant** — it holds today only because `ZONE_COLLECTS` happens to be authored with one `type 2` entry per zone list; nothing checks this at build time or runtime. A future content package editing `ZONE_COLLECTS` (adding a zone, rebalancing an existing one) could silently violate the rule with no test catching it. | Low | 07 | SCHEDULED: any future `07-implementation-planning` package touching `ZONE_COLLECTS` should include an explicit Verification Checklist item confirming exactly one carrot per zone list — not a dedicated remediation on its own, since nothing is broken today. **Upgraded (run #21), implemented run #22:** the rewritten suite's T1.11 asserts exactly one carrot per zone on every run (T1.10/T1.12 add zone-count and bitfield-capacity guards) — once IP-9010 verifies, the rule is a tested property, not a convention; the standing checklist rule remains for future `ZONE_COLLECTS`-touching packages. | SCHEDULED |
| BL-0018 | 2026-07-06 | finding | `03-architecture-design-synthesis` run #9 ([GDS-04](../architecture/04-domain-model.md)) | **`SaveGame` does not persist `Player.Direction`/`Player.AnimationFrame`, or per-zone `ScoreItem` collected-state** — confirmed directly against the save routine's field list (only `CUR_ZONE`, `PLAYER_X`/`PLAYER_Y`, `CARROTS_COUNT`, `SCORE`, and `CARROT_FLAGS` are written). A reload always restores a default facing/frame, and every zone's stars/flowers reset to "all present" regardless of what was collected before the save. Cosmetic/minor-progression-loss only — not a functional defect, and plausibly an intentional simplification — but worth confirming as intended rather than assumed. **Revisited run #17** ([RQ-01](../requirements/01-functional-requirements.md) FR-5210/CR-01, [RQ-03](../requirements/03-requirements-review.md) finding #1): the baseline documented current behavior honestly (FR-5210) but explicitly could not resolve whether it's intended scope — needed a user decision. **Resolved run #18 (2026-07-07), explicit user decision:** facing direction/animation frame — "not important," no persistence added (FR-5210 kept as-is, narrowed to just these two fields). Per-zone ScoreItem collected-state — "should save and persist," approved and promoted to new requirement **FR-5220**; NFR-5200 widened accordingly. RQ-01/02/03/04 all updated as a delta (changelog entries, not a regeneration). | Medium | 04 | **DONE:** user decision recorded; FR-5210 reworded, FR-5220 added, NFR-5200/RQ-03 finding #1/RQ-04 rows updated, run #18. No further action — FR-5220's actual implementation is future `07-implementation-planning`/`08-code-implementation` work, tracked via the traceability matrix, not this backlog entry. | DONE |
| BL-0019 | 2026-07-06 | recommendation | `04-requirements-engineering` run #17 ([RQ-03](../requirements/03-requirements-review.md) finding #4) | **No requirement currently watches ROM-budget headroom as it's consumed.** NFR-4000 (32768-byte single-bank budget) is correctly "Met" today (23148/32768 bytes used, ~9.6KB headroom) and correctly names C7/ADR-0001 as its own future-supersession trigger — but nothing re-checks that status as future content packages consume the remaining headroom; a package could push usage near the ceiling with no requirement flagging it. | Low | 04 | SCHEDULED: any future package that materially increases ROM usage should include a checklist item re-affirming NFR-4000's "Met" status against remaining headroom (a concrete re-affirmation threshold, e.g. below 2KB remaining, is future `03-architecture-design-synthesis`/`05-feature-decomposition` work, not decided here). Not blocking any current work. **Honored (run #21):** NFR-4000 re-affirmation checklist items included in both ROM-growing packages (IP-9020, IP-1010). **Re-affirmed (runs #24/#26):** IP-9020 left ROM unchanged (23148 bytes); IP-1010 grew it to 23404/32768 bytes (+256) — ~9.1KB headroom remains, nowhere near a 2KB-remaining concern line. Standing convention continues for future ROM-growing packages. | SCHEDULED |
| BL-0020 | 2026-07-07 | doc-defect | `05-feature-decomposition` run #19 ([FP-05](../feature-planning/05-feature-review.md) finding #1) | **No numbered functional requirement covers player/collectible sprite (OAM) rendering as an observable behavior**, even though it is shipped, architecturally documented (8×16 OBJ pairs, shadow-OAM DMA — GDS-08 §2, ADR-0005, ADR-0007), and exercised by `test_rom.py`'s T6 suite. FR-2100/FR-2200 cover position/facing; FR-3100 covers collision detection; none states sprites are actually rendered. A genuine RQ-01 baseline gap, surfaced (not invented) by the Feature Decomposition review, which may not add FRs itself. | Low-Medium | 04 | SCHEDULED: a future `04-requirements-engineering` delta should add a plausible `FR-6400` ("Player and Collectible sprite rendering") under the Presentation grouping, citing GDS-08 §2/ADR-0005/ADR-0007. Not blocking any current Feature — the behavior is shipped and working; only the formal requirement statement is missing. | SCHEDULED |
| BL-0021 | 2026-07-07 | design-question | `05-feature-decomposition` run #19 ([FP-03](../feature-planning/03-feature-catalog.md) FEAT-5100 Open Questions) | **FEAT-5100 (per-zone ScoreItem persistence, formalizing FR-5220) has two unresolved design questions that must be answered before implementation:** (1) the exact SRAM byte layout for the new persisted field (a possible GDS-07 Data Model delta); (2) whether a save written *before* this feature ships should be treated on first load as "all ScoreItems already collected" or "all ScoreItems uncollected." Neither is assumed either way in the catalog. **Resolved run #20 ([FS-101](../features/FS-101-per-zone-scoreitem-persistence.md)):** (1) a 9-byte `SCOREITEM_FLAGS` array mirroring `CARROT_FLAGS`, exact WRAM address deferred to `07-implementation-planning`; (2) pre-upgrade saves default to "all uncollected," guarded by a new save-format version byte — both reasoned to confident recommendations, neither needed the user. | Medium | 06 | **DONE:** both questions answered in FS-101 with recorded reasoning. Remaining implementation-level detail (exact WRAM address) rides `07-implementation-planning` as normal package work, not a separate backlog item. | DONE |
| BL-0022 | 2026-07-07 | doc-defect | `06-feature-specification` run #20 ([FS-101](../features/FS-101-per-zone-scoreitem-persistence.md) Open Question 1) | **`FR-3200`'s postcondition is factually incorrect as currently shipped.** It claims a collected `ScoreItem` is "permanently inactive for the remainder of that play session," but direct code reading of `setup_zone_collects` (`asm_game.py`, confirmed by FS-101) shows `ScoreItem`s are reloaded active=1 from ROM on **every** zone entry — only `Carrot`s are protected against this via the `CARROT_FLAGS` check already in that routine. The requirements baseline asserts something not true of the shipped code. | Medium | 04 | SCHEDULED: a future `04-requirements-engineering` delta should correct `FR-3200`'s postcondition to match actual shipped behavior (respawns on zone re-entry within a session) — independent of whether/when FS-101/FEAT-5100 ships, since that Feature will fix the underlying behavior but the requirements text should stop misdescribing the *current* code regardless. Not blocking any current work. | SCHEDULED |
| BL-0023 | 2026-07-07 | bug | `06-feature-specification` run #20 ([FS-101](../features/FS-101-per-zone-scoreitem-persistence.md) Open Question 2, Risks) | **`ScoreItem`s (stars/flowers) currently respawn every time a zone is re-entered within a play session** (see `BL-0022`) — meaning `Score` is infinitely farmable today by walking back and forth between two zones. `SCORE` (`0xC006`) is a single WRAM/SRAM byte with no documented overflow handling if repeatedly incremented past 255. Discovered incidentally while specifying `FEAT-5100`, not previously caught by `GDS-05`/`RQ-01`'s authoring passes. | Medium | 08 | SCHEDULED: `FEAT-5100`'s implementation (per `FS-101`'s design, extending the same zone-entry check `Carrot`s already use) fixes this as a side effect — no separate remediation package needed unless `FEAT-5100` is deprioritized far enough that the farming behavior needs an interim fix on its own. Not blocking any current work; not escalated to `NEEDS-USER` since `FEAT-5100` already fixes it as designed. **Packaged (run #21):** rides [IP-1010](../implementation/packages/IP-1010-per-zone-scoreitem-persistence.md) with a dedicated regression test (T11.a — collected item stays inactive and `SCORE` does not re-increment on zone re-entry). **Implemented run #26:** T11.a4/T11.a5 confirm the fix (score does not re-increment on zone re-entry); closes on IP-1010's Verification Report. **Resolved (run #27, [VR-1010](../implementation/verification/VR-1010-per-zone-scoreitem-persistence.md)): fix independently confirmed** — T11.a4/T11.a5 re-run in a fresh container prove a collected ScoreItem stays inactive and `SCORE` does not re-increment on zone re-entry; IP-1010 VERIFIED. | DONE |
| BL-0024 | 2026-07-07 | gate | `07-implementation-planning` run #21 ([Master Build Plan](../implementation/00-master-build-plan.md)) | **G3 authorization needed for three planned packages before any can start a stage-08 run:** (1) **IP-9010** (test-suite rewrite, BL-0006 Critical + BL-0005) — READY and the critical-path unblocker for *everything*, but BL-0006 is outside the BL-0001…0005 bootstrap carve-out; (2) **IP-9030** (root-doc refresh, BL-0007) — same reason; (3) **IP-1010** (FS-101 per-zone ScoreItem persistence) — genuinely new work, flagged since run #20. IP-9020/IP-9040 are already authorized (carve-out) but blocked on IP-9010, so **the whole build plan is gated on this decision.** | High (blocks the Critical BL-0006 remediation) | 08 | **Resolved (run #21, same day): user explicitly authorized all three** ("Authorize all three", 2026-07-07) — IP-9010, IP-9030, and IP-1010 all have G3 go-ahead on record; combined with the carve-out (IP-9020/IP-9040), every planned package is now authorized. Execution order remains dependency-driven: IP-9010 first. | DONE |
| BL-0025 | 2026-07-07 | recommendation | `08-code-implementation` run #22 (IP-9010) | **Runtime test artifacts are untracked repo-root noise:** the rewritten suite writes screenshots to `test_shots/` and PyBoy writes `BunnyQuest.gbc.ram` beside the ROM during runs (the suite wipes the `.ram` at exit, but an aborted run leaves it). No `.gitignore` exists to exclude them. Cosmetic only — nothing breaks. | Low | 07 | **Resolved (run #22, same day):** a tracked `.gitignore` already existed covering `*.gbc.ram`/`*.gbc.sav`/`__pycache__`; `test_shots/` appended to it (one-line hygiene fix, prompted by the repo's clean-tree hook — no package needed). | DONE |
| BL-0026 | 2026-07-07 | doc-defect | `09-package-verification` run #23 ([VR-9010](../implementation/verification/VR-9010-test-suite-rewrite.md)) | **`IP-9010`'s §3 "Requirements Covered" cites `NFR-7000`, which does not exist anywhere in the requirements baseline** (`docs/requirements/02-non-functional-requirements.md` has no such section; the RTM has no such row). The nearest match by subject is **`NFR-6100`** ("PyBoy headless as the verification target") — its RTM Test cell is still `UNASSIGNED (inspection-based)` even though every one of the rewritten suite's 109 checks now runs headless via PyBoy, direct repeatable evidence for it. Not a functional defect — the underlying capability is real and independently confirmed by VR-9010 — purely a citation/traceability gap. | Low | 04 | SCHEDULED: a future `04-requirements-engineering` delta should fill `NFR-6100`'s RTM Test cell citing the full T1–T10 suite as headless-execution evidence; `07-implementation-planning` should correct `IP-9010`'s citation from `NFR-7000` to `NFR-6100` next time that package is touched (informational only, does not block anything downstream). | SCHEDULED |
| BL-0027 | 2026-07-07 | doc-defect | `08-code-implementation` run #25 (IP-9040) | **`.claude/skills/run-bunnygarden/SKILL.md` still documents the pre-`IP-9010` hardcoded paths** (`/mnt/user-data/outputs/BunnyGarden.gbc[.ram]`, `/home/claude/bunnygarden/`) and the stale `BunnyGarden.gbc` filename, even though `test_rom.py` has run repo-relative against `BunnyQuest.gbc` since `IP-9010` (run #22). Not named by `BL-0007`/`IP-9030` (which scope only `Claude.md`/`memory.md`/`README.md`) — a genuine gap between what those track and what's actually still stale. Found incidentally while archiving the legacy artifacts (`IP-9040`); out of that package's scope to fix. | Low | 07 | SCHEDULED: fold into a future `07-implementation-planning` hygiene pass (natural rider for `IP-9030`'s root-doc refresh, or its own small package) — update the skill doc's commands/paths to match `test_rom.py`'s current repo-relative form. Not blocking any current work. | SCHEDULED |
| BL-0028 | 2026-07-07 | doc-defect | `09-package-verification` runs #27/#28 ([VR-1010](../implementation/verification/VR-1010-per-zone-scoreitem-persistence.md) finding #1, [VR-9020](../implementation/verification/VR-9020-score-bar-vblank-fix.md) finding #1) | **Stale "pending independent verification by `09-package-verification`" clauses in the requirements/architecture record** — the verifications now exist, so the hedges are outdated: RQ-02's `NFR-5200` status line (VR-1010), RQ-02's `NFR-1200` status line + GDS-06 N2's remediation note (VR-9020), and the RTM `NFR-1200` cell's snapshot "111/111" count (125/125 today). Cosmetic only; every underlying cell/claim is accurate. **Widened run #28** to cover both VRs' instances in one entry. | Low | 04 | SCHEDULED: fold into the next `04-requirements-engineering` delta batch alongside BL-0020 (missing sprite-rendering FR), BL-0022 (FR-3200 postcondition correction), and BL-0026 (NFR-6100 RTM Test cell) — one pass; the GDS-06 N2 pointer note rides the same pass (03-owned, one-word edit). Not blocking any current work. | SCHEDULED |
| BL-0029 | 2026-07-09 | feature | intake: project owner — adopted increment plan ([PLAN-requirements-aesthetics-story-map.md](PLAN-requirements-aesthetics-story-map.md) §4 stream A; owner direction D4 in §0) | **Stream A — aesthetic-quality standard.** Adopt a vision-level presentation-quality commitment and a *normative* aesthetic standard (GDS-08 delta), then derive measurable presentation NFRs, per the adopted plan's Phase 1→3→4 path. Owner's quality bar on record (D4): the result must be **smooth** and **every screen/room/view clean**. Research grounding already exists (R202/R203/R208/R209/R210/R211, filed via BL-0013 — adjacent, not duplicate: that entry produced the research; this one produces the standard and requirements). No duplicate found (checked BL-0001…BL-0028). | Medium (owner: part of adopted increment; no urgency stated) | 01 → 03 → 04 (per plan §1) | **DONE (`00-pipeline-manager` run #42):** stage 01 (C8) → stage 03 (GDS-08 §7 normative standard) → stage 04 (NFR-1300/NFR-6500/NFR-6510, RQ-01..04 delta) all complete. This entry's full plan-stated path is closed; remaining work (05-feature-decomposition onward) rides the normal Master Build Plan, not this backlog entry. | DONE |
| BL-0030 | 2026-07-09 | feature | intake: project owner — adopted increment plan ([PLAN-requirements-aesthetics-story-map.md](PLAN-requirements-aesthetics-story-map.md) §4 stream B; owner direction D1/D2 in §0) | **Stream B — visual story narrative via biome-flow grammar.** New vision-level scope: the game tells its story visually through a **logical biome adjacency grammar between whole screens** (e.g. water→beach→grassland→hills→mountains→sky; one biome per screen, never two in a frame, no disjointed pairings), with an **item-agnostic, child-friendly collect-goal** following the narrative (carrots are an instance, not a requirement). **No text dialogue requirement** (D1 — not ruled out, not required; a future dialogue request re-enters at 01). Agent constructs the grammar from research (proposed R212, plan Phase 2). No duplicate found. | Medium (owner: part of adopted increment) | 01 → 02 → 03 → 04 (per plan §1) | **DONE (`00-pipeline-manager` run #42):** stage 01 (C9) → 02 (R212) → 03 (GDS-01 §3a, GDS-04 KeyItem/grammar rules, GDS-08 §8) → 04 (FR-3220/4300/4310, RQ-01..04 delta) all complete. This entry's full plan-stated path is closed. | DONE |
| BL-0031 | 2026-07-09 | feature | intake: project owner — adopted increment plan ([PLAN-requirements-aesthetics-story-map.md](PLAN-requirements-aesthetics-story-map.md) §4 stream C; owner direction D3/D5/D6/D7/D8/D9/D10 in §0) | **Stream C — deterministic procgen world from (seed, scale) + menu-flow delta + 3×3 archival.** The world becomes **deterministically procedurally generated** from a user-modifiable **seed** and **world-scale** parameter, both set **only at new-game start** and immutable per save (D3/D6/D7); named research topics: procgen map algorithms and GBC homebrew with procgen maps (proposed R213/R214) — the agent picks the algorithm from research. **Menu-flow delta (D8/D9/D10):** boot → title → **main menu** (continue / new game → seed & scale entry), **no auto-load at boot** (amends MSTR-001 C2's auto-load clause — deliberate protected-baseline change), and the start-button save menu gains **exit-to-main-menu with auto-save**. The handcrafted 3×3 world is **archived** per the `legacy/` precedent (D5 — second protected-baseline change, C5). Cross-ref: **closes BL-0015** (wider vs deeper) per plan §6 — scale is now a user parameter, the fork dissolves; manager to flip BL-0015 at triage with that rationale. No other duplicate found. | Medium (owner: part of adopted increment) | 01 → 02 → 03 → 04 (per plan §1) | **DONE (`00-pipeline-manager` run #42):** stage 01 → 02 (R213/R214/R111) → 03 (ADR-0009/0010/0011 + six GDS deltas) → 04 (FR-9100–9200, NFR-1300/2200/4200/5300, RQ-01..04 delta) all complete. This entry's full plan-stated path is closed — **the adopted increment plan's terminal deliverable (Phase 4) is reached.** | DONE |
| BL-0032 | 2026-07-09 | doc-defect | `02-research-gbc-hardware` run #33 (R102 extension) | **R101 and R102 cite two different figures for VBlank's duration** — R101 says "~1140 T-state VBlank period," R102 says "lines 144–153, ~4560 T-states total." Neither topic resolves the discrepancy; R102's new full-screen-redraw-cost analysis (grounding MSTR-001 C10/C8) flagged it inline rather than silently picking one, since the qualitative conclusion (a 1152-byte tilemap copy doesn't fit in VBlank regardless of which figure is right, hence the project's existing LCD-off approach) holds either way. | Low | 02 | SCHEDULED: a future `02-research-gbc-hardware` pass should directly fetch Pan Docs' VBlank-duration figure (both prior citations rested on earlier sessions' fetches or WebSearch summaries) and correct whichever of R101/R102 is wrong. Not blocking any current work — the redraw-cost conclusion doesn't depend on the exact figure. | SCHEDULED |
| BL-0033 | 2026-07-09 | doc-defect | `03-architecture-design-synthesis` run #37 (GDS-04 delta, incidental finding) | **GDS-04's "Relationships worth naming explicitly" §3rd bullet is stale relative to the shipped code** — it states "all `ScoreItem` positions/consumed-state per zone[] are not persisted... only the scarce tier's collection state persists," but `IP-1010`/`FS-101` (implemented and verified 2026-07-07, before this GDS-04 delta) added exactly that: `SCOREITEM_FLAGS` (`0xC060`–`0xC068`) persists per-zone `ScoreItem` collected-state to SRAM (`0xA013`–`0xA01B`, per `FR-5220`, confirmed in `GDS-07` §2/§3). GDS-04's own bullet was not updated when `FS-101` shipped — noticed incidentally while authoring the GDS-04 procgen-world delta; out of that delta's scope to fix (it's a pre-existing gap, not part of the increment). | Low-Medium | 03 | SCHEDULED: a future `03-architecture-design-synthesis` touch of GDS-04 (or a dedicated small pass) should correct the bullet to state `ScoreItem` collected-state *does* persist per-zone (citing `FR-5220`/`GDS-07`), matching `IP-1010`'s shipped behavior. Not blocking any current work. | SCHEDULED |
| BL-0034 | 2026-07-09 | doc-defect | `03-architecture-design-synthesis` run #41 (GDS-10 delta, incidental finding) | **GDS-10's own merge decision (2026-07-06) was never executed.** It states: "this level stays authoritative for the traceability scheme... until `RQ-04` is authored, at which point `RQ-04` becomes authoritative for row-level data and this level's §3/§4 become the pointer." `RQ-04` was authored at run #17 (2026-07-06, same day) and is already a populated, correctly-cross-referencing matrix — but GDS-10's §3/§4 were never converted to pointers, and its ID-scheme table (§2) sat stale (showing `RQ`/`FP`/`FS`/`IP`/`VR` as "not yet authored" through the entire bootstrap increment) until this same run refreshed the example column only. Noticed while confirming the ID scheme extends to the procgen-world increment's new artifacts; the deeper restructuring (§3/§4 → pointers to `RQ-04`) is out of that light-delta's scope. | Low | 03 | SCHEDULED: a future `03-architecture-design-synthesis` pass should convert GDS-10 §3/§4 into short pointers to `RQ-04`, per this document's own already-recorded merge decision — a bootstrap-increment housekeeping item, not blocking any current work. | SCHEDULED |
| BL-0035 | 2026-07-10 | doc-defect | `10-integration-review` run (bootstrap tranche, [integration-review-bootstrap-tranche.md](../reviews/integration-review-bootstrap-tranche.md) finding #1) | **`ROADMAP.md`'s Implementation theme table (`IM-00`/`IP-xxxx`/`VR-xxxx` rows) is stale** — `IP-xxxx` still reads "authored 2026-07-07; none implemented" and `VR-xxxx` still reads "⛔", even though all five bootstrap packages are now `VERIFIED` with five Verification Reports on record. No stage-08/09 run touches this particular ROADMAP table. | Medium | 07 | SCHEDULED: a future `07-implementation-planning` touch (or a small standalone hygiene pass) should refresh these three rows to state all five packages `VERIFIED`. Not blocking any current work — every other ledger (Master Build Plan, packages `INDEX.md`, verification `INDEX.md`) already tells the accurate story. **Resolved (`07-implementation-planning` run, 2026-07-10):** all three rows refreshed while authoring the Release-2 tranche's own package rows in the same table — a natural fold-in since this stage owns the table. | DONE |
| BL-0036 | 2026-07-10 | doc-defect | `10-integration-review` run (bootstrap tranche, [integration-review-bootstrap-tranche.md](../reviews/integration-review-bootstrap-tranche.md) finding #2) | **`docs/feature-planning/01-release-plan.md` §Release 1 and `03-feature-catalog.md`'s `FEAT-5100` entry both still describe it as unimplemented** ("no shipped implementation" / "new — not yet implemented"), even though `IP-1010`/`FS-101` shipped and were independently verified 2026-07-07 — these feature-planning docs were authored the same day but never revisited afterward. | Medium | 05 | SCHEDULED: rides the next `05-feature-decomposition` touch — natural to fold in alongside the procgen-world increment's own upcoming Feature Catalog additions (new `FEAT-xxxx` rows for the RQ-01…04 delta), since that pass will already be editing both documents. Not blocking any current work. **Resolved (`05-feature-decomposition` run, 2026-07-10):** both documents corrected — FEAT-5100's catalog entry now states shipped/VERIFIED status with citations to `IP-1010`/`VR-1010`; Release 1's bucket description and header both corrected. Folded in exactly as scheduled, alongside the five new Feature rows. | DONE |
| BL-0037 | 2026-07-10 | doc-defect | `05-feature-decomposition` run ([05-feature-review.md](../feature-planning/05-feature-review.md) finding #5) | **`docs/feature-planning/03-feature-catalog.md`'s `FEAT-6000`/`FEAT-7000` entries still describe `NFR-1200`/`NFR-7100` as current non-compliances in their own Risk fields** — both are now Met, VERIFIED 2026-07-07 (`IP-9020`/`VR-9020`, `IP-9010`/`VR-9010`) and reconfirmed by `10-integration-review` (2026-07-10). The equivalent statements in the Epic Catalog and Release Plan were corrected in the same pass that found this (per `BL-0036`'s instruction), but this pass's scope did not extend to editing FEAT-6000/FEAT-7000's own catalog entries. | Low | 05 | SCHEDULED: a future light `05-feature-decomposition` touch should update FEAT-6000/FEAT-7000's Risk fields to reflect both resolutions. Not blocking any current work — every ledger that actually gates on these NFRs (RTM, Master Build Plan) already shows the correct Met status; only these two Features' own prose lags. | SCHEDULED |
| BL-0038 | 2026-07-10 | design-question | `06-feature-specification` runs ([FS-102](../features/FS-102-procedural-world-generation.md) Open Questions 1–3, [FS-103](../features/FS-103-generated-region-screen-composition.md) Open Questions 1–2) | **FEAT-9000 (procedural world generation) and FEAT-4100 (generated-region screen composition) share five unresolved design questions before implementation can begin, all converging on one underlying deferral:** (1) the concrete biome-adjacency grammar table's exact contents — R212 gives only a conceptual example, GDS-08's delta explicitly defers exact biome-family count/assignment "to the implementation package that sizes it" (FS-102 OQ1 = FS-103 OQ1, the same deferral seen from two Features); (2) the generation algorithm's line-by-line implementation detail (ADR-0009/R213 fix the approach, not the routine's exact step order); (3) whether a generator-data ROM pointer is needed in the `patches` dict, downstream of (1); (4) the exact tile-index ranges/palette assignments for whatever biome-family set is chosen (FS-103 OQ2, also downstream of (1)). All are named with their resolving stage already identified. | Medium | 07 | SCHEDULED: all five ride the same `07-implementation-planning` pass that packages FEAT-9000/FEAT-4100 together — per GDS-08's own explicit deferral (question 1, which questions 3–4 depend on) and this stage's normal implementation-detail authority (question 2). Not escalated to the user — each has a clear, already-identified resolution path, following the FS-101 precedent for confidently-resolvable design questions. **Resolved (`07-implementation-planning` run, 2026-07-10, [IP-1020](../implementation/packages/IP-1020-procedural-world-generation.md)/[IP-1030](../implementation/packages/IP-1030-generated-region-screen-composition-code.md)/[IP-1031](../implementation/packages/IP-1031-generated-region-screen-composition-content.md)):** biome-family vocabulary = the 5 existing shipped terrain families (zero new art); linear grammar axis Water-Sand-Grass-Stone-Brick, adjacency legal iff axis indices differ by ≤1 (no ROM table needed, resolving Q3); flood-fill generation algorithm over the fixed `scale×scale` grid, satisfying determinism/reachability/grammar-validity by construction. | DONE |
| BL-0039 | 2026-07-10 | design-question | `06-feature-specification` run ([FS-104](../features/FS-104-main-menu-new-game-flow.md) Open Questions 1–2) | **FS-104 (FEAT-1100, main menu & new-game flow) has two unresolved design questions:** (1) whether SEED/SCALE ENTRY offers a cancel/back-to-MAIN-MENU path — neither ADR-0010 nor GDS-01 §4a's state diagram names one, leaving a player who selects "new game" by mistake with no way back short of completing a full seed/scale entry and generation; (2) whether "continue"/"new game" selection uses A-confirm-only or a D-pad up/down selector — an input-mapping detail GDS-01 §4a doesn't specify. | Low-Medium | 07 | SCHEDULED: both ride the `07-implementation-planning` pass that packages FEAT-1100 — question 1 is a genuine player-experience gap worth resolving deliberately (most likely a B-cancels convention matching the existing SAVE state's own A/B pattern, per FS-104's own reasoning, but not assumed there); question 2 is ordinary UI implementation detail. Not escalated to the user — both have clear, confidently-resolvable paths, following the FS-101 precedent. **Resolved (`07-implementation-planning` run, 2026-07-10, [IP-1040](../implementation/packages/IP-1040-main-menu-new-game-flow.md)):** B cancels SEED/SCALE ENTRY back to MAIN MENU (matching SAVE's own A/B convention); D-pad up/down selects the highlighted MAIN MENU option, A confirms. | DONE |
