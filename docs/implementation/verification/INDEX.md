# Verification Reports — Index

Owned by `09-package-verification` — the only skill that may advance a package to `VERIFIED`.
One `VR-xxxx-<slug>.md` per verification run, numbered to match the package (IP-1010 → VR-1010).
A `RETURNED` result is a normal outcome, recorded here like any other.

[↑ Master Build Plan](../00-master-build-plan.md)

| VR | Package | Date | Result | Headline findings |
|---|---|---|---|---|
| [VR-9010](VR-9010-test-suite-rewrite.md) | IP-9010 (test-suite rewrite) | 2026-07-07 | **VERIFIED** | 109/109 checks pass, ROM byte-identical, all DoD/checklist items confirmed independently. One Low finding: package cites nonexistent `NFR-7000` (should be `NFR-6100`), whose RTM Test cell remains unfilled. |
| [VR-1010](VR-1010-per-zone-scoreitem-persistence.md) | IP-1010 (per-zone ScoreItem persistence) | 2026-07-07 | **VERIFIED** | 125/125 pass independently re-run (fresh container), ROM byte-identical rebuild, all 5 FS-101 ACs confirmed via T11.a–e, BL-0023 farming-bug fix proven (T11.a4/a5). One Low finding: NFR-5200's "pending independent verification" clause in RQ-02 now stale (04 delta). |
| [VR-9020](VR-9020-score-bar-vblank-fix.md) | IP-9020 (score-bar VBlank fix) | 2026-07-07 | **VERIFIED** | Sole `update_status_disp` call site confirmed at frame-top VBlank (direct read); every other VRAM writer runs LCD-off; T8.10a/b digit-timing checks pass; 125/125, ROM byte-identical. Unblocks IP-9030 → READY. One Low finding: stale "pending verification" clauses in RQ-02 NFR-1200 + GDS-06 N2 (04-delta batch). |
| [VR-9040](VR-9040-legacy-artifact-archival.md) | IP-9040 (legacy artifact archival) | 2026-07-07 | **VERIFIED** | Root clean of `BunnyGarden*`, `legacy/` holds all three artifacts + README via history-preserving `git mv`, zero live references tree-wide (all doc hits historical or already-tracked as BL-0007/BL-0027), ROM byte-identical, 125/125. No findings. |
| [VR-9030](VR-9030-root-doc-refresh.md) | IP-9030 (root documentation refresh) | 2026-07-10 | **VERIFIED** | All three root docs (`Claude.md`/`memory.md`/`README.md`) confirmed accurate against the shipped tree and GDS ladder; stale-term sweep clean; README quick-start commands actually executed (build byte-identical, 125/125 pass); WRAM pointer spot-check matches `asm_game.py` exactly. No findings — bootstrap tranche's fifth and final package now VERIFIED; `BL-0008` umbrella closes. |
| [VR-1020](VR-1020-procedural-world-generation.md) | IP-1020 (procedural world generation & item-agnostic collection) | 2026-07-10 | **VERIFIED** | 133/133 pass independently re-run (fresh container), ROM byte-identical rebuild (23660/32768 bytes), all 8 FS-102 ACs confirmed via T12.a–i + retargeted T8.7/T8.8, oracle/SM83 lockstep confirmed both by T12.b (0 mismatches, 15-entry corpus) and direct side-by-side code read, no-`DIV` determinism independently re-read. Release 2's foundational, dependency-root package now VERIFIED — unblocks `IP-1030`/`1040`/`1050`. One Medium finding: `ROADMAP.md`'s `IM-00`/`IP-xxxx` rows stale (pre-dates this run's authorization/completion), same pattern as `BL-0035`. |
| [VR-1030](VR-1030-generated-region-screen-composition-code.md) | IP-1030 (generated-region screen composition — code) | 2026-07-10 | **VERIFIED** | 180/180 pass on current tree head (IP-1030's own T13: 3/3), ROM byte-identical rebuild, both FS-103 ACs confirmed (T13.a tile-family audit, T13.b call-site audit), `_zone_arrows` retirement + scale=3 arrow-placement regression confirmed byte-for-byte (T13.c). Critical-path package — unblocks `IP-1031` (content half). No new findings; `BL-0041` (ROADMAP.md staleness) reconfirmed still open. |
| [VR-1040](VR-1040-main-menu-new-game-flow.md) | IP-1040 (main menu & new-game flow) | 2026-07-11 | **VERIFIED** | 180/180 pass, ROM byte-identical rebuild, all six FS-104 ACs confirmed via T14's 20/20 checks, auto-load bypass confirmed genuinely retired (sole `try_load_save` call site), B-cancel writes nothing, exit-to-main-menu reuses the exact save-write routine, FR-9110 immutability holds under a systematic sweep. Two Low findings: RTM/FS-104 "163/163" snapshot counts stale (suite grew after `IP-1050` landed); commit message undercounted T14's own check count (cosmetic, no ledger repeats it). |
| [VR-1050](VR-1050-generated-world-save-persistence.md) | IP-1050 (generated-world save persistence) | 2026-07-11 | **VERIFIED** | 180/180 pass, ROM byte-identical rebuild, both FS-105 ACs confirmed via T15's 17/17 checks (matching the implementing commit's own count exactly). Single MBC1 bracket preserved across the save/load extension, version-guard gates only the new fields, `REGION_GRAPH` confirmed never written to SRAM, legacy fields still round-trip, pre-upgrade (`0x01`) saves cleanly rejected. No findings — this closes verification on all five Release-2 packages except `IP-1031`, blocked on a fresh session. |
| [VR-1070](VR-1070-maze-shaped-region-adjacency.md) | IP-1070 (maze-shaped region adjacency) | 2026-07-12 | **VERIFIED** | 226/226 pass (fresh PyBoy install), ROM rebuilds at exactly 32768 bytes, all seven FS-107 ACs confirmed via T19.a–g, oracle/SM83 lockstep confirmed by T19.c (0 mismatches) and a direct side-by-side code read, `dsr_p`/`draw_region_arrows`/`check_zone_transition` confirmed zero-diff via commit-scoped `git diff`. Unblocks `IP-1080` (still separately not authorized). One Low finding: the package's own §6 narrative describes the prune-pass tree-edge test as a single check when the shipped code (and oracle) correctly implement two — a real, necessary correctness property the package's prose undersold; not a code defect. |
| [VR-9070](VR-9070-cur-zone-indexed-structures-generalization.md) | IP-9070 (`CUR_ZONE`-indexed structure generalization) | 2026-07-12 | **VERIFIED** | 231/231 pass (fresh PyBoy install), ROM byte-identical rebuild, `SCOREITEM_FLAGS`/`SRAM_SCOREITEM` relocation confirmed non-overlapping against every neighboring WRAM/SRAM constant, `ZONE_COLLECTS`'s 9→5 biome-family reduction confirmed (`zc_table` lookup now biome-keyed, `build_rom.py` needed no change), `SAVE_VERSION_VAL` v3 guard confirmed via T16.d, all driven at non-default region indices (up to 80, scale=7) per this skill's own tunable-parameter rule. Unblocked `IP-9050`/`IP-9060` (already COMPLETE) at implementation time. One Low finding: RTM's `FR-5220` row cited only `IP-1010`/`T11` — corrected in place by this run to also cite `IP-9070`/`T16`. |
| [VR-9050](VR-9050-generated-world-navigation-fix.md) | IP-9050 (generated-world navigation fix) | 2026-07-12 | **VERIFIED** | 231/231 pass, ROM byte-identical rebuild, `check_zone_transition` confirmed fully `REGION_GRAPH`-driven (shared `czt_region_hl` subroutine, addressing identical to `dsr_p`'s), T17.a/b confirmed at both a genuine scale=5 world (25/25 regions via real navigation) and the scale=3 regression, T9 confirmed fully retired. No findings — the RIGHT-threshold value drift (`156`→`152`) is expected, already-documented `IP-9120` follow-on work. |
| [VR-9060](VR-9060-main-menu-cursor-fix.md) | IP-9060 (main menu cursor fix) | 2026-07-12 | **VERIFIED** | 231/231 pass, ROM byte-identical rebuild, `check_save_valid` confirmed to write no `MM_CURSOR` value, `mm_on_entry`'s reset confirmed gated on `MM_JUST_ENTERED` (exactly 4 real transition sites plus the consuming clear), all 12 T18.* checks confirmed passing. Closes the post-ship remediation tranche end-to-end (`IP-9050`/`9060`/`9070` all now `VERIFIED`). One Low finding: RTM's `FR-1170` row cited only `IP-1040`/`T14` — corrected in place by this run to also cite `IP-9060`/`T18`. |
| [VR-1031](VR-1031-generated-region-screen-composition-content.md) | IP-1031 (generated-region screen composition — content) | 2026-07-12 | **VERIFIED** | 231/231 pass, ROM byte-identical rebuild, `ALL_SCREENS`'s exact 5-family mapping confirmed, `tiles.py`/`build_rom.py` palette tables confirmed diff-clean since `IP-1030`'s commit (zero new art/palette entries), T13.a tile-family audit plus the pre-existing independent content review both confirm no cross-family tile leakage. Closes Release 2's full package set — all five now `VERIFIED`. One Low finding: `FR-4300` not yet promoted to the global RTM (pre-existing gap). |
| [VR-1080](VR-1080-maze-aware-edge-classification.md) | IP-1080 (maze-aware transition-edge classification, logic half) | 2026-07-12 | **VERIFIED** | 231/231 pass, ROM byte-identical rebuild, `DRA_ROW`/`DRA_COL` re-derivation confirmed via repeated-subtraction division, open-edge branch confirmed byte-for-byte unchanged, all four T20.* checks confirmed passing including AC-4 confirmed still honestly open. Closes the maze-shaped region adjacency tranche's full critical path — `IP-1070`/`1080` both now `VERIFIED`. No findings. |
| [VR-9080](VR-9080-save-screen-third-option-labeling.md) | IP-9080 (SAVE screen third-option labeling) | 2026-07-12 | **VERIFIED** | 231/231 pass, ROM byte-identical rebuild, T5.10–T5.12 confirmed passing, independently re-driven via PyBoy (no prior content review existed) — screenshot confirms "SELECT: SAVE"/"AND EXIT" renders cleanly with no overlap. No findings. |
| [VR-9090](VR-9090-movement-clamp-boundary-fix.md) | IP-9090 (movement clamp boundary fix) | 2026-07-12 | **VERIFIED** | 231/231 pass, ROM byte-identical rebuild, UP floor (Y=8) and RIGHT ceiling (X=152) confirmed by direct code read, DOWN/LEFT confirmed unchanged, T7.8/T7.8b/T7.10/T7.10b confirmed passing via genuine movement input. Confirmed the X=152 ceiling is consumed consistently by IP-9120's later fix. No findings. |
| [VR-9100](VR-9100-collectible-pickup-hitbox-fix.md) | IP-9100 (collectible pickup hitbox fix) | 2026-07-12 | **VERIFIED** | 231/231 pass, ROM byte-identical rebuild, asymmetric point-in-box test confirmed by direct code read (unsigned-subtract range check), all four boundary checks (T8.x/T8.y/T8.z1/T8.z2) confirmed passing at the exact BL-0053 reproduction points/boundaries. FR-3100's text-vs-implementation divergence confirmed honestly flagged. Closes the movement/pickup/UI tranche end-to-end — IP-9080/9090/9100 all now VERIFIED. No findings. |
| [VR-9110](VR-9110-gw-prng-step-mixing-step-repair.md) | IP-9110 (`gw_prng_step` mixing-step repair) | 2026-07-12 | **VERIFIED** | 231/231 pass, ROM byte-identical rebuild, 7,9,8 shift-triplet confirmed shipped exactly with worldgen.py's lockstep mirror (zero oracle mismatches), T12.j/T12.k confirmed passing via the live SM83-built ROM. This run independently re-performed the package's own "ad hoc" pre-upgrade-save check live via PyBoy: version=0x03 shows CONTINUE blank, version=0x04 shows it offered. No findings. |
| [VR-9120](VR-9120-right-zone-transition-threshold-fix.md) | IP-9120 (RIGHT zone-transition threshold fix) | 2026-07-12 | **VERIFIED** | 231/231 pass, ROM byte-identical rebuild, RIGHT-edge comparison confirmed reading CP_n(152) matching handle_play_input's own clamp ceiling exactly, T7.11 confirmed passing via real, sustained button-press input. No findings. |
| [VR-9130](VR-9130-zone-transition-intent-gate.md) | IP-9130 (zone-transition intent gate) | 2026-07-12 | **VERIFIED** | 231/231 pass, ROM byte-identical rebuild, all four JOY_CUR gates confirmed by direct code read (correct bit/branch target per direction), T7.12 confirmed passing via the exact BL-0078 reproduction sequence, T11.a2/_t17_do_move confirmed holding real buttons. No findings. |
| [VR-9140](VR-9140-right-arrow-offscreen-position-fix.md) | IP-9140 (right-arrow off-screen position fix) | 2026-07-12 | **VERIFIED** | 231/231 pass, ROM byte-identical rebuild, ARROW_ADDR_R confirmed reading column 18 by direct code read, T13.d confirmed passing, independently re-driven via PyBoy at the exact BL-0084 reproduction sequence — screenshot confirms the right arrow now renders visibly. No findings. |
| [VR-1021](VR-1021-win-condition-redesign.md) | IP-1021 (win-condition redesign — dead-end-anchored treasure placement) | 2026-07-13 | **VERIFIED** | 233/233 pass (fresh PyBoy install), ROM builds at exactly 32768 bytes, all DoD/checklist items confirmed by direct code read (leaf-classification/placement pass confined between `maze_carve_done` and the braid pass, `check_complete`'s only change the comparison operand, zero PRNG draws added). Independently re-driven live via the real MAIN MENU → SEED/SCALE ENTRY UI at two non-corpus scales (6, 8) — exact `WORLD_SCALE`-count placement, oracle parity, and correct victory-threshold triggering confirmed directly, not just via the suite's own fixture. No findings. |
| [VR-1081](VR-1081-maze-blocked-edge-indicator-content.md) | IP-1081 (maze-blocked edge indicator, content half) | 2026-07-13 | **VERIFIED** | 233/233 pass, ROM builds at exactly 32768 bytes, `tiles.py` diff confirmed exactly 4 constants + 4 functions + 4 `put()` calls, `build_rom.py` confirmed untouched (0 new palette entries), `0x1A`-`0x1D` confirmed previously free. Unblocks `IP-1082` → READY. One Medium finding (`BL-0097`): `blocked_up`/`blocked_down` and `blocked_left`/`blocked_right` are pixel-identical pairs rather than direction-specific glyphs, contrary to the package's own §6 text — does not block VERIFIED (deferred to `09-content-review` per the package's own §13), routed there. |
| [VR-1090](VR-1090-select-menu-edge-indicator-legend-screen.md) | IP-1090 (SELECT Menu & Edge-Indicator Legend Screen) | 2026-07-13 | **VERIFIED** | 246/246 pass (fresh PyBoy+Pillow install), ROM builds at exactly 32768 bytes (25544 used), all DoD/checklist items confirmed by direct code read (`handle_play_input`'s SELECT branch retargeted to `GS_SELECT_MENU`, `st_map` reconfirmed byte-for-byte unchanged, `MM_CURSOR`/`MM_JUST_ENTERED` reuse confirmed structurally safe). Independently re-driven live via PyBoy screenshot — SELECT MENU's cursor defaults to MAP and toggles to LEGEND, LEGEND renders the real open-arrow/blocked-bar tiles beside their labels plus a genuinely blank WORLD EDGE cell. No findings. |
| [VR-1082](VR-1082-maze-blocked-edge-indicator-render.md) | IP-1082 (maze-blocked edge indicator, render half) | 2026-07-13 | **VERIFIED** | 246/246 pass (same fresh-session run as VR-1090), ROM builds at exactly 32768 bytes, open-edge branches and `IP-1080`'s `DRA_ROW`/`DRA_COL` classification arithmetic both reconfirmed byte-for-byte unchanged by direct diff, `T20.a`–`e` all confirmed passing. Independently re-driven live via PyBoy at a non-corpus `(seed=42, scale=9)` — the blocked tile (`0x1B`, DOWN) confirmed rendering at a grid-adjacent-but-maze-pruned edge, both by direct WRAM read and screenshot; the open case (RIGHT) confirmed unaffected. This closes every implementation package in the tree to `VERIFIED`. One informational note: a `09-content-review` pass is now owed (first exercise of `IP-1081`'s tile art as live content — should weigh `BL-0097` directly), and the `IP-1081`/`IP-1082` set still needs its own `10-integration-review` pass. |
| [VR-1101](VR-1101-infinite-mode-region-materialization.md) | IP-1101 (Infinite Mode — per-region materialization) | 2026-07-14 | **VERIFIED** | 253/253 pass (fresh session), ROM builds at exactly 32768 bytes, all 7 `T22.a`–`g` checks confirmed passing, treasure draw confirmed a structurally distinct third sequential `gw_prng_step` call from biome/connectivity (not just statistically independent), south/east neighbor reseeds confirmed discarded with no leakage into own-region draws by direct code read, oracle/SM83 lockstep confirmed via T22.b (0 mismatches, 45-entry corpus incl. negative coordinates and origin). Unblocks `IP-1100`/`IP-1102` to `READY`. Three Low-severity documentation findings (none block `VERIFIED`): `IP-1101` §4 misattributes `ADR-0016` point numbers (cites nonexistent point 8); `FS-110` §19 Open Questions 2/4 were not marked Resolved despite `IP-1101` §9's own commitment; `GDS-07`'s "eight new bytes" undercounts the actual 12-byte `INF_MZ_*` WRAM range. |
| [VR-1102](VR-1102-infinite-mode-streaming-window-and-render.md) | IP-1102 (Infinite Mode — streaming window, navigation & render integration) | 2026-07-14 | **VERIFIED** | 260/260 pass (fresh session), ROM builds at exactly 32768 bytes, all 7 `T24.a`–`e` checks confirmed passing, `dsr_p`'s finite-mode path confirmed byte-for-byte unchanged by direct code read (3-instruction gate prefix only) plus T13/T20 regression, `check_zone_transition`'s four finite branches confirmed reachable only when `GAME_MODE==0` with no fallthrough from the new infinite branch, `inf_ensure_window` confirmed to call `IP-1101`'s routine rather than reimplement it. `NFR-1400`'s `NOT MET` result (78,860–81,792 cycles vs. 70,224-cycle budget) independently re-measured via a standalone script against a disjoint corpus — reproduces the same order of magnitude (77,812–81,816 cycles), confirming it is not a measurement artifact. Unblocks `IP-1103` to `READY` (both its dependencies now `VERIFIED`). One Low-severity finding: `IP-1102` §4 cites `ADR-0016` point 7 with content ("no new module") that does not match that point's actual text — a citation-accuracy slip, second instance in the same tranche after `VR-1101`'s Finding 1. |
| [VR-1100](VR-1100-infinite-mode-mode-selection.md) | IP-1100 (Infinite Mode — mode selection & new-game entry) | 2026-07-16 | **VERIFIED** | 296/296 pass (this session, independent of IP-1100's own implementation), ROM builds at exactly 32768 bytes (sha256 byte-identical rebuild), all 19 individual `T25.a`-`f` checks confirmed passing, `st_seed_scale_entry`'s B-cancel target confirmed still `GS_MAIN_MENU` directly by direct code read (the named asymmetric-cancel-path tradeoff shipped as specified), `st_mode_select`'s B-cancel confirmed writing no `GAME_MODE`/`SEED`. Independently live-driven at a seed no `T25` fixture uses (39482) via the real MAIN MENU -> MODE SELECT -> INFINITE SEED ENTRY -> PLAYING path — seed composed correctly, starting region oracle-matched. Unblocks `IP-1104` one dependency closer (still needs `IP-1103`'s own independent verification). Two Low-severity documentation findings (neither blocks `VERIFIED`): `IP-1100` §5/§6 never amended to state the shipped `inf_ensure_window`-call deviation (same class as VR-1101/VR-1102's own citation findings); the suite's documented "10 checks" undercounts the actual 19 `check()` calls. |
| [VR-1103](VR-1103-infinite-mode-treasure-and-win-condition.md) | IP-1103 (Infinite Mode — treasure placement & win-condition state) | 2026-07-16 | **VERIFIED** | 296/296 pass (fresh session, independent of IP-1103's own implementation), ROM builds at exactly 32768 bytes (sha256 byte-identical rebuild), all 16 individual `T26.*` checks confirmed passing — an explicit count-accuracy audit confirmed "16 checks" is correct everywhere it's claimed, unlike `T25`'s stale "10" `VR-1100` caught. `check_collisions`'s new `GAME_MODE==1` branch confirmed by direct code read to leave the existing `KeyItem`/`ScoreItem` branches byte-for-byte unchanged; `inf_check_top_score` confirmed to have zero call sites anywhere in the source or assembled ROM (source grep + `T26.d`'s own ROM scan), the package's own deliberate `BL-0112` deferral, a pass condition not a gap. Independently live-driven at a non-fixture seed (53, distinct from `T26`'s own oracle-searched 10) via the real MAIN MENU -> MODE SELECT -> INFINITE SEED ENTRY -> PLAYING path and a standalone script — spawn/collection/count-increment/no-double-collection all reproduced. Unblocks `IP-1104` to dependency-eligible (all four of `IP-1100`/`1101`/`1102`/`1103` now `VERIFIED`), but `BL-0119`'s own pre-execution `IP-1104` §6 amendment still stands as a named blocker before `08` should pick it up. One Low-severity finding: `FS-110`'s own header still lists `IP-1100` as `COMPLETE` rather than `VERIFIED` (stale since `VR-1100`, not introduced by this run, out of this run's own edit scope). |
| [VR-1104](VR-1104-infinite-mode-ledger-save-persistence.md) | IP-1104 (Infinite Mode — visited-region-ledger save persistence) | 2026-07-16 | **VERIFIED** | 309/309 pass (fresh session, independent of IP-1104's own implementation and its `BL-0119` amendment), ROM builds at exactly 32768 bytes (sha256 byte-identical rebuild), all 13 individual `T27.*` checks confirmed passing across the 7 named scenarios a-g. Single MBC1 bracket confirmed preserved in both `save_to_sram`/`try_load_save`; the `BL-0119` amendment confirmed shipped exactly as specified — `inf_ledger_find`/`inf_ledger_mark_collected`/`inf_ensure_window`'s cross-reference all operate on WRAM `LEDGER`/`LEDGER_COUNT`/`LEDGER_CURSOR` only, `SRAM_LEDGER*` touched only inside the two MBC1-bracketed `memcpy` calls; the ledger cross-reference confirmed unconditional inside `inf_ensure_window`'s existing `_idx==4` block, uniform across every call site. Independently live-driven at a non-fixture seed (9092) via the real UI path in two scenarios: a save/load round trip, and a genuine two-region window-eviction in-session re-entry (stronger than `T27.g`'s own one-step move, which never actually evicts the origin region from the 3x3 window) — both confirm the `BL-0119` core claim (no treasure respawn) independently. This closes the tree's five-package Infinite Mode implementation set and **all 31 of 31 implementation packages in the tree are now `VERIFIED`** — `BL-0112` (the `FR-10400` run-end trigger) remains the tranche's sole standing gap, out of scope. Two Low-severity findings: `T27`'s "7 checks" claim (five documents) undercounts the actual 13 `check()` calls, the same pattern `VR-1100` found for `T25`; the package's own §9 text cites the wrong requirements file for the `NFR-5400` status update (implementation correctly used the right file regardless). |
| [VR-1033](VR-1033-nine-biome-family-collectible-spawn-content.md) | IP-1033 (Collectible-spawn content, Village/Cave/Desert/Plains) | 2026-07-17 | **RETURNED** | 309/309 pass, ROM builds correctly (31362/32768), diff stayed in scope, type-2 counts and player-spawn clearance both correct — but direct tile-grid arithmetic re-derivation (not a screenshot judgment call) found **3 of the 4 staged lists place a collectible on the exact same tile as an existing landmark**: `VILLAGE_COLLECTS`'s `KeyItem` on a fence tile `(17,12)`, `DESERT_COLLECTS`'s star on a cactus tile `(4,12)`, `PLAINS_COLLECTS`'s first two entries both on hand-placed flowers `(3,4)`/`(3,12)`. Only `CAVE_COLLECTS` is clean. Contradicts the package's own "no overlap" DoD/checklist claim. High finding routed back to `08-content-authoring`. |
| [VR-1105](VR-1105-infinite-mode-biome-domain-widening.md) | IP-1105 (Infinite Mode `region_byte` bit-field repack, biome-domain widening phase 1) | 2026-07-17 | **VERIFIED** | 309/309 pass, ROM builds at exactly 32768 bytes, all four consumer sites (`dsr_p_inf`/`czt_infinite`/`draw_region_arrows_inf`/`szc_infinite`) confirmed reading the new bit positions (biome 0-3, connectivity 4-7) by direct code read, `worldgen.py`/SM83 producer routines confirmed in lockstep, the three connectivity-bit test sites the package's own planning grep missed (`T22.g`, `T24`'s `_T24_DIR_BIT` table, `T27.a`) confirmed fixed in the same commit, diff scope confirmed disjoint from `IP-1033`'s `tilemaps.py` change and from every finite-mode file. One Low finding: two stale "bits 3-6" docstring comments (pre-repack positions) in `czt_infinite`/`draw_region_arrows_inf`, folded into a future touch rather than a standalone fix. |
| [VR-1110](VR-1110-procedural-music-generation.md) | IP-1110 (Procedural Music Generation, build-time sub-theme generation) | 2026-07-17 | **VERIFIED** | 309/309 pass, ROM builds correctly (31362/32768, matching `NFR-4400`'s recorded figure exactly), `verify_music_generation.py` re-run confirming all eight non-Grass sub-themes are pure transforms of the main theme and Grass is byte-identical to the existing `music_data()` output, transposition math independently re-derived by hand outside the module's own functions (Water/`C5` case), diff scope confirmed `asm_game.py`-free as the package's own scope boundary requires. The package's own planning deviation (flat `music_table` instead of named patch keys) confirmed a legitimate, already-documented scope-driven correction, not a defect. RTM's `FR-7100`/`NFR-4400` rows corrected `IP-1110 (COMPLETE)`→`IP-1110 (VERIFIED)`. No findings. |
