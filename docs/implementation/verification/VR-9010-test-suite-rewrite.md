# VR-9010 — Test Suite Rewrite (IP-9010)

> Owned by `09-package-verification`. Independent re-check of `IP-9010`'s claims against the
> current tree — nothing here is taken from the Implementation Summary on faith.

## Package

- **ID:** IP-9010 — Test Suite Rewrite (Bunny Quest semantics + portable paths)
- **Commit verified:** `a01fd99` (`feat(tests): IP-9010 -- rewrite test_rom.py for Bunny Quest
  semantics + portable paths`), tree state as of this run (2026-07-07).
- **Independence:** This package was implemented in a prior session/run (#22, commit `a01fd99`).
  This verification run has no memory of writing that code — independence is intact, no caveat
  needed. Environment note: PyBoy/numpy were not present in this (fresh) container; both were
  reinstalled in-session (PyBoy 2.7.0, numpy 2.4.6 — same PyBoy version the implementation used)
  before any gate was run.

## Result

**VERIFIED** — 0 failed checks against every Definition of Done and Verification Checklist item.

## Definition of Done audit

| # | Item | Evidence | Result |
|---|---|---|---|
| 1 | `python3 test_rom.py` runs from a fresh checkout with only PyBoy+numpy installed, finds `BunnyQuest.gbc`, passes 100% against a freshly built ROM | Ran `python3 build_rom.py BunnyQuest.gbc` then `python3 test_rom.py` from repo root in a container with no prior PyBoy install (installed fresh via `pip install pyboy numpy` this run) — **109/109 passed, 0 failed, exit code 0** | PASS |
| 2 | No absolute path remains anywhere in `test_rom.py` | `grep -n "/mnt/\|/home/claude\|BunnyGarden" test_rom.py` → no matches. `BASE = Path(__file__).resolve().parent` (line 30); `ROM_PATH`/`RAM_PATH`/`SHOT_DIR`/`RESULTS_PATH` all derived from `BASE` (lines 31–35) | PASS |
| 3 | No assertion references pre-rewrite semantics (gifts bitfield, 3-zone bounds, stale tiles) | Read all of T2–T10 (lines 125–578): `CUR_ZONE` 0–8 / 3×3 grid (T9), `CARROT_FLAGS`/`CARROTS_COUNT==9` victory (T4.8, T8.14), current tile indices (T2, T5). No `GIFTS`/`0xC009==0x07`/3-zone-bound assertion anywhere | PASS |
| 4 | ROM artifact byte-identical before/after this package | `sha256sum BunnyQuest.gbc` before rebuild: `efc38037…be0bea6cf`; rebuilt via `python3 build_rom.py BunnyQuest.gbc` (23148/32768 bytes reported); `sha256sum` after: **identical hash** | PASS |
| 5 | Checked-in `test_results.txt` reflects the new suite's real output | Regenerated by this run's suite execution; content is `BunnyQuest ROM Tests PASS=109/109 FAIL=0` followed by all 109 `[PASS]` lines; `git diff --stat test_results.txt` shows **no diff** against the checked-in file (byte-for-byte reproducible) | PASS |

## Verification Checklist audit

| # | Item | Evidence | Result |
|---|---|---|---|
| 1 | G5: ROM builds at exactly 32768 bytes with valid header | Rebuild output: `Wrote 32768 bytes → BunnyQuest.gbc`; T1.1–T1.9 all PASS (size, entry vector, GBC flag, cart type 0x03, RAM size 0x02, header checksum, VBlank ISR) | PASS |
| 2 | G5: full `test_rom.py` suite passes, run headless from repo root | `window='null'` throughout (headless); 109/109, exit 0 | PASS |
| 3 | Rebuilt ROM is byte-identical to pre-package `BunnyQuest.gbc` | Same sha256 before/after (see DoD #4) | PASS |
| 4 | Grep confirms no `/mnt/`, `/home/claude`, or `BunnyGarden` strings remain | Confirmed clean (see DoD #2) | PASS |
| 5 | T4/T8 victory checks set both `CARROT_FLAGS` and `CARROTS_COUNT` | T4.8 (lines 202–205) and T8.14 (lines 430–433) both write all nine `CARROT_FLAGS[i]=1` **and** `CARROTS_COUNT=9` before asserting `GAMESTATE==5` — R305's dual-assertion rule followed in both places | PASS |
| 6 | One-carrot-per-zone check present and passing | T1.11 (line 120), reads `tilemaps.ZONE_COLLECTS` host-side; independently ran — `[1, 1, 1, 1, 1, 1, 1, 1, 1]`, PASS | PASS |
| 7 | Map-hearts check present and passing (BL-0001 closure evidence) | T8.12/T8.13 (lines 424–425), asserts `0x9800+6*32+6`=FULL, `0x9800+6*32+11`=EMPTY; both PASS this run | PASS |
| 8 | NFR-7100 status flip and RQ-04 Test-column deltas applied exactly as §9 names | `docs/requirements/02-non-functional-requirements.md` NFR-7100 status reads **MET (2026-07-07, via IP-9010 — pending independent verification by `09-package-verification`)**, cites 109/109 and `test_results.txt`; RTM (`04-requirements-traceability-matrix.md`) Test column cites `T1–T10, 109/109 pass` for NFR-7100 and real check IDs for FR-1120/FR-2300/FR-4100/FR-5100/FR-5200/NFR-5200 — every cited ID exists in the actual file at the cited line ranges | PASS |

## Requirements audit

| Requirement | Where implemented | Where tested | RTM cell state | Result |
|---|---|---|---|---|
| NFR-7100 (full, currently-accurate test suite as completion gate) | `test_rom.py` (this package, wholesale rewrite) | T1–T10, 109/109 | RTM row correctly cites `T1–T10, 109/109 pass (IP-9010, 2026-07-07)` | PASS — traces cleanly |
| NFR-7000 ("suite runs headless via PyBoy — preserved"), cited in IP-9010 §3 Requirements Covered | — | — | **No such requirement exists.** `docs/requirements/02-non-functional-requirements.md` has no `NFR-7000` section; the RTM has no `NFR-7000` row. The nearest match by subject is **`NFR-6100`** ("PyBoy headless as the verification target," line 184/RTM line 58), whose Test cell is still `UNASSIGNED (inspection-based)` despite every suite in `test_rom.py` running via `PyBoy(..., window='null')` headless | **FINDING** (see below) — does not block VERIFIED; the underlying behavior (headless PyBoy execution) is real and independently confirmed by this run, but the citation is to a nonexistent ID and the correct requirement's RTM cell was never updated |

## Test run

- `python3 build_rom.py BunnyQuest.gbc` → `Wrote 32768 bytes → BunnyQuest.gbc` (23148/32768 used); sha256 identical before/after.
- `python3 test_rom.py` → **109 passed / 0 failed**, exit code `0`.
- Environment: Python 3.11.15, PyBoy 2.7.0 (installed fresh this run — matches the version IP-9010 itself installed in run #22), numpy 2.4.6, Pillow not installed (screenshot capture degrades gracefully per `test_rom.py`'s own `shoot()` try/except and `pb.screen.ndarray`-based T6.7 — no check depends on Pillow; confirmed all 109 checks still pass without it).

## Scope audit

Diff (`a01fd99`) touched exactly: `test_rom.py`, `test_results.txt`, `docs/implementation/00-master-build-plan.md`, `docs/implementation/packages/INDEX.md`, `docs/requirements/02-non-functional-requirements.md`, `docs/requirements/04-requirements-traceability-matrix.md`. All six are within the package's declared scope (§6 sole production-tree file `test_rom.py` + `test_results.txt` regeneration; §9's named documentation updates; the stage-08 status-row convention). No excursion.

## Findings

| Description | Severity | Recommended owner |
|---|---|---|
| IP-9010 §3 "Requirements Covered" cites **`NFR-7000`**, which does not exist anywhere in the requirements baseline (`docs/requirements/02-non-functional-requirements.md` or the RTM). The intended requirement is almost certainly **`NFR-6100`** ("PyBoy headless as the verification target") — its subject matches exactly ("suite runs headless via PyBoy — preserved"). `NFR-6100`'s RTM Test cell is still `UNASSIGNED (inspection-based)` even though every one of the 109 checks in the rewritten suite runs headless via PyBoy, which is now direct, repeatable evidence for it. | Low (doc/traceability defect only — no functional or gate impact; the actual capability is real and independently confirmed) | `07-implementation-planning` (correct the package's citation to `NFR-6100`) and/or `04-requirements-engineering` (fill `NFR-6100`'s RTM Test cell citing the full T1–T10 suite as headless-execution evidence) |

## Ledger updates applied by this run

- **Master Build Plan / `packages/INDEX.md`:** IP-9010 → `VERIFIED`. IP-9020, IP-9040, IP-1010 flip `BLOCKED` → `READY` (their sole dependency, IP-9010, is now `VERIFIED`). IP-9030 stays `BLOCKED` (depends on IP-9010 **and** IP-9020; IP-9020 is `READY`, not yet `VERIFIED`).
- **Verification index:** this report added.
